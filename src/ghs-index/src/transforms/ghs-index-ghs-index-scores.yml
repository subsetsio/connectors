# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ghs-index-ghs-index-scores`. Regenerate after model-verify.
# caution: Rows mix overall, category, indicator, subindicator, and question-level scores in the GHS Index hierarchy; filter `level` or specific `indicator_code` values before aggregating.
key: [indicator_label, indicator_code, country, level, year]
temporal: year
sort: [year, indicator_label, indicator_code, country, level]
columns:
  - name: country
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: indicator_code
    type: VARCHAR
  - name: indicator_label
    type: VARCHAR
  - name: level
    type: VARCHAR
  - name: score
    type: DOUBLE
