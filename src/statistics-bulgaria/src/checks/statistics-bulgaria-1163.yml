# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-1163`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, nuts, sex, d_10_years_age_groups_lfs]
temporal: period
checks:
  - kind: unique
    columns: [period, nuts, sex, d_10_years_age_groups_lfs]
  - kind: not_null
    col: period
  - kind: not_null
    col: nuts
  - kind: not_null
    col: sex
  - kind: not_null
    col: d_10_years_age_groups_lfs
  - kind: row_floor
    min: 1953
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
