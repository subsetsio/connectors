# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-1227`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, nomenclature_on_the_countries_for_tourism_statistics_purposes, indicators]
temporal: period
checks:
  - kind: unique
    columns: [period, nomenclature_on_the_countries_for_tourism_statistics_purposes, indicators]
  - kind: not_null
    col: period
  - kind: not_null
    col: nomenclature_on_the_countries_for_tourism_statistics_purposes
  - kind: not_null
    col: indicators
  - kind: row_floor
    min: 4880
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
