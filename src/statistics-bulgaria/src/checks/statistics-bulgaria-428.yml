# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-428`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [consumption_of_main_foods_and_beverages, period, residence]
temporal: period
checks:
  - kind: unique
    columns: [consumption_of_main_foods_and_beverages, period, residence]
  - kind: not_null
    col: consumption_of_main_foods_and_beverages
  - kind: not_null
    col: period
  - kind: not_null
    col: residence
  - kind: row_floor
    min: 2277
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
