# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-298`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, activity_status, age, mean_median_income, unit]
temporal: period
checks:
  - kind: unique
    columns: [period, activity_status, age, mean_median_income, unit]
  - kind: not_null
    col: period
  - kind: not_null
    col: activity_status
  - kind: not_null
    col: age
  - kind: not_null
    col: mean_median_income
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 288
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
