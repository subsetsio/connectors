# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-654`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [district, period, periods, type_of_buildings, type_of_buildings_2]
temporal: period
checks:
  - kind: unique
    columns: [district, period, periods, type_of_buildings, type_of_buildings_2]
  - kind: not_null
    col: district
  - kind: not_null
    col: period
  - kind: not_null
    col: periods
  - kind: not_null
    col: type_of_buildings
  - kind: not_null
    col: type_of_buildings_2
  - kind: row_floor
    min: 6605
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
