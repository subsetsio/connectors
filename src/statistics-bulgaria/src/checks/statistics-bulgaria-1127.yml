# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-1127`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, occupational_classes_isco_2008, sex]
temporal: period
checks:
  - kind: unique
    columns: [period, occupational_classes_isco_2008, sex]
  - kind: not_null
    col: period
  - kind: not_null
    col: occupational_classes_isco_2008
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 915
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
