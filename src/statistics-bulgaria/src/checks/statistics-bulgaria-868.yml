# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-868`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [aggregated_nomenclature_of_economic_activities_a21_2008, period, size_class_by_employed, indicators, unit]
temporal: period
checks:
  - kind: unique
    columns: [aggregated_nomenclature_of_economic_activities_a21_2008, period, size_class_by_employed, indicators, unit]
  - kind: not_null
    col: aggregated_nomenclature_of_economic_activities_a21_2008
  - kind: not_null
    col: period
  - kind: not_null
    col: size_class_by_employed
  - kind: not_null
    col: indicators
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 3590
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
