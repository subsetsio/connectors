# compiled by `hardened compile-checks` — post-publish audit of `statistics-bulgaria-365`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [aggregated_nomenclature_of_economic_activities_a21_2008, period, type_of_ownership]
temporal: period
checks:
  - kind: unique
    columns: [aggregated_nomenclature_of_economic_activities_a21_2008, period, type_of_ownership]
  - kind: not_null
    col: aggregated_nomenclature_of_economic_activities_a21_2008
  - kind: not_null
    col: period
  - kind: not_null
    col: type_of_ownership
  - kind: row_floor
    min: 501
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
