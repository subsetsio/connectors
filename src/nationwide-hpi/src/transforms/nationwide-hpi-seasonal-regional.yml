# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nationwide-hpi-seasonal-regional`. Regenerate after model-verify.
# caution: The category column is the published regional breakdown and the measure is seasonally adjusted index; compare categories rather than summing them.
key: [date, category, measure]
temporal: date
sort: [date, category, measure]
columns:
  - name: date
    type: DATE
  - name: period_label
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: measure
    type: VARCHAR
  - name: value
    type: DOUBLE
