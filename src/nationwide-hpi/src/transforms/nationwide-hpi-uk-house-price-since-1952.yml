# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nationwide-hpi-uk-house-price-since-1952`. Regenerate after model-verify.
# caution: The category column includes the published long-run UK series and any companion breakdowns exposed in the source file; compare values within one measure rather than summing categories.
key: [date, category, measure]
temporal: date
sort: [date, category, measure]
columns:
  - name: date
    type: DATE
  - name: period_label
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: measure
    type: VARCHAR
  - name: value
    type: DOUBLE
