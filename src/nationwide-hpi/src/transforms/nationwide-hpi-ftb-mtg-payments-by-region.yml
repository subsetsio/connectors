# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nationwide-hpi-ftb-mtg-payments-by-region`. Regenerate after model-verify.
# caution: The category column is the published regional breakdown; compare values within one measure rather than summing categories.
key: [date, category, measure]
temporal: date
sort: [date, category, measure]
columns:
  - name: date
    type: DATE
  - name: period_label
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: measure
    type: VARCHAR
  - name: value
    type: DOUBLE
