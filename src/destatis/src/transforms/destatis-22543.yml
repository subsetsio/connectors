# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `destatis-22543`. Regenerate after model-verify.
# caution: Rows are GENESIS-Online observations for one EVAS statistic; `dims` encodes the source cube dimensions and may include totals alongside detailed categories, so filter dimensions before aggregating.
key: [dims, year, table_code, time, table_name]
temporal: time
sort: [time, dims, year, table_code, table_name]
columns:
  - name: statistic_code
    type: BIGINT
  - name: table_code
    type: VARCHAR
  - name: table_name
    type: VARCHAR
  - name: time
    type: DATE
  - name: year
    type: INTEGER
  - name: measure_code
    type: VARCHAR
  - name: dims
    type: VARCHAR
  - name: value
    type: DOUBLE
  - name: status
    type: VARCHAR
