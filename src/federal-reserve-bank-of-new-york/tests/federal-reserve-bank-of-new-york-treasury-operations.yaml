spec_id: federal-reserve-bank-of-new-york-treasury-operations
status: active
tests:
  - not_null: operationId
  - unique: operationId
    reason: one row per Treasury operation; dupes mean the summary search returned overlapping rows
    certainty: 90
  - row_count: {min: 800}
    reason: well over a thousand historical operations observed; a tiny count means the date window broke
    certainty: 80
