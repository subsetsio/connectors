# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxiscprof`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [alder_grp, fsted, isced11_level, komm, sex, aar]
temporal: aar
checks:
  - kind: unique
    columns: [alder_grp, fsted, isced11_level, komm, sex, aar]
  - kind: not_null
    col: alder_grp
  - kind: not_null
    col: fsted
  - kind: not_null
    col: isced11_level
  - kind: not_null
    col: komm
  - kind: not_null
    col: sex
  - kind: not_null
    col: aar
  - kind: row_floor
    min: 18746
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
