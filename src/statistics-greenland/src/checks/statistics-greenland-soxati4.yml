# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-soxati4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [labour_force_status, uddannelsesniveau, calculation_month, time]
temporal: time
checks:
  - kind: unique
    columns: [labour_force_status, uddannelsesniveau, calculation_month, time]
  - kind: not_null
    col: labour_force_status
  - kind: not_null
    col: uddannelsesniveau
  - kind: not_null
    col: calculation_month
  - kind: not_null
    col: time
  - kind: row_floor
    min: 648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
