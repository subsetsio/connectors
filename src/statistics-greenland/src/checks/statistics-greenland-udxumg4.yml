# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxumg4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [distrikt_12a, kon, registrering, aar]
temporal: aar
checks:
  - kind: unique
    columns: [distrikt_12a, kon, registrering, aar]
  - kind: not_null
    col: distrikt_12a
  - kind: not_null
    col: kon
  - kind: not_null
    col: registrering
  - kind: not_null
    col: aar
  - kind: row_floor
    min: 1108
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
