# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxiscproh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [unit, alder_grp, komm, isced11_level_grp, aar, sex]
temporal: aar
checks:
  - kind: unique
    columns: [unit, alder_grp, komm, isced11_level_grp, aar, sex]
  - kind: not_null
    col: unit
  - kind: not_null
    col: alder_grp
  - kind: not_null
    col: komm
  - kind: not_null
    col: isced11_level_grp
  - kind: not_null
    col: aar
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 14772
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
