# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-ofxkomrn`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, transaction_account, purpose_account, type_of_grant, time]
temporal: time
checks:
  - kind: unique
    columns: [municipality, transaction_account, purpose_account, type_of_grant, time]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: transaction_account
  - kind: not_null
    col: purpose_account
  - kind: not_null
    col: type_of_grant
  - kind: not_null
    col: time
  - kind: row_floor
    min: 591465
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
