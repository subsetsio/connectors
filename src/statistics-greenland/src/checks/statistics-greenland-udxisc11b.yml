# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxisc11b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [aktalder, isced, isced_field, sex, skoleomr, taar]
temporal: taar
checks:
  - kind: unique
    columns: [aktalder, isced, isced_field, sex, skoleomr, taar]
  - kind: not_null
    col: aktalder
  - kind: not_null
    col: isced
  - kind: not_null
    col: isced_field
  - kind: not_null
    col: sex
  - kind: not_null
    col: skoleomr
  - kind: not_null
    col: taar
  - kind: row_floor
    min: 11056
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
