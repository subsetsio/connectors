# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-fix006`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time, municipality, month, species, unit]
temporal: time
checks:
  - kind: unique
    columns: [time, municipality, month, species, unit]
  - kind: not_null
    col: time
  - kind: not_null
    col: municipality
  - kind: not_null
    col: month
  - kind: not_null
    col: species
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 79920
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
