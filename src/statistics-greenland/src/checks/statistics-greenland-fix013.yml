# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-fix013`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [species, residence, fishing_segment, vessel_type, enhed, month, time]
temporal: time
checks:
  - kind: unique
    columns: [species, residence, fishing_segment, vessel_type, enhed, month, time]
  - kind: not_null
    col: species
  - kind: not_null
    col: residence
  - kind: not_null
    col: fishing_segment
  - kind: not_null
    col: vessel_type
  - kind: not_null
    col: enhed
  - kind: not_null
    col: month
  - kind: not_null
    col: time
  - kind: row_floor
    min: 5906
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
