# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxisc11i`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [isced, sex, skolenr_ny, uddkode_ny, startalder, startaar]
temporal: startaar
checks:
  - kind: unique
    columns: [isced, sex, skolenr_ny, uddkode_ny, startalder, startaar]
  - kind: not_null
    col: isced
  - kind: not_null
    col: sex
  - kind: not_null
    col: skolenr_ny
  - kind: not_null
    col: uddkode_ny
  - kind: not_null
    col: startalder
  - kind: not_null
    col: startaar
  - kind: row_floor
    min: 4638
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
