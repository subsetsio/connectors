# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxkak`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [eksamensaar, vaegtet_interval, sex, unit]
temporal: eksamensaar
checks:
  - kind: unique
    columns: [eksamensaar, vaegtet_interval, sex, unit]
  - kind: not_null
    col: eksamensaar
  - kind: not_null
    col: vaegtet_interval
  - kind: not_null
    col: sex
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
