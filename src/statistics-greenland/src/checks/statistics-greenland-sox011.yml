# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-sox011`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [no_of_children, no_of_adults, amount_intervals, type, time]
temporal: time
checks:
  - kind: unique
    columns: [no_of_children, no_of_adults, amount_intervals, type, time]
  - kind: not_null
    col: no_of_children
  - kind: not_null
    col: no_of_adults
  - kind: not_null
    col: amount_intervals
  - kind: not_null
    col: type
  - kind: not_null
    col: time
  - kind: row_floor
    min: 2335
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
