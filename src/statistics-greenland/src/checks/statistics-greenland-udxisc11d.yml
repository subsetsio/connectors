# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxisc11d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [eksalder, isced, isced_field, sex, skoleomr, slutaar]
temporal: slutaar
checks:
  - kind: unique
    columns: [eksalder, isced, isced_field, sex, skoleomr, slutaar]
  - kind: not_null
    col: eksalder
  - kind: not_null
    col: isced
  - kind: not_null
    col: isced_field
  - kind: not_null
    col: sex
  - kind: not_null
    col: skoleomr
  - kind: not_null
    col: slutaar
  - kind: row_floor
    min: 4595
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
