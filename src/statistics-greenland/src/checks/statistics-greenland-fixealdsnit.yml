# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-fixealdsnit`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type, municipality, geographic_location, time]
temporal: time
checks:
  - kind: unique
    columns: [type, municipality, geographic_location, time]
  - kind: not_null
    col: type
  - kind: not_null
    col: municipality
  - kind: not_null
    col: geographic_location
  - kind: not_null
    col: time
  - kind: row_floor
    min: 740
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
