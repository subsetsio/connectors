# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-soxbt01`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [no_of_children, municipality, unit, time]
temporal: time
checks:
  - kind: unique
    columns: [no_of_children, municipality, unit, time]
  - kind: not_null
    col: no_of_children
  - kind: not_null
    col: municipality
  - kind: not_null
    col: unit
  - kind: not_null
    col: time
  - kind: row_floor
    min: 390
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
