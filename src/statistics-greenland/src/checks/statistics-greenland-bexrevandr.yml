# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-bexrevandr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [omr, fsted, alder_ult, sex, vandrn, vandrnmd, taar]
temporal: taar
checks:
  - kind: unique
    columns: [omr, fsted, alder_ult, sex, vandrn, vandrnmd, taar]
  - kind: not_null
    col: omr
  - kind: not_null
    col: fsted
  - kind: not_null
    col: alder_ult
  - kind: not_null
    col: sex
  - kind: not_null
    col: vandrn
  - kind: not_null
    col: vandrnmd
  - kind: not_null
    col: taar
  - kind: row_floor
    min: 807767
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
