# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-udxisc11a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [isced, isced_field, sex, skoleomr, startalder, startaar]
temporal: startaar
checks:
  - kind: unique
    columns: [isced, isced_field, sex, skoleomr, startalder, startaar]
  - kind: not_null
    col: isced
  - kind: not_null
    col: isced_field
  - kind: not_null
    col: sex
  - kind: not_null
    col: skoleomr
  - kind: not_null
    col: startalder
  - kind: not_null
    col: startaar
  - kind: row_floor
    min: 8238
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
