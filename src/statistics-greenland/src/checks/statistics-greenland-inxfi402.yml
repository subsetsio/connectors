# compiled by `hardened compile-checks` — post-publish audit of `statistics-greenland-inxfi402`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [district, place_of_residence, family_type, number_of_children, decile, type_of_income, time]
temporal: time
checks:
  - kind: unique
    columns: [district, place_of_residence, family_type, number_of_children, decile, type_of_income, time]
  - kind: not_null
    col: district
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: family_type
  - kind: not_null
    col: number_of_children
  - kind: not_null
    col: decile
  - kind: not_null
    col: type_of_income
  - kind: not_null
    col: time
  - kind: row_floor
    min: 238659
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
