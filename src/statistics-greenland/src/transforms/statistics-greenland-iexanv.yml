# compiled by `hardened compile-transforms`, then curated at the model stage.
# Faithful pass-through of raw asset `statistics-greenland-iexanv` (a PxWeb json-stat2 cube
# flattened to long form: one column per dimension carrying the English
# category label, plus the cell value).
key: [time, end_use, quarter]
temporal: time
sort: [time, end_use, quarter]
columns:
  - name: time
    type: BIGINT
    description: "Observation period -- the cube dimension Statistics Greenland tags role=time. Encoded as calendar year (YYYY)."
  - name: end_use
    type: VARCHAR
    description: "Cube dimension `end-use` (renamed to snake_case); values are the source's English category labels."
  - name: quarter
    type: VARCHAR
    description: "Cube dimension `quarter`; values are the source's English category labels."
  - name: obs_value
    type: DOUBLE
    description: "Observed value of the cube cell."
