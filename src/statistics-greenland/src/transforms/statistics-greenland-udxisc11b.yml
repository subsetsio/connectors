# compiled by `hardened compile-transforms`, then curated at the model stage.
# Faithful pass-through of raw asset `statistics-greenland-udxisc11b` (a PxWeb json-stat2 cube
# flattened to long form: one column per dimension carrying the English
# category label, plus the cell value).
key: [aktalder, isced, isced_field, sex, skoleomr, taar]
temporal: taar
sort: [taar, aktalder, isced, isced_field, sex, skoleomr]
columns:
  - name: aktalder
    type: VARCHAR
    description: "Cube dimension `aktalder`; values are the source's English category labels."
  - name: isced
    type: VARCHAR
    description: "Cube dimension `isced`; values are the source's English category labels."
  - name: isced_field
    type: VARCHAR
    description: "Cube dimension `isced_field`; values are the source's English category labels."
  - name: sex
    type: VARCHAR
    description: "Cube dimension `sex`; values are the source's English category labels."
  - name: skoleomr
    type: VARCHAR
    description: "Cube dimension `skoleomr`; values are the source's English category labels."
  - name: taar
    type: BIGINT
    description: "Observation period -- the cube dimension Statistics Greenland tags role=time. Encoded as calendar year (YYYY)."
  - name: obs_value
    type: DOUBLE
    description: "Observed value of the cube cell."
