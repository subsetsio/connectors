# compiled by `hardened compile-transforms`, then curated at the model stage.
# Faithful pass-through of raw asset `statistics-greenland-udxisc11k` (a PxWeb json-stat2 cube
# flattened to long form: one column per dimension carrying the English
# category label, plus the cell value).
key: [eksalder, isced, sex, skolenr_ny, uddkode_ny, slutaar]
temporal: slutaar
sort: [slutaar, eksalder, isced, sex, skolenr_ny, uddkode_ny]
columns:
  - name: eksalder
    type: VARCHAR
    description: "Cube dimension `eksalder`; values are the source's English category labels."
  - name: isced
    type: VARCHAR
    description: "Cube dimension `Isced` (renamed to snake_case); values are the source's English category labels."
  - name: sex
    type: VARCHAR
    description: "Cube dimension `sex`; values are the source's English category labels."
  - name: skolenr_ny
    type: VARCHAR
    description: "Cube dimension `skolenr_ny`; values are the source's English category labels."
  - name: uddkode_ny
    type: VARCHAR
    description: "Cube dimension `uddkode_ny`; values are the source's English category labels."
  - name: slutaar
    type: BIGINT
    description: "Observation period -- the cube dimension Statistics Greenland tags role=time. Encoded as calendar year (YYYY)."
  - name: obs_value
    type: DOUBLE
    description: "Observed value of the cube cell."
