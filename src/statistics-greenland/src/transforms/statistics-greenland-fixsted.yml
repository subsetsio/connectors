# compiled by `hardened compile-transforms`, then curated at the model stage.
# Faithful pass-through of raw asset `statistics-greenland-fixsted` (a PxWeb json-stat2 cube
# flattened to long form: one column per dimension carrying the English
# category label, plus the cell value).
key: [districts, geographic_location, type, time]
temporal: time
sort: [time, districts, geographic_location, type]
columns:
  - name: districts
    type: VARCHAR
    description: "Cube dimension `districts`; values are the source's English category labels."
  - name: geographic_location
    type: VARCHAR
    description: "Cube dimension `geographic location` (renamed to snake_case); values are the source's English category labels."
  - name: type
    type: VARCHAR
    description: "Cube dimension `type`; values are the source's English category labels."
  - name: time
    type: BIGINT
    description: "Observation period -- the cube dimension Statistics Greenland tags role=time. Encoded as calendar year (YYYY)."
  - name: obs_value
    type: DOUBLE
    description: "Observed value of the cube cell."
