# compiled by `hardened compile-checks` — post-publish audit of `ny-fed-reference-rates-unsecured`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [effectivedate, type, revisionindicator]
temporal: effectivedate
checks:
  - kind: unique
    columns: [effectivedate, type, revisionindicator]
  - kind: not_null
    col: effectivedate
  - kind: not_null
    col: type
  - kind: not_null
    col: revisionindicator
  - kind: row_floor
    min: 2602
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
