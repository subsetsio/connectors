spec_id: statistics-denmark-itav15
status: active
tests:
  - row_count: {min: 1}
    reason: every rank-active StatBank table returns at least one BULK data row
    certainty: 95
  - not_null: INDHOLD
    reason: BULK semicolon-CSV always carries the value column INDHOLD as a string ('..'/'.' confidential cells are still non-null)
    certainty: 80
    severity: warn
