# compiled by `hardened compile-checks` — post-publish audit of `anp-brazil-vdpb-vct-vendas-glp-tipo-vasilhame-m3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, grande_regiao, unidade_da_federacao, vasilhame]
temporal: date
checks:
  - kind: unique
    columns: [date, grande_regiao, unidade_da_federacao, vasilhame]
  - kind: not_null
    col: date
  - kind: not_null
    col: grande_regiao
  - kind: not_null
    col: unidade_da_federacao
  - kind: not_null
    col: vasilhame
  - kind: row_floor
    min: 6291
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
