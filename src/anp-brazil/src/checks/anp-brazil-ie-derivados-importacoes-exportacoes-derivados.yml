# compiled by `hardened compile-checks` — post-publish audit of `anp-brazil-ie-derivados-importacoes-exportacoes-derivados`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, produto, operacao_comercial]
temporal: date
checks:
  - kind: unique
    columns: [date, produto, operacao_comercial]
  - kind: not_null
    col: date
  - kind: not_null
    col: produto
  - kind: not_null
    col: operacao_comercial
  - kind: row_floor
    min: 4755
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
