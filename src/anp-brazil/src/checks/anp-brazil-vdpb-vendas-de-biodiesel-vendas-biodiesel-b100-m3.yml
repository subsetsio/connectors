# compiled by `hardened compile-checks` — post-publish audit of `anp-brazil-vdpb-vendas-de-biodiesel-vendas-biodiesel-b100-m3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, regiao_origem, regiao_destino]
temporal: date
checks:
  - kind: unique
    columns: [date, regiao_origem, regiao_destino]
  - kind: not_null
    col: date
  - kind: not_null
    col: regiao_origem
  - kind: not_null
    col: regiao_destino
  - kind: row_floor
    min: 935
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
