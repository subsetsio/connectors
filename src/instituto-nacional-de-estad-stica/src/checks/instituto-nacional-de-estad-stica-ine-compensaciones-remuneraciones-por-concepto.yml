# compiled by `hardened compile-checks` — post-publish audit of `instituto-nacional-de-estad-stica-ine-compensaciones-remuneraciones-por-concepto`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [monto, source_row_number, tipo_de_contrato, source_resource_id, source_resource_name]
checks:
  - kind: unique
    columns: [monto, source_row_number, tipo_de_contrato, source_resource_id, source_resource_name]
  - kind: not_null
    col: monto
  - kind: not_null
    col: source_row_number
  - kind: not_null
    col: tipo_de_contrato
  - kind: not_null
    col: source_resource_id
  - kind: not_null
    col: source_resource_name
  - kind: row_floor
    min: 12
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
