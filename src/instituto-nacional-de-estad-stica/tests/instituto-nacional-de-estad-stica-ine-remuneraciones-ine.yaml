spec_id: instituto-nacional-de-estad-stica-ine-remuneraciones-ine
status: active
coverage:
  frozen: true
  reason: the CKAN package contains 2017-2018 remuneration CSV snapshots and no current cadence is published
tests:
  - row_count: {min: 1}
    reason: the package has CSV resources for remuneration data and the fetcher raises on zero rows
    certainty: 95
  - not_null: __source_resource_id
    reason: every raw row is enriched with the CKAN resource id it came from
    certainty: 95
