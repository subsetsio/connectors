# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `gem-entrepreneurship-indicators`. Regenerate after model-verify.
# caution: The table records source variables and labels discovered in public national-level files; a canonical indicator can map to multiple year-specific source variables, especially for APS.
key: [survey, variable]
sort: [survey, variable]
columns:
  - name: survey
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: variable
    type: VARCHAR
  - name: label
    type: VARCHAR
  - name: first_year
    type: INTEGER
  - name: last_year
    type: INTEGER
