# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-c16-g44-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2016, lfsff_c16, lfsmf_c16, state, age]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2016, lfsff_c16, lfsmf_c16, state, age]
  - kind: not_null
    col: lga_2016
  - kind: not_null
    col: lfsff_c16
  - kind: not_null
    col: lfsmf_c16
  - kind: not_null
    col: state
  - kind: not_null
    col: age
  - kind: row_floor
    min: 324288
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
