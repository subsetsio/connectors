# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-census2011-t16-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region, strd_bedrd, state, measure, time_period]
temporal: time_period
checks:
  - kind: unique
    columns: [region, strd_bedrd, state, measure, time_period]
  - kind: not_null
    col: region
  - kind: not_null
    col: strd_bedrd
  - kind: not_null
    col: state
  - kind: not_null
    col: measure
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 153360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
