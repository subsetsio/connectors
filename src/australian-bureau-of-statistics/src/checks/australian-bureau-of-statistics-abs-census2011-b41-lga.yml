# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-census2011-b41-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2011, measure, age, state, sex]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2011, measure, age, state, sex]
  - kind: not_null
    col: lga_2011
  - kind: not_null
    col: measure
  - kind: not_null
    col: age
  - kind: not_null
    col: state
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 116842
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
