# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-c16-t26-ts-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2016, rntrd_2016, strd_2016, state, time_period]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2016, rntrd_2016, strd_2016, state, time_period]
  - kind: not_null
    col: lga_2016
  - kind: not_null
    col: rntrd_2016
  - kind: not_null
    col: strd_2016
  - kind: not_null
    col: state
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 256225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
