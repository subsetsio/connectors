# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-seifa-sla`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [asgc_2011, measure, unit_measure, index_type]
temporal: time_period
checks:
  - kind: unique
    columns: [asgc_2011, measure, unit_measure, index_type]
  - kind: not_null
    col: asgc_2011
  - kind: not_null
    col: measure
  - kind: not_null
    col: unit_measure
  - kind: not_null
    col: index_type
  - kind: row_floor
    min: 27345
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
