# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-annual-erp-lga2024`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2024, time_period, age, sex_abs, region_type]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2024, time_period, age, sex_abs, region_type]
  - kind: not_null
    col: lga_2024
  - kind: not_null
    col: time_period
  - kind: not_null
    col: age
  - kind: not_null
    col: sex_abs
  - kind: not_null
    col: region_type
  - kind: row_floor
    min: 380988
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
