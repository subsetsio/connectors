# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-c16-g43-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2016, lfsp_c16, age, state, sex_abs]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2016, lfsp_c16, age, state, sex_abs]
  - kind: not_null
    col: lga_2016
  - kind: not_null
    col: lfsp_c16
  - kind: not_null
    col: age
  - kind: not_null
    col: state
  - kind: not_null
    col: sex_abs
  - kind: row_floor
    min: 101340
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
