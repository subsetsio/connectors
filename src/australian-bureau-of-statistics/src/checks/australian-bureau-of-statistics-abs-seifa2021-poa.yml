# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-seifa2021-poa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [poa, seifa_measure, unit_measure, seifaindextype]
temporal: time_period
checks:
  - kind: unique
    columns: [poa, seifa_measure, unit_measure, seifaindextype]
  - kind: not_null
    col: poa
  - kind: not_null
    col: seifa_measure
  - kind: not_null
    col: unit_measure
  - kind: not_null
    col: seifaindextype
  - kind: row_floor
    min: 58146
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
