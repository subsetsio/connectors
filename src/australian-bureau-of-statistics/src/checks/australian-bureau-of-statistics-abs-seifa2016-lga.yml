# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-seifa2016-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lga_2016, seifa_measure, unit_measure, seifaindextype]
temporal: time_period
checks:
  - kind: unique
    columns: [lga_2016, seifa_measure, unit_measure, seifaindextype]
  - kind: not_null
    col: lga_2016
  - kind: not_null
    col: seifa_measure
  - kind: not_null
    col: unit_measure
  - kind: not_null
    col: seifaindextype
  - kind: row_floor
    min: 10890
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
