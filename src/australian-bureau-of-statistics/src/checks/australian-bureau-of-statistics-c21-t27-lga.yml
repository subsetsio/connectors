# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-c21-t27-lga`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region, state, fmcmdcf, cdcf, time_period]
temporal: time_period
checks:
  - kind: unique
    columns: [region, state, fmcmdcf, cdcf, time_period]
  - kind: not_null
    col: region
  - kind: not_null
    col: state
  - kind: not_null
    col: fmcmdcf
  - kind: not_null
    col: cdcf
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 20850
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
