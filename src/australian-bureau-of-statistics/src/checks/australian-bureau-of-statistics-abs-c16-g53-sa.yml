# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-abs-c16-g53-sa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [asgs_2016, indp_c16, occp_c16, state, regiontype]
temporal: time_period
checks:
  - kind: unique
    columns: [asgs_2016, indp_c16, occp_c16, state, regiontype]
  - kind: not_null
    col: asgs_2016
  - kind: not_null
    col: indp_c16
  - kind: not_null
    col: occp_c16
  - kind: not_null
    col: state
  - kind: not_null
    col: regiontype
  - kind: row_floor
    min: 295995
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
