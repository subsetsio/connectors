# compiled by `hardened compile-checks` — post-publish audit of `australian-bureau-of-statistics-prov-mortality-cause-wk`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [week_occur, cause_death, time_period, measure, unit_measure]
temporal: time_period
checks:
  - kind: unique
    columns: [week_occur, cause_death, time_period, measure, unit_measure]
  - kind: not_null
    col: week_occur
  - kind: not_null
    col: cause_death
  - kind: not_null
    col: time_period
  - kind: not_null
    col: measure
  - kind: not_null
    col: unit_measure
  - kind: row_floor
    min: 6979
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
