spec_id: statistics-estonia-rr306.px
status: deferred
coverage:
  frozen: true
  reason: Upstream PxWeb table endpoint is no longer listed and returns HTTP 400;
    active waiver covers this removed table.
