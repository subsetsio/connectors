# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl036.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, size_of_local_government_unit, sex]
checks:
  - kind: unique
    columns: [age_group, size_of_local_government_unit, sex]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: size_of_local_government_unit
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 363
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
