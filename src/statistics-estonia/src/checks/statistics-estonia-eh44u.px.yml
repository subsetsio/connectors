# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-eh44u.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region_administrative_unit, number_of_rooms, reference_period, indicator]
checks:
  - kind: unique
    columns: [region_administrative_unit, number_of_rooms, reference_period, indicator]
  - kind: not_null
    col: region_administrative_unit
  - kind: not_null
    col: number_of_rooms
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 14580
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
