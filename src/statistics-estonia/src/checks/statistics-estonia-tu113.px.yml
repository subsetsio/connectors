# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tu113.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, reference_period, purpose_of_trip, indicator]
checks:
  - kind: unique
    columns: [county, reference_period, purpose_of_trip, indicator]
  - kind: not_null
    col: county
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: purpose_of_trip
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2772
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
