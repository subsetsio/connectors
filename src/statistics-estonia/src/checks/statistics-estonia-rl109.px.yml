# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl109.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [permanent_place_of_residence, duration_of_temporary_absence, sex]
checks:
  - kind: unique
    columns: [permanent_place_of_residence, duration_of_temporary_absence, sex]
  - kind: not_null
    col: permanent_place_of_residence
  - kind: not_null
    col: duration_of_temporary_absence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 3297
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
