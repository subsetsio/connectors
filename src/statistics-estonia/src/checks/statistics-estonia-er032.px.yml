# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-er032.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [administrative_unit, year, number_of_employees_of_enterprise]
temporal: year
checks:
  - kind: unique
    columns: [administrative_unit, year, number_of_employees_of_enterprise]
  - kind: not_null
    col: administrative_unit
  - kind: not_null
    col: year
  - kind: not_null
    col: number_of_employees_of_enterprise
  - kind: row_floor
    min: 2992
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
