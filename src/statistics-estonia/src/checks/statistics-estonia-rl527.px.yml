# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl527.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, number_and_age_of_children, family_nucleus_composition]
checks:
  - kind: unique
    columns: [place_of_residence, number_and_age_of_children, family_nucleus_composition]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: number_and_age_of_children
  - kind: not_null
    col: family_nucleus_composition
  - kind: row_floor
    min: 10650
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
