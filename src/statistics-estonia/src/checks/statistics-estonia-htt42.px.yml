# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-htt42.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [net_income_per_household_member, year, participation_in_training, indicator]
temporal: year
checks:
  - kind: unique
    columns: [net_income_per_household_member, year, participation_in_training, indicator]
  - kind: not_null
    col: net_income_per_household_member
  - kind: not_null
    col: year
  - kind: not_null
    col: participation_in_training
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 84
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
