# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0726.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_and_number_of_children, county, leibkonna_suurus, number_of_generations_in_household, indicator]
checks:
  - kind: unique
    columns: [age_and_number_of_children, county, leibkonna_suurus, number_of_generations_in_household, indicator]
  - kind: not_null
    col: age_and_number_of_children
  - kind: not_null
    col: county
  - kind: not_null
    col: leibkonna_suurus
  - kind: not_null
    col: number_of_generations_in_household
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 46920
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
