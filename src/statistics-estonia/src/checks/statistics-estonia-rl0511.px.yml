# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0511.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [previous_country_of_residence, place_of_residence, indicator, sex]
checks:
  - kind: unique
    columns: [previous_country_of_residence, place_of_residence, indicator, sex]
  - kind: not_null
    col: previous_country_of_residence
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: indicator
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 4644
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
