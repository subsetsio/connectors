# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pms241.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, kind_of_agricultural_labour_force, reference_period, legal_form_of_holder, indicator]
checks:
  - kind: unique
    columns: [county, kind_of_agricultural_labour_force, reference_period, legal_form_of_holder, indicator]
  - kind: not_null
    col: county
  - kind: not_null
    col: kind_of_agricultural_labour_force
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: legal_form_of_holder
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 1080
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
