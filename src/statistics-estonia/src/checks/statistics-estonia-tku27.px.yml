# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tku27.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_enterprises, evaluation_of_the_adequacy_of_employees_skills_and_knowledge, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [group_of_enterprises, evaluation_of_the_adequacy_of_employees_skills_and_knowledge, year, indicator]
  - kind: not_null
    col: group_of_enterprises
  - kind: not_null
    col: evaluation_of_the_adequacy_of_employees_skills_and_knowledge
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
