# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0406.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sex, county, legal_marital_status, age_group, socio_economic_status, de_facto_marital_status]
checks:
  - kind: unique
    columns: [sex, county, legal_marital_status, age_group, socio_economic_status, de_facto_marital_status]
  - kind: not_null
    col: sex
  - kind: not_null
    col: county
  - kind: not_null
    col: legal_marital_status
  - kind: not_null
    col: age_group
  - kind: not_null
    col: socio_economic_status
  - kind: not_null
    col: de_facto_marital_status
  - kind: row_floor
    min: 143640
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
