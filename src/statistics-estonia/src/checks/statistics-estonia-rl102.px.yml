# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl102.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [administrative_unit, de_facto_and_usual_resident_population, sex]
checks:
  - kind: unique
    columns: [administrative_unit, de_facto_and_usual_resident_population, sex]
  - kind: not_null
    col: administrative_unit
  - kind: not_null
    col: de_facto_and_usual_resident_population
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 32094
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
