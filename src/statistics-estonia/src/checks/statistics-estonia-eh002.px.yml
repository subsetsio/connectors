# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-eh002.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, type_of_construction, type_of_construction_activity]
checks:
  - kind: unique
    columns: [reference_period, type_of_construction, type_of_construction_activity]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: type_of_construction
  - kind: not_null
    col: type_of_construction_activity
  - kind: row_floor
    min: 570
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
