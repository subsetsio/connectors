# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0725.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_and_number_of_children, county, household_size, household_structure, indicator]
checks:
  - kind: unique
    columns: [age_and_number_of_children, county, household_size, household_structure, indicator]
  - kind: not_null
    col: age_and_number_of_children
  - kind: not_null
    col: county
  - kind: not_null
    col: household_size
  - kind: not_null
    col: household_structure
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 54740
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
