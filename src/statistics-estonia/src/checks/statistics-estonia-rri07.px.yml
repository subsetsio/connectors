# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rri07.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [insurance_company, year, type_of_non_life_insurance, month, indicator]
temporal: month
checks:
  - kind: unique
    columns: [insurance_company, year, type_of_non_life_insurance, month, indicator]
  - kind: not_null
    col: insurance_company
  - kind: not_null
    col: year
  - kind: not_null
    col: type_of_non_life_insurance
  - kind: not_null
    col: month
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 88179
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
