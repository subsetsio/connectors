# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tln12.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [city_district, reference_period, type_of_residential_building]
checks:
  - kind: unique
    columns: [city_district, reference_period, type_of_residential_building]
  - kind: not_null
    col: city_district
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: type_of_residential_building
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
