# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rv283u.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, year, former_marital_status, type_of_settlement_region, sex]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence, year, former_marital_status, type_of_settlement_region, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: year
  - kind: not_null
    col: former_marital_status
  - kind: not_null
    col: type_of_settlement_region
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 2835
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
