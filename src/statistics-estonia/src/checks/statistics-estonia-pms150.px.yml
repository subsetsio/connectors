# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pms150.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [size_class_of_sown_area, reference_period, indicator]
checks:
  - kind: unique
    columns: [size_class_of_sown_area, reference_period, indicator]
  - kind: not_null
    col: size_class_of_sown_area
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
