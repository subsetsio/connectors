# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-eh47u.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region_administrative_unit, type_of_non_residential_building, reference_period, indicator]
checks:
  - kind: unique
    columns: [region_administrative_unit, type_of_non_residential_building, reference_period, indicator]
  - kind: not_null
    col: region_administrative_unit
  - kind: not_null
    col: type_of_non_residential_building
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 48600
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
