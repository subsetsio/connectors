# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-er0308.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [administrative_unit, economic_activity_emtak_2025]
temporal: year
checks:
  - kind: unique
    columns: [administrative_unit, economic_activity_emtak_2025]
  - kind: not_null
    col: administrative_unit
  - kind: not_null
    col: economic_activity_emtak_2025
  - kind: row_floor
    min: 1518
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
