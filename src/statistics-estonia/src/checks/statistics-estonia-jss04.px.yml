# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-jss04.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [experience_of_intimate_partner_or_non_partner_violence_stalking_or_sexual_harassment_at_work, indicator]
checks:
  - kind: unique
    columns: [experience_of_intimate_partner_or_non_partner_violence_stalking_or_sexual_harassment_at_work, indicator]
  - kind: not_null
    col: experience_of_intimate_partner_or_non_partner_violence_stalking_or_sexual_harassment_at_work
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
