# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0536.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, county, main_source_of_subsistence, native_and_foreign_origin_population, sex]
checks:
  - kind: unique
    columns: [age_group, county, main_source_of_subsistence, native_and_foreign_origin_population, sex]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: county
  - kind: not_null
    col: main_source_of_subsistence
  - kind: not_null
    col: native_and_foreign_origin_population
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 41695
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
