# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0325.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, secondary_place_of_residence]
checks:
  - kind: unique
    columns: [place_of_residence, secondary_place_of_residence]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: secondary_place_of_residence
  - kind: row_floor
    min: 2037
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
