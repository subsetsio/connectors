# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl216.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citizenship, place_of_residence, country_of_birth, sex]
checks:
  - kind: unique
    columns: [citizenship, place_of_residence, country_of_birth, sex]
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: country_of_birth
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 106074
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
