# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0135.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sex, county, age_group, mother_tongue, labour_status, command_of_foreign_languages]
checks:
  - kind: unique
    columns: [sex, county, age_group, mother_tongue, labour_status, command_of_foreign_languages]
  - kind: not_null
    col: sex
  - kind: not_null
    col: county
  - kind: not_null
    col: age_group
  - kind: not_null
    col: mother_tongue
  - kind: not_null
    col: labour_status
  - kind: not_null
    col: command_of_foreign_languages
  - kind: row_floor
    min: 29925
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
