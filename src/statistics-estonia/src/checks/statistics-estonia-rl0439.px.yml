# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0439.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, age_group, command_of_foreign_languages, sex]
checks:
  - kind: unique
    columns: [place_of_residence, age_group, command_of_foreign_languages, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: age_group
  - kind: not_null
    col: command_of_foreign_languages
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 102201
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
