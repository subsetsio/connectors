# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tt3539.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, major_group_of_occupations_of_the_last_job]
checks:
  - kind: unique
    columns: [reference_period, major_group_of_occupations_of_the_last_job]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: major_group_of_occupations_of_the_last_job
  - kind: row_floor
    min: 143
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
