# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl514.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, household_composition, number_of_economically_active_members]
checks:
  - kind: unique
    columns: [place_of_residence, household_composition, number_of_economically_active_members]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: household_composition
  - kind: not_null
    col: number_of_economically_active_members
  - kind: row_floor
    min: 27477
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
