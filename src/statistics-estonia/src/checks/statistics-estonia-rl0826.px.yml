# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0826.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, age_and_number_of_children, number_of_rooms_per_inhabitant]
checks:
  - kind: unique
    columns: [place_of_residence, age_and_number_of_children, number_of_rooms_per_inhabitant]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: age_and_number_of_children
  - kind: not_null
    col: number_of_rooms_per_inhabitant
  - kind: row_floor
    min: 18337
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
