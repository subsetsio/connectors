# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-it43.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_individuals, reference_period, seller_s_country_of_location_and_goods_services]
checks:
  - kind: unique
    columns: [group_of_individuals, reference_period, seller_s_country_of_location_and_goods_services]
  - kind: not_null
    col: group_of_individuals
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: seller_s_country_of_location_and_goods_services
  - kind: row_floor
    min: 840
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
