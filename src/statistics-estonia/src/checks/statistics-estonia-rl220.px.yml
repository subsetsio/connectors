# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl220.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citizenship, place_of_residence, main_source_of_subsistence, sex]
checks:
  - kind: unique
    columns: [citizenship, place_of_residence, main_source_of_subsistence, sex]
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: main_source_of_subsistence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 108630
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
