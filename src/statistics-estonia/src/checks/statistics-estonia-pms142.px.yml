# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pms142.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [agricultural_land_field_crop, reference_period, indicator]
checks:
  - kind: unique
    columns: [agricultural_land_field_crop, reference_period, indicator]
  - kind: not_null
    col: agricultural_land_field_crop
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
