# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tt43.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, group_of_persons, duration_of_unemployment]
checks:
  - kind: unique
    columns: [reference_period, group_of_persons, duration_of_unemployment]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: group_of_persons
  - kind: not_null
    col: duration_of_unemployment
  - kind: row_floor
    min: 1072
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
