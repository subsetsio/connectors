# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0618.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, age_group, household_size, extent_of_limitation, sex]
checks:
  - kind: unique
    columns: [place_of_residence, age_group, household_size, extent_of_limitation, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: age_group
  - kind: not_null
    col: household_size
  - kind: not_null
    col: extent_of_limitation
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 139365
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
