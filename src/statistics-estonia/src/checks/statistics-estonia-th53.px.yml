# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-th53.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, labour_status, kind_of_healthcare, access_to_healthcare]
checks:
  - kind: unique
    columns: [reference_period, labour_status, kind_of_healthcare, access_to_healthcare]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: labour_status
  - kind: not_null
    col: kind_of_healthcare
  - kind: not_null
    col: access_to_healthcare
  - kind: row_floor
    min: 330
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
