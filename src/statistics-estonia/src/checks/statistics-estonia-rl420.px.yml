# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl420.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [branch_of_economic_activity, place_of_residence, occupation, sex]
checks:
  - kind: unique
    columns: [branch_of_economic_activity, place_of_residence, occupation, sex]
  - kind: not_null
    col: branch_of_economic_activity
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: occupation
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 74088
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
