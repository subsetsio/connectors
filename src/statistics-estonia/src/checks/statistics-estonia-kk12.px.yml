# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-kk12.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity_emtak_2008_category, indicator, year]
temporal: year
checks:
  - kind: unique
    columns: [economic_activity_emtak_2008_category, indicator, year]
  - kind: not_null
    col: economic_activity_emtak_2008_category
  - kind: not_null
    col: indicator
  - kind: not_null
    col: year
  - kind: row_floor
    min: 10320
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
