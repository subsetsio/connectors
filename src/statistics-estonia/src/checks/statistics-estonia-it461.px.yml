# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-it461.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_individuals, electronic_identification_eid, indicator]
checks:
  - kind: unique
    columns: [group_of_individuals, electronic_identification_eid, indicator]
  - kind: not_null
    col: group_of_individuals
  - kind: not_null
    col: electronic_identification_eid
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
