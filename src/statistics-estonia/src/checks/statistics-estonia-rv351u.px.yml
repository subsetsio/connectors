# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rv351u.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence_of_husband, number_of_common_under_18_year_old_children, year, type_of_settlement_region]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence_of_husband, number_of_common_under_18_year_old_children, year, type_of_settlement_region]
  - kind: not_null
    col: place_of_residence_of_husband
  - kind: not_null
    col: number_of_common_under_18_year_old_children
  - kind: not_null
    col: year
  - kind: not_null
    col: type_of_settlement_region
  - kind: row_floor
    min: 2268
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
