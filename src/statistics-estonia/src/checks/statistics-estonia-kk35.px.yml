# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-kk35.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [payer_economic_activity_emtak_2008, year, environmental_tax]
temporal: year
checks:
  - kind: unique
    columns: [payer_economic_activity_emtak_2008, year, environmental_tax]
  - kind: not_null
    col: payer_economic_activity_emtak_2008
  - kind: not_null
    col: year
  - kind: not_null
    col: environmental_tax
  - kind: row_floor
    min: 2618
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
