# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0214.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, time_of_construction, area_of_dwelling, type_of_building]
checks:
  - kind: unique
    columns: [county, time_of_construction, area_of_dwelling, type_of_building]
  - kind: not_null
    col: county
  - kind: not_null
    col: time_of_construction
  - kind: not_null
    col: area_of_dwelling
  - kind: not_null
    col: type_of_building
  - kind: row_floor
    min: 9660
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
