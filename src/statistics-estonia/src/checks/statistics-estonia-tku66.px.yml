# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tku66.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_enterprises, discussion_with_employees_about_activity_work_management_and_working_conditions, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [group_of_enterprises, discussion_with_employees_about_activity_work_management_and_working_conditions, year, indicator]
  - kind: not_null
    col: group_of_enterprises
  - kind: not_null
    col: discussion_with_employees_about_activity_work_management_and_working_conditions
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 192
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
