# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pm0281.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [agricultural_land_field_crop, reference_period, county_and_agricultural_unit, indicator]
checks:
  - kind: unique
    columns: [agricultural_land_field_crop, reference_period, county_and_agricultural_unit, indicator]
  - kind: not_null
    col: agricultural_land_field_crop
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: county_and_agricultural_unit
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 117576
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
