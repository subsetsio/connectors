# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-kk31.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [source_of_air_emissions_economic_activity_emtak_2008, year, pollutant]
temporal: year
checks:
  - kind: unique
    columns: [source_of_air_emissions_economic_activity_emtak_2008, year, pollutant]
  - kind: not_null
    col: source_of_air_emissions_economic_activity_emtak_2008
  - kind: not_null
    col: year
  - kind: not_null
    col: pollutant
  - kind: row_floor
    min: 9296
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
