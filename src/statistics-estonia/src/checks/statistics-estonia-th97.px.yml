# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-th97.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region_ethnic_nationality_education_health_status, age_group, year]
temporal: year
checks:
  - kind: unique
    columns: [region_ethnic_nationality_education_health_status, age_group, year]
  - kind: not_null
    col: region_ethnic_nationality_education_health_status
  - kind: not_null
    col: age_group
  - kind: not_null
    col: year
  - kind: row_floor
    min: 292
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
