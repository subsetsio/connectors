# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rv288u.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence_of_groom_or_older_spouse, month_of_divorce, year, type_of_settlement_region]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence_of_groom_or_older_spouse, month_of_divorce, year, type_of_settlement_region]
  - kind: not_null
    col: place_of_residence_of_groom_or_older_spouse
  - kind: not_null
    col: month_of_divorce
  - kind: not_null
    col: year
  - kind: not_null
    col: type_of_settlement_region
  - kind: row_floor
    min: 3685
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
