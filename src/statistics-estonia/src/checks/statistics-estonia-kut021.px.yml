# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-kut021.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence_group_of_persons, participation_in_cultural_activities, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence_group_of_persons, participation_in_cultural_activities, year, indicator]
  - kind: not_null
    col: place_of_residence_group_of_persons
  - kind: not_null
    col: participation_in_cultural_activities
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
