# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0722.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, age_and_number_of_children, indicator]
checks:
  - kind: unique
    columns: [place_of_residence, age_and_number_of_children, indicator]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: age_and_number_of_children
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 17248
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
