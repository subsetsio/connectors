# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-td050.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, institutional_sector, type_of_costs]
checks:
  - kind: unique
    columns: [reference_period, institutional_sector, type_of_costs]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: institutional_sector
  - kind: not_null
    col: type_of_costs
  - kind: row_floor
    min: 360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
