# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-mm04.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, county, felling_type, type_of_forest_land, indicator]
temporal: year
checks:
  - kind: unique
    columns: [year, county, felling_type, type_of_forest_land, indicator]
  - kind: not_null
    col: year
  - kind: not_null
    col: county
  - kind: not_null
    col: felling_type
  - kind: not_null
    col: type_of_forest_land
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 39168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
