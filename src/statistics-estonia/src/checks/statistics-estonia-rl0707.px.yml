# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0707.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_and_number_of_household_members, county, household_size, indicator]
checks:
  - kind: unique
    columns: [age_and_number_of_household_members, county, household_size, indicator]
  - kind: not_null
    col: age_and_number_of_household_members
  - kind: not_null
    col: county
  - kind: not_null
    col: household_size
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 7337
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
