# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl21135.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, current_activity_status, mother_tongue, age_group, sex]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence, current_activity_status, mother_tongue, age_group, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: current_activity_status
  - kind: not_null
    col: mother_tongue
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 10692
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
