# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tt440.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, reason_for_not_contacting_the_unemployment_insurance_fund, sex]
checks:
  - kind: unique
    columns: [reference_period, reason_for_not_contacting_the_unemployment_insurance_fund, sex]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: reason_for_not_contacting_the_unemployment_insurance_fund
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 261
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
