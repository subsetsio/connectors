# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0704.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, persons_status_in_household, age_group, legal_marital_status, sex]
checks:
  - kind: unique
    columns: [county, persons_status_in_household, age_group, legal_marital_status, sex]
  - kind: not_null
    col: county
  - kind: not_null
    col: persons_status_in_household
  - kind: not_null
    col: age_group
  - kind: not_null
    col: legal_marital_status
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 62928
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
