# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0329.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [secondary_place_of_residence, socio_economic_status, sex]
checks:
  - kind: unique
    columns: [secondary_place_of_residence, socio_economic_status, sex]
  - kind: not_null
    col: secondary_place_of_residence
  - kind: not_null
    col: socio_economic_status
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 5280
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
