# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0401.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, legal_de_facto_marital_status, place_of_residence, sex]
checks:
  - kind: unique
    columns: [age, legal_de_facto_marital_status, place_of_residence, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: legal_de_facto_marital_status
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 10512
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
