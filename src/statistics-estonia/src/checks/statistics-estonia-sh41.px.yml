# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-sh41.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_persons, mental_and_physical_health_diseases_consumption_of_medicines, year]
temporal: year
checks:
  - kind: unique
    columns: [group_of_persons, mental_and_physical_health_diseases_consumption_of_medicines, year]
  - kind: not_null
    col: group_of_persons
  - kind: not_null
    col: mental_and_physical_health_diseases_consumption_of_medicines
  - kind: not_null
    col: year
  - kind: row_floor
    min: 2220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
