# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rlv439.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [elukoht, voorkeelte_oskus, vanuseruhm, sugu, aasta]
checks:
  - kind: unique
    columns: [elukoht, voorkeelte_oskus, vanuseruhm, sugu, aasta]
  - kind: not_null
    col: elukoht
  - kind: not_null
    col: voorkeelte_oskus
  - kind: not_null
    col: vanuseruhm
  - kind: not_null
    col: sugu
  - kind: not_null
    col: aasta
  - kind: row_floor
    min: 64071
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
