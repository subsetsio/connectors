# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0177.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity_of_the_last_job, county, ethnic_nationality, sex]
checks:
  - kind: unique
    columns: [economic_activity_of_the_last_job, county, ethnic_nationality, sex]
  - kind: not_null
    col: economic_activity_of_the_last_job
  - kind: not_null
    col: county
  - kind: not_null
    col: ethnic_nationality
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 3420
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
