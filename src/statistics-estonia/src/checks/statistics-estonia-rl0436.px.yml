# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0436.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [command_of_foreign_languages, place_of_residence, mother_tongue, sex]
checks:
  - kind: unique
    columns: [command_of_foreign_languages, place_of_residence, mother_tongue, sex]
  - kind: not_null
    col: command_of_foreign_languages
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: mother_tongue
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 4572
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
