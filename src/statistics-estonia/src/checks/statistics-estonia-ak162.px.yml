# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-ak162.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location, day_of_the_week, sex, period]
temporal: period
checks:
  - kind: unique
    columns: [location, day_of_the_week, sex, period]
  - kind: not_null
    col: location
  - kind: not_null
    col: day_of_the_week
  - kind: not_null
    col: sex
  - kind: not_null
    col: period
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
