# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rr057.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, type_of_revenue_expenditure, revenue_and_expenditure]
checks:
  - kind: unique
    columns: [reference_period, type_of_revenue_expenditure, revenue_and_expenditure]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: type_of_revenue_expenditure
  - kind: not_null
    col: revenue_and_expenditure
  - kind: row_floor
    min: 4251
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
