# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pms246.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [labour_force_with_other_gainful_activity_not_related_to_holding, reference_period, indicator]
checks:
  - kind: unique
    columns: [labour_force_with_other_gainful_activity_not_related_to_holding, reference_period, indicator]
  - kind: not_null
    col: labour_force_with_other_gainful_activity_not_related_to_holding
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 14
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
