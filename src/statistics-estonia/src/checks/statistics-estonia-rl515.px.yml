# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl515.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [children_of_different_age_and_number_of_children_of_different_age, place_of_residence, household_size]
checks:
  - kind: unique
    columns: [children_of_different_age_and_number_of_children_of_different_age, place_of_residence, household_size]
  - kind: not_null
    col: children_of_different_age_and_number_of_children_of_different_age
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: household_size
  - kind: row_floor
    min: 17517
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
