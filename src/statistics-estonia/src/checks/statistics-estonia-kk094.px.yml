# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-kk094.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [service, economic_activity_and_institutional_sector]
temporal: year
checks:
  - kind: unique
    columns: [service, economic_activity_and_institutional_sector]
  - kind: not_null
    col: service
  - kind: not_null
    col: economic_activity_and_institutional_sector
  - kind: row_floor
    min: 65
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
