# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl226.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ethnic_nationality, command_of_foreign_languages, citizenship, place_of_residence, sex]
checks:
  - kind: unique
    columns: [ethnic_nationality, command_of_foreign_languages, citizenship, place_of_residence, sex]
  - kind: not_null
    col: ethnic_nationality
  - kind: not_null
    col: command_of_foreign_languages
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 18648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
