# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tku70.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_employees, possibility_to_determine_one_s_work_pace, yera, indicator]
checks:
  - kind: unique
    columns: [group_of_employees, possibility_to_determine_one_s_work_pace, yera, indicator]
  - kind: not_null
    col: group_of_employees
  - kind: not_null
    col: possibility_to_determine_one_s_work_pace
  - kind: not_null
    col: yera
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 288
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
