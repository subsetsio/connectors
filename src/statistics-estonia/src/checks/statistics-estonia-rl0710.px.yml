# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0710.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, main_source_of_subsistence, age_group, household_size, sex]
checks:
  - kind: unique
    columns: [place_of_residence, main_source_of_subsistence, age_group, household_size, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: main_source_of_subsistence
  - kind: not_null
    col: age_group
  - kind: not_null
    col: household_size
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 96822
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
