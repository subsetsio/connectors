# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-it142.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity_emtak_2008, purpose_of_using_social_media, number_of_persons_employed]
temporal: year
checks:
  - kind: unique
    columns: [economic_activity_emtak_2008, purpose_of_using_social_media, number_of_persons_employed]
  - kind: not_null
    col: economic_activity_emtak_2008
  - kind: not_null
    col: purpose_of_using_social_media
  - kind: not_null
    col: number_of_persons_employed
  - kind: row_floor
    min: 342
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
