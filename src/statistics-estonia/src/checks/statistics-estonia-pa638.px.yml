# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pa638.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity, reference_period, sex, type_of_employment_contract]
checks:
  - kind: unique
    columns: [economic_activity, reference_period, sex, type_of_employment_contract]
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: sex
  - kind: not_null
    col: type_of_employment_contract
  - kind: row_floor
    min: 240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
