# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl21814.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, type_and_tenure_status_of_dwelling, time_of_construction_of_building, household_structure]
temporal: year
checks:
  - kind: unique
    columns: [place_of_residence, type_and_tenure_status_of_dwelling, time_of_construction_of_building, household_structure]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: type_and_tenure_status_of_dwelling
  - kind: not_null
    col: time_of_construction_of_building
  - kind: not_null
    col: household_structure
  - kind: row_floor
    min: 360150
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
