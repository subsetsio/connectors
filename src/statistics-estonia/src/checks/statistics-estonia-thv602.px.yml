# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-thv602.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, disability, capacity_for_work, age_group]
checks:
  - kind: unique
    columns: [reference_period, disability, capacity_for_work, age_group]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: disability
  - kind: not_null
    col: capacity_for_work
  - kind: not_null
    col: age_group
  - kind: row_floor
    min: 1944
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
