# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-ko11.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_settlement_administrative_unit, satisfaction_with_living_environment_services, assessment, sex, indicator]
temporal: year
checks:
  - kind: unique
    columns: [type_of_settlement_administrative_unit, satisfaction_with_living_environment_services, assessment, sex, indicator]
  - kind: not_null
    col: type_of_settlement_administrative_unit
  - kind: not_null
    col: satisfaction_with_living_environment_services
  - kind: not_null
    col: assessment
  - kind: not_null
    col: sex
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 21255
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
