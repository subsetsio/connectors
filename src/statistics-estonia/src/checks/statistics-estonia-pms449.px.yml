# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-pms449.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [working_time_percent_of_awu, reference_period, sex]
checks:
  - kind: unique
    columns: [working_time_percent_of_awu, reference_period, sex]
  - kind: not_null
    col: working_time_percent_of_awu
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
