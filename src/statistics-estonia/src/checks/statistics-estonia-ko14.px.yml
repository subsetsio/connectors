# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-ko14.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_settlement_administrative_unit, assessment, satisfaction, net_monthly_income_per_household_member, indicator]
temporal: year
checks:
  - kind: unique
    columns: [type_of_settlement_administrative_unit, assessment, satisfaction, net_monthly_income_per_household_member, indicator]
  - kind: not_null
    col: type_of_settlement_administrative_unit
  - kind: not_null
    col: assessment
  - kind: not_null
    col: satisfaction
  - kind: not_null
    col: net_monthly_income_per_household_member
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 6540
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
