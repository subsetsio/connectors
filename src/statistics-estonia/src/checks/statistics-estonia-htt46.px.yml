# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-htt46.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [training_provider, group_of_persons, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [training_provider, group_of_persons, year, indicator]
  - kind: not_null
    col: training_provider
  - kind: not_null
    col: group_of_persons
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 192
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
