# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0823.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location, occupancy_and_household_structure, type_of_dwelling, indicator]
checks:
  - kind: unique
    columns: [location, occupancy_and_household_structure, type_of_dwelling, indicator]
  - kind: not_null
    col: location
  - kind: not_null
    col: occupancy_and_household_structure
  - kind: not_null
    col: type_of_dwelling
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 13692
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
