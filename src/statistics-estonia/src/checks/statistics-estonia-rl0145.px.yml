# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0145.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, socio_economic_status, main_source_of_subsistence, age_group, sex]
checks:
  - kind: unique
    columns: [county, socio_economic_status, main_source_of_subsistence, age_group, sex]
  - kind: not_null
    col: county
  - kind: not_null
    col: socio_economic_status
  - kind: not_null
    col: main_source_of_subsistence
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 26334
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
