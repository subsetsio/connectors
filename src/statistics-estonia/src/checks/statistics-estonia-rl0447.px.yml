# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0447.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_birth, ability_to_speak_a_dialect, county, age_group, sex]
checks:
  - kind: unique
    columns: [place_of_birth, ability_to_speak_a_dialect, county, age_group, sex]
  - kind: not_null
    col: place_of_birth
  - kind: not_null
    col: ability_to_speak_a_dialect
  - kind: not_null
    col: county
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 73872
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
