# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl105.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location_at_the_moment_of_census, permanent_place_of_residence, sex]
checks:
  - kind: unique
    columns: [location_at_the_moment_of_census, permanent_place_of_residence, sex]
  - kind: not_null
    col: location_at_the_moment_of_census
  - kind: not_null
    col: permanent_place_of_residence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 9936
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
