# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-tdi1623.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [types_of_co_operation, group_of_economic_activities, number_of_persons_employed, reference_period, innovativeness]
checks:
  - kind: unique
    columns: [types_of_co_operation, group_of_economic_activities, number_of_persons_employed, reference_period, innovativeness]
  - kind: not_null
    col: types_of_co_operation
  - kind: not_null
    col: group_of_economic_activities
  - kind: not_null
    col: number_of_persons_employed
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: innovativeness
  - kind: row_floor
    min: 7200
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
