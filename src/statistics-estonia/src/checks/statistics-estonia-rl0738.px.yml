# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0738.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, number_and_age_of_children, type_of_family, indicator]
checks:
  - kind: unique
    columns: [county, number_and_age_of_children, type_of_family, indicator]
  - kind: not_null
    col: county
  - kind: not_null
    col: number_and_age_of_children
  - kind: not_null
    col: type_of_family
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 5750
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
