# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0819.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, comfort_characteristics, household_size, main_heating_option]
checks:
  - kind: unique
    columns: [place_of_residence, comfort_characteristics, household_size, main_heating_option]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: comfort_characteristics
  - kind: not_null
    col: household_size
  - kind: not_null
    col: main_heating_option
  - kind: row_floor
    min: 100408
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
