# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl21707.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_and_number_of_household_members, place_of_residence, household_size, indicator]
temporal: year
checks:
  - kind: unique
    columns: [age_and_number_of_household_members, place_of_residence, household_size, indicator]
  - kind: not_null
    col: age_and_number_of_household_members
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: household_size
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 4752
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
