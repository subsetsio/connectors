# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0333.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_of_birth, duration_of_staying_in_estonia, age_group, sex]
checks:
  - kind: unique
    columns: [country_of_birth, duration_of_staying_in_estonia, age_group, sex]
  - kind: not_null
    col: country_of_birth
  - kind: not_null
    col: duration_of_staying_in_estonia
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 390
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
