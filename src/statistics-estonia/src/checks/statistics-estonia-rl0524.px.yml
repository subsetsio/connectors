# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0524.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, country_of_birth_parents_country_of_birth, place_of_residence, ethnic_nationality, sex]
checks:
  - kind: unique
    columns: [age_group, country_of_birth_parents_country_of_birth, place_of_residence, ethnic_nationality, sex]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: country_of_birth_parents_country_of_birth
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: ethnic_nationality
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 13680
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
