# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl21222.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location, area_of_dwelling, comfort_characteristics, occupancy]
temporal: year
checks:
  - kind: unique
    columns: [location, area_of_dwelling, comfort_characteristics, occupancy]
  - kind: not_null
    col: location
  - kind: not_null
    col: area_of_dwelling
  - kind: not_null
    col: comfort_characteristics
  - kind: not_null
    col: occupancy
  - kind: row_floor
    min: 57330
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
