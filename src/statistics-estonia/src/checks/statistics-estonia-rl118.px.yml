# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl118.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_birth, place_of_residence_in_1989_and_2000, sex]
checks:
  - kind: unique
    columns: [place_of_birth, place_of_residence_in_1989_and_2000, sex]
  - kind: not_null
    col: place_of_birth
  - kind: not_null
    col: place_of_residence_in_1989_and_2000
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 13644
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
