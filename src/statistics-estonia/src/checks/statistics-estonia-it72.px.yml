# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-it72.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [group_of_individuals, reason_for_not_using_generative_ai_tools, indicator]
checks:
  - kind: unique
    columns: [group_of_individuals, reason_for_not_using_generative_ai_tools, indicator]
  - kind: not_null
    col: group_of_individuals
  - kind: not_null
    col: reason_for_not_using_generative_ai_tools
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 150
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
