# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl210.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, number_of_children_given_birth_to, legal_marital_status]
checks:
  - kind: unique
    columns: [place_of_residence, number_of_children_given_birth_to, legal_marital_status]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: number_of_children_given_birth_to
  - kind: not_null
    col: legal_marital_status
  - kind: row_floor
    min: 16470
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
