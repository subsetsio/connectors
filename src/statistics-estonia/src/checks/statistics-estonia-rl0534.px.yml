# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0534.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, age_group, number_of_live_born_children, native_and_foreign_origin_population]
checks:
  - kind: unique
    columns: [county, age_group, number_of_live_born_children, native_and_foreign_origin_population]
  - kind: not_null
    col: county
  - kind: not_null
    col: age_group
  - kind: not_null
    col: number_of_live_born_children
  - kind: not_null
    col: native_and_foreign_origin_population
  - kind: row_floor
    min: 9576
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
