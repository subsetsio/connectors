# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-shl030.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [birth_cohort, occurrence_of_long_term_health_problem, indicator, year]
temporal: year
checks:
  - kind: unique
    columns: [birth_cohort, occurrence_of_long_term_health_problem, indicator, year]
  - kind: not_null
    col: birth_cohort
  - kind: not_null
    col: occurrence_of_long_term_health_problem
  - kind: not_null
    col: indicator
  - kind: not_null
    col: year
  - kind: row_floor
    min: 54
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
