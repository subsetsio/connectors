# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl205.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, legal_marital_status, age, ethnic_nationality, de_facto_marital_status, sex]
checks:
  - kind: unique
    columns: [place_of_residence, legal_marital_status, age, ethnic_nationality, de_facto_marital_status, sex]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: legal_marital_status
  - kind: not_null
    col: age
  - kind: not_null
    col: ethnic_nationality
  - kind: not_null
    col: de_facto_marital_status
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 79200
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
