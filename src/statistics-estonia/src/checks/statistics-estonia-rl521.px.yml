# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl521.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, children_of_different_age]
checks:
  - kind: unique
    columns: [place_of_residence, children_of_different_age]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: children_of_different_age
  - kind: row_floor
    min: 5295
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
