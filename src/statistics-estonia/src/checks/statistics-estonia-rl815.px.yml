# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl815.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_residence, type_and_tenure_status_of_dwelling, household_size]
checks:
  - kind: unique
    columns: [place_of_residence, type_and_tenure_status_of_dwelling, household_size]
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: type_and_tenure_status_of_dwelling
  - kind: not_null
    col: household_size
  - kind: row_floor
    min: 2964
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
