# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-les23.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, group_of_persons, poverty_threshold, indicator]
checks:
  - kind: unique
    columns: [reference_period, group_of_persons, poverty_threshold, indicator]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: group_of_persons
  - kind: not_null
    col: poverty_threshold
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2912
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
