# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-ku63.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, county, field_of_activity, national_origin]
temporal: year
checks:
  - kind: unique
    columns: [year, county, field_of_activity, national_origin]
  - kind: not_null
    col: year
  - kind: not_null
    col: county
  - kind: not_null
    col: field_of_activity
  - kind: not_null
    col: national_origin
  - kind: row_floor
    min: 4590
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
