# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-raa0052.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, component_economic_activity_emtak_2008, region, indicator]
temporal: year
checks:
  - kind: unique
    columns: [year, component_economic_activity_emtak_2008, region, indicator]
  - kind: not_null
    col: year
  - kind: not_null
    col: component_economic_activity_emtak_2008
  - kind: not_null
    col: region
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2970
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
