# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0216.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [county, number_of_rooms, area_of_dwelling, owner, occupancy]
checks:
  - kind: unique
    columns: [county, number_of_rooms, area_of_dwelling, owner, occupancy]
  - kind: not_null
    col: county
  - kind: not_null
    col: number_of_rooms
  - kind: not_null
    col: area_of_dwelling
  - kind: not_null
    col: owner
  - kind: not_null
    col: occupancy
  - kind: row_floor
    min: 46368
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
