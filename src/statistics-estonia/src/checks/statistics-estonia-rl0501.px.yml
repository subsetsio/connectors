# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl0501.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_of_birth_region, place_of_residence, sex]
checks:
  - kind: unique
    columns: [country_of_birth_region, place_of_residence, sex]
  - kind: not_null
    col: country_of_birth_region
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1467
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
