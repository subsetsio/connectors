# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-ht290.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [reference_period, education_level, type_of_institution, indicator]
checks:
  - kind: unique
    columns: [reference_period, education_level, type_of_institution, indicator]
  - kind: not_null
    col: reference_period
  - kind: not_null
    col: education_level
  - kind: not_null
    col: type_of_institution
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 1584
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
