# compiled by `hardened compile-checks` — post-publish audit of `statistics-estonia-rl21166.px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity, place_of_residence, location_of_place_of_work, sex]
temporal: year
checks:
  - kind: unique
    columns: [economic_activity, place_of_residence, location_of_place_of_work, sex]
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: place_of_residence
  - kind: not_null
    col: location_of_place_of_work
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 38478
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
