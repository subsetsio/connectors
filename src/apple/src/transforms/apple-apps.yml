# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `apple-apps`. Regenerate after model-verify.
# caution: Rows are chart snapshots, not a catalog of all apps; the same app can appear in multiple storefronts, feed types, and snapshots.
key: [snapshot_date, storefront, feed_type, rank]
temporal: snapshot_date
sort: [snapshot_date, storefront, feed_type, rank]
columns:
  - name: snapshot_date
    type: DATE
  - name: feed_updated
    type: VARCHAR
  - name: storefront
    type: VARCHAR
  - name: media
    type: VARCHAR
  - name: feed_type
    type: VARCHAR
  - name: rank
    type: BIGINT
  - name: entity_id
    type: BIGINT
  - name: name
    type: VARCHAR
  - name: artist_name
    type: VARCHAR
  - name: artist_id
    type: JSON
  - name: artist_url
    type: JSON
  - name: kind
    type: VARCHAR
  - name: release_date
    type: DATE
  - name: content_advisory_rating
    type: JSON
  - name: genre_names
    type: "VARCHAR[]"
  - name: artwork_url
    type: VARCHAR
  - name: url
    type: VARCHAR
