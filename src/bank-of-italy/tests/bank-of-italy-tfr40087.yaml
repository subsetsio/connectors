spec_id: bank-of-italy-tfr40087
status: active
tests:
  - not_null: series_id
  - not_null: date
  - row_count: {min: 1}
  - coverage: {col: series_id, expected: 3}
    reason: collect enumerated this table's member cubes; every one must land - a shortfall is silent partial loss, not a successful run
    certainty: 95
  - freshness: {col: date, reaches: today - 1500d}
    reason: no coverage end advertised for this table; bound is the observed worst-case publication lag for unknown-frequency BDS tables
    certainty: 70
    points_outward: true
  - nullable: {col: value, max_null_frac: 0.05}
    reason: VALORE is populated on essentially every observation; a spike of nulls means the value column moved
    certainty: 80
    severity: warn
