# compiled by `hardened compile-checks` — post-publish audit of `bundesagentur-f-r-arbeit-stea-current`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [metric, month, period_label, date, year]
temporal: date
checks:
  - kind: unique
    columns: [metric, month, period_label, date, year]
  - kind: not_null
    col: metric
  - kind: not_null
    col: month
  - kind: not_null
    col: period_label
  - kind: not_null
    col: date
  - kind: not_null
    col: year
  - kind: row_floor
    min: 91
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
