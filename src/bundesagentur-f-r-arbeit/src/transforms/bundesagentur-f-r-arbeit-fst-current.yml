# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bundesagentur-f-r-arbeit-fst-current`. Regenerate after model-verify.
# caution: This is a current-period extract for Foerderung/berufliche Rehabilitation; use the corresponding timeseries table for historical trend analysis.
# caution: Rows are split by funding group in group_code/group_name; filter or aggregate those groups deliberately before comparing totals.
key: [metric, group_code, group_name, period_label, date]
temporal: date
sort: [date, metric, group_code, group_name, period_label]
columns:
  - name: metric
    type: VARCHAR
  - name: period_label
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: month
    type: INTEGER
  - name: date
    type: DATE
  - name: group_code
    type: VARCHAR
  - name: group_name
    type: VARCHAR
  - name: value
    type: DOUBLE
