# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `psmsl-rlr-monthly`. Regenerate after model-verify.
# caution: Rows are distinguished by PSMSL data and missing-data flags as well as station and month, so filter or group flags deliberately when building station-month series.
key: [station_id, year, missing_flag, month, data_flag]
temporal: year
sort: [year, station_id, missing_flag, month, data_flag]
columns:
  - name: station_id
    type: INTEGER
  - name: decimal_year
    type: DOUBLE
  - name: year
    type: INTEGER
  - name: month
    type: INTEGER
  - name: msl_mm
    type: DOUBLE
  - name: missing_flag
    type: VARCHAR
  - name: data_flag
    type: VARCHAR
