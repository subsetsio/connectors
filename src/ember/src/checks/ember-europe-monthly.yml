# compiled by `hardened compile-checks` — post-publish audit of `ember-europe-monthly`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area, date, category, subcategory, variable, unit]
temporal: date
checks:
  - kind: unique
    columns: [area, date, category, subcategory, variable, unit]
  - kind: not_null
    col: area
  - kind: not_null
    col: date
  - kind: not_null
    col: category
  - kind: not_null
    col: subcategory
  - kind: not_null
    col: variable
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 117295
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
