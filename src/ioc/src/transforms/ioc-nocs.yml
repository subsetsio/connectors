# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ioc-nocs`. Regenerate after model-verify.
key: [edition, noc_code]
sort: [edition, noc_code]
columns:
  - name: edition
    type: VARCHAR
  - name: games
    type: VARCHAR
  - name: noc_code
    type: VARCHAR
  - name: source_url
    type: VARCHAR
  - name: archive_timestamp
    type: BIGINT
  - name: payload
    type: "STRUCT(nocBio STRUCT(organisationId VARCHAR, isHistoric BOOLEAN, anthem STRUCT(title VARCHAR, composer VARCHAR, inducted VARCHAR), membership STRUCT(officialNocName VARCHAR), interest STRUCT(ocFlagBearer VARCHAR[], highlights VARCHAR), officials STRUCT(nocPresident VARCHAR), participation STRUCT(firstOGAppearance VARCHAR, numOGAppearance VARCHAR), ocFlagBearers STRUCT(code VARCHAR, TVName VARCHAR, \"name\" VARCHAR, shortName VARCHAR, givenName VARCHAR, familyName VARCHAR, birthDate DATE, height BIGINT, image STRUCT(imageType VARCHAR, imageExtension VARCHAR, imageVersion VARCHAR), mainFunction STRUCT(functionCode VARCHAR, category VARCHAR, description VARCHAR), personGender STRUCT(code VARCHAR, description VARCHAR), disciplines STRUCT(code VARCHAR, description VARCHAR, descriptionOrder BIGINT)[], organisation STRUCT(code VARCHAR, description VARCHAR, longDescription VARCHAR, protocolOrder BIGINT, descriptionOrder BIGINT, longDescriptionOrder BIGINT))[]))"
