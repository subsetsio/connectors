# compiled by `hardened compile-checks` — post-publish audit of `un-habitat-dd5f3698e17942d5b44f573bd6d41f79`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [source_row_number, source_name, ciudad]
checks:
  - kind: unique
    columns: [source_row_number, source_name, ciudad]
  - kind: not_null
    col: source_row_number
  - kind: not_null
    col: source_name
  - kind: not_null
    col: ciudad
  - kind: row_floor
    min: 2126
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
