# compiled by `hardened compile-checks` — post-publish audit of `mpa-singapore-d-835d43b9238c6fc877dfcd62d73054a9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, cargo_type_primary, cargo_type_secondary]
temporal: date
checks:
  - kind: unique
    columns: [date, cargo_type_primary, cargo_type_secondary]
  - kind: not_null
    col: date
  - kind: not_null
    col: cargo_type_primary
  - kind: not_null
    col: cargo_type_secondary
  - kind: row_floor
    min: 754
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
