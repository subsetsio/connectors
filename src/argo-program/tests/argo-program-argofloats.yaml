spec_id: argo-program-argofloats
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["ArgoFloats"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 50}
    reason: ArgoFloats ERDDAP metadata lists dozens of variables and attributes
    certainty: 95
