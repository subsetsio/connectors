spec_id: argo-program-argofloats-index
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["ArgoFloats-index"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 8}
    reason: the profile index exposes eight documented ERDDAP variables plus attributes
    certainty: 95
