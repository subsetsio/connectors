spec_id: argo-program-oacp-argo-global
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["OACP-Argo-Global"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 20}
    reason: the OACP global grid exposes pycnocline and mode-water variables
    certainty: 95
