spec_id: argo-program-argofloats-reference
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["ArgoFloats-reference"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 12}
    reason: the reference profile dataset exposes documented ERDDAP variables plus attributes
    certainty: 95
