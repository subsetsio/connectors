spec_id: argo-program-pcm-argo
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["PCM-Argo"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 100}
    reason: the PCM-Argo table exposes classification variables plus documented attributes
    certainty: 95
