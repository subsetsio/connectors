spec_id: argo-program-oacp-argo-natl
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["OACP-Argo-NATL"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 150}
    reason: the OACP North Atlantic grid exposes pycnocline and mode-water variables plus attributes
    certainty: 95
