spec_id: argo-program-argofloats-synthetic-bgc
status: active
tests:
  - not_null: dataset_id
  - not_null: variable_name
  - enum: {col: dataset_id, values: ["ArgoFloats-synthetic-BGC"]}
  - enum: {col: row_type, values: ["variable", "attribute"]}
  - row_count: {min: 100}
    reason: the BGC synthetic profile dataset exposes many biogeochemical variables
    certainty: 95
