-- compiled by `hardened compile-transforms` from the measured model
-- profiles (model/tables + columns). Faithful pass-through: verified
-- pure casts only, no data fixes. Regenerate after model-verify;
-- durable edits belong in the model stage, not here.
-- caution: Stop-level records for a single published jurisdiction; available fields, raw_* columns, and local coding systems vary by jurisdiction, so compare like columns carefully across tables.
SELECT
    "_source_entity_id" AS source_entity_id,
    "_source_file" AS source_file,
    "_source_member" AS source_member,
    "_row_number" AS row_number,
    "raw_row_number",
    strptime("date", '%Y-%m-%d')::DATE AS date,
    "time",
    "location",
    CAST("lat" AS DOUBLE) AS lat,
    CAST("lng" AS DOUBLE) AS lng,
    "precinct",
    CAST("reporting_area" AS BIGINT) AS reporting_area,
    "zone",
    CAST("subject_age" AS BIGINT) AS subject_age,
    "subject_race",
    "subject_sex",
    "officer_id_hash",
    "type",
    "violation",
    CAST("arrest_made" AS BOOLEAN) AS arrest_made,
    CAST("citation_issued" AS BOOLEAN) AS citation_issued,
    CAST("warning_issued" AS BOOLEAN) AS warning_issued,
    "outcome",
    CAST("contraband_found" AS BOOLEAN) AS contraband_found,
    CAST("contraband_drugs" AS BOOLEAN) AS contraband_drugs,
    CAST("contraband_weapons" AS BOOLEAN) AS contraband_weapons,
    CAST("frisk_performed" AS BOOLEAN) AS frisk_performed,
    CAST("search_conducted" AS BOOLEAN) AS search_conducted,
    CAST("search_person" AS BOOLEAN) AS search_person,
    CAST("search_vehicle" AS BOOLEAN) AS search_vehicle,
    "search_basis",
    "reason_for_stop",
    "vehicle_registration_state",
    "notes",
    CAST("raw_verbal_warning_issued" AS BOOLEAN) AS raw_verbal_warning_issued,
    CAST("raw_written_warning_issued" AS BOOLEAN) AS raw_written_warning_issued,
    CAST("raw_traffic_citation_issued" AS BOOLEAN) AS raw_traffic_citation_issued,
    CAST("raw_misd_state_citation_issued" AS BOOLEAN) AS raw_misd_state_citation_issued,
    "raw_suspect_ethnicity",
    CAST("raw_driver_searched" AS BOOLEAN) AS raw_driver_searched,
    CAST("raw_passenger_searched" AS BOOLEAN) AS raw_passenger_searched,
    CAST("raw_search_consent" AS BOOLEAN) AS raw_search_consent,
    CAST("raw_search_arrest" AS BOOLEAN) AS raw_search_arrest,
    CAST("raw_search_warrant" AS BOOLEAN) AS raw_search_warrant,
    CAST("raw_search_inventory" AS BOOLEAN) AS raw_search_inventory,
    CAST("raw_search_plain_view" AS BOOLEAN) AS raw_search_plain_view
FROM "stanford-open-policing-project-tn-nashville"
