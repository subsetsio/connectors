# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-w2w4-tdxt`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [new_york_state_agency_occupation_title, occupation_code, occupation_title, street_address, agency]
checks:
  - kind: unique
    columns: [new_york_state_agency_occupation_title, occupation_code, occupation_title, street_address, agency]
  - kind: not_null
    col: new_york_state_agency_occupation_title
  - kind: not_null
    col: occupation_code
  - kind: not_null
    col: occupation_title
  - kind: not_null
    col: street_address
  - kind: not_null
    col: agency
  - kind: row_floor
    min: 83
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
