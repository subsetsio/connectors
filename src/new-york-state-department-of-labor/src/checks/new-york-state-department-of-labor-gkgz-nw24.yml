# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-gkgz-nw24`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [entry_wage, mean, median, occupational_title, standard_occupational_code]
checks:
  - kind: unique
    columns: [entry_wage, mean, median, occupational_title, standard_occupational_code]
  - kind: not_null
    col: entry_wage
  - kind: not_null
    col: mean
  - kind: not_null
    col: median
  - kind: not_null
    col: occupational_title
  - kind: not_null
    col: standard_occupational_code
  - kind: row_floor
    min: 3518
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
