# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-qkrk-6v78`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [average_duration, county, year, month, region]
checks:
  - kind: unique
    columns: [average_duration, county, year, month, region]
  - kind: not_null
    col: average_duration
  - kind: not_null
    col: county
  - kind: not_null
    col: year
  - kind: not_null
    col: month
  - kind: not_null
    col: region
  - kind: row_floor
    min: 10548
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
