# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-83wd-skw3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [license_number, issued_date, first_name, expiration_date, class]
temporal: issued_date
checks:
  - kind: unique
    columns: [license_number, issued_date, first_name, expiration_date, class]
  - kind: not_null
    col: license_number
  - kind: not_null
    col: issued_date
  - kind: not_null
    col: first_name
  - kind: not_null
    col: expiration_date
  - kind: not_null
    col: class
  - kind: row_floor
    min: 214
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
