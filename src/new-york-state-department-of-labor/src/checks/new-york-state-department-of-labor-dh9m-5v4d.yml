# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-dh9m-5v4d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area, year, month]
checks:
  - kind: unique
    columns: [area_type, area, year, month]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: month
  - kind: row_floor
    min: 3733
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
