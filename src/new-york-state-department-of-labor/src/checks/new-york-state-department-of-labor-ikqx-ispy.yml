# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-ikqx-ispy`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [license_number, license_type, business_name, issued_date, expiration_date]
temporal: issued_date
checks:
  - kind: unique
    columns: [license_number, license_type, business_name, issued_date, expiration_date]
  - kind: not_null
    col: license_number
  - kind: not_null
    col: license_type
  - kind: not_null
    col: business_name
  - kind: not_null
    col: issued_date
  - kind: not_null
    col: expiration_date
  - kind: row_floor
    min: 1224
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
