# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-jrac-r9vc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [address, license_number, business_name, zip_code, issued_date]
temporal: issued_date
checks:
  - kind: unique
    columns: [address, license_number, business_name, zip_code, issued_date]
  - kind: not_null
    col: address
  - kind: not_null
    col: license_number
  - kind: not_null
    col: business_name
  - kind: not_null
    col: zip_code
  - kind: not_null
    col: issued_date
  - kind: row_floor
    min: 149
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
