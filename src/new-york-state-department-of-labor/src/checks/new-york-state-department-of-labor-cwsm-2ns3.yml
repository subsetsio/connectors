# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-cwsm-2ns3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area, year, quarter, ownership, naics]
checks:
  - kind: unique
    columns: [area_type, area, year, quarter, ownership, naics]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: quarter
  - kind: not_null
    col: ownership
  - kind: not_null
    col: naics
  - kind: row_floor
    min: 2317703
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
