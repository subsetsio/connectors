# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-department-of-labor-i4jv-zkey`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [certificate_number, zip_code, city, issued_date, expiration_date]
temporal: issued_date
checks:
  - kind: unique
    columns: [certificate_number, zip_code, city, issued_date, expiration_date]
  - kind: not_null
    col: certificate_number
  - kind: not_null
    col: zip_code
  - kind: not_null
    col: city
  - kind: not_null
    col: issued_date
  - kind: not_null
    col: expiration_date
  - kind: row_floor
    min: 7004
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
