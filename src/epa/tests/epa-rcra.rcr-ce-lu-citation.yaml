# rcra.rcr_ce_lu_citation -- Envirofacts table pulled in full via [first]:[last] row-windows.
# Envirofacts serves 401 live tables across 13 program systems; this table's column
# types are not published (no OpenAPI spec) and the API infers them per row-window,
# so the schema is genuinely unknown until the first pull. Assertions are authored
# from the first pulled chunk at the model stage, certainty-discounted.
spec_id: epa-rcra.rcr-ce-lu-citation
status: deferred
