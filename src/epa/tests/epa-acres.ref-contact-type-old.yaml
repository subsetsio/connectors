# acres.ref_contact_type_old -- Envirofacts table pulled in full via [first]:[last] row-windows.
# Upstream returns a permanent HTTP 404 (table retired). The fetch fn records a
# TTL-bound skipped marker; no raw lands, so there is nothing to assert yet.
spec_id: epa-acres.ref-contact-type-old
status: deferred
