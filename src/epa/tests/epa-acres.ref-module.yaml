# acres.ref_module -- Envirofacts table pulled in full via [first]:[last] row-windows.
# Probed empty upstream (0 rows at any row-window). Assertions are authored
# from the first pulled chunk once/if the table carries rows.
spec_id: epa-acres.ref-module
status: deferred
