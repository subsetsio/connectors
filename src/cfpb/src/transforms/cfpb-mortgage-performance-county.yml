# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cfpb-mortgage-performance-county`. Regenerate after model-verify.
# caution: County rows are separate from state and metro tables; do not combine geographic levels without selecting one level.
key: [fips_code, region_name, period, bucket]
temporal: period
sort: [period, fips_code, region_name, bucket]
columns:
  - name: region_type
    type: VARCHAR
  - name: region_name
    type: VARCHAR
  - name: fips_code
    type: VARCHAR
  - name: bucket
    type: VARCHAR
  - name: period
    type: DATE
  - name: delinquency_rate
    type: DOUBLE
