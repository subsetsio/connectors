# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cfpb-mortgage-performance-state`. Regenerate after model-verify.
# caution: State rows are separate from county and metro tables; do not combine geographic levels without selecting one level.
key: [period, region_name, fips_code, bucket]
temporal: period
sort: [period, region_name, fips_code, bucket]
columns:
  - name: region_type
    type: VARCHAR
  - name: region_name
    type: VARCHAR
  - name: fips_code
    type: VARCHAR
  - name: bucket
    type: VARCHAR
  - name: period
    type: DATE
  - name: delinquency_rate
    type: DOUBLE
