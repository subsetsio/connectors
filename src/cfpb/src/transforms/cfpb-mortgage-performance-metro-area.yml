# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cfpb-mortgage-performance-metro-area`. Regenerate after model-verify.
# caution: Metro and non-metro rows are separate from county and state tables; do not combine geographic levels without selecting one level.
key: [region_name, fips_code, period, region_type, bucket]
temporal: period
sort: [period, region_name, fips_code, region_type, bucket]
columns:
  - name: region_type
    type: VARCHAR
  - name: region_name
    type: VARCHAR
  - name: fips_code
    type: VARCHAR
  - name: bucket
    type: VARCHAR
  - name: period
    type: DATE
  - name: delinquency_rate
    type: DOUBLE
