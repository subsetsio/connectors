{
  "cadence_days": 7,
  "paused": false,
  "cadence_note": "Weekly full refresh. SingStat Table Builder tables carry source-specific update frequencies and dataLastUpdated metadata, but the API has no global since/modifiedAfter cursor; weekly polling catches daily/monthly releases without relying on per-table incremental state."
}
