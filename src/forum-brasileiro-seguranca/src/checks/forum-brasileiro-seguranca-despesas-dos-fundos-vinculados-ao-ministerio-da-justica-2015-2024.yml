# compiled by `hardened compile-checks` — post-publish audit of `forum-brasileiro-seguranca-despesas-dos-fundos-vinculados-ao-ministerio-da-justica-2015-2024`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, geography, measure]
temporal: year
checks:
  - kind: unique
    columns: [year, geography, measure]
  - kind: not_null
    col: year
  - kind: not_null
    col: geography
  - kind: not_null
    col: measure
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
