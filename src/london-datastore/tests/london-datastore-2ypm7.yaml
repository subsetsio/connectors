spec_id: london-datastore-2ypm7
status: active
tests:
  - row_count: {min: 1}
    reason: each rank-active package exposes a single CSV resource with at least one data row; an empty publish means the resource stopped serving real CSV
    certainty: 90
    points_outward: true
    severity: block
