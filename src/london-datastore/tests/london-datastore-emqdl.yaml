spec_id: london-datastore-emqdl
status: active
tests:
  - row_count: {min: 1}
    reason: each accepted package has at least one supported tabular resource with data rows
    certainty: 75
    points_outward: true
    severity: block
