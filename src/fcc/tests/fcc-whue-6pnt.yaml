spec_id: fcc-whue-6pnt
status: deferred
coverage:
  frozen: true
  reason: >-
    Fixed Broadband Deployment (FCC Form 477) fixed release for the December 2019 filing period. The FCC publishes each
    Form 477 filing period as its own immutable dataset rather than appending to a
    running table, so this corpus stopped advancing when the December 2019 release was
    finalised and carries no freshness assertion by design; a later period arrives
    as a new dataset id, which collect surfaces as a new entity.
