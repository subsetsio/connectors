# compiled by `hardened compile-checks` — post-publish audit of `swiss-national-bank-bsta@snb.mona-u.bil.pas.vke.kov`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period_start, period, series_label, dimensions_json, series_key]
temporal: period
checks:
  - kind: unique
    columns: [period_start, period, series_label, dimensions_json, series_key]
  - kind: not_null
    col: period_start
  - kind: not_null
    col: period
  - kind: not_null
    col: series_label
  - kind: not_null
    col: dimensions_json
  - kind: not_null
    col: series_key
  - kind: row_floor
    min: 41851
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
