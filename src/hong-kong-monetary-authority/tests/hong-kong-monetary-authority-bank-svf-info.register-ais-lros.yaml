spec_id: hong-kong-monetary-authority-bank-svf-info.register-ais-lros
status: active
tests:
  - row_count: {min: 1}
    reason: every HKMA Open API endpoint in scope returns at least one record; an empty table means the endpoint changed shape or started requiring a new parameter.
    certainty: 95
    severity: block
