# hand-authored 2026-07-26 — post-publish audit of `unctad-us.iff-trademisinvoicing`.
# Row floor from the measured bulk CSV (392 rows, 2011-2024). Regenerate via
# `compile-checks` after the next model-verify.
temporal: year
checks:
  - kind: row_floor
    min: 196
regression:
  row_shrink_tolerance: 0.1
  temporal_monotonic: true
