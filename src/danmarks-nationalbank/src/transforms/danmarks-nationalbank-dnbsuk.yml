# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `danmarks-nationalbank-dnbsuk`. Regenerate after model-verify.
key: [value, kortype, korttek, time]
temporal: time
sort: [time, value, kortype, korttek]
columns:
  - name: kortype
    type: VARCHAR
  - name: korttek
    type: VARCHAR
  - name: time
    type: VARCHAR
  - name: value
    type: VARCHAR
