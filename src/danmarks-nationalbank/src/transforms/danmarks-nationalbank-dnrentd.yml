# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `danmarks-nationalbank-dnrentd`. Regenerate after model-verify.
# caution: Raw StatBank extract contains duplicate rows even across all emitted fields; treat rows as source observations and aggregate with care.
key: [time, value, instrument, opgoer]
temporal: time
sort: [time, value, instrument, opgoer]
columns:
  - name: instrument
    type: VARCHAR
  - name: land
    type: VARCHAR
  - name: opgoer
    type: VARCHAR
  - name: time
    type: VARCHAR
  - name: value
    type: VARCHAR
