# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `danmarks-nationalbank-dnpnogl`. Regenerate after model-verify.
# caution: Raw StatBank extract contains duplicate rows even across all emitted fields; treat rows as source observations and aggregate with care.
key: [value, time, aktp, sekkornat]
temporal: time
sort: [time, value, aktp, sekkornat]
columns:
  - name: aktp
    type: VARCHAR
  - name: rapinst
    type: VARCHAR
  - name: sekkornat
    type: VARCHAR
  - name: time
    type: VARCHAR
  - name: value
    type: VARCHAR
