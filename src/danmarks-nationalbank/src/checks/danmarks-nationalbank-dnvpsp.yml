# provisional post-publish audit for `danmarks-nationalbank-dnvpsp`.
key: [value, data, papir, l_betid2, invsektor, valuta, time]
temporal: time
checks:
  - kind: unique
    columns: [value, data, papir, l_betid2, invsektor, valuta, time]
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
