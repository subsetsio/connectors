# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnbsk1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, transakt1, kortudsted, data, time]
temporal: time
checks:
  - kind: unique
    columns: [value, transakt1, kortudsted, data, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: transakt1
  - kind: not_null
    col: kortudsted
  - kind: not_null
    col: data
  - kind: not_null
    col: time
  - kind: row_floor
    min: 24624
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
