# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnfphen`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, virktypnb, vaerdi1, balpostnat1, time]
temporal: time
checks:
  - kind: unique
    columns: [value, virktypnb, vaerdi1, balpostnat1, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: virktypnb
  - kind: not_null
    col: vaerdi1
  - kind: not_null
    col: balpostnat1
  - kind: not_null
    col: time
  - kind: row_floor
    min: 3030
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
