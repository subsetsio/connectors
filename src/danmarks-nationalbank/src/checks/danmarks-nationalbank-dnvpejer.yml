# provisional post-publish audit for `danmarks-nationalbank-dnvpejer`.
key: [value, isin, time]
temporal: time
checks:
  - kind: unique
    columns: [value, isin, time]
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
