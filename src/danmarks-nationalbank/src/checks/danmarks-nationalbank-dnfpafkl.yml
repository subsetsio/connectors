# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnfpafkl`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, virktypnb, vaerdi1, investnat, sektornat, land, time]
temporal: time
checks:
  - kind: unique
    columns: [value, virktypnb, vaerdi1, investnat, sektornat, land, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: virktypnb
  - kind: not_null
    col: vaerdi1
  - kind: not_null
    col: investnat
  - kind: not_null
    col: sektornat
  - kind: not_null
    col: land
  - kind: not_null
    col: time
  - kind: row_floor
    min: 897357
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
