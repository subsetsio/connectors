# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnvp2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, data, papir, invsektor, valuta, udstland1, time]
temporal: time
checks:
  - kind: unique
    columns: [value, data, papir, invsektor, valuta, udstland1, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: data
  - kind: not_null
    col: papir
  - kind: not_null
    col: invsektor
  - kind: not_null
    col: valuta
  - kind: not_null
    col: udstland1
  - kind: not_null
    col: time
  - kind: row_floor
    min: 2589300
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
