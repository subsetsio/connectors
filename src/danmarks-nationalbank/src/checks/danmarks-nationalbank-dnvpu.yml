# provisional post-publish audit for `danmarks-nationalbank-dnvpu`.
key: [value, papir, kupon, valuta, l_betid, udstedsektor, udstland, vaerdian, data, time]
temporal: time
checks:
  - kind: unique
    columns: [value, papir, kupon, valuta, l_betid, udstedsektor, udstland, vaerdian, data, time]
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
