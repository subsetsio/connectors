# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnruddkb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, datat, valuta, branche, time]
temporal: time
checks:
  - kind: unique
    columns: [value, datat, valuta, branche, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: datat
  - kind: not_null
    col: valuta
  - kind: not_null
    col: branche
  - kind: not_null
    col: time
  - kind: row_floor
    min: 21114
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
