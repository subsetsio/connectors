# compiled by `hardened compile-checks` — post-publish audit of `danmarks-nationalbank-dnbald`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, disaggbalpost, data, rapinst, time]
temporal: time
checks:
  - kind: unique
    columns: [value, disaggbalpost, data, rapinst, time]
  - kind: not_null
    col: value
  - kind: not_null
    col: disaggbalpost
  - kind: not_null
    col: data
  - kind: not_null
    col: rapinst
  - kind: not_null
    col: time
  - kind: row_floor
    min: 40464
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
