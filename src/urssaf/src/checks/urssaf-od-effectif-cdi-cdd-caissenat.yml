# compiled by `hardened compile-checks` — post-publish audit of `urssaf-od-effectif-cdi-cdd-caissenat`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date_dernier_jour_du_mois, code_region, region, sexe]
temporal: date_dernier_jour_du_mois
checks:
  - kind: unique
    columns: [date_dernier_jour_du_mois, code_region, region, sexe]
  - kind: not_null
    col: date_dernier_jour_du_mois
  - kind: not_null
    col: code_region
  - kind: not_null
    col: region
  - kind: not_null
    col: sexe
  - kind: row_floor
    min: 616
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
