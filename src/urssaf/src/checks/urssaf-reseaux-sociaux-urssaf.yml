# compiled by `hardened compile-checks` — post-publish audit of `urssaf-reseaux-sociaux-urssaf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [url_reseau, libelle_organisme, reseau_social, services]
checks:
  - kind: unique
    columns: [url_reseau, libelle_organisme, reseau_social, services]
  - kind: not_null
    col: url_reseau
  - kind: not_null
    col: libelle_organisme
  - kind: not_null
    col: reseau_social
  - kind: not_null
    col: services
  - kind: row_floor
    min: 24
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
