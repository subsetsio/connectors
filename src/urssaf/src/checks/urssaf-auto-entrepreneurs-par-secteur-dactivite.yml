# compiled by `hardened compile-checks` — post-publish audit of `urssaf-auto-entrepreneurs-par-secteur-dactivite`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dernier_jour_du_trimestre, secteur_d_activite_me, annee]
temporal: annee
checks:
  - kind: unique
    columns: [dernier_jour_du_trimestre, secteur_d_activite_me, annee]
  - kind: not_null
    col: dernier_jour_du_trimestre
  - kind: not_null
    col: secteur_d_activite_me
  - kind: not_null
    col: annee
  - kind: row_floor
    min: 1188
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
