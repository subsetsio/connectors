# compiled by `hardened compile-checks` — post-publish audit of `urssaf-masse-salariale-et-assiette-chomage-partiel-mensuelles-secteur-prive-par-region`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dernier_jour_du_mois, region, code_region]
temporal: dernier_jour_du_mois
checks:
  - kind: unique
    columns: [dernier_jour_du_mois, region, code_region]
  - kind: not_null
    col: dernier_jour_du_mois
  - kind: not_null
    col: region
  - kind: not_null
    col: code_region
  - kind: row_floor
    min: 841
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
