# compiled by `hardened compile-checks` — post-publish audit of `urssaf-prime-partage-valeur-du-secteur-prive-par-an-par-sexe-x-age`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [libelle_de_l_age, tranche_age, libelle_du_sexe, sexe]
temporal: annee
checks:
  - kind: unique
    columns: [libelle_de_l_age, tranche_age, libelle_du_sexe, sexe]
  - kind: not_null
    col: libelle_de_l_age
  - kind: not_null
    col: tranche_age
  - kind: not_null
    col: libelle_du_sexe
  - kind: not_null
    col: sexe
  - kind: row_floor
    min: 11
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
