# compiled by `hardened compile-checks` — post-publish audit of `urssaf-prime-partage-valeur-du-secteur-prive-par-trimestre-par-region`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dernier_jour_du_trimestre, ancienne_region]
temporal: annee
checks:
  - kind: unique
    columns: [dernier_jour_du_trimestre, ancienne_region]
  - kind: not_null
    col: dernier_jour_du_trimestre
  - kind: not_null
    col: ancienne_region
  - kind: row_floor
    min: 195
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
