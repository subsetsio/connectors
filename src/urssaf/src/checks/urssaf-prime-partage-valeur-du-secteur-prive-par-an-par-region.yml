# compiled by `hardened compile-checks` — post-publish audit of `urssaf-prime-partage-valeur-du-secteur-prive-par-an-par-region`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ancienne_region, code_ancienne_region, region, code_region]
temporal: annee
checks:
  - kind: unique
    columns: [ancienne_region, code_ancienne_region, region, code_region]
  - kind: not_null
    col: ancienne_region
  - kind: not_null
    col: code_ancienne_region
  - kind: not_null
    col: region
  - kind: not_null
    col: code_region
  - kind: row_floor
    min: 13
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
