# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `uk-parliament-oral-questions-and-motions`. Regenerate after model-verify.
# caution: Rows are raw oral-question and motion API items with nested JSON in `record`; use source documentation for the fields inside each item.
key: []
columns:
  - name: source_entity
    type: VARCHAR
  - name: source_endpoint
    type: VARCHAR
  - name: source_skip
    type: BIGINT
  - name: record
    type: "STRUCT(Id BIGINT, QuestionType BIGINT, QuestionText VARCHAR, Status BIGINT, Number BIGINT, TabledWhen TIMESTAMP, RemovedFromToBeAskedWhen VARCHAR, DeclarableInterestDetail VARCHAR, HansardLink VARCHAR, UIN BIGINT, AnsweringWhen TIMESTAMP, AnsweringBodyId BIGINT, AnsweringBody VARCHAR, AnsweringMinisterTitle VARCHAR, AskingMember STRUCT(MnisId BIGINT, PimsId BIGINT, \"Name\" VARCHAR, ListAs VARCHAR, Constituency VARCHAR, Status VARCHAR, Party VARCHAR, PartyId BIGINT, PartyColour VARCHAR, PhotoUrl VARCHAR), AnsweringMinister STRUCT(MnisId BIGINT, PimsId BIGINT, \"Name\" VARCHAR, ListAs VARCHAR, Constituency VARCHAR, Status VARCHAR, Party VARCHAR, PartyId BIGINT, PartyColour VARCHAR, PhotoUrl VARCHAR), AskingMemberId BIGINT, AnsweringMinisterId BIGINT)"
