# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `uk-parliament-committees`. Regenerate after model-verify.
# caution: Rows are raw committee API items with nested JSON in `record`; use source documentation for the fields inside each item.
key: []
columns:
  - name: source_entity
    type: VARCHAR
  - name: source_endpoint
    type: VARCHAR
  - name: source_skip
    type: BIGINT
  - name: record
    type: "STRUCT(contact STRUCT(email VARCHAR, phone VARCHAR, address VARCHAR, contactDisclaimer VARCHAR), id BIGINT, \"name\" VARCHAR, parentCommittee STRUCT(contact STRUCT(email VARCHAR, phone VARCHAR, address VARCHAR, contactDisclaimer VARCHAR), id BIGINT, \"name\" VARCHAR, parentCommittee JSON, subCommittees STRUCT(contact JSON, id BIGINT, \"name\" VARCHAR, parentCommittee JSON, subCommittees JSON, house VARCHAR, leadHouse JSON, category STRUCT(id BIGINT, \"name\" VARCHAR), committeeTypes STRUCT(id BIGINT, \"name\" VARCHAR, committeeCategory STRUCT(id BIGINT, \"name\" VARCHAR))[], showOnWebsite BOOLEAN, websiteLegacyUrl VARCHAR, websiteLegacyRedirectEnabled BOOLEAN, startDate TIMESTAMP, endDate JSON, dateCommonsAppointed TIMESTAMP, dateLordsAppointed TIMESTAMP, scrutinisingDepartments STRUCT(departmentId BIGINT, \"name\" VARCHAR)[], isLeadCommittee JSON, nameHistory STRUCT(id BIGINT, committeeId BIGINT, \"name\" VARCHAR, startDate TIMESTAMP, endDate TIMESTAMP)[])[], house VARCHAR, leadHouse JSON, category STRUCT(id BIGINT, \"name\" VARCHAR), committeeTypes STRUCT(id BIGINT, \"name\" VARCHAR, committeeCategory STRUCT(id BIGINT, \"name\" VARCHAR))[], showOnWebsite BOOLEAN, websiteLegacyUrl VARCHAR, websiteLegacyRedirectEnabled BOOLEAN, startDate TIMESTAMP, endDate JSON, dateCommonsAppointed TIMESTAMP, dateLordsAppointed TIMESTAMP, scrutinisingDepartments JSON[], isLeadCommittee JSON, nameHistory STRUCT(id BIGINT, committeeId BIGINT, \"name\" VARCHAR, startDate TIMESTAMP, endDate TIMESTAMP)[]), subCommittees STRUCT(contact JSON, id BIGINT, \"name\" VARCHAR, parentCommittee JSON, subCommittees JSON, house VARCHAR, leadHouse JSON, category JSON, committeeTypes JSON[], showOnWebsite BOOLEAN, websiteLegacyUrl VARCHAR, websiteLegacyRedirectEnabled BOOLEAN, startDate TIMESTAMP, endDate TIMESTAMP, dateCommonsAppointed TIMESTAMP, dateLordsAppointed TIMESTAMP, scrutinisingDepartments JSON[], isLeadCommittee JSON, nameHistory STRUCT(id BIGINT, committeeId BIGINT, \"name\" VARCHAR, startDate TIMESTAMP, endDate TIMESTAMP)[])[], house VARCHAR, leadHouse STRUCT(isCommons BOOLEAN, isLords BOOLEAN), category STRUCT(id BIGINT, \"name\" VARCHAR), committeeTypes STRUCT(id BIGINT, \"name\" VARCHAR, committeeCategory STRUCT(id BIGINT, \"name\" VARCHAR))[], showOnWebsite BOOLEAN, websiteLegacyUrl VARCHAR, websiteLegacyRedirectEnabled BOOLEAN, startDate TIMESTAMP, endDate JSON, dateCommonsAppointed TIMESTAMP, dateLordsAppointed TIMESTAMP, scrutinisingDepartments STRUCT(departmentId BIGINT, \"name\" VARCHAR)[], isLeadCommittee JSON, nameHistory STRUCT(id BIGINT, committeeId BIGINT, \"name\" VARCHAR, startDate VARCHAR, endDate TIMESTAMP)[])"
