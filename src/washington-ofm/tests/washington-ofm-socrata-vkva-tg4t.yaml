spec_id: washington-ofm-socrata-vkva-tg4t
status: active
coverage:
  frozen: true
  reason: Population forecast vintage is published as a fixed projection series with no incremental refresh cadence in the source metadata.
tests:
  - row_count: {min: 1}
    reason: each accepted OFM file or Socrata dataset should yield at least one SQL-readable raw row.
    certainty: 90
  - not_null: _entity_id
    reason: downloader stamps the accepted collect entity on every raw row.
    certainty: 99
  - not_null: _source_type
    reason: source_type distinguishes OFM file-cell rows from Socrata record rows for modeling.
    certainty: 99
