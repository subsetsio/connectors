# compiled by `hardened compile-checks` — post-publish audit of `harvard-growth-lab-atlas-of-economic-complexity-top-edges-hs92`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [product_hs92_code_source, product_hs92_code_target]
checks:
  - kind: unique
    columns: [product_hs92_code_source, product_hs92_code_target]
  - kind: not_null
    col: product_hs92_code_source
  - kind: not_null
    col: product_hs92_code_target
  - kind: row_floor
    min: 2158
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
