# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `harvard-growth-lab-atlas-of-economic-complexity-hs92-country-country-year`. Regenerate after model-verify.
# caution: Each bilateral relationship is stored as two mirrored rows, (country, partner) and (partner, country): a flow appears once as the reporter's `export_value` and again as the partner row's `import_value`. Sum `export_value` alone (or `import_value` alone) for world totals — adding both across all rows double-counts every flow.
key: [country_id, partner_country_id, year]
temporal: year
sort: [year, country_id, partner_country_id]
columns:
  - name: country_id
    type: BIGINT
    description: "Growth Lab numeric country identifier; joins `location-country` on `country_id`."
  - name: country_iso3_code
    type: VARCHAR
    description: "ISO 3166-1 alpha-3 code of the country."
  - name: partner_country_id
    type: BIGINT
    description: "Growth Lab numeric identifier of the partner (counterpart) country."
  - name: partner_iso3_code
    type: VARCHAR
    description: "ISO 3166-1 alpha-3 code of the partner country."
  - name: year
    type: INTEGER
    description: "Calendar year of the observation."
  - name: export_value
    type: BIGINT
    description: "Exports from the country to the partner, in current (nominal) US dollars."
  - name: import_value
    type: BIGINT
    description: "Imports by the country from the partner, in current (nominal) US dollars."
