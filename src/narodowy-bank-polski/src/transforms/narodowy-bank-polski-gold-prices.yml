# migrated from the module TRANSFORM_SPECS snapshot (scripts/migrate_transforms.py)
key: [date]
temporal: date
write_mode: overwrite
sort: [date]
columns:
  - name: date
    type: DATE
  - name: price_pln_per_g
    type: DOUBLE
