spec_id: global-fund-programmaticdatasetslist
status: active
tests:
  - not_null: dataSetName
    reason: each row names one programmatic dataset (the codebook for the programmatic results)
    certainty: 90
  - row_count: {min: 1}
    reason: ~1 row observed; an empty pull signals a broken fetch
    certainty: 80
