# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `global-fund-financialcategories`. Regenerate after model-verify.
key: [hierarchyid, name, type]
sort: [hierarchyid, name, type]
columns:
  - name: hierarchyid
    type: UUID
  - name: name
    type: VARCHAR
  - name: type
    type: VARCHAR
  - name: id
    type: UUID
  - name: parentid
    type: UUID
