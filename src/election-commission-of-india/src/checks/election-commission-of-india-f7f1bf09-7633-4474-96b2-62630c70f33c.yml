# compiled by `hardened compile-checks` — post-publish audit of `election-commission-of-india-f7f1bf09-7633-4474-96b2-62630c70f33c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [state_name, constituency_type]
checks:
  - kind: unique
    columns: [state_name, constituency_type]
  - kind: not_null
    col: state_name
  - kind: not_null
    col: constituency_type
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
