# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-schuel-schueler-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, gemeinde, berichtsjahr, schulstufe]
checks:
  - kind: unique
    columns: [row_number, gemeinde, berichtsjahr, schulstufe]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: gemeinde
  - kind: not_null
    col: berichtsjahr
  - kind: not_null
    col: schulstufe
  - kind: row_floor
    min: 148152
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
