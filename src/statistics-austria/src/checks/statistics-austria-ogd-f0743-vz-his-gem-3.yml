# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-f0743-vz-his-gem-3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, commune_of_residence_2411, census_year_14]
checks:
  - kind: unique
    columns: [row_number, commune_of_residence_2411, census_year_14]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: commune_of_residence_2411
  - kind: not_null
    col: census_year_14
  - kind: row_floor
    min: 15878
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
