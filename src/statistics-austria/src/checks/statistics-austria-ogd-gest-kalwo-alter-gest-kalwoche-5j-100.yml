# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-gest-kalwo-alter-gest-kalwoche-5j-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, calendar_week, c_5_years_age_group_of_deceased, province_nuts_2_unit_of_deceased, gender_of_deceased]
checks:
  - kind: unique
    columns: [row_number, calendar_week, c_5_years_age_group_of_deceased, province_nuts_2_unit_of_deceased, gender_of_deceased]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: calendar_week
  - kind: not_null
    col: c_5_years_age_group_of_deceased
  - kind: not_null
    col: province_nuts_2_unit_of_deceased
  - kind: not_null
    col: gender_of_deceased
  - kind: row_floor
    min: 147643
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
