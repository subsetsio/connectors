# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-krankenbewegungen-ex-entlassungen-ka-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, diagnosis_ishmt_shortlist_diagnoses, year_of_discharge, nuts_2_region_place_of_residence, sex]
checks:
  - kind: unique
    columns: [row_number, diagnosis_ishmt_shortlist_diagnoses, year_of_discharge, nuts_2_region_place_of_residence, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: diagnosis_ishmt_shortlist_diagnoses
  - kind: not_null
    col: year_of_discharge
  - kind: not_null
    col: nuts_2_region_place_of_residence
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 40596
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
