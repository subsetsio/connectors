# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevstprog-pr-bevjd-9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, year, age_in_single_years, province, sex]
checks:
  - kind: unique
    columns: [row_number, year, age_in_single_years, province, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: year
  - kind: not_null
    col: age_in_single_years
  - kind: not_null
    col: province
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 115011
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
