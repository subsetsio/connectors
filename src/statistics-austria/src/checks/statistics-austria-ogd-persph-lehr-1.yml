# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-persph-lehr-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, federal_country_province, academic_year, type_of_teacher]
checks:
  - kind: unique
    columns: [row_number, federal_country_province, academic_year, type_of_teacher]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: federal_country_province
  - kind: not_null
    col: academic_year
  - kind: not_null
    col: type_of_teacher
  - kind: row_floor
    min: 54
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
