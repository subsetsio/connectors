# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevstandjbab2002-bevstand-2022`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, commune_aggregation_by_political_district, number, age_in_single_years, sex]
checks:
  - kind: unique
    columns: [row_number, commune_aggregation_by_political_district, number, age_in_single_years, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: commune_aggregation_by_political_district
  - kind: not_null
    col: number
  - kind: not_null
    col: age_in_single_years
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 196277
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
