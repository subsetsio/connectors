# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevjahresanf-pr-bevja-7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, year, alter_in_5_jahresgruppen, province_nuts_2_unit, sex]
checks:
  - kind: unique
    columns: [row_number, year, alter_in_5_jahresgruppen, province_nuts_2_unit, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: year
  - kind: not_null
    col: alter_in_5_jahresgruppen
  - kind: not_null
    col: province_nuts_2_unit
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 27000
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
