# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-hvpi25-hvd-hvpi-2025-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, coicop_5_digit_code, time]
checks:
  - kind: unique
    columns: [row_number, coicop_5_digit_code, time]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: coicop_5_digit_code
  - kind: not_null
    col: time
  - kind: row_floor
    min: 3247
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
