# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevwan020-aussenwand-300`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, country_of_origin_destination, time_section, c_5_years_age_group, nationality_pol_breakdown_level_3]
checks:
  - kind: unique
    columns: [row_number, country_of_origin_destination, time_section, c_5_years_age_group, nationality_pol_breakdown_level_3]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: country_of_origin_destination
  - kind: not_null
    col: time_section
  - kind: not_null
    col: c_5_years_age_group
  - kind: not_null
    col: nationality_pol_breakdown_level_3
  - kind: row_floor
    min: 42906
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
