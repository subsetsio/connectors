# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-f0760ext-od-pkwgzl-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, time_monthly_values, passenger_car_makes]
checks:
  - kind: unique
    columns: [row_number, time_monthly_values, passenger_car_makes]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: time_monthly_values
  - kind: not_null
    col: passenger_car_makes
  - kind: row_floor
    min: 20719
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
