# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-krankenbewegungen-ex-leistungen-20212`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, medical_procedures_codes_from_2009_subchapters, year_of_discharge, age_in_4_classes, sex]
checks:
  - kind: unique
    columns: [row_number, medical_procedures_codes_from_2009_subchapters, year_of_discharge, age_in_4_classes, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: medical_procedures_codes_from_2009_subchapters
  - kind: not_null
    col: year_of_discharge
  - kind: not_null
    col: age_in_4_classes
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 6515
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
