# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-vpi15c18-vpi-2015coicop18-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, coicop_5_steller, time]
checks:
  - kind: unique
    columns: [row_number, coicop_5_steller, time]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: coicop_5_steller
  - kind: not_null
    col: time
  - kind: row_floor
    min: 25789
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
