# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-betrbil-betbildg-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, economic_activities_and_size_classes, time_series]
checks:
  - kind: unique
    columns: [row_number, economic_activities_and_size_classes, time_series]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: economic_activities_and_size_classes
  - kind: not_null
    col: time_series
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
