# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-vgrrgr103-rgr103-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, nuts3_units, time, nace_2008]
checks:
  - kind: unique
    columns: [row_number, nuts3_units, time, nace_2008]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: nuts3_units
  - kind: not_null
    col: time
  - kind: not_null
    col: nace_2008
  - kind: row_floor
    min: 10024
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
