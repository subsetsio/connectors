# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-veste309-veste309-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, citizenship, region_nuts2, form_of_employment, sex]
checks:
  - kind: unique
    columns: [row_number, citizenship, region_nuts2, form_of_employment, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: region_nuts2
  - kind: not_null
    col: form_of_employment
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
