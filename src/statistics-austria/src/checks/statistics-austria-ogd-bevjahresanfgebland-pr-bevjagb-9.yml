# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevjahresanfgebland-pr-bevjagb-9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, year, age_in_5_year_groups, province_nuts_2_unit, country_of_birth]
checks:
  - kind: unique
    columns: [row_number, year, age_in_5_year_groups, province_nuts_2_unit, country_of_birth]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: year
  - kind: not_null
    col: age_in_5_year_groups
  - kind: not_null
    col: province_nuts_2_unit
  - kind: not_null
    col: country_of_birth
  - kind: row_floor
    min: 20520
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
