# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-gem-unterabschn-ghd-ua-6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, commune_2413, estimate_of_costs_sections]
checks:
  - kind: unique
    columns: [row_number, commune_2413, estimate_of_costs_sections]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: commune_2413
  - kind: not_null
    col: estimate_of_costs_sections
  - kind: row_floor
    min: 89125
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
