# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-tli16wk15-tli-107`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, zeitreihe, wirtschaftskammersystematik_2015, soziale_stellung]
checks:
  - kind: unique
    columns: [row_number, zeitreihe, wirtschaftskammersystematik_2015, soziale_stellung]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: zeitreihe
  - kind: not_null
    col: wirtschaftskammersystematik_2015
  - kind: not_null
    col: soziale_stellung
  - kind: row_floor
    min: 18117
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
