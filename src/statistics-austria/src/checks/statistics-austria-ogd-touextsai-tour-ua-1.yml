# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-touextsai-tour-ua-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, season_tourism_month, accomodation_establishment, bundesland]
checks:
  - kind: unique
    columns: [row_number, season_tourism_month, accomodation_establishment, bundesland]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: season_tourism_month
  - kind: not_null
    col: accomodation_establishment
  - kind: not_null
    col: bundesland
  - kind: row_floor
    min: 39321
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
