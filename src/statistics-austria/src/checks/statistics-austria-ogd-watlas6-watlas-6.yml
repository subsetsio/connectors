# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-watlas6-watlas-6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, nace, year_starting_1995, federal_provinces]
checks:
  - kind: unique
    columns: [row_number, nace, year_starting_1995, federal_provinces]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: nace
  - kind: not_null
    col: year_starting_1995
  - kind: not_null
    col: federal_provinces
  - kind: row_floor
    min: 6595
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
