# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-watlas12-watlas-12`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, partnergrou_s_countries, year_starting_1995, goods_sitc]
checks:
  - kind: unique
    columns: [row_number, partnergrou_s_countries, year_starting_1995, goods_sitc]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: partnergrou_s_countries
  - kind: not_null
    col: year_starting_1995
  - kind: not_null
    col: goods_sitc
  - kind: row_floor
    min: 7938
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
