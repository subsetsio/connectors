# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-innov013-cis-013-unt-innovation-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, industries_nace_and_size_classes, time_section]
checks:
  - kind: unique
    columns: [row_number, industries_nace_and_size_classes, time_section]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: industries_nace_and_size_classes
  - kind: not_null
    col: time_section
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
