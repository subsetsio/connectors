# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-watlas14-watlas-14`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, federal_provices, year_starting_2021]
checks:
  - kind: unique
    columns: [row_number, federal_provices, year_starting_2021]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: federal_provices
  - kind: not_null
    col: year_starting_2021
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
