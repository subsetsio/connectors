# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-gvd-schleusungen-gvd-schleus-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, time_period, locks, vessel_type, loading_status]
checks:
  - kind: unique
    columns: [row_number, time_period, locks, vessel_type, loading_status]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: time_period
  - kind: not_null
    col: locks
  - kind: not_null
    col: vessel_type
  - kind: not_null
    col: loading_status
  - kind: row_floor
    min: 30716
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
