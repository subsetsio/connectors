# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogdext-klassdb-oecpa2015-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, code, edv_code, source_file, ebene]
checks:
  - kind: unique
    columns: [row_number, code, edv_code, source_file, ebene]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: code
  - kind: not_null
    col: edv_code
  - kind: not_null
    col: source_file
  - kind: not_null
    col: ebene
  - kind: row_floor
    min: 64495
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
