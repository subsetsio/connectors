# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevjahresanf-pr-bevja-8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, year, province_nuts_2_unit, alter_in_15_jahresgruppen, sex]
checks:
  - kind: unique
    columns: [row_number, year, province_nuts_2_unit, alter_in_15_jahresgruppen, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: year
  - kind: not_null
    col: province_nuts_2_unit
  - kind: not_null
    col: alter_in_15_jahresgruppen
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 8100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
