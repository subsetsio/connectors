# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevstprog-pr-bevjd-10`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, year, age_in_5_year_groups, province, sex]
checks:
  - kind: unique
    columns: [row_number, year, age_in_5_year_groups, province, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: year
  - kind: not_null
    col: age_in_5_year_groups
  - kind: not_null
    col: province
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 23220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
