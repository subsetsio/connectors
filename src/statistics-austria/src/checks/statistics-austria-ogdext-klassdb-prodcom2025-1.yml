# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogdext-klassdb-prodcom2025-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [titel, row_number, code, edv_code, source_file]
checks:
  - kind: unique
    columns: [titel, row_number, code, edv_code, source_file]
  - kind: not_null
    col: titel
  - kind: not_null
    col: row_number
  - kind: not_null
    col: code
  - kind: not_null
    col: edv_code
  - kind: not_null
    col: source_file
  - kind: row_floor
    min: 11835
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
