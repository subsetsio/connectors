# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-oeff-fin-q-oeff-fin-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, time_series_quaterly_data, general_government_4]
checks:
  - kind: unique
    columns: [row_number, time_series_quaterly_data, general_government_4]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: time_series_quaterly_data
  - kind: not_null
    col: general_government_4
  - kind: row_floor
    min: 68
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
