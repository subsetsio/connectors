# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-kjebet25m-kje25-b-m-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, berichtsmonat, nace_2025_teilw_abo_ebene_4]
checks:
  - kind: unique
    columns: [row_number, berichtsmonat, nace_2025_teilw_abo_ebene_4]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: berichtsmonat
  - kind: not_null
    col: nace_2025_teilw_abo_ebene_4
  - kind: row_floor
    min: 30
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
