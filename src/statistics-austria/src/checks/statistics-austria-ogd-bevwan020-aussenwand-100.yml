# compiled by `hardened compile-checks` — post-publish audit of `statistics-austria-ogd-bevwan020-aussenwand-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, nationality_aggregated_by_political_breakdown, age_in_single_years, time_section, sex]
checks:
  - kind: unique
    columns: [row_number, nationality_aggregated_by_political_breakdown, age_in_single_years, time_section, sex]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: nationality_aggregated_by_political_breakdown
  - kind: not_null
    col: age_in_single_years
  - kind: not_null
    col: time_section
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 143561
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
