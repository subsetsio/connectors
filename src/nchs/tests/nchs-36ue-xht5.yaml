spec_id: nchs-36ue-xht5
status: deferred  # catalog connector: per-dataset Socrata schema is
                  # unknown until first pull; assertions (incl. freshness
                  # for temporal tables) are authored at the model stage
                  # once model-verify profiles the real raw.
