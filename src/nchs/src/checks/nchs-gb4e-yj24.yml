# compiled by `hardened compile-checks` — post-publish audit of `nchs-gb4e-yj24`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [countyname, state_name, monthendingdate, st_abbrev, month]
temporal: data_as_of
checks:
  - kind: unique
    columns: [countyname, state_name, monthendingdate, st_abbrev, month]
  - kind: not_null
    col: countyname
  - kind: not_null
    col: state_name
  - kind: not_null
    col: monthendingdate
  - kind: not_null
    col: st_abbrev
  - kind: not_null
    col: month
  - kind: row_floor
    min: 108468
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
