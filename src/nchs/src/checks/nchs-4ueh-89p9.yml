# compiled by `hardened compile-checks` — post-publish audit of `nchs-4ueh-89p9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [race_or_hispanic_origin, age_group, education_level, year, sex]
temporal: data_as_of
checks:
  - kind: unique
    columns: [race_or_hispanic_origin, age_group, education_level, year, sex]
  - kind: not_null
    col: race_or_hispanic_origin
  - kind: not_null
    col: age_group
  - kind: not_null
    col: education_level
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 381
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
