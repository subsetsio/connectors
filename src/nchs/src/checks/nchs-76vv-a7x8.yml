# compiled by `hardened compile-checks` — post-publish audit of `nchs-76vv-a7x8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indicator, topic_subgroup, year_and_quarter, topic, race_ethnicity_category]
checks:
  - kind: unique
    columns: [indicator, topic_subgroup, year_and_quarter, topic, race_ethnicity_category]
  - kind: not_null
    col: indicator
  - kind: not_null
    col: topic_subgroup
  - kind: not_null
    col: year_and_quarter
  - kind: not_null
    col: topic
  - kind: not_null
    col: race_ethnicity_category
  - kind: row_floor
    min: 450
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
