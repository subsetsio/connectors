# compiled by `hardened compile-checks` — post-publish audit of `nchs-7siw-u4fz`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [subgroup, subgroup_id, time_period_id, footnote_id_list, subtopic]
checks:
  - kind: unique
    columns: [subgroup, subgroup_id, time_period_id, footnote_id_list, subtopic]
  - kind: not_null
    col: subgroup
  - kind: not_null
    col: subgroup_id
  - kind: not_null
    col: time_period_id
  - kind: not_null
    col: footnote_id_list
  - kind: not_null
    col: subtopic
  - kind: row_floor
    min: 1175
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
