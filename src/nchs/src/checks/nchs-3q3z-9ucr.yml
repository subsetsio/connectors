# compiled by `hardened compile-checks` — post-publish audit of `nchs-3q3z-9ucr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hp2020_objective, baseline_year_s, final_year_s, topic_area, population_group]
checks:
  - kind: unique
    columns: [hp2020_objective, baseline_year_s, final_year_s, topic_area, population_group]
  - kind: not_null
    col: hp2020_objective
  - kind: not_null
    col: baseline_year_s
  - kind: not_null
    col: final_year_s
  - kind: not_null
    col: topic_area
  - kind: not_null
    col: population_group
  - kind: row_floor
    min: 3741
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
