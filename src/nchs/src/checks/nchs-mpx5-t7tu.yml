# compiled by `hardened compile-checks` — post-publish audit of `nchs-mpx5-t7tu`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_period_end, data_period_start, jurisdiction_residence, group, footnote]
temporal: data_as_of
checks:
  - kind: unique
    columns: [data_period_end, data_period_start, jurisdiction_residence, group, footnote]
  - kind: not_null
    col: data_period_end
  - kind: not_null
    col: data_period_start
  - kind: not_null
    col: jurisdiction_residence
  - kind: not_null
    col: group
  - kind: not_null
    col: footnote
  - kind: row_floor
    min: 22132
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
