# compiled by `hardened compile-checks` — post-publish audit of `nchs-mqmc-4b9n`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [start_date, end_date, week_ending_date, hrr_number, hrr_name]
temporal: data_as_of
checks:
  - kind: unique
    columns: [start_date, end_date, week_ending_date, hrr_number, hrr_name]
  - kind: not_null
    col: start_date
  - kind: not_null
    col: end_date
  - kind: not_null
    col: week_ending_date
  - kind: not_null
    col: hrr_number
  - kind: not_null
    col: hrr_name
  - kind: row_floor
    min: 69309
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
