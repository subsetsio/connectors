# compiled by `hardened compile-checks` — post-publish audit of `nchs-8wmh-yzz9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, survey_years, nutrient, race_and_hispanic_origin, sex]
checks:
  - kind: unique
    columns: [age_group, survey_years, nutrient, race_and_hispanic_origin, sex]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: survey_years
  - kind: not_null
    col: nutrient
  - kind: not_null
    col: race_and_hispanic_origin
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1785
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
