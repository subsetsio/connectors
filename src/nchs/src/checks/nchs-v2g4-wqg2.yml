# compiled by `hardened compile-checks` — post-publish audit of `nchs-v2g4-wqg2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [week_ending_date, state_abbreviation, state, mmwr_year, outcome]
temporal: week_ending_date
checks:
  - kind: unique
    columns: [week_ending_date, state_abbreviation, state, mmwr_year, outcome]
  - kind: not_null
    col: week_ending_date
  - kind: not_null
    col: state_abbreviation
  - kind: not_null
    col: state
  - kind: not_null
    col: mmwr_year
  - kind: not_null
    col: outcome
  - kind: row_floor
    min: 23332
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
