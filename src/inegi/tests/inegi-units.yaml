spec_id: inegi-units
status: active
tests:
  - column_type: {col: code, type: string}
  - column_type: {col: description, type: string}
  - not_null: code
  - unique: code
  - row_count: {min: 200}
    reason: CL_UNIT held 223 unit codes in the latest raw run; a much smaller table means the code-list fetch degraded
    certainty: 85
