# compiled by `hardened compile-checks` — post-publish audit of `gem-global-energy-monitor-iron-steel-iron-unit`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [gem_unit_id, gem_plant_id, current_capacity_ttpa, gem_wiki_page, unit_name]
checks:
  - kind: unique
    columns: [gem_unit_id, gem_plant_id, current_capacity_ttpa, gem_wiki_page, unit_name]
  - kind: not_null
    col: gem_unit_id
  - kind: not_null
    col: gem_plant_id
  - kind: not_null
    col: current_capacity_ttpa
  - kind: not_null
    col: gem_wiki_page
  - kind: not_null
    col: unit_name
  - kind: row_floor
    min: 828
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
