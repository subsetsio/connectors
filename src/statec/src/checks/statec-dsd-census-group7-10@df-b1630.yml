# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group7-10@df-b1630`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, c_birth_country_of_birth, yat_year_of_arrival_in_the_country_since_2010, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, c_birth_country_of_birth, yat_year_of_arrival_in_the_country_since_2010, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: c_birth_country_of_birth
  - kind: not_null
    col: yat_year_of_arrival_in_the_country_since_2010
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 1176
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
