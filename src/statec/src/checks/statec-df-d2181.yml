# compiled by `hardened compile-checks` — post-publish audit of `statec-df-d2181`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality_municipality, specification_specification, decimals_decimals]
checks:
  - kind: unique
    columns: [municipality_municipality, specification_specification, decimals_decimals]
  - kind: not_null
    col: municipality_municipality
  - kind: not_null
    col: specification_specification
  - kind: not_null
    col: decimals_decimals
  - kind: row_floor
    min: 436
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
