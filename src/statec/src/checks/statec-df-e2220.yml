# compiled by `hardened compile-checks` — post-publish audit of `statec-df-e2220`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, measure_measure, accounting_entry_accounting_entry, sector_sector]
checks:
  - kind: unique
    columns: [time_period_time_period, measure_measure, accounting_entry_accounting_entry, sector_sector]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: measure_measure
  - kind: not_null
    col: accounting_entry_accounting_entry
  - kind: not_null
    col: sector_sector
  - kind: row_floor
    min: 19062
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
