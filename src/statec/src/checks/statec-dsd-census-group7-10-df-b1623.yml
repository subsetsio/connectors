# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group7-10@df-b1623`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, activity_economic_activity_nace_rev_2, lpw_place_of_work, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, activity_economic_activity_nace_rev_2, lpw_place_of_work, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: activity_economic_activity_nace_rev_2
  - kind: not_null
    col: lpw_place_of_work
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 4914
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
