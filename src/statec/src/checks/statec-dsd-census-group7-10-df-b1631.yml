# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group7-10@df-b1631`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, citizen_citizenship, wstatus_working_status, yat_year_of_arrival_in_the_country_since_2010, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, citizen_citizenship, wstatus_working_status, yat_year_of_arrival_in_the_country_since_2010, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: citizen_citizenship
  - kind: not_null
    col: wstatus_working_status
  - kind: not_null
    col: yat_year_of_arrival_in_the_country_since_2010
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 7056
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
