# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-nb-l-hab@df-b1711`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo_geographic_level, type_live_quarter_type_of_living_quarter]
checks:
  - kind: unique
    columns: [geo_geographic_level, type_live_quarter_type_of_living_quarter]
  - kind: not_null
    col: geo_geographic_level
  - kind: not_null
    col: type_live_quarter_type_of_living_quarter
  - kind: row_floor
    min: 287
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
