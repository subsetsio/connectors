# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group7-10@df-b1626`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo_geographic_level, c_birth_country_of_birth, sex_sex]
checks:
  - kind: unique
    columns: [geo_geographic_level, c_birth_country_of_birth, sex_sex]
  - kind: not_null
    col: geo_geographic_level
  - kind: not_null
    col: c_birth_country_of_birth
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 1207
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
