# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group15-17@df-b1652`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, isco08_occupation_isco_08, citizen_citizenship, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, isco08_occupation_isco_08, citizen_citizenship, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: isco08_occupation_isco_08
  - kind: not_null
    col: citizen_citizenship
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 3822
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
