# compiled by `hardened compile-checks` — post-publish audit of `statec-df-a2106`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, specification_specification, freq_frequency, note_specification_2_note_specification_2]
checks:
  - kind: unique
    columns: [time_period_time_period, specification_specification, freq_frequency, note_specification_2_note_specification_2]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: specification_specification
  - kind: not_null
    col: freq_frequency
  - kind: not_null
    col: note_specification_2_note_specification_2
  - kind: row_floor
    min: 403
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
