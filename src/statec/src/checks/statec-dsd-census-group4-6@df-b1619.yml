# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group4-6@df-b1619`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [edu_educational_level, activity_economic_activity_nace_rev_2, age_age_class, sex_sex]
checks:
  - kind: unique
    columns: [edu_educational_level, activity_economic_activity_nace_rev_2, age_age_class, sex_sex]
  - kind: not_null
    col: edu_educational_level
  - kind: not_null
    col: activity_economic_activity_nace_rev_2
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 1638
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
