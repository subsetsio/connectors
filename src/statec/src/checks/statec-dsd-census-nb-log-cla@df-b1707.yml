# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-nb-log-cla@df-b1707`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo_geographic_level, type_build_dwe_type_of_building, occ_status_conv_occupancy_status]
checks:
  - kind: unique
    columns: [geo_geographic_level, type_build_dwe_type_of_building, occ_status_conv_occupancy_status]
  - kind: not_null
    col: geo_geographic_level
  - kind: not_null
    col: type_build_dwe_type_of_building
  - kind: not_null
    col: occ_status_conv_occupancy_status
  - kind: row_floor
    min: 1610
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
