# compiled by `hardened compile-checks` — post-publish audit of `statec-df-x049`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [specification_specification, party_party, variables_variables]
checks:
  - kind: unique
    columns: [specification_specification, party_party, variables_variables]
  - kind: not_null
    col: specification_specification
  - kind: not_null
    col: party_party
  - kind: not_null
    col: variables_variables
  - kind: row_floor
    min: 322
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
