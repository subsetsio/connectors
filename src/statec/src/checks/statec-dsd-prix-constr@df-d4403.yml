# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-prix-constr@df-d4403`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [constr_trade_construction_trades, time_period_time_period]
checks:
  - kind: unique
    columns: [constr_trade_construction_trades, time_period_time_period]
  - kind: not_null
    col: constr_trade_construction_trades
  - kind: not_null
    col: time_period_time_period
  - kind: row_floor
    min: 836
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
