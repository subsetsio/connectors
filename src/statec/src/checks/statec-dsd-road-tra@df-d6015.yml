# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-road-tra@df-d6015`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, unit_measure_unit_of_measure, measure_measure, tra_cov_transport_coverage, tra_type_type_of_transport]
checks:
  - kind: unique
    columns: [time_period_time_period, unit_measure_unit_of_measure, measure_measure, tra_cov_transport_coverage, tra_type_type_of_transport]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: unit_measure_unit_of_measure
  - kind: not_null
    col: measure_measure
  - kind: not_null
    col: tra_cov_transport_coverage
  - kind: not_null
    col: tra_type_type_of_transport
  - kind: row_floor
    min: 270
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
