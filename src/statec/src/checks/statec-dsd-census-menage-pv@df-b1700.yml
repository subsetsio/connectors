# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-menage-pv@df-b1700`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_prv_hh_type_of_household, size_prv_hh_size_of_household, tenure_status_hh_tenure_status_of_household]
checks:
  - kind: unique
    columns: [type_prv_hh_type_of_household, size_prv_hh_size_of_household, tenure_status_hh_tenure_status_of_household]
  - kind: not_null
    col: type_prv_hh_type_of_household
  - kind: not_null
    col: size_prv_hh_size_of_household
  - kind: not_null
    col: tenure_status_hh_tenure_status_of_household
  - kind: row_floor
    min: 360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
