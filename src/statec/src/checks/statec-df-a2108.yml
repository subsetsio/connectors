# compiled by `hardened compile-checks` — post-publish audit of `statec-df-a2108`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month_month, measure_measure, meteo_station_meteo_station, norme_climat_climatological_normals, unit_measure_unit_of_measure]
checks:
  - kind: unique
    columns: [month_month, measure_measure, meteo_station_meteo_station, norme_climat_climatological_normals, unit_measure_unit_of_measure]
  - kind: not_null
    col: month_month
  - kind: not_null
    col: measure_measure
  - kind: not_null
    col: meteo_station_meteo_station
  - kind: not_null
    col: norme_climat_climatological_normals
  - kind: not_null
    col: unit_measure_unit_of_measure
  - kind: row_floor
    min: 1260
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
