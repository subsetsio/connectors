# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group1-3@df-b1603`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hst_household_status, lms_legal_marital_status, sex_sex]
checks:
  - kind: unique
    columns: [hst_household_status, lms_legal_marital_status, sex_sex]
  - kind: not_null
    col: hst_household_status
  - kind: not_null
    col: lms_legal_marital_status
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
