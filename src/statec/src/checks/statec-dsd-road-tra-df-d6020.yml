# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-road-tra@df-d6020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, nst07_goods_transported_nst_2007]
checks:
  - kind: unique
    columns: [time_period_time_period, nst07_goods_transported_nst_2007]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: nst07_goods_transported_nst_2007
  - kind: row_floor
    min: 428
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
