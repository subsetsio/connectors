# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group11-14@df-b1636`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citizen_citizenship, c_birth_country_of_birth, roy_place_of_usual_residence_one_year_prior_to_the_census, sex_sex]
checks:
  - kind: unique
    columns: [citizen_citizenship, c_birth_country_of_birth, roy_place_of_usual_residence_one_year_prior_to_the_census, sex_sex]
  - kind: not_null
    col: citizen_citizenship
  - kind: not_null
    col: c_birth_country_of_birth
  - kind: not_null
    col: roy_place_of_usual_residence_one_year_prior_to_the_census
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 2281
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
