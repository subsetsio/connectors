# compiled by `hardened compile-checks` — post-publish audit of `statec-df-c5300`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years_year, specification_specification, district_court_district_court]
checks:
  - kind: unique
    columns: [years_year, specification_specification, district_court_district_court]
  - kind: not_null
    col: years_year
  - kind: not_null
    col: specification_specification
  - kind: not_null
    col: district_court_district_court
  - kind: row_floor
    min: 184
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
