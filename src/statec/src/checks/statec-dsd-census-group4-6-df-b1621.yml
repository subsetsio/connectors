# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group4-6@df-b1621`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, edu_educational_level, lpw_place_of_work, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, edu_educational_level, lpw_place_of_work, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: edu_educational_level
  - kind: not_null
    col: lpw_place_of_work
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 3024
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
