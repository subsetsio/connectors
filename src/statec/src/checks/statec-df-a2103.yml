# compiled by `hardened compile-checks` — post-publish audit of `statec-df-a2103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years_year, month_month, meteo_station_meteo_station]
checks:
  - kind: unique
    columns: [years_year, month_month, meteo_station_meteo_station]
  - kind: not_null
    col: years_year
  - kind: not_null
    col: month_month
  - kind: not_null
    col: meteo_station_meteo_station
  - kind: row_floor
    min: 2457
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
