# compiled by `hardened compile-checks` — post-publish audit of `statec-df-d8450`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indicator_indicator, nace_rev_2_nace_rev_2, unit_unit]
checks:
  - kind: unique
    columns: [indicator_indicator, nace_rev_2_nace_rev_2, unit_unit]
  - kind: not_null
    col: indicator_indicator
  - kind: not_null
    col: nace_rev_2_nace_rev_2
  - kind: not_null
    col: unit_unit
  - kind: row_floor
    min: 196
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
