# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-nb-famille@df-b1705`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo_geographic_level, size_fam_nuc_size_of_family_nucleus]
checks:
  - kind: unique
    columns: [geo_geographic_level, size_fam_nuc_size_of_family_nucleus]
  - kind: not_null
    col: geo_geographic_level
  - kind: not_null
    col: size_fam_nuc_size_of_family_nucleus
  - kind: row_floor
    min: 517
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
