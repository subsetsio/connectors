# compiled by `hardened compile-checks` — post-publish audit of `statec-df-x056`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [commune_municipality, parti_party, variable_variable, decimals_decimals]
checks:
  - kind: unique
    columns: [commune_municipality, parti_party, variable_variable, decimals_decimals]
  - kind: not_null
    col: commune_municipality
  - kind: not_null
    col: parti_party
  - kind: not_null
    col: variable_variable
  - kind: not_null
    col: decimals_decimals
  - kind: row_floor
    min: 1136
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
