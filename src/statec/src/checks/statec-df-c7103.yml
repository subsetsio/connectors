# compiled by `hardened compile-checks` — post-publish audit of `statec-df-c7103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [parti_party, suffrage_vote, circonscription_constituency, decimals_decimals]
checks:
  - kind: unique
    columns: [parti_party, suffrage_vote, circonscription_constituency, decimals_decimals]
  - kind: not_null
    col: parti_party
  - kind: not_null
    col: suffrage_vote
  - kind: not_null
    col: circonscription_constituency
  - kind: not_null
    col: decimals_decimals
  - kind: row_floor
    min: 94
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
