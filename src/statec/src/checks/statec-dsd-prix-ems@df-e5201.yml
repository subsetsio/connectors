# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-prix-ems@df-e5201`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, measure_measure, unit_measure_unit_of_measure]
temporal: time_period_time_period
checks:
  - kind: unique
    columns: [time_period_time_period, measure_measure, unit_measure_unit_of_measure]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: measure_measure
  - kind: not_null
    col: unit_measure_unit_of_measure
  - kind: row_floor
    min: 146
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
