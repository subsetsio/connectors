# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-prix-ppi@df-d3202`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, activity_economic_activity_nace_rev_2, freq_frequency, base_period_base_period]
checks:
  - kind: unique
    columns: [time_period_time_period, activity_economic_activity_nace_rev_2, freq_frequency, base_period_base_period]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: activity_economic_activity_nace_rev_2
  - kind: not_null
    col: freq_frequency
  - kind: not_null
    col: base_period_base_period
  - kind: row_floor
    min: 1507
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
