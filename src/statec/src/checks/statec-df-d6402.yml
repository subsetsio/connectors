# compiled by `hardened compile-checks` — post-publish audit of `statec-df-d6402`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_type, years_year, specification_specification]
checks:
  - kind: unique
    columns: [type_type, years_year, specification_specification]
  - kind: not_null
    col: type_type
  - kind: not_null
    col: years_year
  - kind: not_null
    col: specification_specification
  - kind: row_floor
    min: 378
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
