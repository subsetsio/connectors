# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group15-17@df-b1650`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [yae_year_of_arrival_in_the_country_since_1980, citizen_citizenship, wstatus_working_status, sex_sex]
checks:
  - kind: unique
    columns: [yae_year_of_arrival_in_the_country_since_1980, citizen_citizenship, wstatus_working_status, sex_sex]
  - kind: not_null
    col: yae_year_of_arrival_in_the_country_since_1980
  - kind: not_null
    col: citizen_citizenship
  - kind: not_null
    col: wstatus_working_status
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 1890
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
