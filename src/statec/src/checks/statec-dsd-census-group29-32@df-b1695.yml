# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group29-32@df-b1695`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, fst_familly_status, sie_employment_status, c_birth_country_of_birth, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, fst_familly_status, sie_employment_status, c_birth_country_of_birth, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: fst_familly_status
  - kind: not_null
    col: sie_employment_status
  - kind: not_null
    col: c_birth_country_of_birth
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 12348
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
