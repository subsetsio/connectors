# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-med-price@df-c3110`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, med_consult_type_type_of_medical_consultation]
temporal: time_period_time_period
checks:
  - kind: unique
    columns: [time_period_time_period, med_consult_type_type_of_medical_consultation]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: med_consult_type_type_of_medical_consultation
  - kind: row_floor
    min: 99
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
