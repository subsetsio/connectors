# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-emploi@df-b3212`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, age_age_class, edu_educational_level, freq_frequency]
checks:
  - kind: unique
    columns: [time_period_time_period, age_age_class, edu_educational_level, freq_frequency]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: edu_educational_level
  - kind: not_null
    col: freq_frequency
  - kind: row_floor
    min: 300
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
