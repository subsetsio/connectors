# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-ecoicop-prix@df-e5405`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ecoicop_2018_products_ecoicop_v_2, time_period_time_period]
temporal: time_period_time_period
checks:
  - kind: unique
    columns: [ecoicop_2018_products_ecoicop_v_2, time_period_time_period]
  - kind: not_null
    col: ecoicop_2018_products_ecoicop_v_2
  - kind: not_null
    col: time_period_time_period
  - kind: row_floor
    min: 85941
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
