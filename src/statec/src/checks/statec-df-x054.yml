# compiled by `hardened compile-checks` — post-publish audit of `statec-df-x054`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [specification_specification, variables_variables, decimals_decimals]
checks:
  - kind: unique
    columns: [specification_specification, variables_variables, decimals_decimals]
  - kind: not_null
    col: specification_specification
  - kind: not_null
    col: variables_variables
  - kind: not_null
    col: decimals_decimals
  - kind: row_floor
    min: 212
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
