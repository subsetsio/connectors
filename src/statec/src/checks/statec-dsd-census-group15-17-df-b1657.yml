# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group15-17@df-b1657`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_age_class, activity_economic_activity_nace_rev_2, citizen_citizenship, sex_sex]
checks:
  - kind: unique
    columns: [age_age_class, activity_economic_activity_nace_rev_2, citizen_citizenship, sex_sex]
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: activity_economic_activity_nace_rev_2
  - kind: not_null
    col: citizen_citizenship
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 8526
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
