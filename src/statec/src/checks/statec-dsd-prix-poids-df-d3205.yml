# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-prix-poids@df-d3205`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [activity_economic_activity_nace_rev_2, base_period_base_period, decimals_decimals]
checks:
  - kind: unique
    columns: [activity_economic_activity_nace_rev_2, base_period_base_period, decimals_decimals]
  - kind: not_null
    col: activity_economic_activity_nace_rev_2
  - kind: not_null
    col: base_period_base_period
  - kind: not_null
    col: decimals_decimals
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
