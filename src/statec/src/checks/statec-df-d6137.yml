# compiled by `hardened compile-checks` — post-publish audit of `statec-df-d6137`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, vehicle_type_vehicle_type, motor_capacity_engine_size_in_cm3]
temporal: time_period_time_period
checks:
  - kind: unique
    columns: [time_period_time_period, vehicle_type_vehicle_type, motor_capacity_engine_size_in_cm3]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: vehicle_type_vehicle_type
  - kind: not_null
    col: motor_capacity_engine_size_in_cm3
  - kind: row_floor
    min: 622
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
