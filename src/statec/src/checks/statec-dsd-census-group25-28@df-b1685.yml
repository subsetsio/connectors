# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-census-group25-28@df-b1685`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [yae_year_of_arrival_in_the_country_since_1980, hst_household_status, age_age_class, sex_sex]
checks:
  - kind: unique
    columns: [yae_year_of_arrival_in_the_country_since_1980, hst_household_status, age_age_class, sex_sex]
  - kind: not_null
    col: yae_year_of_arrival_in_the_country_since_1980
  - kind: not_null
    col: hst_household_status
  - kind: not_null
    col: age_age_class
  - kind: not_null
    col: sex_sex
  - kind: row_floor
    min: 1092
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
