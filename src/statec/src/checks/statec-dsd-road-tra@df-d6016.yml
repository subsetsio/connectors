# compiled by `hardened compile-checks` — post-publish audit of `statec-dsd-road-tra@df-d6016`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period_time_period, unit_measure_unit_of_measure, measure_measure, loadstat_loading_status, tra_cov_transport_coverage]
checks:
  - kind: unique
    columns: [time_period_time_period, unit_measure_unit_of_measure, measure_measure, loadstat_loading_status, tra_cov_transport_coverage]
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: unit_measure_unit_of_measure
  - kind: not_null
    col: measure_measure
  - kind: not_null
    col: loadstat_loading_status
  - kind: not_null
    col: tra_cov_transport_coverage
  - kind: row_floor
    min: 225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
