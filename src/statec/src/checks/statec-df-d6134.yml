# compiled by `hardened compile-checks` — post-publish audit of `statec-df-d6134`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [brand_brand, time_period_time_period, vehicle_type_vehicle_type]
temporal: time_period_time_period
checks:
  - kind: unique
    columns: [brand_brand, time_period_time_period, vehicle_type_vehicle_type]
  - kind: not_null
    col: brand_brand
  - kind: not_null
    col: time_period_time_period
  - kind: not_null
    col: vehicle_type_vehicle_type
  - kind: row_floor
    min: 477361
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
