spec_id: insee-ds-rp-education
status: active
tests:
- row_count:
    min: 41792
  reason: metadata/code-list extract row count; this raw table is not the full observation
    cube advertised by catalog numObservations
  certainty: 85
  severity: block
- not_null: OBS_MEASURE
  reason: every Melodi observation carries exactly one measure key; flatten lifts
    it to OBS_MEASURE, so it is never null
  certainty: 90
