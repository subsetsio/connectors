# compiled by `hardened compile-checks` — post-publish audit of `insee-ds-flores-economic-sphere`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo, legal_form_with_public, obs_status, economic_sphere, flores_measure]
temporal: time_period
checks:
  - kind: unique
    columns: [geo, legal_form_with_public, obs_status, economic_sphere, flores_measure]
  - kind: not_null
    col: geo
  - kind: not_null
    col: legal_form_with_public
  - kind: not_null
    col: obs_status
  - kind: not_null
    col: economic_sphere
  - kind: not_null
    col: flores_measure
  - kind: row_floor
    min: 376317
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
