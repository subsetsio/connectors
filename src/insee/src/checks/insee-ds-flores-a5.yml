# compiled by `hardened compile-checks` — post-publish audit of `insee-ds-flores-a5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [geo, number_empl, activity, obs_status, flores_measure]
temporal: time_period
checks:
  - kind: unique
    columns: [geo, number_empl, activity, obs_status, flores_measure]
  - kind: not_null
    col: geo
  - kind: not_null
    col: number_empl
  - kind: not_null
    col: activity
  - kind: not_null
    col: obs_status
  - kind: not_null
    col: flores_measure
  - kind: row_floor
    min: 2383341
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
