# compiled by `hardened compile-checks` — post-publish audit of `ocha-food-security-nutrition-poverty-food-security`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
temporal: reference_period_start
checks:
  - kind: row_floor
    min: 223285
regression:
  # HAPI food-security rows are IPC assessment/projection slices; upstream
  # restatements can legitimately remove a full cycle while freshness advances.
  row_shrink_tolerance: 0.25
  temporal_monotonic: true
