# compiled by `hardened compile-checks` — post-publish audit of `ons-older-people-economic-activity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [administrative_geography, geography, sex, sex_1, economic_activity]
checks:
  - kind: unique
    columns: [administrative_geography, geography, sex, sex_1, economic_activity]
  - kind: not_null
    col: administrative_geography
  - kind: not_null
    col: geography
  - kind: not_null
    col: sex
  - kind: not_null
    col: sex_1
  - kind: not_null
    col: economic_activity
  - kind: row_floor
    min: 1149
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
