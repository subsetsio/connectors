# compiled by `hardened compile-checks` — post-publish audit of `ons-ts079`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lower_tier_local_authorities_code, lower_tier_local_authorities, sexual_orientation_9_categories_code, sexual_orientation_9_categories]
checks:
  - kind: unique
    columns: [lower_tier_local_authorities_code, lower_tier_local_authorities, sexual_orientation_9_categories_code, sexual_orientation_9_categories]
  - kind: not_null
    col: lower_tier_local_authorities_code
  - kind: not_null
    col: lower_tier_local_authorities
  - kind: not_null
    col: sexual_orientation_9_categories_code
  - kind: not_null
    col: sexual_orientation_9_categories
  - kind: row_floor
    min: 1489
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
