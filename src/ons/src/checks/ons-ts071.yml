# compiled by `hardened compile-checks` — post-publish audit of `ons-ts071`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lower_tier_local_authorities_code, lower_tier_local_authorities, uk_armed_forces_veteran_indicator_5_categories_code, uk_armed_forces_veteran_indicator_5_categories]
checks:
  - kind: unique
    columns: [lower_tier_local_authorities_code, lower_tier_local_authorities, uk_armed_forces_veteran_indicator_5_categories_code, uk_armed_forces_veteran_indicator_5_categories]
  - kind: not_null
    col: lower_tier_local_authorities_code
  - kind: not_null
    col: lower_tier_local_authorities
  - kind: not_null
    col: uk_armed_forces_veteran_indicator_5_categories_code
  - kind: not_null
    col: uk_armed_forces_veteran_indicator_5_categories
  - kind: row_floor
    min: 827
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
