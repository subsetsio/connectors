# compiled by `hardened compile-checks` — post-publish audit of `ons-ts053`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lower_tier_local_authorities_code, lower_tier_local_authorities, occupancy_rating_for_rooms_6_categories_code, occupancy_rating_for_rooms_6_categories]
checks:
  - kind: unique
    columns: [lower_tier_local_authorities_code, lower_tier_local_authorities, occupancy_rating_for_rooms_6_categories_code, occupancy_rating_for_rooms_6_categories]
  - kind: not_null
    col: lower_tier_local_authorities_code
  - kind: not_null
    col: lower_tier_local_authorities
  - kind: not_null
    col: occupancy_rating_for_rooms_6_categories_code
  - kind: not_null
    col: occupancy_rating_for_rooms_6_categories
  - kind: row_floor
    min: 993
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
