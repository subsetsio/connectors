# compiled by `hardened compile-checks` — post-publish audit of `ons-st013`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_86_categories_code, age_86_categories, regions, regions_code, sex_2_categories_code]
checks:
  - kind: unique
    columns: [age_86_categories_code, age_86_categories, regions, regions_code, sex_2_categories_code]
  - kind: not_null
    col: age_86_categories_code
  - kind: not_null
    col: age_86_categories
  - kind: not_null
    col: regions
  - kind: not_null
    col: regions_code
  - kind: not_null
    col: sex_2_categories_code
  - kind: row_floor
    min: 860
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
