# compiled by `hardened compile-checks` — post-publish audit of `ons-ts078`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lower_tier_local_authorities_code, lower_tier_local_authorities, gender_identity_7_categories_code, gender_identity_7_categories]
checks:
  - kind: unique
    columns: [lower_tier_local_authorities_code, lower_tier_local_authorities, gender_identity_7_categories_code, gender_identity_7_categories]
  - kind: not_null
    col: lower_tier_local_authorities_code
  - kind: not_null
    col: lower_tier_local_authorities
  - kind: not_null
    col: gender_identity_7_categories_code
  - kind: not_null
    col: gender_identity_7_categories
  - kind: row_floor
    min: 1158
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
