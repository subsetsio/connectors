# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `npm-package-inventory`. Regenerate after model-verify.
key: [package]
sort: [package]
columns:
  - name: package
    type: VARCHAR
    description: Current npm package document id.
  - name: rev
    type: VARCHAR
    description: CouchDB revision token for the package document.
