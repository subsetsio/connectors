spec_id: smhi-hydroobs-parameters
status: active
coverage:
  frozen: true
  reason: parameter catalog is reference metadata and has no temporal observation column
tests:
  - row_count: {min: 8}
  - not_null: parameter_key
  - distinct_count: {col: parameter_key, min: 8}
