# compiled by `hardened compile-checks` — post-publish audit of `dbt-uk-tariff-2021-01-01--quota-associations`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sub_quota_id, trackedmodel_ptr_id, main_quota_id, coefficient, sub_quota_relation_type]
checks:
  - kind: unique
    columns: [sub_quota_id, trackedmodel_ptr_id, main_quota_id, coefficient, sub_quota_relation_type]
  - kind: not_null
    col: sub_quota_id
  - kind: not_null
    col: trackedmodel_ptr_id
  - kind: not_null
    col: main_quota_id
  - kind: not_null
    col: coefficient
  - kind: not_null
    col: sub_quota_relation_type
  - kind: row_floor
    min: 1341
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
