# compiled by `hardened compile-checks` — post-publish audit of `dbt-uk-trade-quotas--quota--commodities`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quota_definition_sid, commodity, idx]
checks:
  - kind: unique
    columns: [quota_definition_sid, commodity, idx]
  - kind: not_null
    col: quota_definition_sid
  - kind: not_null
    col: commodity
  - kind: not_null
    col: idx
  - kind: row_floor
    min: 36807
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
