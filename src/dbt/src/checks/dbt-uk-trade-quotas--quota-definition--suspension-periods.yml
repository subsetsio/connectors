# compiled by `hardened compile-checks` — post-publish audit of `dbt-uk-trade-quotas--quota-definition--suspension-periods`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [suspension_period_end, idx, quota_definition_sid, suspension_period_start]
temporal: suspension_period_start
checks:
  - kind: unique
    columns: [suspension_period_end, idx, quota_definition_sid, suspension_period_start]
  - kind: not_null
    col: suspension_period_end
  - kind: not_null
    col: idx
  - kind: not_null
    col: quota_definition_sid
  - kind: not_null
    col: suspension_period_start
  - kind: row_floor
    min: 4490
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
