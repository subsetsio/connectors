# compiled by `hardened compile-checks` — post-publish audit of `dbt-uk-tariff-2021-01-01--report--measures-on-declarable-commodities`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [measure_effective_start_date, id]
temporal: measure_effective_start_date
checks:
  - kind: unique
    columns: [measure_effective_start_date, id]
  - kind: not_null
    col: measure_effective_start_date
  - kind: not_null
    col: id
  - kind: row_floor
    min: 547093
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
