# compiled by `hardened compile-checks` — post-publish audit of `dbt-uk-tariff-2021-01-01--measure-components`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [component_measure_id, trackedmodel_ptr_id, component_measurement_id, duty_expression_id, monetary_unit_id]
checks:
  - kind: unique
    columns: [component_measure_id, trackedmodel_ptr_id, component_measurement_id, duty_expression_id, monetary_unit_id]
  - kind: not_null
    col: component_measure_id
  - kind: not_null
    col: trackedmodel_ptr_id
  - kind: not_null
    col: component_measurement_id
  - kind: not_null
    col: duty_expression_id
  - kind: not_null
    col: monetary_unit_id
  - kind: row_floor
    min: 1212925
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
