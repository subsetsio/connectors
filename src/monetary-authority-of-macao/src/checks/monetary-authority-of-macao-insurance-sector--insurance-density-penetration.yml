# compiled by `hardened compile-checks` — post-publish audit of `monetary-authority-of-macao-insurance-sector--insurance-density-penetration`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, date, period, series]
temporal: date
checks:
  - kind: unique
    columns: [year, date, period, series]
  - kind: not_null
    col: year
  - kind: not_null
    col: date
  - kind: not_null
    col: period
  - kind: not_null
    col: series
  - kind: row_floor
    min: 39
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
