# compiled by `hardened compile-checks` — post-publish audit of `monetary-authority-of-macao-4-domestic-interbank-market--m-bills`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, date, year, series, part]
temporal: period
checks:
  - kind: unique
    columns: [period, date, year, series, part]
  - kind: not_null
    col: period
  - kind: not_null
    col: date
  - kind: not_null
    col: year
  - kind: not_null
    col: series
  - kind: not_null
    col: part
  - kind: row_floor
    min: 6843
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
