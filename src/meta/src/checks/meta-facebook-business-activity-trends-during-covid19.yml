# compiled by `hardened compile-checks` — post-publish audit of `meta-facebook-business-activity-trends-during-covid19`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ds, gadm_id, gadm_name, gadm0_name, business_vertical]
temporal: crisis_ds
checks:
  - kind: unique
    columns: [ds, gadm_id, gadm_name, gadm0_name, business_vertical]
  - kind: not_null
    col: ds
  - kind: not_null
    col: gadm_id
  - kind: not_null
    col: gadm_name
  - kind: not_null
    col: gadm0_name
  - kind: not_null
    col: business_vertical
  - kind: row_floor
    min: 1198274
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
