# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `meta-international-migration-flows`. Regenerate after model-verify.
# caution: Rows are directional monthly country-to-country flows; reversing country_from and country_to changes the measure.
key: [country_from, country_to, migration_month]
temporal: migration_month
sort: [migration_month, country_from, country_to]
columns:
  - name: country_from
    type: VARCHAR
  - name: country_to
    type: VARCHAR
  - name: migration_month
    type: DATE
  - name: num_migrants
    type: BIGINT
  - name: source_file
    type: VARCHAR
