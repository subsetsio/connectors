# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `meta-relative-wealth-index`. Regenerate after model-verify.
# caution: Some source files do not provide quadkeys, so the row identity is the source file plus coordinate and measured values.
# caution: Relative Wealth Index values are comparable as modeled estimates, not counts or totals.
key: [source_file, latitude, longitude, rwi, error]
sort: [source_file, latitude, longitude, rwi, error]
columns:
  - name: quadkey
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: rwi
    type: DOUBLE
  - name: error
    type: DOUBLE
  - name: source_file
    type: VARCHAR
