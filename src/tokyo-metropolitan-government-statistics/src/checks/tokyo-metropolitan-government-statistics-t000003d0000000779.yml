# compiled by `hardened compile-checks` — post-publish audit of `tokyo-metropolitan-government-statistics-t000003d0000000779`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [resource_id, resource_url_basename, row_number]
checks:
  - kind: unique
    columns: [resource_id, resource_url_basename, row_number]
  - kind: not_null
    col: resource_id
  - kind: not_null
    col: resource_url_basename
  - kind: not_null
    col: row_number
  - kind: row_floor
    min: 675
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
