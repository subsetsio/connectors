# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nhtsa-fars-weather`. Regenerate after model-verify.
# caution: Weather rows are multi-response observations for a crash; do not aggregate as one row per crash.
key: []
columns:
  - name: state
    type: BIGINT
  - name: statename
    type: VARCHAR
  - name: st_case
    type: BIGINT
  - name: weather
    type: BIGINT
  - name: weathername
    type: VARCHAR
  - name: case_year
    type: INTEGER
