# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nhtsa-fars-crashrf`. Regenerate after model-verify.
# caution: Multi-response crash factor file; rows are factor observations for a crash, not one row per crash.
key: []
columns:
  - name: state
    type: BIGINT
  - name: statename
    type: VARCHAR
  - name: st_case
    type: BIGINT
  - name: crashrf
    type: BIGINT
  - name: crashrfname
    type: VARCHAR
  - name: case_year
    type: INTEGER
