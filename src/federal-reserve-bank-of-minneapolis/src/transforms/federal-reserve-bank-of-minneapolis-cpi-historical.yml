# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `federal-reserve-bank-of-minneapolis-cpi-historical`. Regenerate after model-verify.
# caution: The `series` values use different index bases and source methodologies; compare `annual_average_index` values only within a single series.
key: [year, series]
temporal: year
sort: [year, series]
columns:
  - name: series
    type: VARCHAR
  - name: year
    type: VARCHAR
  - name: annual_average_index
    type: DOUBLE
  - name: annual_percent_change
    type: VARCHAR
