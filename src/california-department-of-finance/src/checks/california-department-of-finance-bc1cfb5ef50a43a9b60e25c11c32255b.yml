# compiled by `hardened compile-checks` — post-publish audit of `california-department-of-finance-bc1cfb5ef50a43a9b60e25c11c32255b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [name, county, city, year]
checks:
  - kind: unique
    columns: [name, county, city, year]
  - kind: not_null
    col: name
  - kind: not_null
    col: county
  - kind: not_null
    col: city
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1785
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
