# compiled by `hardened compile-checks` — post-publish audit of `california-department-of-finance-4c2eb24715614327bd8b30f630234ef0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jurisdiction, city, county, year]
temporal: year
checks:
  - kind: unique
    columns: [jurisdiction, city, county, year]
  - kind: not_null
    col: jurisdiction
  - kind: not_null
    col: city
  - kind: not_null
    col: county
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1188
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
