# compiled by `hardened compile-checks` — post-publish audit of `california-department-of-finance-e27cb8246ca14b5d830083689b7c6f46`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [placecode, county, fips, co_fips, year]
checks:
  - kind: unique
    columns: [placecode, county, fips, co_fips, year]
  - kind: not_null
    col: placecode
  - kind: not_null
    col: county
  - kind: not_null
    col: fips
  - kind: not_null
    col: co_fips
  - kind: not_null
    col: year
  - kind: row_floor
    min: 92
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
