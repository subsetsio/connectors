# compiled by `hardened compile-checks` — post-publish audit of `california-department-of-finance-603566ca1e3041c699fe924a12dc6f1b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [countyv1, county, yearv1]
checks:
  - kind: unique
    columns: [countyv1, county, yearv1]
  - kind: not_null
    col: countyv1
  - kind: not_null
    col: county
  - kind: not_null
    col: yearv1
  - kind: row_floor
    min: 354
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
