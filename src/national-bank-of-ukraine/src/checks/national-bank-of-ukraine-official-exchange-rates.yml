# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-ukraine-official-exchange-rates`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [exchangedate, calcdate, txt, cc, group]
temporal: exchangedate
checks:
  - kind: unique
    columns: [exchangedate, calcdate, txt, cc, group]
  - kind: not_null
    col: exchangedate
  - kind: not_null
    col: calcdate
  - kind: not_null
    col: txt
  - kind: not_null
    col: cc
  - kind: not_null
    col: group
  - kind: row_floor
    min: 313802
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
