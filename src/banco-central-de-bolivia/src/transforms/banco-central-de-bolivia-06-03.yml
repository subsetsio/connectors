# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `banco-central-de-bolivia-06-03`. Regenerate after model-verify.
# caution: Rows represent non-empty cells from formatted BCB bulletin spreadsheets; interpret row and column positions with the original table layout rather than summing cells as a normalized panel.
key: [table_code, sheet, row, col]
sort: [table_code, sheet, row, col]
columns:
  - name: table_code
    type: VARCHAR
  - name: sheet
    type: BIGINT
  - name: row
    type: INTEGER
  - name: col
    type: INTEGER
  - name: value_num
    type: DOUBLE
  - name: value_text
    type: VARCHAR
