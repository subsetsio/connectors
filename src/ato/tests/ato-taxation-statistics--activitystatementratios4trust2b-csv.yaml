spec_id: ato-taxation-statistics--activitystatementratios4trust2b-csv
status: active
tests:
  - row_count: {min: 1}
    reason: every accepted entity has at least one flat CSV edition that returned thousands of rows during probing; an empty pull means the resource download URL changed or the CSV was truncated.
    certainty: 90
    severity: block
