# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-slovakia-exchange-rate-foreign-monthly`. Regenerate after model-verify.
# caution: These are monthly rates for selected foreign currencies not covered by the ECB daily feed; they should not be mixed with daily ECB reference rates without aligning frequency.
key: [valid_from, country, currency_name, currency_code, month_number]
temporal: valid_from
sort: [valid_from, country, currency_name, currency_code, month_number]
columns:
  - name: valid_from
    type: DATE
  - name: month_number
    type: INTEGER
  - name: country
    type: VARCHAR
  - name: currency_code
    type: VARCHAR
  - name: currency_name
    type: VARCHAR
  - name: value
    type: DOUBLE
