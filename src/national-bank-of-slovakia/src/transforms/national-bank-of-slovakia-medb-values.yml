# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-slovakia-medb-values`. Regenerate after model-verify.
# caution: The table mixes monthly, quarterly, and annual observations; filter `frequency` before time-series comparisons or aggregations.
# caution: The source workbook values do not carry the MEDB catalog `timeseries_id`; the derived `series_key` is not unique across the catalog, so this table is intentionally keyless.
key: []
temporal: date
sort: [date]
columns:
  - name: frequency
    type: VARCHAR
  - name: series_key
    type: VARCHAR
  - name: classcode
    type: VARCHAR
  - name: variable
    type: VARCHAR
  - name: detail
    type: VARCHAR
  - name: source
    type: VARCHAR
  - name: period_label
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
