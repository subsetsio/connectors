# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `czech-national-bank-exrates-daily`. Regenerate after model-verify.
key: [validfor, currencycode]
temporal: validfor
sort: [validfor, currencycode]
columns:
  - name: currencycode
    type: VARCHAR
  - name: amount
    type: BIGINT
  - name: validfor
    type: DATE
  - name: rate
    type: DOUBLE
