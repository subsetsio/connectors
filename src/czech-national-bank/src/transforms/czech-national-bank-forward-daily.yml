# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `czech-national-bank-forward-daily`. Regenerate after model-verify.
key: [validfor, ccypair, maturity]
temporal: validfor
sort: [validfor, ccypair, maturity]
columns:
  - name: validfor
    type: DATE
  - name: ccypair
    type: VARCHAR
  - name: maturity
    type: VARCHAR
  - name: forwardpoints
    type: DOUBLE
