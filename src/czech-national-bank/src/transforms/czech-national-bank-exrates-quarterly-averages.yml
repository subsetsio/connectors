# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `czech-national-bank-exrates-quarterly-averages`. Regenerate after model-verify.
# caution: The source stores the quarter in the `month` column; treat the period as a quarter label, not a calendar month.
key: [currencycode, year, month]
temporal: year
sort: [year, currencycode, month]
columns:
  - name: month
    type: VARCHAR
  - name: average
    type: DOUBLE
  - name: year
    type: BIGINT
  - name: currencycode
    type: VARCHAR
  - name: amount
    type: BIGINT
