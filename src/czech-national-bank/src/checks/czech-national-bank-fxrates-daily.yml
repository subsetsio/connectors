# compiled by `hardened compile-checks` — post-publish audit of `czech-national-bank-fxrates-daily`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [validfor, country, currencycode, currency]
temporal: validfor
checks:
  - kind: unique
    columns: [validfor, country, currencycode, currency]
  - kind: not_null
    col: validfor
  - kind: not_null
    col: country
  - kind: not_null
    col: currencycode
  - kind: not_null
    col: currency
  - kind: row_floor
    min: 16465
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
