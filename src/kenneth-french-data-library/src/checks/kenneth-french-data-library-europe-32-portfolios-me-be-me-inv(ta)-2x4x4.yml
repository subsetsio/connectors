# compiled by `hardened compile-checks` — post-publish audit of `kenneth-french-data-library-europe-32-portfolios-me-be-me-inv(ta)-2x4x4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, variable, statistic, period]
temporal: date
checks:
  - kind: unique
    columns: [date, variable, statistic, period]
  - kind: not_null
    col: date
  - kind: not_null
    col: variable
  - kind: not_null
    col: statistic
  - kind: not_null
    col: period
  - kind: row_floor
    min: 28704
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
