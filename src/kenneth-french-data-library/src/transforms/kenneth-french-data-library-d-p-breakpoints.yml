# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `kenneth-french-data-library-d-p-breakpoints`. Regenerate after model-verify.
# caution: Rows the source marked missing (-99.99 / -999 sentinels) are absent, not zero: a missing (date, variable) pair means the portfolio had no observation that period.
key: [date, variable]
temporal: date
sort: [date, variable]
columns:
  - name: block
    type: INTEGER
  - name: statistic
    type: VARCHAR
  - name: period
    type: VARCHAR
  - name: date
    type: DATE
  - name: variable
    type: VARCHAR
  - name: value
    type: DOUBLE
