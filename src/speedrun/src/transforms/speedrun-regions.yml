# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `speedrun-regions`. Regenerate after model-verify.
key: [id]
temporal: fetched_at
sort: [fetched_at, id]
columns:
  - name: id
    type: VARCHAR
  - name: name
    type: VARCHAR
  - name: links
    type: "STRUCT(rel VARCHAR, uri VARCHAR)[]"
  - name: fetched_at
    type: TIMESTAMP
  - name: resource
    type: VARCHAR
  - name: page
    type: BIGINT
