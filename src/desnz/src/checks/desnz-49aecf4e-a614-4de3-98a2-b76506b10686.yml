# compiled by `hardened compile-checks` — post-publish audit of `desnz-49aecf4e-a614-4de3-98a2-b76506b10686`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value_text, row_label, resource, sheet, series]
checks:
  - kind: unique
    columns: [value_text, row_label, resource, sheet, series]
  - kind: not_null
    col: value_text
  - kind: not_null
    col: row_label
  - kind: not_null
    col: resource
  - kind: not_null
    col: sheet
  - kind: not_null
    col: series
  - kind: row_floor
    min: 23
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
