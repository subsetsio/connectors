# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `desnz-2057327e-d1f2-4f63-894f-72fc75235188`. Regenerate after model-verify.
# caution: Rows are extracted spreadsheet cells from multiple resources and sheets; use resource, sheet, row_label, and series together before aggregating values.
key: [value_text, row_label, series, resource, sheet]
sort: [value_text, row_label, series, resource, sheet]
columns:
  - name: resource
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: row_label
    type: VARCHAR
  - name: series
    type: VARCHAR
  - name: value_text
    type: VARCHAR
  - name: value_num
    type: DOUBLE
