spec_id: nhs-digital-pathology-laboratories
status: active
tests:
  - row_count: {min: 0}
    reason: >
      This accepted entity is actively waived because the only CKAN data URL
      returns HTTP 403 from the factory environment; the run may only contain
      an empty placeholder raw artifact until NHS Digital restores access.
    certainty: 99
