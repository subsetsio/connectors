# Deferred: NIFC is a catalog connector of 94 heterogeneous ArcGIS
# feature/table layers, each with its own attribute schema. The meaningful
# per-table invariants (grain unique/not-null, row-count floors, temporal
# monotonicity) are authored as compiled post-publish checks (compile-checks)
# once the model stage settles each table's grain + temporal column.
spec_id: nifc-dece90af1a0242dcbf0ca36d30276aa3-0
status: deferred
