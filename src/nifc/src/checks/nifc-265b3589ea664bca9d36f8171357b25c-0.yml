# compiled by `hardened compile-checks` — post-publish audit of `nifc-265b3589ea664bca9d36f8171357b25c-0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [globalid, datasource, landownercategory, landownerkind]
checks:
  - kind: unique
    columns: [globalid, datasource, landownercategory, landownerkind]
  - kind: not_null
    col: globalid
  - kind: not_null
    col: datasource
  - kind: not_null
    col: landownercategory
  - kind: not_null
    col: landownerkind
  - kind: row_floor
    min: 714003
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
