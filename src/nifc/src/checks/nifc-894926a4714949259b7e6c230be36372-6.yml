# compiled by `hardened compile-checks` — post-publish audit of `nifc-894926a4714949259b7e6c230be36372-6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [globalid, sourceglobalid, featurecategory]
checks:
  - kind: unique
    columns: [globalid, sourceglobalid, featurecategory]
  - kind: not_null
    col: globalid
  - kind: not_null
    col: sourceglobalid
  - kind: not_null
    col: featurecategory
  - kind: row_floor
    min: 58266
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
