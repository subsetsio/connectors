# compiled by `hardened compile-checks` — post-publish audit of `unicef-unicef:cme-subnat-npl:1.0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dimensions, ref_area, ref_area_name, time_period, indicator]
temporal: time_period
checks:
  - kind: unique
    columns: [dimensions, ref_area, ref_area_name, time_period, indicator]
  - kind: not_null
    col: dimensions
  - kind: not_null
    col: ref_area
  - kind: not_null
    col: ref_area_name
  - kind: not_null
    col: time_period
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 2597
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
