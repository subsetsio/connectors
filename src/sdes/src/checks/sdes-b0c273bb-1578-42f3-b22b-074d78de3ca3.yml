# compiled by `hardened compile-checks` — post-publish audit of `sdes-b0c273bb-1578-42f3-b22b-074d78de3ca3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [commune_libelle, consotot, commune_code, id, consot]
checks:
  - kind: unique
    columns: [commune_libelle, consotot, commune_code, id, consot]
  - kind: not_null
    col: commune_libelle
  - kind: not_null
    col: consotot
  - kind: not_null
    col: commune_code
  - kind: not_null
    col: id
  - kind: not_null
    col: consot
  - kind: row_floor
    min: 496
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
