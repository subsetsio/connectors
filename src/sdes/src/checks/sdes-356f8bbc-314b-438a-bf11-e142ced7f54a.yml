# compiled by `hardened compile-checks` — post-publish audit of `sdes-356f8bbc-314b-438a-bf11-e142ced7f54a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [trimestre, nb_pieces_coll, nature_projet]
checks:
  - kind: unique
    columns: [trimestre, nb_pieces_coll, nature_projet]
  - kind: not_null
    col: trimestre
  - kind: not_null
    col: nb_pieces_coll
  - kind: not_null
    col: nature_projet
  - kind: row_floor
    min: 318
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
