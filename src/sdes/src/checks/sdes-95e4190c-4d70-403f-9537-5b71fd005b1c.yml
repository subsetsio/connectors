# compiled by `hardened compile-checks` — post-publish audit of `sdes-95e4190c-4d70-403f-9537-5b71fd005b1c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [prix_m2, dep_code, dep_libelle, trimestre, type_lgt]
checks:
  - kind: unique
    columns: [prix_m2, dep_code, dep_libelle, trimestre, type_lgt]
  - kind: not_null
    col: prix_m2
  - kind: not_null
    col: dep_code
  - kind: not_null
    col: dep_libelle
  - kind: not_null
    col: trimestre
  - kind: not_null
    col: type_lgt
  - kind: row_floor
    min: 12189
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
