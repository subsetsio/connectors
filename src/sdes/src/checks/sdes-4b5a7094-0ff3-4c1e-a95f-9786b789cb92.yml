# compiled by `hardened compile-checks` — post-publish audit of `sdes-4b5a7094-0ff3-4c1e-a95f-9786b789cb92`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id, commune_code, ncc, consot, consor]
checks:
  - kind: unique
    columns: [id, commune_code, ncc, consot, consor]
  - kind: not_null
    col: id
  - kind: not_null
    col: commune_code
  - kind: not_null
    col: ncc
  - kind: not_null
    col: consot
  - kind: not_null
    col: consor
  - kind: row_floor
    min: 485
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
