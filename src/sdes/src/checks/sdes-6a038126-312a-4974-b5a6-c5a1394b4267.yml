# compiled by `hardened compile-checks` — post-publish audit of `sdes-6a038126-312a-4974-b5a6-c5a1394b4267`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [trimestre, nature_projet, type_lgt]
checks:
  - kind: unique
    columns: [trimestre, nature_projet, type_lgt]
  - kind: not_null
    col: trimestre
  - kind: not_null
    col: nature_projet
  - kind: not_null
    col: type_lgt
  - kind: row_floor
    min: 298
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
