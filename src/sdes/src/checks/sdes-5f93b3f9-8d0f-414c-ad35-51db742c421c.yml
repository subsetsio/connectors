# compiled by `hardened compile-checks` — post-publish audit of `sdes-5f93b3f9-8d0f-414c-ad35-51db742c421c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id, consor, commune_code, consot, pdl]
checks:
  - kind: unique
    columns: [id, consor, commune_code, consot, pdl]
  - kind: not_null
    col: id
  - kind: not_null
    col: consor
  - kind: not_null
    col: commune_code
  - kind: not_null
    col: consot
  - kind: not_null
    col: pdl
  - kind: row_floor
    min: 498
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
