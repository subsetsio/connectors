# compiled by `hardened compile-checks` — post-publish audit of `sdes-1a9ad5c2-0929-4687-8455-78aac6530a76`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [valeur_exposition, epci_libelle, epci_code, indicateur_exposition]
checks:
  - kind: unique
    columns: [valeur_exposition, epci_libelle, epci_code, indicateur_exposition]
  - kind: not_null
    col: valeur_exposition
  - kind: not_null
    col: epci_libelle
  - kind: not_null
    col: epci_code
  - kind: not_null
    col: indicateur_exposition
  - kind: row_floor
    min: 3702
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
