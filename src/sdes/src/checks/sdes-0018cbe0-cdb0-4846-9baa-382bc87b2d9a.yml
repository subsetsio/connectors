# compiled by `hardened compile-checks` — post-publish audit of `sdes-0018cbe0-cdb0-4846-9baa-382bc87b2d9a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [trimestre, nature_projet, nb_pieces_ind]
checks:
  - kind: unique
    columns: [trimestre, nature_projet, nb_pieces_ind]
  - kind: not_null
    col: trimestre
  - kind: not_null
    col: nature_projet
  - kind: not_null
    col: nb_pieces_ind
  - kind: row_floor
    min: 238
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
