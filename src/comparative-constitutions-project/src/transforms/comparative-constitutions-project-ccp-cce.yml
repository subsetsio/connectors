# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `comparative-constitutions-project-ccp-cce`. Regenerate after model-verify.
# caution: Rows are constitutional chronology events, not one row per constitution or one row per country-year; aggregate only after choosing the event types relevant to the analysis.
key: [year, country, cowcode, evnttype]
temporal: year
sort: [year, country, cowcode, evnttype]
columns:
  - name: cowcode
    type: BIGINT
  - name: country
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: systid
    type: BIGINT
  - name: evntid
    type: BIGINT
  - name: evnttype
    type: VARCHAR
