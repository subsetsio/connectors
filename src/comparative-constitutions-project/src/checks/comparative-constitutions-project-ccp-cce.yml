# compiled by `hardened compile-checks` — post-publish audit of `comparative-constitutions-project-ccp-cce`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, country, cowcode, evnttype]
temporal: year
checks:
  - kind: unique
    columns: [year, country, cowcode, evnttype]
  - kind: not_null
    col: year
  - kind: not_null
    col: country
  - kind: not_null
    col: cowcode
  - kind: not_null
    col: evnttype
  - kind: row_floor
    min: 10319
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
