spec_id: imf-its
coverage:
  frozen: true
  reason: closed/vintage dataflow — the raw corpus stops at TIME_PERIOD max ~2015; it does not advance, so no freshness assertion applies.
status: active
tests:
  - row_count: {min: 1}
    reason: a full-dataflow SDMX-CSV pull returns observations even for a closed corpus.
    certainty: 95
