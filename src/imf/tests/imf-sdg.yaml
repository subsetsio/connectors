spec_id: imf-sdg
status: active
tests:
  - row_count: {min: 1}
    reason: raw rows exist even though the IMF endpoint returns them with empty dimensions/values; the transform (not this raw test) drops them to zero, which the waiver covers.
    certainty: 90
