# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `kobe-customs-chugoku`. Regenerate after model-verify.
# caution: This is a long-form extraction of multi-sheet Excel crosstabs; sheets, row labels, and column headers include totals, subtotals, ratios, shares, and year-on-year comparisons, so filter to the intended measure before aggregating values.
key: [year, sheet, classification, row_idx, col_idx]
temporal: year
sort: [year, sheet, classification, row_idx, col_idx]
columns:
  - name: year
    type: INTEGER
  - name: classification
    type: VARCHAR
  - name: sheet
    type: BIGINT
  - name: row_label
    type: VARCHAR
  - name: col_header
    type: VARCHAR
  - name: row_idx
    type: INTEGER
  - name: col_idx
    type: INTEGER
  - name: value
    type: DOUBLE
