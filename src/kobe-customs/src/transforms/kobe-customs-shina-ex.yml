# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `kobe-customs-shina-ex`. Regenerate after model-verify.
# caution: This port-by-commodity export matrix includes commodity totals, composition shares, year-on-year rates, and contribution measures alongside trade values, so filter to the intended measure before aggregating values.
key: [col_header, row_label, sheet, year]
temporal: year
sort: [year, col_header, row_label, sheet]
columns:
  - name: year
    type: INTEGER
  - name: classification
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: row_label
    type: VARCHAR
  - name: col_header
    type: VARCHAR
  - name: row_idx
    type: INTEGER
  - name: col_idx
    type: INTEGER
  - name: value
    type: DOUBLE
