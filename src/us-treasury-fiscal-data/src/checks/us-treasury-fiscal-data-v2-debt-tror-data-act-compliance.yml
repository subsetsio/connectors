# compiled by `hardened compile-checks` — post-publish audit of `us-treasury-fiscal-data-v2-debt-tror-data-act-compliance`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [record_date, agency_nm, bureau_nm]
temporal: record_date
checks:
  - kind: unique
    columns: [record_date, agency_nm, bureau_nm]
  - kind: not_null
    col: record_date
  - kind: not_null
    col: agency_nm
  - kind: not_null
    col: bureau_nm
  - kind: row_floor
    min: 6254
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
