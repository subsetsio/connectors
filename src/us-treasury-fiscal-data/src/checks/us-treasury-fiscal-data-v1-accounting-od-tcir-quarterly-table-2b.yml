# compiled by `hardened compile-checks` — post-publish audit of `us-treasury-fiscal-data-v1-accounting-od-tcir-quarterly-table-2b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [record_date, effective_date_range, src_line_nbr, legislation]
temporal: record_date
checks:
  - kind: unique
    columns: [record_date, effective_date_range, src_line_nbr, legislation]
  - kind: not_null
    col: record_date
  - kind: not_null
    col: effective_date_range
  - kind: not_null
    col: src_line_nbr
  - kind: not_null
    col: legislation
  - kind: row_floor
    min: 258
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
