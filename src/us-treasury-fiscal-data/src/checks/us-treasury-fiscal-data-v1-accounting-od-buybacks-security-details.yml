# compiled by `hardened compile-checks` — post-publish audit of `us-treasury-fiscal-data-v1-accounting-od-buybacks-security-details`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [operation_date, operation_start_time_est, cusip_nbr]
temporal: operation_date
checks:
  - kind: unique
    columns: [operation_date, operation_start_time_est, cusip_nbr]
  - kind: not_null
    col: operation_date
  - kind: not_null
    col: operation_start_time_est
  - kind: not_null
    col: cusip_nbr
  - kind: row_floor
    min: 2745
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
