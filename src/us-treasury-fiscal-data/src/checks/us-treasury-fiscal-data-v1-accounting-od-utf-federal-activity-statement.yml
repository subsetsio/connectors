# compiled by `hardened compile-checks` — post-publish audit of `us-treasury-fiscal-data-v1-accounting-od-utf-federal-activity-statement`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [eff_date, memo_nbr, shares_per_par]
temporal: eff_date
checks:
  - kind: unique
    columns: [eff_date, memo_nbr, shares_per_par]
  - kind: not_null
    col: eff_date
  - kind: not_null
    col: memo_nbr
  - kind: not_null
    col: shares_per_par
  - kind: row_floor
    min: 493164
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
