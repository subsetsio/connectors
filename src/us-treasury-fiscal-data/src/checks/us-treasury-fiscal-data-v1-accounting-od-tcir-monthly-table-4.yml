# compiled by `hardened compile-checks` — post-publish audit of `us-treasury-fiscal-data-v1-accounting-od-tcir-monthly-table-4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [record_date, from_and_including, up_to_but_not_including, table_nm, src_line_nbr]
temporal: record_date
checks:
  - kind: unique
    columns: [record_date, from_and_including, up_to_but_not_including, table_nm, src_line_nbr]
  - kind: not_null
    col: record_date
  - kind: not_null
    col: from_and_including
  - kind: not_null
    col: up_to_but_not_including
  - kind: not_null
    col: table_nm
  - kind: not_null
    col: src_line_nbr
  - kind: row_floor
    min: 2220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
