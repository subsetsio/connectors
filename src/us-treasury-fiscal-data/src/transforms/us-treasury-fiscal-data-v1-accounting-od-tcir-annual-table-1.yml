# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `us-treasury-fiscal-data-v1-accounting-od-tcir-annual-table-1`. Regenerate after model-verify.
# caution: One `record_date` carries many `fiscal_year` rows — `fiscal_year` is part of the row identity, because each publication restates a whole block of years. Filter or group by `fiscal_year` before aggregating.
key: [record_date, up_to_but_not_including, from_and_including, src_line_nbr, fiscal_year]
temporal: record_date
sort: [record_date, up_to_but_not_including, from_and_including, src_line_nbr, fiscal_year]
columns:
  - name: record_date
    type: DATE
  - name: table_nm
    type: VARCHAR
  - name: from_and_including
    type: VARCHAR
  - name: up_to_but_not_including
    type: VARCHAR
  - name: fiscal_year
    type: BIGINT
  - name: fiscal_year_rate
    type: VARCHAR
  - name: src_line_nbr
    type: BIGINT
