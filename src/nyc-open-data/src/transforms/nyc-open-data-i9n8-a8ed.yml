# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `nyc-open-data-i9n8-a8ed`. Regenerate after model-verify.
key: [date, profile_id, award]
temporal: date
sort: [date, profile_id, award]
columns:
  - name: profile_id
    type: VARCHAR
  - name: date
    type: DATE
  - name: award
    type: VARCHAR
  - name: export_date
    type: DATE
