# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-v988-8fd7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [published_date, health_event, forecast_description]
temporal: published_date
checks:
  - kind: unique
    columns: [published_date, health_event, forecast_description]
  - kind: not_null
    col: published_date
  - kind: not_null
    col: health_event
  - kind: not_null
    col: forecast_description
  - kind: row_floor
    min: 14
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
