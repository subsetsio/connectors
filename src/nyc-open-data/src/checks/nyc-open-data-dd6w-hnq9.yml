# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-dd6w-hnq9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [respondent_serial, cfmc_case_id, qtimehome1, qtimework1, qzipwork1]
temporal: starttime_est
checks:
  - kind: unique
    columns: [respondent_serial, cfmc_case_id, qtimehome1, qtimework1, qzipwork1]
  - kind: not_null
    col: respondent_serial
  - kind: not_null
    col: cfmc_case_id
  - kind: not_null
    col: qtimehome1
  - kind: not_null
    col: qtimework1
  - kind: not_null
    col: qzipwork1
  - kind: row_floor
    min: 1801
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
