# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-rv63-53db`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [multipolygon, freq_resfuseid, schedulecode, section, district]
checks:
  - kind: unique
    columns: [multipolygon, freq_resfuseid, schedulecode, section, district]
  - kind: not_null
    col: multipolygon
  - kind: not_null
    col: freq_resfuseid
  - kind: not_null
    col: schedulecode
  - kind: not_null
    col: section
  - kind: not_null
    col: district
  - kind: row_floor
    min: 305
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
