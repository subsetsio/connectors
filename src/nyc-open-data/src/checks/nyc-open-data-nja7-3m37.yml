# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-nja7-3m37`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [key_performance_indicator, baseline_measure_3q_2015, metric, "4q_2015_measure", objective]
checks:
  - kind: unique
    columns: [key_performance_indicator, baseline_measure_3q_2015, metric, "4q_2015_measure", objective]
  - kind: not_null
    col: key_performance_indicator
  - kind: not_null
    col: baseline_measure_3q_2015
  - kind: not_null
    col: metric
  - kind: not_null
    col: "4q_2015_measure"
  - kind: not_null
    col: objective
  - kind: row_floor
    min: 31
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
