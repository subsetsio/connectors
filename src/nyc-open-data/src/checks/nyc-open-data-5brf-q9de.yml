# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-5brf-q9de`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location_name, dbn, sy201415_enrollment, ratio_gc_only, ratio_gc_sw]
checks:
  - kind: unique
    columns: [location_name, dbn, sy201415_enrollment, ratio_gc_only, ratio_gc_sw]
  - kind: not_null
    col: location_name
  - kind: not_null
    col: dbn
  - kind: not_null
    col: sy201415_enrollment
  - kind: not_null
    col: ratio_gc_only
  - kind: not_null
    col: ratio_gc_sw
  - kind: row_floor
    min: 822
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
