# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-kg6x-kv5b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, sy1617_total_removalssuspensions, "09_principal", "10_principal", "08_principal"]
checks:
  - kind: unique
    columns: [dbn, sy1617_total_removalssuspensions, "09_principal", "10_principal", "08_principal"]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: sy1617_total_removalssuspensions
  - kind: not_null
    col: "09_principal"
  - kind: not_null
    col: "10_principal"
  - kind: not_null
    col: "08_principal"
  - kind: row_floor
    min: 832
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
