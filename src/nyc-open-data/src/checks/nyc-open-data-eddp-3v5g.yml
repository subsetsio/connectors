# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-eddp-3v5g`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [doc_employee_id, rank_title, leave_status_desc, gender]
checks:
  - kind: unique
    columns: [doc_employee_id, rank_title, leave_status_desc, gender]
  - kind: not_null
    col: doc_employee_id
  - kind: not_null
    col: rank_title
  - kind: not_null
    col: leave_status_desc
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 22766
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
