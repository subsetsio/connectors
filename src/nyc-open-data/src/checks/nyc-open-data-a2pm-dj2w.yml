# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-a2pm-dj2w`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [development, number_of_public_housing_residents_who_passed_the_test_assessing_secondary_completion_tasc_1]
checks:
  - kind: unique
    columns: [development, number_of_public_housing_residents_who_passed_the_test_assessing_secondary_completion_tasc_1]
  - kind: not_null
    col: development
  - kind: not_null
    col: number_of_public_housing_residents_who_passed_the_test_assessing_secondary_completion_tasc_1
  - kind: row_floor
    min: 169
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
