# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-n8q6-i44s`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [multipolygon, system, parks_district, featurestatus]
checks:
  - kind: unique
    columns: [multipolygon, system, parks_district, featurestatus]
  - kind: not_null
    col: multipolygon
  - kind: not_null
    col: system
  - kind: not_null
    col: parks_district
  - kind: not_null
    col: featurestatus
  - kind: row_floor
    min: 1426
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
