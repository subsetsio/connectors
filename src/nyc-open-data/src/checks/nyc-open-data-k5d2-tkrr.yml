# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-k5d2-tkrr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [remote, blended, remote_1, blended_1, category]
checks:
  - kind: unique
    columns: [remote, blended, remote_1, blended_1, category]
  - kind: not_null
    col: remote
  - kind: not_null
    col: blended
  - kind: not_null
    col: remote_1
  - kind: not_null
    col: blended_1
  - kind: not_null
    col: category
  - kind: row_floor
    min: 438
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
