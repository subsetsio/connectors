# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-34zf-iv73`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [entity_name, project_id, project_name, reporting_quarter, entity_role]
checks:
  - kind: unique
    columns: [entity_name, project_id, project_name, reporting_quarter, entity_role]
  - kind: not_null
    col: entity_name
  - kind: not_null
    col: project_id
  - kind: not_null
    col: project_name
  - kind: not_null
    col: reporting_quarter
  - kind: not_null
    col: entity_role
  - kind: row_floor
    min: 18094
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
