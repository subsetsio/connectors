# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-3btx-p4av`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [manhattan_cooperatives_comparable_properties_boroblocklot]
checks:
  - kind: unique
    columns: [manhattan_cooperatives_comparable_properties_boroblocklot]
  - kind: not_null
    col: manhattan_cooperatives_comparable_properties_boroblocklot
  - kind: row_floor
    min: 1315
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
