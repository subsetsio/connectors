# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-scjj-6yaf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indexnumber, tenantname, floorsuiterequested, ownername, tenantfloor]
temporal: requestdateofentry
checks:
  - kind: unique
    columns: [indexnumber, tenantname, floorsuiterequested, ownername, tenantfloor]
  - kind: not_null
    col: indexnumber
  - kind: not_null
    col: tenantname
  - kind: not_null
    col: floorsuiterequested
  - kind: not_null
    col: ownername
  - kind: not_null
    col: tenantfloor
  - kind: row_floor
    min: 1182
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
