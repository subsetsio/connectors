# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-q7ra-ebu4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [school_code, building_name, building_code, borough_of_building, building_level]
checks:
  - kind: unique
    columns: [school_code, building_name, building_code, borough_of_building, building_level]
  - kind: not_null
    col: school_code
  - kind: not_null
    col: building_name
  - kind: not_null
    col: building_code
  - kind: not_null
    col: borough_of_building
  - kind: not_null
    col: building_level
  - kind: row_floor
    min: 431
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
