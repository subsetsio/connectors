# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ne9b-qgmm`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [school_dbn, of_students_over_five_active_on_register]
checks:
  - kind: unique
    columns: [school_dbn, of_students_over_five_active_on_register]
  - kind: not_null
    col: school_dbn
  - kind: not_null
    col: of_students_over_five_active_on_register
  - kind: row_floor
    min: 792
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
