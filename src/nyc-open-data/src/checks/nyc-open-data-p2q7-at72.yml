# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-p2q7-at72`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [registration_date, contract_description, epp_product_types, agency]
temporal: registration_date
checks:
  - kind: unique
    columns: [registration_date, contract_description, epp_product_types, agency]
  - kind: not_null
    col: registration_date
  - kind: not_null
    col: contract_description
  - kind: not_null
    col: epp_product_types
  - kind: not_null
    col: agency
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
