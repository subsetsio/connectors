# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-4bae-3jps`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [bphys_id, the_geom, stname_lab, plntseas]
checks:
  - kind: unique
    columns: [bphys_id, the_geom, stname_lab, plntseas]
  - kind: not_null
    col: bphys_id
  - kind: not_null
    col: the_geom
  - kind: not_null
    col: stname_lab
  - kind: not_null
    col: plntseas
  - kind: row_floor
    min: 10256
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
