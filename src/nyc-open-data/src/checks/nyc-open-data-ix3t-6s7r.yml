# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ix3t-6s7r`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [org_id, bldg_id, org_name, bldg_name, room_category]
checks:
  - kind: unique
    columns: [org_id, bldg_id, org_name, bldg_name, room_category]
  - kind: not_null
    col: org_id
  - kind: not_null
    col: bldg_id
  - kind: not_null
    col: org_name
  - kind: not_null
    col: bldg_name
  - kind: not_null
    col: room_category
  - kind: row_floor
    min: 6462
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
