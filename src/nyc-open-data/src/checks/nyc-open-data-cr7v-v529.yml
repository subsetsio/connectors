# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-cr7v-v529`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [schooldbn, total_consented_students_ytd, hispanic_of_any_race, iep, total_consents_withdrawn]
checks:
  - kind: unique
    columns: [schooldbn, total_consented_students_ytd, hispanic_of_any_race, iep, total_consents_withdrawn]
  - kind: not_null
    col: schooldbn
  - kind: not_null
    col: total_consented_students_ytd
  - kind: not_null
    col: hispanic_of_any_race
  - kind: not_null
    col: iep
  - kind: not_null
    col: total_consents_withdrawn
  - kind: row_floor
    min: 792
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
