# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-q9xk-w9iv`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [org_id, organization_name, bldg_id, building_name, incl_class]
checks:
  - kind: unique
    columns: [org_id, organization_name, bldg_id, building_name, incl_class]
  - kind: not_null
    col: org_id
  - kind: not_null
    col: organization_name
  - kind: not_null
    col: bldg_id
  - kind: not_null
    col: building_name
  - kind: not_null
    col: incl_class
  - kind: row_floor
    min: 1329
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
