# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-28rh-vpvr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [vendor_name, vehicle_typedescription, school_year]
checks:
  - kind: unique
    columns: [vendor_name, vehicle_typedescription, school_year]
  - kind: not_null
    col: vendor_name
  - kind: not_null
    col: vehicle_typedescription
  - kind: not_null
    col: school_year
  - kind: row_floor
    min: 671
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
