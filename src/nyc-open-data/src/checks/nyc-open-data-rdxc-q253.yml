# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-rdxc-q253`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [schedule_date, permit_number, schedule_time]
temporal: schedule_date
checks:
  - kind: unique
    columns: [schedule_date, permit_number, schedule_time]
  - kind: not_null
    col: schedule_date
  - kind: not_null
    col: permit_number
  - kind: not_null
    col: schedule_time
  - kind: row_floor
    min: 3885
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
