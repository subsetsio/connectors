# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9cw8-7heb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [domain_registration_date, domain_name, nexus_category]
temporal: domain_registration_date
checks:
  - kind: unique
    columns: [domain_registration_date, domain_name, nexus_category]
  - kind: not_null
    col: domain_registration_date
  - kind: not_null
    col: domain_name
  - kind: not_null
    col: nexus_category
  - kind: row_floor
    min: 25711
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
