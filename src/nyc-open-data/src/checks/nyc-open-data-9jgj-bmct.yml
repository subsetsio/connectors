# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9jgj-bmct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date_received, complaint_number, complaint_status]
temporal: date_received
checks:
  - kind: unique
    columns: [date_received, complaint_number, complaint_status]
  - kind: not_null
    col: date_received
  - kind: not_null
    col: complaint_number
  - kind: not_null
    col: complaint_status
  - kind: row_floor
    min: 55497
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
