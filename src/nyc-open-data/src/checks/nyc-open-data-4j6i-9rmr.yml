# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-4j6i-9rmr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [recommendation_date, ulurp_application_name, ulurp_numbers, borough_president_recommendation]
temporal: recommendation_date
checks:
  - kind: unique
    columns: [recommendation_date, ulurp_application_name, ulurp_numbers, borough_president_recommendation]
  - kind: not_null
    col: recommendation_date
  - kind: not_null
    col: ulurp_application_name
  - kind: not_null
    col: ulurp_numbers
  - kind: not_null
    col: borough_president_recommendation
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
