# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-n9yn-6q3f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, principal, removals, superintendent, location_category]
checks:
  - kind: unique
    columns: [dbn, principal, removals, superintendent, location_category]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: principal
  - kind: not_null
    col: removals
  - kind: not_null
    col: superintendent
  - kind: not_null
    col: location_category
  - kind: row_floor
    min: 871
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
