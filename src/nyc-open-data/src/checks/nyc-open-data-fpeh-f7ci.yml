# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-fpeh-f7ci`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location, business_legal_name, assumed_names, license_type]
temporal: license_issue_date
checks:
  - kind: unique
    columns: [location, business_legal_name, assumed_names, license_type]
  - kind: not_null
    col: location
  - kind: not_null
    col: business_legal_name
  - kind: not_null
    col: assumed_names
  - kind: not_null
    col: license_type
  - kind: row_floor
    min: 917
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
