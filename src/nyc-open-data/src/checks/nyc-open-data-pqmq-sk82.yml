# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-pqmq-sk82`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [report_date, engagement_category, age_group]
temporal: report_date
checks:
  - kind: unique
    columns: [report_date, engagement_category, age_group]
  - kind: not_null
    col: report_date
  - kind: not_null
    col: engagement_category
  - kind: not_null
    col: age_group
  - kind: row_floor
    min: 3178
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
