# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-x4w5-m7uh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [candidate_id, name, election_cycle, penalty_or_public_funds_still_outstanding]
temporal: date_draft_audit_report_dar_sent_to_campaign
checks:
  - kind: unique
    columns: [candidate_id, name, election_cycle, penalty_or_public_funds_still_outstanding]
  - kind: not_null
    col: candidate_id
  - kind: not_null
    col: name
  - kind: not_null
    col: election_cycle
  - kind: not_null
    col: penalty_or_public_funds_still_outstanding
  - kind: row_floor
    min: 1851
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
