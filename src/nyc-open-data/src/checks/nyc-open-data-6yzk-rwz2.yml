# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-6yzk-rwz2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [broadband_relationship_to_household_or_person_and_the_referenced_category]
checks:
  - kind: unique
    columns: [broadband_relationship_to_household_or_person_and_the_referenced_category]
  - kind: not_null
    col: broadband_relationship_to_household_or_person_and_the_referenced_category
  - kind: row_floor
    min: 2
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
