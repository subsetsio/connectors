# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-tkdy-59zg`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [staten_island_a_condominiums_comparable_properties_a_condo_section]
checks:
  - kind: unique
    columns: [staten_island_a_condominiums_comparable_properties_a_condo_section]
  - kind: not_null
    col: staten_island_a_condominiums_comparable_properties_a_condo_section
  - kind: row_floor
    min: 13
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
