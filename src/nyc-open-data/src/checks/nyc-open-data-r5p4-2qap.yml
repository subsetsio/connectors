# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-r5p4-2qap`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, "15_principal", "16_principal", "13_principal", "17_principal"]
checks:
  - kind: unique
    columns: [dbn, "15_principal", "16_principal", "13_principal", "17_principal"]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: "15_principal"
  - kind: not_null
    col: "16_principal"
  - kind: not_null
    col: "13_principal"
  - kind: not_null
    col: "17_principal"
  - kind: row_floor
    min: 871
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
