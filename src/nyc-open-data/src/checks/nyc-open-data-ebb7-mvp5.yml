# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ebb7-mvp5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month, communitydistrict, borough_id, borough]
checks:
  - kind: unique
    columns: [month, communitydistrict, borough_id, borough]
  - kind: not_null
    col: month
  - kind: not_null
    col: communitydistrict
  - kind: not_null
    col: borough_id
  - kind: not_null
    col: borough
  - kind: row_floor
    min: 12559
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
