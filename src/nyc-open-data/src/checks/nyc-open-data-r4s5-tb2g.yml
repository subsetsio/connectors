# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-r4s5-tb2g`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [queens_cooperatives_comparable_properties_boroblocklot]
checks:
  - kind: unique
    columns: [queens_cooperatives_comparable_properties_boroblocklot]
  - kind: not_null
    col: queens_cooperatives_comparable_properties_boroblocklot
  - kind: row_floor
    min: 480
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
