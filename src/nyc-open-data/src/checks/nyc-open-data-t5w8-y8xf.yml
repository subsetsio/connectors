# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-t5w8-y8xf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [school_name, dbn, building_ids, geo_district_ofbuildings, borough]
checks:
  - kind: unique
    columns: [school_name, dbn, building_ids, geo_district_ofbuildings, borough]
  - kind: not_null
    col: school_name
  - kind: not_null
    col: dbn
  - kind: not_null
    col: building_ids
  - kind: not_null
    col: geo_district_ofbuildings
  - kind: not_null
    col: borough
  - kind: row_floor
    min: 531
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
