# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-3wxk-qa8q`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date_collected, kit_id_number, received_date, borough]
temporal: date_collected
checks:
  - kind: unique
    columns: [date_collected, kit_id_number, received_date, borough]
  - kind: not_null
    col: date_collected
  - kind: not_null
    col: kit_id_number
  - kind: not_null
    col: received_date
  - kind: not_null
    col: borough
  - kind: row_floor
    min: 2302
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
