# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ws4c-4g69`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [procurement_title, epin, service_name, procurement_description, agency]
checks:
  - kind: unique
    columns: [procurement_title, epin, service_name, procurement_description, agency]
  - kind: not_null
    col: procurement_title
  - kind: not_null
    col: epin
  - kind: not_null
    col: service_name
  - kind: not_null
    col: procurement_description
  - kind: not_null
    col: agency
  - kind: row_floor
    min: 297
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
