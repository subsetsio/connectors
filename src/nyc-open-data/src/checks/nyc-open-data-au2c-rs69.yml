# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-au2c-rs69`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_as_of, community_district, data_published_in_district_resource_statement, borough]
temporal: data_as_of
checks:
  - kind: unique
    columns: [data_as_of, community_district, data_published_in_district_resource_statement, borough]
  - kind: not_null
    col: data_as_of
  - kind: not_null
    col: community_district
  - kind: not_null
    col: data_published_in_district_resource_statement
  - kind: not_null
    col: borough
  - kind: row_floor
    min: 177
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
