# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-c4xj-jp46`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [location_name, locationcode, location_category_description]
checks:
  - kind: unique
    columns: [location_name, locationcode, location_category_description]
  - kind: not_null
    col: location_name
  - kind: not_null
    col: locationcode
  - kind: not_null
    col: location_category_description
  - kind: row_floor
    min: 460
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
