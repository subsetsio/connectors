# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-bwhw-9jek`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [service_taxonomy_id, service_id, taxonomy_id]
checks:
  - kind: unique
    columns: [service_taxonomy_id, service_id, taxonomy_id]
  - kind: not_null
    col: service_taxonomy_id
  - kind: not_null
    col: service_id
  - kind: not_null
    col: taxonomy_id
  - kind: row_floor
    min: 5652
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
