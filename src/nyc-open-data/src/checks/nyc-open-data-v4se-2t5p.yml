# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-v4se-2t5p`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [school_name, dbn, program_cip_code, program_name, industry_cluster]
checks:
  - kind: unique
    columns: [school_name, dbn, program_cip_code, program_name, industry_cluster]
  - kind: not_null
    col: school_name
  - kind: not_null
    col: dbn
  - kind: not_null
    col: program_cip_code
  - kind: not_null
    col: program_name
  - kind: not_null
    col: industry_cluster
  - kind: row_floor
    min: 139
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
