# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-imfq-nf3j`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [the_geom, ct2020, nta2020, ntaname, cdtaname]
checks:
  - kind: unique
    columns: [the_geom, ct2020, nta2020, ntaname, cdtaname]
  - kind: not_null
    col: the_geom
  - kind: not_null
    col: ct2020
  - kind: not_null
    col: nta2020
  - kind: not_null
    col: ntaname
  - kind: not_null
    col: cdtaname
  - kind: row_floor
    min: 1163
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
