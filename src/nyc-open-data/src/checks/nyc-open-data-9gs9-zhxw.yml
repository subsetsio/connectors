# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9gs9-zhxw`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [program_code, school_name, school_dbn, selection_criteria]
checks:
  - kind: unique
    columns: [program_code, school_name, school_dbn, selection_criteria]
  - kind: not_null
    col: program_code
  - kind: not_null
    col: school_name
  - kind: not_null
    col: school_dbn
  - kind: not_null
    col: selection_criteria
  - kind: row_floor
    min: 231
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
