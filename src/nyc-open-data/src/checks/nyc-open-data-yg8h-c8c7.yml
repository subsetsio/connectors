# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-yg8h-c8c7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [program_cip_code, school_name, dbn, program_name, industry_cluster]
checks:
  - kind: unique
    columns: [program_cip_code, school_name, dbn, program_name, industry_cluster]
  - kind: not_null
    col: program_cip_code
  - kind: not_null
    col: school_name
  - kind: not_null
    col: dbn
  - kind: not_null
    col: program_name
  - kind: not_null
    col: industry_cluster
  - kind: row_floor
    min: 133
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
