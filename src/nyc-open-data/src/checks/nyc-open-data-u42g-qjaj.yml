# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-u42g-qjaj`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [agency_name, agency_code, fiscal_year, actualplan, cost_category]
checks:
  - kind: unique
    columns: [agency_name, agency_code, fiscal_year, actualplan, cost_category]
  - kind: not_null
    col: agency_name
  - kind: not_null
    col: agency_code
  - kind: not_null
    col: fiscal_year
  - kind: not_null
    col: actualplan
  - kind: not_null
    col: cost_category
  - kind: row_floor
    min: 3032
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
