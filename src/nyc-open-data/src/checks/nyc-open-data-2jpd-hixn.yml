# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-2jpd-hixn`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [administrative_district_code, total_removalssuspensions, principal, removal, superintendent]
checks:
  - kind: unique
    columns: [administrative_district_code, total_removalssuspensions, principal, removal, superintendent]
  - kind: not_null
    col: administrative_district_code
  - kind: not_null
    col: total_removalssuspensions
  - kind: not_null
    col: principal
  - kind: not_null
    col: removal
  - kind: not_null
    col: superintendent
  - kind: row_floor
    min: 17
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
