# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-hjvj-jfc9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nycha_development, number_of_residents_enrolled_in_courses_or_otherwise_received_guidance, year]
checks:
  - kind: unique
    columns: [nycha_development, number_of_residents_enrolled_in_courses_or_otherwise_received_guidance, year]
  - kind: not_null
    col: nycha_development
  - kind: not_null
    col: number_of_residents_enrolled_in_courses_or_otherwise_received_guidance
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1401
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
