# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9tnu-6rdi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [development, year, number_of_public_housing_residents_who_earned_hse_diplomas]
checks:
  - kind: unique
    columns: [development, year, number_of_public_housing_residents_who_earned_hse_diplomas]
  - kind: not_null
    col: development
  - kind: not_null
    col: year
  - kind: not_null
    col: number_of_public_housing_residents_who_earned_hse_diplomas
  - kind: row_floor
    min: 382
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
