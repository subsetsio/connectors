# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-54rr-tdmz`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, sy1617_total_removalssuspensions, gen_ed_principal, swd_principal, gen_ed_removals]
checks:
  - kind: unique
    columns: [dbn, sy1617_total_removalssuspensions, gen_ed_principal, swd_principal, gen_ed_removals]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: sy1617_total_removalssuspensions
  - kind: not_null
    col: gen_ed_principal
  - kind: not_null
    col: swd_principal
  - kind: not_null
    col: gen_ed_removals
  - kind: row_floor
    min: 832
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
