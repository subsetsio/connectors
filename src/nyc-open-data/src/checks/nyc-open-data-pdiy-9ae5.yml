# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-pdiy-9ae5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [expires_on, holder_name, cof_id, cof_type]
temporal: expires_on
checks:
  - kind: unique
    columns: [expires_on, holder_name, cof_id, cof_type]
  - kind: not_null
    col: expires_on
  - kind: not_null
    col: holder_name
  - kind: not_null
    col: cof_id
  - kind: not_null
    col: cof_type
  - kind: row_floor
    min: 64146
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
