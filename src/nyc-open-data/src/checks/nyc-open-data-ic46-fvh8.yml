# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ic46-fvh8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [neighborhood_tabulation_area, health_events]
temporal: published_date
checks:
  - kind: unique
    columns: [neighborhood_tabulation_area, health_events]
  - kind: not_null
    col: neighborhood_tabulation_area
  - kind: not_null
    col: health_events
  - kind: row_floor
    min: 262
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
