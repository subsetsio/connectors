# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-fpci-ws56`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [school_name, beds_code, district_name, school_year]
checks:
  - kind: unique
    columns: [school_name, beds_code, district_name, school_year]
  - kind: not_null
    col: school_name
  - kind: not_null
    col: beds_code
  - kind: not_null
    col: district_name
  - kind: not_null
    col: school_year
  - kind: row_floor
    min: 5463
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
