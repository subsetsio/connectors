# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-gkd7-3vk7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_as_of, organization_name, org_id, bldg_name, bldg_id]
temporal: data_as_of
checks:
  - kind: unique
    columns: [data_as_of, organization_name, org_id, bldg_name, bldg_id]
  - kind: not_null
    col: data_as_of
  - kind: not_null
    col: organization_name
  - kind: not_null
    col: org_id
  - kind: not_null
    col: bldg_name
  - kind: not_null
    col: bldg_id
  - kind: row_floor
    min: 8811
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
