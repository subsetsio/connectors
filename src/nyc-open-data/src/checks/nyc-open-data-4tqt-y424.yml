# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-4tqt-y424`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [vendor_name, job_type, school_year, type_of_service]
checks:
  - kind: unique
    columns: [vendor_name, job_type, school_year, type_of_service]
  - kind: not_null
    col: vendor_name
  - kind: not_null
    col: job_type
  - kind: not_null
    col: school_year
  - kind: not_null
    col: type_of_service
  - kind: row_floor
    min: 664
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
