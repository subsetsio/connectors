# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9a87-6m4x`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [eo50_approval_date, business_name, business_city, business_state]
temporal: eo50_approval_date
checks:
  - kind: unique
    columns: [eo50_approval_date, business_name, business_city, business_state]
  - kind: not_null
    col: eo50_approval_date
  - kind: not_null
    col: business_name
  - kind: not_null
    col: business_city
  - kind: not_null
    col: business_state
  - kind: row_floor
    min: 340
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
