# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-nxmt-wszr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [report_ending_weekly_starting_on_sundays]
temporal: report_ending_weekly_starting_on_sundays
checks:
  - kind: unique
    columns: [report_ending_weekly_starting_on_sundays]
  - kind: not_null
    col: report_ending_weekly_starting_on_sundays
  - kind: row_floor
    min: 170
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
