# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-4e9g-bgra`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, sy1718_total_removalssuspensions, principal, removals, superintendent]
checks:
  - kind: unique
    columns: [dbn, sy1718_total_removalssuspensions, principal, removals, superintendent]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: sy1718_total_removalssuspensions
  - kind: not_null
    col: principal
  - kind: not_null
    col: removals
  - kind: not_null
    col: superintendent
  - kind: row_floor
    min: 830
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
