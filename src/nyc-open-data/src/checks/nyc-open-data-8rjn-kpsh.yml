# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-8rjn-kpsh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [unitid, the_geom, treatment, ownership, outfall_ty]
checks:
  - kind: unique
    columns: [unitid, the_geom, treatment, ownership, outfall_ty]
  - kind: not_null
    col: unitid
  - kind: not_null
    col: the_geom
  - kind: not_null
    col: treatment
  - kind: not_null
    col: ownership
  - kind: not_null
    col: outfall_ty
  - kind: row_floor
    min: 2714
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
