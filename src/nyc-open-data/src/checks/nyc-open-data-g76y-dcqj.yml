# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-g76y-dcqj`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [variance_end_datetime, ahv_permit_number, variance_start_datetime]
temporal: variance_start_datetime
checks:
  - kind: unique
    columns: [variance_end_datetime, ahv_permit_number, variance_start_datetime]
  - kind: not_null
    col: variance_end_datetime
  - kind: not_null
    col: ahv_permit_number
  - kind: not_null
    col: variance_start_datetime
  - kind: row_floor
    min: 127650
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
