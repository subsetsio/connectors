# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-5ri2-49bt`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [goal_description, service_name, agency_name, agency, goal]
checks:
  - kind: unique
    columns: [goal_description, service_name, agency_name, agency, goal]
  - kind: not_null
    col: goal_description
  - kind: not_null
    col: service_name
  - kind: not_null
    col: agency_name
  - kind: not_null
    col: agency
  - kind: not_null
    col: goal
  - kind: row_floor
    min: 157
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
