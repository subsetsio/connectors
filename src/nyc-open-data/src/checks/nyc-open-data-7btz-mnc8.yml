# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-7btz-mnc8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [provider_last_name, provider_first_name, practice_name, site_name, practice_zip_code]
checks:
  - kind: unique
    columns: [provider_last_name, provider_first_name, practice_name, site_name, practice_zip_code]
  - kind: not_null
    col: provider_last_name
  - kind: not_null
    col: provider_first_name
  - kind: not_null
    col: practice_name
  - kind: not_null
    col: site_name
  - kind: not_null
    col: practice_zip_code
  - kind: row_floor
    min: 1188
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
