# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-jign-uhe6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [address, organization_name, target_geographic_area]
checks:
  - kind: unique
    columns: [address, organization_name, target_geographic_area]
  - kind: not_null
    col: address
  - kind: not_null
    col: organization_name
  - kind: not_null
    col: target_geographic_area
  - kind: row_floor
    min: 11
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
