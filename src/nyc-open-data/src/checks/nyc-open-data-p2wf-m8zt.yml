# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-p2wf-m8zt`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_as_of, community_district_name, borough]
temporal: data_as_of
checks:
  - kind: unique
    columns: [data_as_of, community_district_name, borough]
  - kind: not_null
    col: data_as_of
  - kind: not_null
    col: community_district_name
  - kind: not_null
    col: borough
  - kind: row_floor
    min: 348
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
