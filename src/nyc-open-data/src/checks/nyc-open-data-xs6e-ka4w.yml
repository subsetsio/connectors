# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-xs6e-ka4w`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [project, school, construction_start, design_start, actual_est_comp]
checks:
  - kind: unique
    columns: [project, school, construction_start, design_start, actual_est_comp]
  - kind: not_null
    col: project
  - kind: not_null
    col: school
  - kind: not_null
    col: construction_start
  - kind: not_null
    col: design_start
  - kind: not_null
    col: actual_est_comp
  - kind: row_floor
    min: 24
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
