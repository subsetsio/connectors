# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-ffnc-f3aa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, school_level, progress_report_type]
checks:
  - kind: unique
    columns: [dbn, school_level, progress_report_type]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: school_level
  - kind: not_null
    col: progress_report_type
  - kind: row_floor
    min: 838
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
