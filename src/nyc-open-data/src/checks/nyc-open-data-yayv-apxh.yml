# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-yayv-apxh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, principal, district, "200708_school_support_organization", school_level]
checks:
  - kind: unique
    columns: [dbn, principal, district, "200708_school_support_organization", school_level]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: principal
  - kind: not_null
    col: district
  - kind: not_null
    col: "200708_school_support_organization"
  - kind: not_null
    col: school_level
  - kind: row_floor
    min: 746
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
