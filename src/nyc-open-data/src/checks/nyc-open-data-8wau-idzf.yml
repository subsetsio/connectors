# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-8wau-idzf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [site_name, opt_code, vendor_name, school_year]
checks:
  - kind: unique
    columns: [site_name, opt_code, vendor_name, school_year]
  - kind: not_null
    col: site_name
  - kind: not_null
    col: opt_code
  - kind: not_null
    col: vendor_name
  - kind: not_null
    col: school_year
  - kind: row_floor
    min: 1866
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
