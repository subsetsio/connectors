# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-8vqd-3345`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [n_students_enrolled_in_schools_offering_cte, n_students_enrolled_in_cte, school_name, dbn, category]
checks:
  - kind: unique
    columns: [n_students_enrolled_in_schools_offering_cte, n_students_enrolled_in_cte, school_name, dbn, category]
  - kind: not_null
    col: n_students_enrolled_in_schools_offering_cte
  - kind: not_null
    col: n_students_enrolled_in_cte
  - kind: not_null
    col: school_name
  - kind: not_null
    col: dbn
  - kind: not_null
    col: category
  - kind: row_floor
    min: 737
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
