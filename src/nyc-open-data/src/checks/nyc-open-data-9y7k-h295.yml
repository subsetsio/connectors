# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9y7k-h295`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [created_month, additional_details, created_year]
checks:
  - kind: unique
    columns: [created_month, additional_details, created_year]
  - kind: not_null
    col: created_month
  - kind: not_null
    col: additional_details
  - kind: not_null
    col: created_year
  - kind: row_floor
    min: 432
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
