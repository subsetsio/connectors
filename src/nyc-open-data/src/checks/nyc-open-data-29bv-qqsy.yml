# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-29bv-qqsy`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dbn, "09_principal", "10_principal", "07_principal", "07_removals"]
checks:
  - kind: unique
    columns: [dbn, "09_principal", "10_principal", "07_principal", "07_removals"]
  - kind: not_null
    col: dbn
  - kind: not_null
    col: "09_principal"
  - kind: not_null
    col: "10_principal"
  - kind: not_null
    col: "07_principal"
  - kind: not_null
    col: "07_removals"
  - kind: row_floor
    min: 871
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
