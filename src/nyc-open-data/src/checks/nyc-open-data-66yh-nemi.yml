# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-66yh-nemi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [organization, project_title, community_board, council_district]
checks:
  - kind: unique
    columns: [organization, project_title, community_board, council_district]
  - kind: not_null
    col: organization
  - kind: not_null
    col: project_title
  - kind: not_null
    col: community_board
  - kind: not_null
    col: council_district
  - kind: row_floor
    min: 130
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
