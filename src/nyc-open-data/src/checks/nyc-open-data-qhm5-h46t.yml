# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-qhm5-h46t`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [description, fiscal_year, comes_fromgoes_to_indicator]
checks:
  - kind: unique
    columns: [description, fiscal_year, comes_fromgoes_to_indicator]
  - kind: not_null
    col: description
  - kind: not_null
    col: fiscal_year
  - kind: not_null
    col: comes_fromgoes_to_indicator
  - kind: row_floor
    min: 56
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
