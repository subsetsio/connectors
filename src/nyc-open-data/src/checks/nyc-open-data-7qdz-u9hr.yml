# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-7qdz-u9hr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [person_id, hh_id, second_home_county, school_county, work_county]
checks:
  - kind: unique
    columns: [person_id, hh_id, second_home_county, school_county, work_county]
  - kind: not_null
    col: person_id
  - kind: not_null
    col: hh_id
  - kind: not_null
    col: second_home_county
  - kind: not_null
    col: school_county
  - kind: not_null
    col: work_county
  - kind: row_floor
    min: 3443
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
