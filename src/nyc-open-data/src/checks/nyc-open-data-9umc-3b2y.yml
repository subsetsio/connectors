# compiled by `hardened compile-checks` — post-publish audit of `nyc-open-data-9umc-3b2y`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [project_title, organization_address, organization_name, community_board, council_district]
checks:
  - kind: unique
    columns: [project_title, organization_address, organization_name, community_board, council_district]
  - kind: not_null
    col: project_title
  - kind: not_null
    col: organization_address
  - kind: not_null
    col: organization_name
  - kind: not_null
    col: community_board
  - kind: not_null
    col: council_district
  - kind: row_floor
    min: 74
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
