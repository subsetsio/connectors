# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bceao-series`. Regenerate after model-verify.
# caution: A series code can appear with different frequencies, units, localities, or labeling context; join to observations using the frequency and locality context rather than treating series_code alone as globally unique.
key: [label, series_code, localities, unit, subsector]
sort: [label, series_code, localities, unit, subsector]
columns:
  - name: series_code
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: label
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: subsector
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: valuation
    type: VARCHAR
  - name: localities
    type: VARCHAR
  - name: locality_count
    type: BIGINT
