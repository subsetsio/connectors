# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9138-fa46-7320-a39a-07864bfca1b6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [establishment_type, time_period, exam_cohort, sex, establishment_type_group]
checks:
  - kind: unique
    columns: [establishment_type, time_period, exam_cohort, sex, establishment_type_group]
  - kind: not_null
    col: establishment_type
  - kind: not_null
    col: time_period
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: sex
  - kind: not_null
    col: establishment_type_group
  - kind: row_floor
    min: 450
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
