# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d4320-2bde-7059-8d7a-b685013eb1e6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, social_care_group, sen_provision, ethnicity_major, placement, period_of_care_length, version]
checks:
  - kind: unique
    columns: [time_period, social_care_group, sen_provision, ethnicity_major, placement, period_of_care_length, version]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: social_care_group
  - kind: not_null
    col: sen_provision
  - kind: not_null
    col: ethnicity_major
  - kind: not_null
    col: placement
  - kind: not_null
    col: period_of_care_length
  - kind: not_null
    col: version
  - kind: row_floor
    min: 700
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
