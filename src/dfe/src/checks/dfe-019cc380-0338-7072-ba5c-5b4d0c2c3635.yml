# compiled by `hardened compile-checks` — post-publish audit of `dfe-019cc380-0338-7072-ba5c-5b4d0c2c3635`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, total_neet_net, health_condition_grouping, health_condition]
checks:
  - kind: unique
    columns: [time_period, total_neet_net, health_condition_grouping, health_condition]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: total_neet_net
  - kind: not_null
    col: health_condition_grouping
  - kind: not_null
    col: health_condition
  - kind: row_floor
    min: 161
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
