# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9136-41d3-7160-8b01-7a4e59931a47`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, breakdown_topic, breakdown, subject, qualification_type, qualification, version]
checks:
  - kind: unique
    columns: [time_period, breakdown_topic, breakdown, subject, qualification_type, qualification, version]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: subject
  - kind: not_null
    col: qualification_type
  - kind: not_null
    col: qualification
  - kind: not_null
    col: version
  - kind: row_floor
    min: 6000
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
