# compiled by `hardened compile-checks` — post-publish audit of `dfe-019c295f-9422-76b6-bdac-a844e6992850`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, school_urn, sex, subject]
checks:
  - kind: unique
    columns: [time_period, version, school_urn, sex, subject]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: school_urn
  - kind: not_null
    col: sex
  - kind: not_null
    col: subject
  - kind: row_floor
    min: 25128
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
