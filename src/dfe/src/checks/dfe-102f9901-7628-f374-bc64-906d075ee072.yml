# compiled by `hardened compile-checks` — post-publish audit of `dfe-102f9901-7628-f374-bc64-906d075ee072`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, disadvantage_status]
checks:
  - kind: unique
    columns: [time_period, version, disadvantage_status]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: disadvantage_status
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
