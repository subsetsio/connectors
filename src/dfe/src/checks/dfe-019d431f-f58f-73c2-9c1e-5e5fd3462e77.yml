# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d431f-f58f-73c2-9c1e-5e5fd3462e77`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sen_primary_need, social_care_group, time_period, sen_provision]
checks:
  - kind: unique
    columns: [sen_primary_need, social_care_group, time_period, sen_provision]
  - kind: not_null
    col: sen_primary_need
  - kind: not_null
    col: social_care_group
  - kind: not_null
    col: time_period
  - kind: not_null
    col: sen_provision
  - kind: row_floor
    min: 1303
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
