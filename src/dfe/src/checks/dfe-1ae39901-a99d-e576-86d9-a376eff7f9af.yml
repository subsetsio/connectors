# compiled by `hardened compile-checks` — post-publish audit of `dfe-1ae39901-a99d-e576-86d9-a376eff7f9af`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, establishment_type_group, qualification_type, subject, test_year]
checks:
  - kind: unique
    columns: [time_period, version, establishment_type_group, qualification_type, subject, test_year]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: qualification_type
  - kind: not_null
    col: subject
  - kind: not_null
    col: test_year
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
