# compiled by `hardened compile-checks` — post-publish audit of `dfe-19e39901-560d-f972-b6f3-dd085539c095`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, establishment_type_group, breakdown_topic, breakdown]
checks:
  - kind: unique
    columns: [time_period, version, establishment_type_group, breakdown_topic, breakdown]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: breakdown
  - kind: row_floor
    min: 176
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
