# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9136-dee1-70a6-8372-a29c82db2dd0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, establishment_type_group, establishment_type, sex, exam_cohort, grade]
checks:
  - kind: unique
    columns: [time_period, version, establishment_type_group, establishment_type, sex, exam_cohort, grade]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: establishment_type
  - kind: not_null
    col: sex
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: grade
  - kind: row_floor
    min: 2040
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
