# compiled by `hardened compile-checks` — post-publish audit of `dfe-18e39901-9dc2-d872-9121-5284f9af111e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, establishment_type_group, breakdown_topic, years_open]
checks:
  - kind: unique
    columns: [time_period, version, establishment_type_group, breakdown_topic, years_open]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: years_open
  - kind: row_floor
    min: 122
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
