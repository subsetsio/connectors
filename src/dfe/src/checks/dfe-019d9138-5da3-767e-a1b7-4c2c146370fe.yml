# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9138-5da3-767e-a1b7-4c2c146370fe`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, exam_cohort, version, qualification_level]
checks:
  - kind: unique
    columns: [time_period, exam_cohort, version, qualification_level]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: version
  - kind: not_null
    col: qualification_level
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
