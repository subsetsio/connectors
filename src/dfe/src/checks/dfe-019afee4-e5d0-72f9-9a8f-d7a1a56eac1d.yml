# compiled by `hardened compile-checks` — post-publish audit of `dfe-019afee4-e5d0-72f9-9a8f-d7a1a56eac1d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, school_urn, breakdown_topic, breakdown, subject]
checks:
  - kind: unique
    columns: [time_period, version, school_urn, breakdown_topic, breakdown, subject]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: school_urn
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: subject
  - kind: row_floor
    min: 598892
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
