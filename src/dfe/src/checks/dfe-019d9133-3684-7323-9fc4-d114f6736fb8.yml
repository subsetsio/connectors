# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9133-3684-7323-9fc4-d114f6736fb8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, breakdown_topic, breakdown, disadvantage_status, exam_cohort]
checks:
  - kind: unique
    columns: [time_period, version, breakdown_topic, breakdown, disadvantage_status, exam_cohort]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: disadvantage_status
  - kind: not_null
    col: exam_cohort
  - kind: row_floor
    min: 1600
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
