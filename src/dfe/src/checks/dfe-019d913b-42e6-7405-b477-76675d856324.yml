# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d913b-42e6-7405-b477-76675d856324`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value_added, breakdown, breakdown_topic, exam_cohort, time_period]
checks:
  - kind: unique
    columns: [value_added, breakdown, breakdown_topic, exam_cohort, time_period]
  - kind: not_null
    col: value_added
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 375
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
