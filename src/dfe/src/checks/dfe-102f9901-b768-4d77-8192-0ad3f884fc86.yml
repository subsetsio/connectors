# compiled by `hardened compile-checks` — post-publish audit of `dfe-102f9901-b768-4d77-8192-0ad3f884fc86`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, establishment_type_group, sex, subject, scaled_scores]
checks:
  - kind: unique
    columns: [time_period, version, establishment_type_group, sex, subject, scaled_scores]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: sex
  - kind: not_null
    col: subject
  - kind: not_null
    col: scaled_scores
  - kind: row_floor
    min: 933
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
