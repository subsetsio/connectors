# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9139-63e7-737e-8114-ae669b2f6fc7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [breakdown, breakdown_topic, time_period, exam_cohort, version]
checks:
  - kind: unique
    columns: [breakdown, breakdown_topic, time_period, exam_cohort, version]
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: time_period
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: version
  - kind: row_floor
    min: 925
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
