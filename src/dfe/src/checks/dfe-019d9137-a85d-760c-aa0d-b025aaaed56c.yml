# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9137-a85d-760c-aa0d-b025aaaed56c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [establishment_type, establishment_type_group, sex]
checks:
  - kind: unique
    columns: [establishment_type, establishment_type_group, sex]
  - kind: not_null
    col: establishment_type
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
