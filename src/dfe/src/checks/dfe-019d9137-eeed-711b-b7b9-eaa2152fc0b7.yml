# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d9137-eeed-711b-b7b9-eaa2152fc0b7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [breakdown, subject_combination, breakdown_topic, time_period, version]
checks:
  - kind: unique
    columns: [breakdown, subject_combination, breakdown_topic, time_period, version]
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: subject_combination
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: row_floor
    min: 13312
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
