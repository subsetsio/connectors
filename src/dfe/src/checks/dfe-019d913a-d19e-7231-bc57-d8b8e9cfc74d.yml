# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d913a-d19e-7231-bc57-d8b8e9cfc74d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [establishment_type, grade, establishment_type_group, time_period, sex]
checks:
  - kind: unique
    columns: [establishment_type, grade, establishment_type_group, time_period, sex]
  - kind: not_null
    col: establishment_type
  - kind: not_null
    col: grade
  - kind: not_null
    col: establishment_type_group
  - kind: not_null
    col: time_period
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1147
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
