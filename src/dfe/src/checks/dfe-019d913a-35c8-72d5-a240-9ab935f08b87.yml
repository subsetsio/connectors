# compiled by `hardened compile-checks` — post-publish audit of `dfe-019d913a-35c8-72d5-a240-9ab935f08b87`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [breakdown, breakdown_topic, time_period, exam_cohort, disadvantage_status]
checks:
  - kind: unique
    columns: [breakdown, breakdown_topic, time_period, exam_cohort, disadvantage_status]
  - kind: not_null
    col: breakdown
  - kind: not_null
    col: breakdown_topic
  - kind: not_null
    col: time_period
  - kind: not_null
    col: exam_cohort
  - kind: not_null
    col: disadvantage_status
  - kind: row_floor
    min: 1000
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
