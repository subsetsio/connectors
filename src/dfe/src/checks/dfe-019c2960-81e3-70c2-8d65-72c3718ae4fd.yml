# compiled by `hardened compile-checks` — post-publish audit of `dfe-019c2960-81e3-70c2-8d65-72c3718ae4fd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, version, school_urn, disadvantage_status, exam_cohort]
checks:
  - kind: unique
    columns: [time_period, version, school_urn, disadvantage_status, exam_cohort]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: version
  - kind: not_null
    col: school_urn
  - kind: not_null
    col: disadvantage_status
  - kind: not_null
    col: exam_cohort
  - kind: row_floor
    min: 89344
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
