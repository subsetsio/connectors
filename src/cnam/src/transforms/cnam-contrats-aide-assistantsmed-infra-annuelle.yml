# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-contrats-aide-assistantsmed-infra-annuelle`. Regenerate after model-verify.
# caution: A single in-progress-year snapshot (`annee` is constant, `date` is the end of the reference period), not a time series.
# caution: The territory columns mix levels: département rows, région rows and a national row coexist. Filter before summing.
key: [region, departement]
temporal: annee
sort: [annee, region, departement]
columns:
  - name: annee
    type: DATE
  - name: date
    type: DATE
  - name: profession_sante_nbr_contrats
    type: VARCHAR
  - name: profession_sante_taux
    type: VARCHAR
  - name: region
    type: VARCHAR
  - name: libelle_region
    type: VARCHAR
  - name: departement
    type: VARCHAR
  - name: libelle_departement
    type: VARCHAR
  - name: nombre_contrat_assmed
    type: BIGINT
  - name: taux_adhesions
    type: DOUBLE
