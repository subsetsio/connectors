# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-medecins-optam-infra-annuelle`. Regenerate after model-verify.
# caution: A single in-progress-year snapshot (`annee` is constant, `date` is the end of the reference period), not a time series.
# caution: `taux_adhesion` is a percentage; the territory columns mix département, région and national rows.
key: [profession_sante, region, departement]
temporal: annee
sort: [annee, profession_sante, region, departement]
columns:
  - name: annee
    type: DATE
  - name: date
    type: DATE
  - name: profession_sante
    type: VARCHAR
  - name: region
    type: VARCHAR
  - name: libelle_region
    type: VARCHAR
  - name: departement
    type: VARCHAR
  - name: libelle_departement
    type: VARCHAR
  - name: taux_adhesion
    type: VARCHAR
  - name: taux_adhesion_integer
    type: DOUBLE
