# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-patients-longueduree-infra-annuelle`. Regenerate after model-verify.
# caution: A single in-progress-year snapshot (`annee` is constant, `date` is the end of the reference period), not a time series.
# caution: `taux_patients_ald_sans_mt` is a percentage; the territory columns mix département, région and national rows.
key: [region, departement]
temporal: annee
sort: [annee, region, departement]
columns:
  - name: annee
    type: DATE
  - name: date
    type: DATE
  - name: region
    type: VARCHAR
  - name: libelle_region
    type: VARCHAR
  - name: departement
    type: VARCHAR
  - name: libelle_departement
    type: VARCHAR
  - name: taux_patients_ald_sans_mt
    type: DOUBLE
