# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-patientele-medecintraitant-generalistes-infra-annuelle`. Regenerate after model-verify.
# caution: A single in-progress-year snapshot (`annee` is constant, `date` is the end of the reference period), not a time series.
# caution: `nombre_patients_medecin_traitant` is a mean per practitioner; the territory columns mix département, région and national rows.
key: [profession_sante, region, departement]
temporal: annee
sort: [annee, profession_sante, region, departement]
columns:
  - name: annee
    type: DATE
  - name: date
    type: DATE
  - name: profession_sante
    type: VARCHAR
  - name: region
    type: BIGINT
  - name: libelle_region
    type: VARCHAR
  - name: departement
    type: VARCHAR
  - name: libelle_departement
    type: VARCHAR
  - name: nombre_patients_medecin_traitant
    type: BIGINT
  - name: taux_evolution_annuel
    type: DOUBLE
