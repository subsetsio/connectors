# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-prescriptions`. Regenerate after model-verify.
# caution: `poste_prescription` carries an all-posts aggregate alongside the individual prescription posts, and aggregate members also exist in `profession_sante` and the territory columns — filter each before summing `montant_total_prescription`.
# caution: `montant_moyen_prescription` is a per-practitioner mean and is never summable.
key: [annee, profession_sante, region, departement, poste_prescription]
temporal: annee
sort: [annee, profession_sante, region, departement, poste_prescription]
columns:
  - name: annee
    type: DATE
  - name: profession_sante
    type: VARCHAR
  - name: region
    type: VARCHAR
  - name: libelle_region
    type: VARCHAR
  - name: departement
    type: VARCHAR
  - name: libelle_departement
    type: VARCHAR
  - name: poste_prescription
    type: VARCHAR
  - name: libelle_poste_prescription
    type: VARCHAR
  - name: montant_total_prescription
    type: VARCHAR
  - name: montant_moyen_prescription
    type: VARCHAR
  - name: vision_generale_all
    type: VARCHAR
  - name: vision_generale_prescriptions
    type: VARCHAR
  - name: vision_profession_territoire
    type: VARCHAR
  - name: montant_total_prescription_integer
    type: BIGINT
  - name: montant_moyen_prescription_integer
    type: BIGINT
