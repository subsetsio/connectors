# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cnam-referentiel-departements`. Regenerate after model-verify.
# caution: Includes the overseas départements and the aggregate pseudo-départements used by the indicator tables, so a plain row count is not the number of real départements.
key: [dep_code]
sort: [dep_code]
columns:
  - name: reg_code
    type: VARCHAR
  - name: reg_name
    type: VARCHAR
  - name: dep_code
    type: VARCHAR
  - name: dep_name
    type: VARCHAR
  - name: dep_name_upper
    type: VARCHAR
  - name: dep_type
    type: VARCHAR
  - name: tri
    type: BIGINT
