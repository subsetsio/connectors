# compiled by `hardened compile-checks` — post-publish audit of `indeed-hiring-lab-remote-postings-sector`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, jobcountry, normtitlecategory_consistent]
temporal: date
checks:
  - kind: unique
    columns: [date, jobcountry, normtitlecategory_consistent]
  - kind: not_null
    col: date
  - kind: not_null
    col: jobcountry
  - kind: not_null
    col: normtitlecategory_consistent
  - kind: row_floor
    min: 326314
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
