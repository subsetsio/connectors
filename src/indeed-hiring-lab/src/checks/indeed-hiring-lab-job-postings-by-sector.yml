# compiled by `hardened compile-checks` — post-publish audit of `indeed-hiring-lab-job-postings-by-sector`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, jobcountry, variable, display_name]
temporal: date
checks:
  - kind: unique
    columns: [date, jobcountry, variable, display_name]
  - kind: not_null
    col: date
  - kind: not_null
    col: jobcountry
  - kind: not_null
    col: variable
  - kind: not_null
    col: display_name
  - kind: row_floor
    min: 598528
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
