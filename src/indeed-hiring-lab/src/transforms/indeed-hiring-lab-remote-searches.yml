# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `indeed-hiring-lab-remote-searches`. Regenerate after model-verify.
key: [date, jobcountry]
temporal: date
sort: [date, jobcountry]
columns:
  - name: date
    type: DATE
  - name: jobcountry
    type: VARCHAR
  - name: remote_share_searches
    type: DOUBLE
