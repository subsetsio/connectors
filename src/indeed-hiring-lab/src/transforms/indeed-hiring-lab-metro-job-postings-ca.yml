# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `indeed-hiring-lab-metro-job-postings-ca`. Regenerate after model-verify.
key: [date, metro]
temporal: date
sort: [date, metro]
columns:
  - name: date
    type: DATE
  - name: metro
    type: VARCHAR
  - name: indeed_job_postings_index
    type: DOUBLE
