# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `indeed-hiring-lab-aggregate-job-postings`. Regenerate after model-verify.
# caution: Contains both seasonally adjusted and non-seasonally adjusted index columns for each observation; choose the appropriate measure before comparing trends.
key: [date, jobcountry, variable]
temporal: date
sort: [date, jobcountry, variable]
columns:
  - name: date
    type: DATE
  - name: jobcountry
    type: VARCHAR
  - name: indeed_job_postings_index_sa
    type: DOUBLE
  - name: indeed_job_postings_index_nsa
    type: DOUBLE
  - name: variable
    type: VARCHAR
