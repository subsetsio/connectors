# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `indeed-hiring-lab-provincial-postings-ca`. Regenerate after model-verify.
key: [date, province]
temporal: date
sort: [date, province]
columns:
  - name: date
    type: DATE
  - name: province
    type: VARCHAR
  - name: indeed_job_postings_index
    type: DOUBLE
