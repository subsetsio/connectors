# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `indeed-hiring-lab-state-job-postings-us`. Regenerate after model-verify.
key: [date, state]
temporal: date
sort: [date, state]
columns:
  - name: date
    type: DATE
  - name: state
    type: VARCHAR
  - name: indeed_job_postings_index
    type: DOUBLE
