# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-registered-deaths-by-nationality-and-gender`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, lnw, gender, ljnsy, nationality]
temporal: year
checks:
  - kind: unique
    columns: [year, lnw, gender, ljnsy, nationality]
  - kind: not_null
    col: year
  - kind: not_null
    col: lnw
  - kind: not_null
    col: gender
  - kind: not_null
    col: ljnsy
  - kind: not_null
    col: nationality
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
