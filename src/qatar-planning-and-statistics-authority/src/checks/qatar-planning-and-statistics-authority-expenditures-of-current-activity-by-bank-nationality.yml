# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-expenditures-of-current-activity-by-bank-nationality`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [expenditures, expenditures_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [expenditures, expenditures_ar, year]
  - kind: not_null
    col: expenditures
  - kind: not_null
    col: expenditures_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
