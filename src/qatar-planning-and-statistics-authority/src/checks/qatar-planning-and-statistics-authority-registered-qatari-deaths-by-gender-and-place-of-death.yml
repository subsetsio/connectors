# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-registered-qatari-deaths-by-gender-and-place-of-death`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [place_of_death, mkn_lwf, year, quarter, lrb]
temporal: year
checks:
  - kind: unique
    columns: [place_of_death, mkn_lwf, year, quarter, lrb]
  - kind: not_null
    col: place_of_death
  - kind: not_null
    col: mkn_lwf
  - kind: not_null
    col: year
  - kind: not_null
    col: quarter
  - kind: not_null
    col: lrb
  - kind: row_floor
    min: 42
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
