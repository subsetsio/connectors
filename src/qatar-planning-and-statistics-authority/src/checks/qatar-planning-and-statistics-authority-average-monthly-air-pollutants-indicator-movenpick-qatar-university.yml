# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-average-monthly-air-pollutants-indicator-movenpick-qatar-university`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, item, lbyn, january, march]
temporal: year
checks:
  - kind: unique
    columns: [year, item, lbyn, january, march]
  - kind: not_null
    col: year
  - kind: not_null
    col: item
  - kind: not_null
    col: lbyn
  - kind: not_null
    col: january
  - kind: not_null
    col: march
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
