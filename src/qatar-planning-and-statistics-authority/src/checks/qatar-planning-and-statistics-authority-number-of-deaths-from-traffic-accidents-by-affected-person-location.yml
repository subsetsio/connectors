# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-of-deaths-from-traffic-accidents-by-affected-person-location`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, affected_person_location, affected_person_location_ar]
temporal: year
checks:
  - kind: unique
    columns: [year, affected_person_location, affected_person_location_ar]
  - kind: not_null
    col: year
  - kind: not_null
    col: affected_person_location
  - kind: not_null
    col: affected_person_location_ar
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
