# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-total-population-by-age-groups-and-sex-in-census-2010-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, census_year]
checks:
  - kind: unique
    columns: [age_group, census_year]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: census_year
  - kind: row_floor
    min: 17
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
