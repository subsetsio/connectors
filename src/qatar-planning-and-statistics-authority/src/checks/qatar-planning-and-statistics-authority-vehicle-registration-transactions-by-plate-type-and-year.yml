# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-vehicle-registration-transactions-by-plate-type-and-year`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [plate_type, nw_llwh, lm_ml, transaction, year]
temporal: year
checks:
  - kind: unique
    columns: [plate_type, nw_llwh, lm_ml, transaction, year]
  - kind: not_null
    col: plate_type
  - kind: not_null
    col: nw_llwh
  - kind: not_null
    col: lm_ml
  - kind: not_null
    col: transaction
  - kind: not_null
    col: year
  - kind: row_floor
    min: 378
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
