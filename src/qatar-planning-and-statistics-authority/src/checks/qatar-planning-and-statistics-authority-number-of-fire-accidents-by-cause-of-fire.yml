# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-of-fire-accidents-by-cause-of-fire`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, cause_of_fire_ar, cause_of_fire]
temporal: year
checks:
  - kind: unique
    columns: [year, cause_of_fire_ar, cause_of_fire]
  - kind: not_null
    col: year
  - kind: not_null
    col: cause_of_fire_ar
  - kind: not_null
    col: cause_of_fire
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
