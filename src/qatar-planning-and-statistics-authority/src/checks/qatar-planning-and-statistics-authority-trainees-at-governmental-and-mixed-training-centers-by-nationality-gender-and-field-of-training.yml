# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-trainees-at-governmental-and-mixed-training-centers-by-nationality-gender-and-field-of-training`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [field_of_training, mjl_ltdryb, year]
temporal: year
checks:
  - kind: unique
    columns: [field_of_training, mjl_ltdryb, year]
  - kind: not_null
    col: field_of_training
  - kind: not_null
    col: mjl_ltdryb
  - kind: not_null
    col: year
  - kind: row_floor
    min: 41
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
