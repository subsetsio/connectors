# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-health-care-facility-classification-by-type-2024`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [health_care_facility_classification]
checks:
  - kind: unique
    columns: [health_care_facility_classification]
  - kind: not_null
    col: health_care_facility_classification
  - kind: row_floor
    min: 6
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
