# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-media-culture-and-tourism-number-of-animals-and-different-type-of-species-in-zoological-garden-by`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_animal]
temporal: year
checks:
  - kind: unique
    columns: [type_of_animal]
  - kind: not_null
    col: type_of_animal
  - kind: row_floor
    min: 2
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
