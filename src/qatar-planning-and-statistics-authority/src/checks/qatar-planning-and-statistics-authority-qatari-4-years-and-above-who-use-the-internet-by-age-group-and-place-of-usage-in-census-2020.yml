# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-qatari-4-years-and-above-who-use-the-internet-by-age-group-and-place-of-usage-in-census-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [qatari_population]
checks:
  - kind: unique
    columns: [qatari_population]
  - kind: not_null
    col: qatari_population
  - kind: row_floor
    min: 4
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
