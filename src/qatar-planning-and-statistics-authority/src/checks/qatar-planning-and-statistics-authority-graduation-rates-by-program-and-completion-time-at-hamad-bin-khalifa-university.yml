# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-graduation-rates-by-program-and-completion-time-at-hamad-bin-khalifa-university`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [college, program]
checks:
  - kind: unique
    columns: [college, program]
  - kind: not_null
    col: college
  - kind: not_null
    col: program
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
