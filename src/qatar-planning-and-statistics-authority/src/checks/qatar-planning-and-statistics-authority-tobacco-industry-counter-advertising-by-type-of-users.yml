# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-tobacco-industry-counter-advertising-by-type-of-users`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tobacco_industry_counter_advertising]
temporal: year
checks:
  - kind: unique
    columns: [tobacco_industry_counter_advertising]
  - kind: not_null
    col: tobacco_industry_counter_advertising
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
