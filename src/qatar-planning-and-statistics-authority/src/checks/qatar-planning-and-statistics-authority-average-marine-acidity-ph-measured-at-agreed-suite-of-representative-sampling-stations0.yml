# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-average-marine-acidity-ph-measured-at-agreed-suite-of-representative-sampling-stations0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, nutrients_by_location, lmgdhyt_hsb_lmwq, year]
temporal: year
checks:
  - kind: unique
    columns: [value, nutrients_by_location, lmgdhyt_hsb_lmwq, year]
  - kind: not_null
    col: value
  - kind: not_null
    col: nutrients_by_location
  - kind: not_null
    col: lmgdhyt_hsb_lmwq
  - kind: not_null
    col: year
  - kind: row_floor
    min: 48
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
