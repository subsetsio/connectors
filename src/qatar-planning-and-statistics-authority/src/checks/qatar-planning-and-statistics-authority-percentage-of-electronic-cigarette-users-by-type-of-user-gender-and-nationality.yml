# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-percentage-of-electronic-cigarette-users-by-type-of-user-gender-and-nationality`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_electronic_cigarette_user, nationality]
temporal: year
checks:
  - kind: unique
    columns: [type_of_electronic_cigarette_user, nationality]
  - kind: not_null
    col: type_of_electronic_cigarette_user
  - kind: not_null
    col: nationality
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
