# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-district-cooling-plants-by-municipality-sector-water-source-and-quantity-used-m`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, lbldy, year, source_of_compensatory_cooling_water, msdr_myh_ltbryd_lt_wydy]
temporal: year
checks:
  - kind: unique
    columns: [municipality, lbldy, year, source_of_compensatory_cooling_water, msdr_myh_ltbryd_lt_wydy]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: year
  - kind: not_null
    col: source_of_compensatory_cooling_water
  - kind: not_null
    col: msdr_myh_ltbryd_lt_wydy
  - kind: row_floor
    min: 29
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
