# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-the-growth-of-the-number-of-buildings-by-building-condition-2010-and-2015-censuses`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [building_condition]
checks:
  - kind: unique
    columns: [building_condition]
  - kind: not_null
    col: building_condition
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
