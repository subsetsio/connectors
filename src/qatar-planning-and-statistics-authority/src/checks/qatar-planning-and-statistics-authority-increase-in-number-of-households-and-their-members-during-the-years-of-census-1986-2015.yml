# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-increase-in-number-of-households-and-their-members-during-the-years-of-census-1986-2015`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [households_and_their_members]
checks:
  - kind: unique
    columns: [households_and_their_members]
  - kind: not_null
    col: households_and_their_members
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
