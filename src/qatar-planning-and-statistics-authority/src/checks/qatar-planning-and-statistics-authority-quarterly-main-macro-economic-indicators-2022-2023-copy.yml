# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-quarterly-main-macro-economic-indicators-2022-2023-copy`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indicator, lmw_shr, date, unit, lwhd]
temporal: date
checks:
  - kind: unique
    columns: [indicator, lmw_shr, date, unit, lwhd]
  - kind: not_null
    col: indicator
  - kind: not_null
    col: lmw_shr
  - kind: not_null
    col: date
  - kind: not_null
    col: unit
  - kind: not_null
    col: lwhd
  - kind: row_floor
    min: 136
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
