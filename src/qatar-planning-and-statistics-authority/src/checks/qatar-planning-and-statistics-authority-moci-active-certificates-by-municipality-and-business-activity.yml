# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-moci-active-certificates-by-municipality-and-business-activity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lnsht_ltjry, rmz_lnsht_ltjry, lbldy]
checks:
  - kind: unique
    columns: [lnsht_ltjry, rmz_lnsht_ltjry, lbldy]
  - kind: not_null
    col: lnsht_ltjry
  - kind: not_null
    col: rmz_lnsht_ltjry
  - kind: not_null
    col: lbldy
  - kind: row_floor
    min: 6594
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
