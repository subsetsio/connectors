# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-economically-active-population-15-years-and-above-by-nationality-gender-and-sector`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lfy, sector, lqt, category]
checks:
  - kind: unique
    columns: [lfy, sector, lqt, category]
  - kind: not_null
    col: lfy
  - kind: not_null
    col: sector
  - kind: not_null
    col: lqt
  - kind: not_null
    col: category
  - kind: row_floor
    min: 31
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
