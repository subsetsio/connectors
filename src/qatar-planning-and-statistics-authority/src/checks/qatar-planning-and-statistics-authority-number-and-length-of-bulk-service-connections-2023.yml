# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-and-length-of-bulk-service-connections-2023`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_service, nw_lkhdm, year]
temporal: year
checks:
  - kind: unique
    columns: [type_of_service, nw_lkhdm, year]
  - kind: not_null
    col: type_of_service
  - kind: not_null
    col: nw_lkhdm
  - kind: not_null
    col: year
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
