# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-deposits-at-commercial-banks`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [end_of_period_nhy_lftr, type, lnw, sector, lqt]
temporal: end_of_period_nhy_lftr
checks:
  - kind: unique
    columns: [end_of_period_nhy_lftr, type, lnw, sector, lqt]
  - kind: not_null
    col: end_of_period_nhy_lftr
  - kind: not_null
    col: type
  - kind: not_null
    col: lnw
  - kind: not_null
    col: sector
  - kind: not_null
    col: lqt
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
