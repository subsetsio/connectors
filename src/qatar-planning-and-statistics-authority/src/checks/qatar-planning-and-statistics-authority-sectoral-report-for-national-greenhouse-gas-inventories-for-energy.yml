# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-sectoral-report-for-national-greenhouse-gas-inventories-for-energy`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [msdr_gzt_ldfyy, greenhouse_gas_source, sink_sub_category, sink_category]
checks:
  - kind: unique
    columns: [msdr_gzt_ldfyy, greenhouse_gas_source, sink_sub_category, sink_category]
  - kind: not_null
    col: msdr_gzt_ldfyy
  - kind: not_null
    col: greenhouse_gas_source
  - kind: not_null
    col: sink_sub_category
  - kind: not_null
    col: sink_category
  - kind: row_floor
    min: 12
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
