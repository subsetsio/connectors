# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-trainees-at-training-centers-by-training-agency-gender-and-age-group`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age_groups_in_years, age_groups_in_years_ar]
temporal: year
checks:
  - kind: unique
    columns: [year, age_groups_in_years, age_groups_in_years_ar]
  - kind: not_null
    col: year
  - kind: not_null
    col: age_groups_in_years
  - kind: not_null
    col: age_groups_in_years_ar
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
