# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-cooling-energy-consumption-and-savings-by-municipality-and-economic-activity-mwh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, lbldy, year, item, lbyn]
temporal: year
checks:
  - kind: unique
    columns: [municipality, lbldy, year, item, lbyn]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: year
  - kind: not_null
    col: item
  - kind: not_null
    col: lbyn
  - kind: row_floor
    min: 56
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
