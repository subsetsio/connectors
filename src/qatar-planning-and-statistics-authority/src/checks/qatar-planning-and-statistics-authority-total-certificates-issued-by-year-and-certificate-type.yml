# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-total-certificates-issued-by-year-and-certificate-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [certificate_type, nw_lshhd, year]
temporal: year
checks:
  - kind: unique
    columns: [certificate_type, nw_lshhd, year]
  - kind: not_null
    col: certificate_type
  - kind: not_null
    col: nw_lshhd
  - kind: not_null
    col: year
  - kind: row_floor
    min: 17
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
