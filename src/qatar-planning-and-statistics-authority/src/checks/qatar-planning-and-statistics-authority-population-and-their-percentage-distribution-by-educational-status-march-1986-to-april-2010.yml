# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-population-and-their-percentage-distribution-by-educational-status-march-1986-to-april-2010`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [educational_status]
checks:
  - kind: unique
    columns: [educational_status]
  - kind: not_null
    col: educational_status
  - kind: row_floor
    min: 4
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
