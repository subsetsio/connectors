# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-registered-qatari-deaths-by-place-of-death-gender-and-age-groups`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lfy_t_l_mry, age_groups, year, gender, jns]
temporal: year
checks:
  - kind: unique
    columns: [lfy_t_l_mry, age_groups, year, gender, jns]
  - kind: not_null
    col: lfy_t_l_mry
  - kind: not_null
    col: age_groups
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: jns
  - kind: row_floor
    min: 228
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
