# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-deaths-and-injuries-in-traffic-accidents-by-driver-s-experience`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [experience_years_ar, experience_years, year, statement_ar, statement]
temporal: year
checks:
  - kind: unique
    columns: [experience_years_ar, experience_years, year, statement_ar, statement]
  - kind: not_null
    col: experience_years_ar
  - kind: not_null
    col: experience_years
  - kind: not_null
    col: year
  - kind: not_null
    col: statement_ar
  - kind: not_null
    col: statement
  - kind: row_floor
    min: 61
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
