# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-of-enrolled-students-by-academic-program`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [study_program, lbrnmj_ldrsy, year]
temporal: year
checks:
  - kind: unique
    columns: [study_program, lbrnmj_ldrsy, year]
  - kind: not_null
    col: study_program
  - kind: not_null
    col: lbrnmj_ldrsy
  - kind: not_null
    col: year
  - kind: row_floor
    min: 154
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
