# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-aircraft-and-cargo-activity-at-hamad-international-airport`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [number, year, flow, lhrk, category]
temporal: year
checks:
  - kind: unique
    columns: [number, year, flow, lhrk, category]
  - kind: not_null
    col: number
  - kind: not_null
    col: year
  - kind: not_null
    col: flow
  - kind: not_null
    col: lhrk
  - kind: not_null
    col: category
  - kind: row_floor
    min: 22
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
