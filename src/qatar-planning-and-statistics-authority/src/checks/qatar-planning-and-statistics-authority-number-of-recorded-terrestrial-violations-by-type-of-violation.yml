# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-of-recorded-terrestrial-violations-by-type-of-violation`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nw_lmkhlf, year, type_of_violation]
temporal: year
checks:
  - kind: unique
    columns: [nw_lmkhlf, year, type_of_violation]
  - kind: not_null
    col: nw_lmkhlf
  - kind: not_null
    col: year
  - kind: not_null
    col: type_of_violation
  - kind: row_floor
    min: 40
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
