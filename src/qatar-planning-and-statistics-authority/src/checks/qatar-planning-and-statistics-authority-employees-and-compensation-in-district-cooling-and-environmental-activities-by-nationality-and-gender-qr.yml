# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-employees-and-compensation-in-district-cooling-and-environmental-activities-by-nationality-and-gender-qr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lbyn, item, year]
temporal: year
checks:
  - kind: unique
    columns: [lbyn, item, year]
  - kind: not_null
    col: lbyn
  - kind: not_null
    col: item
  - kind: not_null
    col: year
  - kind: row_floor
    min: 17
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
