# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-percentage-distribution-of-gross-domestic-product-by-components-of-expenditure-at-current-prices`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lnsht_lqtsdy, code, year, economic_activity]
temporal: year
checks:
  - kind: unique
    columns: [lnsht_lqtsdy, code, year, economic_activity]
  - kind: not_null
    col: lnsht_lqtsdy
  - kind: not_null
    col: code
  - kind: not_null
    col: year
  - kind: not_null
    col: economic_activity
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
