# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-public-libraries-by-number-of-borrowers-and-books-borrowed`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sn, name_of_library, name_of_library_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [sn, name_of_library, name_of_library_ar, year]
  - kind: not_null
    col: sn
  - kind: not_null
    col: name_of_library
  - kind: not_null
    col: name_of_library_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 35
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
