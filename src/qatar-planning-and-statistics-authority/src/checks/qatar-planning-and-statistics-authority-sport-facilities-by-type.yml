# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-sport-facilities-by-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nw_lmnsh_lrydy, sport_facility_type, year]
temporal: year
checks:
  - kind: unique
    columns: [nw_lmnsh_lrydy, sport_facility_type, year]
  - kind: not_null
    col: nw_lmnsh_lrydy
  - kind: not_null
    col: sport_facility_type
  - kind: not_null
    col: year
  - kind: row_floor
    min: 108
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
