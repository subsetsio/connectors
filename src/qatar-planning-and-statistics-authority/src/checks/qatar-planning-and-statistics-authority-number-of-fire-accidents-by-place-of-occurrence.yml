# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-number-of-fire-accidents-by-place-of-occurrence`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, place_of_occurrence, place_of_occurrence_ar]
temporal: year
checks:
  - kind: unique
    columns: [year, place_of_occurrence, place_of_occurrence_ar]
  - kind: not_null
    col: year
  - kind: not_null
    col: place_of_occurrence
  - kind: not_null
    col: place_of_occurrence_ar
  - kind: row_floor
    min: 50
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
