# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-characteristics-of-operational-district-cooling-plants-by-municipality-and-economic-activity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [item, lbyn, municipality, lbldy, year]
temporal: year
checks:
  - kind: unique
    columns: [item, lbyn, municipality, lbldy, year]
  - kind: not_null
    col: item
  - kind: not_null
    col: lbyn
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: year
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
