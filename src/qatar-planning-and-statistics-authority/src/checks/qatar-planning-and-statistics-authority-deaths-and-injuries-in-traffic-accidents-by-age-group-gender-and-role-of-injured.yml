# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-deaths-and-injuries-in-traffic-accidents-by-age-group-gender-and-role-of-injured`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_groups, age_groups_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [age_groups, age_groups_ar, year]
  - kind: not_null
    col: age_groups
  - kind: not_null
    col: age_groups_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 19
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
