# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-registered-deaths-by-nationality-and-gender-2001-2022`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, gender, ljns, nationality, ljnsy]
temporal: year
checks:
  - kind: unique
    columns: [year, gender, ljns, nationality, ljnsy]
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: ljns
  - kind: not_null
    col: nationality
  - kind: not_null
    col: ljnsy
  - kind: row_floor
    min: 44
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
