# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-distribution-of-the-number-of-households-and-their-members-by-municipality`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, year, lbldy, nw_bynt_l_sr, type_of_household_data]
temporal: year
checks:
  - kind: unique
    columns: [municipality, year, lbldy, nw_bynt_l_sr, type_of_household_data]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: year
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: nw_bynt_l_sr
  - kind: not_null
    col: type_of_household_data
  - kind: row_floor
    min: 114
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
