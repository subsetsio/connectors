# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-quantities-of-fertilizers-used-by-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nw_lsmd, type_of_fertilizer, year]
temporal: year
checks:
  - kind: unique
    columns: [nw_lsmd, type_of_fertilizer, year]
  - kind: not_null
    col: nw_lsmd
  - kind: not_null
    col: type_of_fertilizer
  - kind: not_null
    col: year
  - kind: row_floor
    min: 6
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
