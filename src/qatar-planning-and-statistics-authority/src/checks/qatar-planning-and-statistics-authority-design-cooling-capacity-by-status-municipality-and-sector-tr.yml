# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-design-cooling-capacity-by-status-municipality-and-sector-tr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, lbldy, year, project_status, hl_lmshry]
temporal: year
checks:
  - kind: unique
    columns: [municipality, lbldy, year, project_status, hl_lmshry]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: year
  - kind: not_null
    col: project_status
  - kind: not_null
    col: hl_lmshry
  - kind: row_floor
    min: 34
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
