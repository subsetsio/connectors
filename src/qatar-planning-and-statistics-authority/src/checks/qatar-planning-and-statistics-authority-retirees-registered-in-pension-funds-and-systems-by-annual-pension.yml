# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-retirees-registered-in-pension-funds-and-systems-by-annual-pension`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, annual_pension]
temporal: year
checks:
  - kind: unique
    columns: [year, annual_pension]
  - kind: not_null
    col: year
  - kind: not_null
    col: annual_pension
  - kind: row_floor
    min: 50
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
