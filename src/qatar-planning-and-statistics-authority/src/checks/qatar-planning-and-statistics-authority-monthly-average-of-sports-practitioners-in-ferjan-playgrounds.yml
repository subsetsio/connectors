# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-monthly-average-of-sports-practitioners-in-ferjan-playgrounds`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [name_of_playground, sm_lml_b, month, lshhr]
temporal: year
checks:
  - kind: unique
    columns: [name_of_playground, sm_lml_b, month, lshhr]
  - kind: not_null
    col: name_of_playground
  - kind: not_null
    col: sm_lml_b
  - kind: not_null
    col: month
  - kind: not_null
    col: lshhr
  - kind: row_floor
    min: 84
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
