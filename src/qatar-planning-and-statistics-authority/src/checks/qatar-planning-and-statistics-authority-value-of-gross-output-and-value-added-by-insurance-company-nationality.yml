# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-value-of-gross-output-and-value-added-by-insurance-company-nationality`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [particulars, particulars_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [particulars, particulars_ar, year]
  - kind: not_null
    col: particulars
  - kind: not_null
    col: particulars_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 35
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
