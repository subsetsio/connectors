# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-environmental-commitments-in-district-cooling-service-providers`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [environment_activities, l_nsht_lbyy_y, year]
temporal: year
checks:
  - kind: unique
    columns: [environment_activities, l_nsht_lbyy_y, year]
  - kind: not_null
    col: environment_activities
  - kind: not_null
    col: l_nsht_lbyy_y
  - kind: not_null
    col: year
  - kind: row_floor
    min: 22
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
