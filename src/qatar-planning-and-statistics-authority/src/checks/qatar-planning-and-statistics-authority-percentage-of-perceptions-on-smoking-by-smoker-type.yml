# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-percentage-of-perceptions-on-smoking-by-smoker-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [types_of_perceptions, smokers_type]
temporal: year
checks:
  - kind: unique
    columns: [types_of_perceptions, smokers_type]
  - kind: not_null
    col: types_of_perceptions
  - kind: not_null
    col: smokers_type
  - kind: row_floor
    min: 6
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
