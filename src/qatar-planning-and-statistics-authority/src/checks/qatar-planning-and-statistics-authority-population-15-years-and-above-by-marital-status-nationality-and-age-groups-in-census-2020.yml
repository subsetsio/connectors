# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-population-15-years-and-above-by-marital-status-nationality-and-age-groups-in-census-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age_group, nationality, ljnsy]
checks:
  - kind: unique
    columns: [age_group, nationality, ljnsy]
  - kind: not_null
    col: age_group
  - kind: not_null
    col: nationality
  - kind: not_null
    col: ljnsy
  - kind: row_floor
    min: 14
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
