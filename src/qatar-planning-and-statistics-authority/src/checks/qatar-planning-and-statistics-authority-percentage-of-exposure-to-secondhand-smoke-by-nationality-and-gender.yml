# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-percentage-of-exposure-to-secondhand-smoke-by-nationality-and-gender`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [exposure_to_secondhand_smoke, nationality]
temporal: year
checks:
  - kind: unique
    columns: [exposure_to_secondhand_smoke, nationality]
  - kind: not_null
    col: exposure_to_secondhand_smoke
  - kind: not_null
    col: nationality
  - kind: row_floor
    min: 4
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
