# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-sectoral-maximum-electricity-demand-and-dates-mw`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [demand_date_mm_dd_yyyy, demand_type, nw_lhml, year]
temporal: demand_date_mm_dd_yyyy
checks:
  - kind: unique
    columns: [demand_date_mm_dd_yyyy, demand_type, nw_lhml, year]
  - kind: not_null
    col: demand_date_mm_dd_yyyy
  - kind: not_null
    col: demand_type
  - kind: not_null
    col: nw_lhml
  - kind: not_null
    col: year
  - kind: row_floor
    min: 3
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
