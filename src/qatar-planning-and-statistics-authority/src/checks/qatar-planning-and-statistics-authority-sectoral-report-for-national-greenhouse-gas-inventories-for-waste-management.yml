# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-sectoral-report-for-national-greenhouse-gas-inventories-for-waste-management`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [greenhouse_gas_source, fy_t_msdr_wmsrf_lgzt_ldfyn, sink_category]
checks:
  - kind: unique
    columns: [greenhouse_gas_source, fy_t_msdr_wmsrf_lgzt_ldfyn, sink_category]
  - kind: not_null
    col: greenhouse_gas_source
  - kind: not_null
    col: fy_t_msdr_wmsrf_lgzt_ldfyn
  - kind: not_null
    col: sink_category
  - kind: row_floor
    min: 4
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
