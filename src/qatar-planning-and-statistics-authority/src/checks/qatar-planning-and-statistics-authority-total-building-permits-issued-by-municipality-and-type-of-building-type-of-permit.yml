# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-total-building-permits-issued-by-municipality-and-type-of-building-type-of-permit`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [permit_type, nw_lrkhs, municipality, lbldy, year]
temporal: year
checks:
  - kind: unique
    columns: [permit_type, nw_lrkhs, municipality, lbldy, year]
  - kind: not_null
    col: permit_type
  - kind: not_null
    col: nw_lrkhs
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: year
  - kind: row_floor
    min: 132
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
