# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-health-statistics-number-and-rate-for-health-indicators-in-private-sector`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [number, indicator, indicator_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [number, indicator, indicator_ar, year]
  - kind: not_null
    col: number
  - kind: not_null
    col: indicator
  - kind: not_null
    col: indicator_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 39
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
