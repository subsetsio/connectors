# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-activities-rendered-by-the-family-consulting-center-and-number-of-beneficiaries`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, activities, activities_ar, activities_beneficiaries, activities_beneficiaries_ar]
temporal: year
checks:
  - kind: unique
    columns: [year, activities, activities_ar, activities_beneficiaries, activities_beneficiaries_ar]
  - kind: not_null
    col: year
  - kind: not_null
    col: activities
  - kind: not_null
    col: activities_ar
  - kind: not_null
    col: activities_beneficiaries
  - kind: not_null
    col: activities_beneficiaries_ar
  - kind: row_floor
    min: 12
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
