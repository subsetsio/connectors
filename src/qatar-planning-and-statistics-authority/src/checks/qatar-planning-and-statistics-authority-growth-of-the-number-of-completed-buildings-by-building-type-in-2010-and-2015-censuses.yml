# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-growth-of-the-number-of-completed-buildings-by-building-type-in-2010-and-2015-censuses`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [building_type]
checks:
  - kind: unique
    columns: [building_type]
  - kind: not_null
    col: building_type
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
