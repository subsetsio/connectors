# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-trainees-by-nationality-gender-and-training-agency`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, training_agency, training_agency_ar]
temporal: year
checks:
  - kind: unique
    columns: [year, training_agency, training_agency_ar]
  - kind: not_null
    col: year
  - kind: not_null
    col: training_agency
  - kind: not_null
    col: training_agency_ar
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
