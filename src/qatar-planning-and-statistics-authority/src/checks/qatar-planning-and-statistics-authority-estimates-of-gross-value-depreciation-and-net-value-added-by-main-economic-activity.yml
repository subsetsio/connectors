# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-estimates-of-gross-value-depreciation-and-net-value-added-by-main-economic-activity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [activity_code, main_economic_activity, lnsht_lqtsd_lry_ysy, year]
temporal: year
checks:
  - kind: unique
    columns: [activity_code, main_economic_activity, lnsht_lqtsd_lry_ysy, year]
  - kind: not_null
    col: activity_code
  - kind: not_null
    col: main_economic_activity
  - kind: not_null
    col: lnsht_lqtsd_lry_ysy
  - kind: not_null
    col: year
  - kind: row_floor
    min: 31
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
