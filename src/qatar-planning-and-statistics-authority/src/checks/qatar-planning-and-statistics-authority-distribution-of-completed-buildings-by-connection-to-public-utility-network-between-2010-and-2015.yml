# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-distribution-of-completed-buildings-by-connection-to-public-utility-network-between-2010-and-2015`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [public_utility_network, public_utility_network_ar, year]
temporal: year
checks:
  - kind: unique
    columns: [public_utility_network, public_utility_network_ar, year]
  - kind: not_null
    col: public_utility_network
  - kind: not_null
    col: public_utility_network_ar
  - kind: not_null
    col: year
  - kind: row_floor
    min: 3
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
