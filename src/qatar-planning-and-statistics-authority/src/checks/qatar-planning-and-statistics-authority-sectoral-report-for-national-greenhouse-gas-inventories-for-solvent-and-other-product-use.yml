# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-sectoral-report-for-national-greenhouse-gas-inventories-for-solvent-and-other-product-use`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [greenhouse_gas_source_and_sink_categories]
checks:
  - kind: unique
    columns: [greenhouse_gas_source_and_sink_categories]
  - kind: not_null
    col: greenhouse_gas_source_and_sink_categories
  - kind: row_floor
    min: 2
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
