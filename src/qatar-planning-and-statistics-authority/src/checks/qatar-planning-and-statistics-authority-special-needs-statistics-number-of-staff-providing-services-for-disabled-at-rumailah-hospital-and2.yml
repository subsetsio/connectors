# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-special-needs-statistics-number-of-staff-providing-services-for-disabled-at-rumailah-hospital-and2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [department_at, department, year, gender, nationality]
temporal: year
checks:
  - kind: unique
    columns: [department_at, department, year, gender, nationality]
  - kind: not_null
    col: department_at
  - kind: not_null
    col: department
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: nationality
  - kind: row_floor
    min: 150
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
