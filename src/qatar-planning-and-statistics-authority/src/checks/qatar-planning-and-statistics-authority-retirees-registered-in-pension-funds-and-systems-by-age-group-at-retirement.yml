# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-retirees-registered-in-pension-funds-and-systems-by-age-group-at-retirement`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age_group_at_retirement]
temporal: year
checks:
  - kind: unique
    columns: [year, age_group_at_retirement]
  - kind: not_null
    col: year
  - kind: not_null
    col: age_group_at_retirement
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
