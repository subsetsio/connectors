# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-quantities-of-pesticides-for-the-control-of-palm-pests-by-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lnw, type, year, unit, lwhd]
temporal: year
checks:
  - kind: unique
    columns: [lnw, type, year, unit, lwhd]
  - kind: not_null
    col: lnw
  - kind: not_null
    col: type
  - kind: not_null
    col: year
  - kind: not_null
    col: unit
  - kind: not_null
    col: lwhd
  - kind: row_floor
    min: 249
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
