# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-educational-attainment-for-population-10-years-and-above-by-nationality-in-census-2010-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [change_precentage]
checks:
  - kind: unique
    columns: [change_precentage]
  - kind: not_null
    col: change_precentage
  - kind: row_floor
    min: 6
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
