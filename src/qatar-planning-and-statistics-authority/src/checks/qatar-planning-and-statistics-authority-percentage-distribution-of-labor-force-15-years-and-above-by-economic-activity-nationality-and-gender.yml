# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-percentage-distribution-of-labor-force-15-years-and-above-by-economic-activity-nationality-and-gender`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lnsht_lqtsdy]
checks:
  - kind: unique
    columns: [lnsht_lqtsdy]
  - kind: not_null
    col: lnsht_lqtsdy
  - kind: row_floor
    min: 8
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
