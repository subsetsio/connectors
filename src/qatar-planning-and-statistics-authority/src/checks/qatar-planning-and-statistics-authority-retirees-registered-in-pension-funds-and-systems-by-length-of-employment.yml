# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-retirees-registered-in-pension-funds-and-systems-by-length-of-employment`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, length_of_service_years_ar, length_of_service_in_years]
temporal: year
checks:
  - kind: unique
    columns: [year, length_of_service_years_ar, length_of_service_in_years]
  - kind: not_null
    col: year
  - kind: not_null
    col: length_of_service_years_ar
  - kind: not_null
    col: length_of_service_in_years
  - kind: row_floor
    min: 25
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
