# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-waste-and-scrap-imports`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tfsyl_rmz_lt_rf_ljmrky, details_of_hs8, year]
temporal: year
checks:
  - kind: unique
    columns: [tfsyl_rmz_lt_rf_ljmrky, details_of_hs8, year]
  - kind: not_null
    col: tfsyl_rmz_lt_rf_ljmrky
  - kind: not_null
    col: details_of_hs8
  - kind: not_null
    col: year
  - kind: row_floor
    min: 77
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
