# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-buildings-by-buildings-status-and-municipality-in-census-2010-20200`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, municipality_ar, building_status, building_status_ar]
checks:
  - kind: unique
    columns: [municipality, municipality_ar, building_status, building_status_ar]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: municipality_ar
  - kind: not_null
    col: building_status
  - kind: not_null
    col: building_status_ar
  - kind: row_floor
    min: 12
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
