# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-completed-infrastructure-projects-by-work-category-type-and-year`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [work_type, year, work_category, unit]
temporal: year
checks:
  - kind: unique
    columns: [work_type, year, work_category, unit]
  - kind: not_null
    col: work_type
  - kind: not_null
    col: year
  - kind: not_null
    col: work_category
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
