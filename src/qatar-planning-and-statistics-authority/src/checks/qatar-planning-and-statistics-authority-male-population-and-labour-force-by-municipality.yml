# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-male-population-and-labour-force-by-municipality`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, lbldy, indicator, lmw_shr, year]
temporal: year
checks:
  - kind: unique
    columns: [municipality, lbldy, indicator, lmw_shr, year]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: lbldy
  - kind: not_null
    col: indicator
  - kind: not_null
    col: lmw_shr
  - kind: not_null
    col: year
  - kind: row_floor
    min: 80
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
