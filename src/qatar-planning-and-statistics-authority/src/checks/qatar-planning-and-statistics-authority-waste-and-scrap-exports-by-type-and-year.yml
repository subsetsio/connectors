# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-waste-and-scrap-exports-by-type-and-year`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, groups_of_goods, lmjw_t_lsl_y]
temporal: year
checks:
  - kind: unique
    columns: [year, groups_of_goods, lmjw_t_lsl_y]
  - kind: not_null
    col: year
  - kind: not_null
    col: groups_of_goods
  - kind: not_null
    col: lmjw_t_lsl_y
  - kind: row_floor
    min: 21
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
