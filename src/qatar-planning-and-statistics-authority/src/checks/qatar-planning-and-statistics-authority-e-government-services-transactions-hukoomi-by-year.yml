# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-e-government-services-transactions-hukoomi-by-year`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [service_title, page_url, service_action_url, year]
temporal: year
checks:
  - kind: unique
    columns: [service_title, page_url, service_action_url, year]
  - kind: not_null
    col: service_title
  - kind: not_null
    col: page_url
  - kind: not_null
    col: service_action_url
  - kind: not_null
    col: year
  - kind: row_floor
    min: 8
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
