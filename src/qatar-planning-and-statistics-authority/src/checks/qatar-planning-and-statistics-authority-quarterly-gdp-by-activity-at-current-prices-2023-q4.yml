# compiled by `hardened compile-checks` — post-publish audit of `qatar-planning-and-statistics-authority-quarterly-gdp-by-activity-at-current-prices-2023-q4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity_isic_rev_4]
checks:
  - kind: unique
    columns: [economic_activity_isic_rev_4]
  - kind: not_null
    col: economic_activity_isic_rev_4
  - kind: row_floor
    min: 11
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
