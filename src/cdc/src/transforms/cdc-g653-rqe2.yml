# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cdc-g653-rqe2`. Regenerate after model-verify.
key: [date, key_plot_id, normalization]
temporal: date
sort: [date, key_plot_id, normalization]
columns:
  - name: key_plot_id
    type: VARCHAR
  - name: date
    type: DATE
  - name: pcr_conc_lin
    type: DOUBLE
  - name: normalization
    type: VARCHAR
