# compiled by `hardened compile-checks` — post-publish audit of `cdc-hhvg-83jq`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sample, in_house_cdc_elisa_s_t_values, in_house_cdc_elisa_s_t_values_log10, abbott_s_c_values, abbott_s_c_values_log10]
checks:
  - kind: unique
    columns: [sample, in_house_cdc_elisa_s_t_values, in_house_cdc_elisa_s_t_values_log10, abbott_s_c_values, abbott_s_c_values_log10]
  - kind: not_null
    col: sample
  - kind: not_null
    col: in_house_cdc_elisa_s_t_values
  - kind: not_null
    col: in_house_cdc_elisa_s_t_values_log10
  - kind: not_null
    col: abbott_s_c_values
  - kind: not_null
    col: abbott_s_c_values_log10
  - kind: row_floor
    min: 102
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
