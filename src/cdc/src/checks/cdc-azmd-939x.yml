# compiled by `hardened compile-checks` — post-publish audit of `cdc-azmd-939x`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citation, county_name, date, fips_county, state_tribe_territory]
checks:
  - kind: unique
    columns: [citation, county_name, date, fips_county, state_tribe_territory]
  - kind: not_null
    col: citation
  - kind: not_null
    col: county_name
  - kind: not_null
    col: date
  - kind: not_null
    col: fips_county
  - kind: not_null
    col: state_tribe_territory
  - kind: row_floor
    min: 838963
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
