# compiled by `hardened compile-checks` — post-publish audit of `cdc-hea5-6w9c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [cumulative_doses, mmwr_week_order, mmwr_week, week_id, location_and_flu_season_order]
checks:
  - kind: unique
    columns: [cumulative_doses, mmwr_week_order, mmwr_week, week_id, location_and_flu_season_order]
  - kind: not_null
    col: cumulative_doses
  - kind: not_null
    col: mmwr_week_order
  - kind: not_null
    col: mmwr_week
  - kind: not_null
    col: week_id
  - kind: not_null
    col: location_and_flu_season_order
  - kind: row_floor
    min: 111
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
