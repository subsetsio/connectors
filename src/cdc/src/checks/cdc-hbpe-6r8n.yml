# compiled by `hardened compile-checks` — post-publish audit of `cdc-hbpe-6r8n`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [placename, placefips, geolocation, locationname, obesity_crude95ci]
checks:
  - kind: unique
    columns: [placename, placefips, geolocation, locationname, obesity_crude95ci]
  - kind: not_null
    col: placename
  - kind: not_null
    col: placefips
  - kind: not_null
    col: geolocation
  - kind: not_null
    col: locationname
  - kind: not_null
    col: obesity_crude95ci
  - kind: row_floor
    min: 14961
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
