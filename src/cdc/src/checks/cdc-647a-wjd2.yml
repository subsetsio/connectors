# compiled by `hardened compile-checks` — post-publish audit of `cdc-647a-wjd2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [fips_code, county_name, date, state_tribe_territory, restaurants_action_order_code]
temporal: date
checks:
  - kind: unique
    columns: [fips_code, county_name, date, state_tribe_territory, restaurants_action_order_code]
  - kind: not_null
    col: fips_code
  - kind: not_null
    col: county_name
  - kind: not_null
    col: date
  - kind: not_null
    col: state_tribe_territory
  - kind: not_null
    col: restaurants_action_order_code
  - kind: row_floor
    min: 1492952
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
