# compiled by `hardened compile-checks` — post-publish audit of `cdc-2ew6-ywp6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date_start, date_end, wwtp_id, key_plot_id, population_served]
temporal: date_start
checks:
  - kind: unique
    columns: [date_start, date_end, wwtp_id, key_plot_id, population_served]
  - kind: not_null
    col: date_start
  - kind: not_null
    col: date_end
  - kind: not_null
    col: wwtp_id
  - kind: not_null
    col: key_plot_id
  - kind: not_null
    col: population_served
  - kind: row_floor
    min: 418691
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
