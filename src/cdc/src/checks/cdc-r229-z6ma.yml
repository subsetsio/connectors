# compiled by `hardened compile-checks` — post-publish audit of `cdc-r229-z6ma`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [pct_positive, mmwr_week, mmwr_week_end, n_enrolled, pathogen]
temporal: mmwr_week_end
checks:
  - kind: unique
    columns: [pct_positive, mmwr_week, mmwr_week_end, n_enrolled, pathogen]
  - kind: not_null
    col: pct_positive
  - kind: not_null
    col: mmwr_week
  - kind: not_null
    col: mmwr_week_end
  - kind: not_null
    col: n_enrolled
  - kind: not_null
    col: pathogen
  - kind: row_floor
    min: 238
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
