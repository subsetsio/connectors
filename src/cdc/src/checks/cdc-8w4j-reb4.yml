# compiled by `hardened compile-checks` — post-publish audit of `cdc-8w4j-reb4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [estimate, geography, dimension, survey_year_influenza_season, dimension_type]
checks:
  - kind: unique
    columns: [estimate, geography, dimension, survey_year_influenza_season, dimension_type]
  - kind: not_null
    col: estimate
  - kind: not_null
    col: geography
  - kind: not_null
    col: dimension
  - kind: not_null
    col: survey_year_influenza_season
  - kind: not_null
    col: dimension_type
  - kind: row_floor
    min: 22259
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
