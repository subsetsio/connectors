# compiled by `hardened compile-checks` — post-publish audit of `cdc-c8as-e7h6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citation_name, effective_date, date_signed, state, vaccination_mandate_group]
checks:
  - kind: unique
    columns: [citation_name, effective_date, date_signed, state, vaccination_mandate_group]
  - kind: not_null
    col: citation_name
  - kind: not_null
    col: effective_date
  - kind: not_null
    col: date_signed
  - kind: not_null
    col: state
  - kind: not_null
    col: vaccination_mandate_group
  - kind: row_floor
    min: 47
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
