# compiled by `hardened compile-checks` — post-publish audit of `cdc-kipu-qxy8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [mmwr_week_end, mmwr_week, age_group, pathogen]
temporal: mmwr_week_end
checks:
  - kind: unique
    columns: [mmwr_week_end, mmwr_week, age_group, pathogen]
  - kind: not_null
    col: mmwr_week_end
  - kind: not_null
    col: mmwr_week
  - kind: not_null
    col: age_group
  - kind: not_null
    col: pathogen
  - kind: row_floor
    min: 22522
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
