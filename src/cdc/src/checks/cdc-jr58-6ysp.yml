# compiled by `hardened compile-checks` — post-publish audit of `cdc-jr58-6ysp`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [share, week_ending, published_date, variant, usa_or_hhsregion]
temporal: week_ending
checks:
  - kind: unique
    columns: [share, week_ending, published_date, variant, usa_or_hhsregion]
  - kind: not_null
    col: share
  - kind: not_null
    col: week_ending
  - kind: not_null
    col: published_date
  - kind: not_null
    col: variant
  - kind: not_null
    col: usa_or_hhsregion
  - kind: row_floor
    min: 2237102
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
