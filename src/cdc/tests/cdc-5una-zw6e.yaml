spec_id: cdc-5una-zw6e
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''DateReopened'' = 2022-04-11T00:00:00 (~1545d before profiling) and the series has not advanced (source data_updated_at 2024-05-17); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
