spec_id: cdc-489q-934x
status: active
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
- freshness:
    col: Year and Quarter
    reaches: 2024 Q3
  reason: published series still advancing (source data_updated_at 2026-04-02, newest 'Year and Quarter' = 2025 Q3); a load older than the reaches slack means the Socrata export went stale or the series was discontinued upstream
  certainty: 75
  points_outward: true
