spec_id: cdc-9xb7-9z99
status: active
tests:
  - row_count: {min: 16204}
    reason: exported 32,409 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
