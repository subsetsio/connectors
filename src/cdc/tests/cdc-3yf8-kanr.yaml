spec_id: cdc-3yf8-kanr
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Week Ending Date'' = 2019-12-28 (~2380d before profiling) and the series has not advanced (source data_updated_at 2021-01-21); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
