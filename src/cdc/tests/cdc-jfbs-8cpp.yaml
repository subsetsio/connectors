spec_id: cdc-jfbs-8cpp
status: active
tests:
  - row_count: {min: 3489}
    reason: exported 6,978 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
