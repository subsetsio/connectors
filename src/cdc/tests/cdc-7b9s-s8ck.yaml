spec_id: cdc-7b9s-s8ck
status: active
tests:
  - row_count: {min: 2885120}
    reason: exported 5,770,240 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
