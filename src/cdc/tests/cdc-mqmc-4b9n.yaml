spec_id: cdc-mqmc-4b9n
status: active
tests:
  - row_count: {min: 69309}
    reason: exported 138,618 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
