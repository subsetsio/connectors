spec_id: cdc-8dyx-9z99
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Week_Ending'' = 2022-03-26T00:00:00 (~1561d before profiling) and the series has not advanced (source data_updated_at 2022-09-28); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
