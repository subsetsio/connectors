spec_id: cdc-sumd-iwm8
status: active
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
- freshness:
    col: Date
    reaches: today - 75d
  reason: published series still advancing (source data_updated_at 2026-06-12, newest 'Date' = 2026-06-06); a load older than the reaches slack means the Socrata export went stale or the series was discontinued upstream
  certainty: 75
  points_outward: true
