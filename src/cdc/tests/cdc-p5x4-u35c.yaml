spec_id: cdc-p5x4-u35c
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''minute_bucket'' = 2024-07-31T23:59:00 (~703d before profiling) and the series has not advanced (source data_updated_at 2025-12-04); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
