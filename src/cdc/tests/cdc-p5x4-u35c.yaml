spec_id: cdc-p5x4-u35c
status: active
tests:
  - row_count: {min: 110160}
    reason: exported 220,320 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
