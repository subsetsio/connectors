spec_id: cdc-uc9k-vc2j
status: active
tests:
  - row_count: {min: 551936}
    reason: exported 1,103,872 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
