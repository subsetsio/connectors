spec_id: cdc-abzs-b3gw
status: active
tests:
  - row_count: {min: 232991}
    reason: exported 465,983 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
