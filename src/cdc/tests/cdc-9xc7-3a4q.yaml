spec_id: cdc-9xc7-3a4q
status: active
tests:
  - row_count: {min: 32375}
    reason: exported 64,750 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
