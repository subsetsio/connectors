spec_id: cdc-65mz-jvh5
status: active
tests:
  - row_count: {min: 1980}
    reason: exported 3,960 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
