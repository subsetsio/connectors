spec_id: cdc-vba9-s8jp
status: active
tests:
  - row_count: {min: 30030}
    reason: exported 60,060 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
