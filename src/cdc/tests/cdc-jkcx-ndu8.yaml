spec_id: cdc-jkcx-ndu8
status: active
tests:
  - row_count: {min: 4294}
    reason: exported 8,588 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
