spec_id: cdc-xy7w-35q7
status: active
tests:
  - row_count: {min: 55161}
    reason: exported 110,323 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
