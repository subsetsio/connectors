spec_id: cdc-wgvr-7mvz
status: active
tests:
  - row_count: {min: 8498}
    reason: exported 16,996 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
