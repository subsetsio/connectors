spec_id: cdc-55uq-699y
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''CMS Data As of date'' = 2024-09-05T00:00:00 (~667d before profiling) and the series has not advanced (source data_updated_at 2024-09-10); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
