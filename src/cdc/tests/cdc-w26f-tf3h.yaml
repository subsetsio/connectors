spec_id: cdc-w26f-tf3h
status: active
tests:
  - row_count: {min: 693}
    reason: exported 1,386 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
