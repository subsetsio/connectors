spec_id: cdc-kw6u-z8u2
status: active
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed
    or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
coverage:
  frozen: true
  reason: CDC Socrata dataset is a historical or irregularly revised table; raw row-count tests still verify the full export,
    but no rolling freshness cadence is promised for this table.
