spec_id: cdc-nw2y-v4gm
status: active
tests:
  - row_count: {min: 1080771}
    reason: exported 2,161,543 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
