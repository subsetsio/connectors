spec_id: cdc-cj8b-94cj
status: active
tests:
  - row_count: {min: 14242}
    reason: exported 28,484 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
