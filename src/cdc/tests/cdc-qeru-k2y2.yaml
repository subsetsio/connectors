spec_id: cdc-qeru-k2y2
status: active
tests:
  - row_count: {min: 10282817}
    reason: exported 20,565,634 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
