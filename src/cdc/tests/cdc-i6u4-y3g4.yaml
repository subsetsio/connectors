spec_id: cdc-i6u4-y3g4
status: active
tests:
  - row_count: {min: 14143}
    reason: exported 28,287 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
