spec_id: cdc-ite7-j2w7
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Data As Of'' = 2023-05-04 (~1157d before profiling) and the series has not advanced (source data_updated_at 2023-04-05); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
