spec_id: cdc-ix4g-rt8v
status: active
tests:
  - row_count: {min: 3073}
    reason: exported 6,146 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
