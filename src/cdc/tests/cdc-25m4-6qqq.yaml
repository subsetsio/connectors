spec_id: cdc-25m4-6qqq
status: active
tests:
  - row_count: {min: 13104}
    reason: exported 26,208 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
