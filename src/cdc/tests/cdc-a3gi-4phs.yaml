spec_id: cdc-a3gi-4phs
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Week_ID'' = 2024-06-29T00:00:00 (~735d before profiling) and the series has not advanced (source data_updated_at 2024-09-11); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
