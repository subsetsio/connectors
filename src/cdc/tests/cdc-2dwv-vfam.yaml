spec_id: cdc-2dwv-vfam
status: active
tests:
  - row_count: {min: 105829}
    reason: exported 211,659 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
