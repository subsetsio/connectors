spec_id: cdc-k4cb-dxd7
status: active
tests:
  - row_count: {min: 6210}
    reason: exported 12,420 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
