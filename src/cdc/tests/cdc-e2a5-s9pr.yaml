spec_id: cdc-e2a5-s9pr
status: active
tests:
  - row_count: {min: 137759}
    reason: exported 275,518 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
