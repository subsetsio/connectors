spec_id: cdc-5eh7-pjx8
status: active
tests:
  - row_count: {min: 543}
    reason: exported 1,086 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
