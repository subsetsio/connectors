spec_id: cdc-g57i-yx3r
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Current_Season_Week_Ending_Label'' = 2025-05-24T00:00:00 (~406d before profiling) and the series has not advanced (source data_updated_at 2025-04-22); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
