spec_id: cdc-judz-8etw
status: active
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
- freshness:
    col: Current_Season_Week_Ending_Label
    reaches: today - 45d
  reason: published series still advancing (source data_updated_at 2026-06-09, newest 'Current_Season_Week_Ending_Label' = 2026-07-02T00:00:00); a load older than the reaches slack means the Socrata export went stale or the series was discontinued upstream
  certainty: 75
  points_outward: true
