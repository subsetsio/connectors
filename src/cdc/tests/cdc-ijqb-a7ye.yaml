spec_id: cdc-ijqb-a7ye
status: active
tests:
  - row_count: {min: 4083}
    reason: exported 8,166 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
