spec_id: cdc-qeq7-f3ir
status: active
tests:
  - row_count: {min: 3700}
    reason: exported 7,400 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
