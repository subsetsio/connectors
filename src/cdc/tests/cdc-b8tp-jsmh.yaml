spec_id: cdc-b8tp-jsmh
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''RASH_ONSET'' = 2021-10-15T00:00:00 (~1723d before profiling) and the series has not advanced (source data_updated_at 2023-07-28); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
