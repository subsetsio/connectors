spec_id: cdc-ezfr-g6hf
status: active
tests:
  - row_count: {min: 1705}
    reason: exported 3,410 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
