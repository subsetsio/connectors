spec_id: cdc-iyx3-z4r8
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''DateFirstIll'' = 2018-12-30 (~2743d before profiling) and the series has not advanced (source data_updated_at 2022-10-17); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
