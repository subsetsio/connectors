spec_id: cdc-y5bj-9g5w
status: active
tests:
  - row_count: {min: 134508}
    reason: exported 269,016 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
