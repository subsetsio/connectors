spec_id: cdc-m74n-4hbs
status: active
tests:
  - row_count: {min: 87318}
    reason: exported 174,637 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
