spec_id: cdc-k87d-gv3u
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''End_Date'' = 2024-03-09 (~847d before profiling) and the series has not advanced (source data_updated_at 2024-03-29); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
