spec_id: cdc-4day-mt2f
status: active
tests:
  - row_count: {min: 231744}
    reason: exported 463,488 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
