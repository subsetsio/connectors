spec_id: cdc-ymmh-divb
status: active
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
- freshness:
    col: date_updated
    reaches: today - 45d
  reason: published series still advancing (source data_updated_at 2026-06-12, newest 'date_updated' = 2026-06-26T11:01:00); a load older than the reaches slack means the Socrata export went stale or the series was discontinued upstream
  certainty: 75
  points_outward: true
