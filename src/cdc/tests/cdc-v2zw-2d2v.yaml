spec_id: cdc-v2zw-2d2v
status: active
tests:
  - row_count: {min: 627}
    reason: exported 1,254 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
