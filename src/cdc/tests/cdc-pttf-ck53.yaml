spec_id: cdc-pttf-ck53
status: active
tests:
- row_count:
    min: 29048
  reason: exported 58,096 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within
    half of that, so fewer rows means the stream truncated
  certainty: 80
  points_outward: true
  severity: block
coverage:
  frozen: true
  reason: CDC Socrata table has a temporal axis, but the accepted corpus mixes historical snapshots, annual series, and irregular
    revisions; row-count tests verify complete export coverage without asserting a rolling freshness cadence.
