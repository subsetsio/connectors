spec_id: cdc-q84f-e68r
status: active
tests:
  - row_count: {min: 6300}
    reason: exported 12,600 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
