spec_id: cdc-mpgq-jmmr
status: active
tests:
  - row_count: {min: 10351}
    reason: exported 20,703 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
