spec_id: cdc-x6ag-8y7r
status: active
tests:
  - row_count: {min: 7645}
    reason: exported 15,291 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
