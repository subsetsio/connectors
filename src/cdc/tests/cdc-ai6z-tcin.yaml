spec_id: cdc-ai6z-tcin
status: active
tests:
  - row_count: {min: 1591524}
    reason: exported 3,183,048 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
