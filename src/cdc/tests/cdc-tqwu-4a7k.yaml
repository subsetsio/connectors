spec_id: cdc-tqwu-4a7k
status: active
tests:
  - row_count: {min: 29547}
    reason: exported 59,094 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
