spec_id: cdc-yni7-er2q
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Time Period End Date'' = 2022-05-09 (~1517d before profiling) and the series has not advanced (source data_updated_at 2022-11-28); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
