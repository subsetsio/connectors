spec_id: cdc-gd4x-jyhw
status: active
tests:
  - row_count: {min: 484511}
    reason: exported 969,023 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
