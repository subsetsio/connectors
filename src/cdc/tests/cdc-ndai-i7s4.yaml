spec_id: cdc-ndai-i7s4
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''Week Ending'' = 2025-04-26T00:00:00 (~434d before profiling) and the series has not advanced (source data_updated_at 2025-05-16); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
