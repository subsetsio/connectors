spec_id: cdc-fhky-rtsk
status: active
tests:
  - row_count: {min: 70526}
    reason: exported 141,052 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
