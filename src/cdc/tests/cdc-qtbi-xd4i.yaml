spec_id: cdc-qtbi-xd4i
status: active
tests:
  - row_count: {min: 20234}
    reason: exported 40,468 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
