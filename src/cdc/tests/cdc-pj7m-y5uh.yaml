spec_id: cdc-pj7m-y5uh
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''End Date'' = 2023-12-31 (~916d before profiling) and the series has not advanced (source data_updated_at 2023-09-27); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
