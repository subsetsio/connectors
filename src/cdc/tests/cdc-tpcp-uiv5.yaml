spec_id: cdc-tpcp-uiv5
status: active
tests:
  - row_count: {min: 97020}
    reason: exported 194,040 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
