spec_id: cdc-62d6-pm5i
status: active
tests:
  - row_count: {min: 796934}
    reason: exported 1,593,869 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
