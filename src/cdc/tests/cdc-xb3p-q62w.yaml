spec_id: cdc-xb3p-q62w
status: active
tests:
  - row_count: {min: 3559}
    reason: exported 7,118 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
