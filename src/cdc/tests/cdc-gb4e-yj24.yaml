spec_id: cdc-gb4e-yj24
status: active
tests:
  - row_count: {min: 108468}
    reason: exported 216,936 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
