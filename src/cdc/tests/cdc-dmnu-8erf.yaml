spec_id: cdc-dmnu-8erf
status: active
tests:
  - row_count: {min: 88654}
    reason: exported 177,309 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
