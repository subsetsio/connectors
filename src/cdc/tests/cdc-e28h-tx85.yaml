spec_id: cdc-e28h-tx85
status: active
tests:
  - row_count: {min: 14301384}
    reason: exported 28,602,768 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
