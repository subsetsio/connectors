spec_id: cdc-hj2x-85ya
status: active
tests:
  - row_count: {min: 2581}
    reason: exported 5,162 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
