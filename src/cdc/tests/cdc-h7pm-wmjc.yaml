spec_id: cdc-h7pm-wmjc
status: active
tests:
  - row_count: {min: 2399}
    reason: exported 4,798 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
