spec_id: cdc-3h58-x6cd
status: active
tests:
  - row_count: {min: 28233}
    reason: exported 56,466 rows when measured against the SODA count(*) endpoint on 2026-07-09; a full export stays within half of that, so fewer rows means the stream truncated
    certainty: 80
    points_outward: true
    severity: block
