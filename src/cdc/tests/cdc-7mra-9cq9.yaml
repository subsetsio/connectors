spec_id: cdc-7mra-9cq9
status: active
coverage:
  frozen: true
  reason: 'frozen historical corpus: newest ''week_end'' = 2024-10-12 (~630d before profiling) and the series has not advanced (source data_updated_at 2024-10-18); re-add a freshness assertion if CDC resumes publishing'
tests:
- row_count:
    min: 1
  reason: a published CDC Socrata dataset exports at least one data row; an empty CSV means the bulk export silently failed or the dataset was retired upstream
  certainty: 85
  points_outward: true
  severity: block
