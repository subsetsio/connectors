# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-fjaropinber-fjarmal-heilbr-thj05571`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, health_care_providers_hp, unit]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, health_care_providers_hp, unit]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: health_care_providers_hp
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 787
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
