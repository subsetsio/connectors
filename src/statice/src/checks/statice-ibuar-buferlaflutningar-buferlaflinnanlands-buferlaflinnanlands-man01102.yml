# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-buferlaflutningar-buferlaflinnanlands-buferlaflinnanlands-man01102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, obs_time, year, migration_region, sex]
temporal: source_updated
checks:
  - kind: unique
    columns: [age, obs_time, year, migration_region, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: migration_region
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 26400
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
