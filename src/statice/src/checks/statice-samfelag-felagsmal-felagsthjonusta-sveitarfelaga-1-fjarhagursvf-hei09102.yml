# compiled by `hardened compile-checks` — post-publish audit of `statice-samfelag-felagsmal-felagsthjonusta-sveitarfelaga-1-fjarhagursvf-hei09102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, family_type, age_group, region]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, family_type, age_group, region]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: family_type
  - kind: not_null
    col: age_group
  - kind: not_null
    col: region
  - kind: row_floor
    min: 1154
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
