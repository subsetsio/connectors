# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-thjodhagsreikningar-framluppgj-isat2008-thj08420`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [activity, obs_time, year, production_approach_component]
temporal: source_updated
checks:
  - kind: unique
    columns: [activity, obs_time, year, production_approach_component]
  - kind: not_null
    col: activity
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: production_approach_component
  - kind: row_floor
    min: 11160
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
