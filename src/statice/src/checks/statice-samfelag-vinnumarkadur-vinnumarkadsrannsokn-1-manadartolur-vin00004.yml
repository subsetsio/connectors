# compiled by `hardened compile-checks` — post-publish audit of `statice-samfelag-vinnumarkadur-vinnumarkadsrannsokn-1-manadartolur-vin00004`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, month, slack_unmet_need_for_employment, sex, units]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, month, slack_unmet_need_for_employment, sex, units]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: month
  - kind: not_null
    col: slack_unmet_need_for_employment
  - kind: not_null
    col: sex
  - kind: not_null
    col: units
  - kind: row_floor
    min: 3793
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
