# compiled by `hardened compile-checks` — post-publish audit of `statice-samfelag-vinnumarkadur-vinnumarkadsrannsokn-3-arstolur-vin01100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, occupational_group, education, sex]
temporal: source_updated
checks:
  - kind: unique
    columns: [year, occupational_group, education, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: occupational_group
  - kind: not_null
    col: education
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 2100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
