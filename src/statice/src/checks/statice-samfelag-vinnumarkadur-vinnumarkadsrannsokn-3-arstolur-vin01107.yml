# compiled by `hardened compile-checks` — post-publish audit of `statice-samfelag-vinnumarkadur-vinnumarkadsrannsokn-3-arstolur-vin01107`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity_main_and_second_job, year, sex_og_region, unit]
temporal: source_updated
checks:
  - kind: unique
    columns: [economic_activity_main_and_second_job, year, sex_og_region, unit]
  - kind: not_null
    col: economic_activity_main_and_second_job
  - kind: not_null
    col: year
  - kind: not_null
    col: sex_og_region
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 6350
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
