# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-mannfjoldi-2-byggdir-sveitarfelog-man10001`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipality, obs_time, quarter, sex_and_citizenship]
temporal: source_updated
checks:
  - kind: unique
    columns: [municipality, obs_time, quarter, sex_and_citizenship]
  - kind: not_null
    col: municipality
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: quarter
  - kind: not_null
    col: sex_and_citizenship
  - kind: row_floor
    min: 12090
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
