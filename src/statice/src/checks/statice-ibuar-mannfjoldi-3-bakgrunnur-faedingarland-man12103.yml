# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-mannfjoldi-3-bakgrunnur-faedingarland-man12103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_of_birth, obs_time, year, age, sex]
temporal: source_updated
checks:
  - kind: unique
    columns: [country_of_birth, obs_time, year, age, sex]
  - kind: not_null
    col: country_of_birth
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 274340
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
