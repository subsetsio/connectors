# compiled by `hardened compile-checks` — post-publish audit of `statice-samfelag-lifskjor-5-skuldastada-heimili-1-skuldir-eignir-thj09007`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, percentile, liabilities_assets_net_worth_total_annual_income_disposable_income]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, percentile, liabilities_assets_net_worth_total_annual_income_disposable_income]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: percentile
  - kind: not_null
    col: liabilities_assets_net_worth_total_annual_income_disposable_income
  - kind: row_floor
    min: 700
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
