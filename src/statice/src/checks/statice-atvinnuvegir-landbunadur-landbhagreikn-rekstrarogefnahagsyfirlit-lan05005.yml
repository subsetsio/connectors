# compiled by `hardened compile-checks` — post-publish audit of `statice-atvinnuvegir-landbunadur-landbhagreikn-rekstrarogefnahagsyfirlit-lan05005`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [account_heading, obs_time, year, region]
temporal: source_updated
checks:
  - kind: unique
    columns: [account_heading, obs_time, year, region]
  - kind: not_null
    col: account_heading
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: region
  - kind: row_floor
    min: 1141
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
