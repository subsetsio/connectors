# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-manntal-2021-6-tekjurheimila-cen1604`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [minor_statistical_output_area, income_quintile, unit]
temporal: source_updated
checks:
  - kind: unique
    columns: [minor_statistical_output_area, income_quintile, unit]
  - kind: not_null
    col: minor_statistical_output_area
  - kind: not_null
    col: income_quintile
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 1648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
