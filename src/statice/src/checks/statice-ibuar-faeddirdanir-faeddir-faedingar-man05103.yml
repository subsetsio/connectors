# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-faeddirdanir-faeddir-faedingar-man05103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, unit, age_of_parents, type_of_births]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, unit, age_of_parents, type_of_births]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: unit
  - kind: not_null
    col: age_of_parents
  - kind: not_null
    col: type_of_births
  - kind: row_floor
    min: 328
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
