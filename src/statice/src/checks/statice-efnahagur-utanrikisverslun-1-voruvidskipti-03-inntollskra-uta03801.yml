# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-utanrikisverslun-1-voruvidskipti-03-inntollskra-uta03801`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hs_number, country, obs_time, month, unit]
temporal: source_updated
checks:
  - kind: unique
    columns: [hs_number, country, obs_time, month, unit]
  - kind: not_null
    col: hs_number
  - kind: not_null
    col: country
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: month
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 50955465
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
