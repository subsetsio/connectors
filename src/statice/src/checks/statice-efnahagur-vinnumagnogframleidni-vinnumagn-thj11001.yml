# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-vinnumagnogframleidni-vinnumagn-thj11001`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, quarter, economic_activity, value_unit, employment]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, quarter, economic_activity, value_unit, employment]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: quarter
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: value_unit
  - kind: not_null
    col: employment
  - kind: row_floor
    min: 13959
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
