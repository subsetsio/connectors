# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-utanrikisverslun-1-voruvidskipti-01-voruskipti-uta06106`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, mode_of_transport, product, units]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, mode_of_transport, product, units]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: mode_of_transport
  - kind: not_null
    col: product
  - kind: not_null
    col: units
  - kind: row_floor
    min: 260
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
