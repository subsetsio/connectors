# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-faeddirdanir-faeddir-faedingar-man05100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_time, year, number_of_children, sex, categories]
temporal: source_updated
checks:
  - kind: unique
    columns: [obs_time, year, number_of_children, sex, categories]
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: number_of_children
  - kind: not_null
    col: sex
  - kind: not_null
    col: categories
  - kind: row_floor
    min: 1125
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
