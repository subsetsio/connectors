# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-manntal-2021-6-tekjurheimila-cen1611`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [statistical_output_area, employed_adults, year]
temporal: source_updated
checks:
  - kind: unique
    columns: [statistical_output_area, employed_adults, year]
  - kind: not_null
    col: statistical_output_area
  - kind: not_null
    col: employed_adults
  - kind: not_null
    col: year
  - kind: row_floor
    min: 215
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
