# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-kosningar-kosningayfirlit-kos01892`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [elections, size_of_municipality, age, sex]
temporal: source_updated
checks:
  - kind: unique
    columns: [elections, size_of_municipality, age, sex]
  - kind: not_null
    col: elections
  - kind: not_null
    col: size_of_municipality
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 378
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
