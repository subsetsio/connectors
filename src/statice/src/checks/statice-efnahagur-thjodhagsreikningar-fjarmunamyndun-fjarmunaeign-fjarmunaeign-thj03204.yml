# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-thjodhagsreikningar-fjarmunamyndun-fjarmunaeign-fjarmunaeign-thj03204`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [activity, obs_time, year, value_unit, category]
temporal: source_updated
checks:
  - kind: unique
    columns: [activity, obs_time, year, value_unit, category]
  - kind: not_null
    col: activity
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: value_unit
  - kind: not_null
    col: category
  - kind: row_floor
    min: 4931
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
