# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-buferlaflutningar-buferlaflmillilanda-man01406`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citizenship, obs_time, year, sex, migration]
temporal: source_updated
checks:
  - kind: unique
    columns: [citizenship, obs_time, year, sex, migration]
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: migration
  - kind: row_floor
    min: 36360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
