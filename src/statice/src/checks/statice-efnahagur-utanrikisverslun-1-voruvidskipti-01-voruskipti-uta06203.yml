# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-utanrikisverslun-1-voruvidskipti-01-voruskipti-uta06203`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country, obs_time, month, item, units]
temporal: source_updated
checks:
  - kind: unique
    columns: [country, obs_time, month, item, units]
  - kind: not_null
    col: country
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: month
  - kind: not_null
    col: item
  - kind: not_null
    col: units
  - kind: row_floor
    min: 2807541
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
