# compiled by `hardened compile-checks` — post-publish audit of `statice-ibuar-kosningar-althingi-althkjornir-kos02055`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, sex, unit, candidates_elected_members]
temporal: source_updated
checks:
  - kind: unique
    columns: [year, sex, unit, candidates_elected_members]
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: unit
  - kind: not_null
    col: candidates_elected_members
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
