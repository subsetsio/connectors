# compiled by `hardened compile-checks` — post-publish audit of `statice-umhverfi-3-efnisflaedi-3-efnisflaedireikningar-umh34055`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [material_class, obs_time, year, flow_path_table]
temporal: source_updated
checks:
  - kind: unique
    columns: [material_class, obs_time, year, flow_path_table]
  - kind: not_null
    col: material_class
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: flow_path_table
  - kind: row_floor
    min: 2566
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
