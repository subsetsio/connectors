# compiled by `hardened compile-checks` — post-publish audit of `statice-efnahagur-thjodhagsreikningar-fjarmalareikningar-fjarmalareikningar-thj10001`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sectors, obs_time, year, financial_instrument, measurement]
temporal: source_updated
checks:
  - kind: unique
    columns: [sectors, obs_time, year, financial_instrument, measurement]
  - kind: not_null
    col: sectors
  - kind: not_null
    col: obs_time
  - kind: not_null
    col: year
  - kind: not_null
    col: financial_instrument
  - kind: not_null
    col: measurement
  - kind: row_floor
    min: 16516
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
