# compiled by `hardened compile-checks` — post-publish audit of `eea-bathing-water-mapping-lov-bathingwateridentifier-groupidentifier`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [bathingwateridentifier, fromseason, toseason]
temporal: fromseason
checks:
  - kind: unique
    columns: [bathingwateridentifier, fromseason, toseason]
  - kind: not_null
    col: bathingwateridentifier
  - kind: not_null
    col: fromseason
  - kind: not_null
    col: toseason
  - kind: row_floor
    min: 14597
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
