# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `sveriges-riksbank-values`. Regenerate after model-verify.
# caution: Observations span different economic measures and currencies; filter to comparable series before aggregating values.
key: [date, series_id, group_id]
temporal: date
sort: [date, series_id, group_id]
columns:
  - name: series_id
    type: VARCHAR
  - name: group_id
    type: BIGINT
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
