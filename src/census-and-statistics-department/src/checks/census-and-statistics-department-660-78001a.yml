# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-660-78001a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ind, ccyy, stat_var, stat_pres, ent]
checks:
  - kind: unique
    columns: [ind, ccyy, stat_var, stat_pres, ent]
  - kind: not_null
    col: ind
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: stat_var
  - kind: not_null
    col: stat_pres
  - kind: not_null
    col: ent
  - kind: row_floor
    min: 2030
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
