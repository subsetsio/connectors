# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-315-38001`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, q, stat_var, di_component]
checks:
  - kind: unique
    columns: [ccyy, q, stat_var, di_component]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: q
  - kind: not_null
    col: stat_var
  - kind: not_null
    col: di_component
  - kind: row_floor
    min: 411
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
