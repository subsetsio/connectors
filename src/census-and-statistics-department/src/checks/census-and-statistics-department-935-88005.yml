# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-935-88005`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, mm, ffyy, type_serv_eld]
checks:
  - kind: unique
    columns: [ccyy, mm, ffyy, type_serv_eld]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: mm
  - kind: not_null
    col: ffyy
  - kind: not_null
    col: type_serv_eld
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
