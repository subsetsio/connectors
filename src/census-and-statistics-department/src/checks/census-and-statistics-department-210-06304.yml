# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-210-06304`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, q, edu, age, sex, stat_var, stat_pres]
checks:
  - kind: unique
    columns: [ccyy, q, edu, age, sex, stat_var, stat_pres]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: q
  - kind: not_null
    col: edu
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: not_null
    col: stat_var
  - kind: not_null
    col: stat_pres
  - kind: row_floor
    min: 24720
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
