# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-130-06611a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, mdhi_xbonus, hhsize, stat_pres]
checks:
  - kind: unique
    columns: [ccyy, mdhi_xbonus, hhsize, stat_pres]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: mdhi_xbonus
  - kind: not_null
    col: hhsize
  - kind: not_null
    col: stat_pres
  - kind: row_floor
    min: 3150
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
