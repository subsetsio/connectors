# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-915-91001`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, en_req_type, prod_type, stat_var, stat_pres]
checks:
  - kind: unique
    columns: [ccyy, en_req_type, prod_type, stat_var, stat_pres]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: en_req_type
  - kind: not_null
    col: prod_type
  - kind: not_null
    col: stat_var
  - kind: not_null
    col: stat_pres
  - kind: row_floor
    min: 422
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
