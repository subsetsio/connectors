# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-210-06704`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, rsnei, q, sex, stat_pres]
checks:
  - kind: unique
    columns: [ccyy, rsnei, q, sex, stat_pres]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: rsnei
  - kind: not_null
    col: q
  - kind: not_null
    col: sex
  - kind: not_null
    col: stat_pres
  - kind: row_floor
    min: 3708
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
