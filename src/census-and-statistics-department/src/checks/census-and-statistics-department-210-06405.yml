# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-210-06405`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [pind, ccyy, m3m, stat_pres, stat_var]
checks:
  - kind: unique
    columns: [pind, ccyy, m3m, stat_pres, stat_var]
  - kind: not_null
    col: pind
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: m3m
  - kind: not_null
    col: stat_pres
  - kind: not_null
    col: stat_var
  - kind: row_floor
    min: 13511
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
