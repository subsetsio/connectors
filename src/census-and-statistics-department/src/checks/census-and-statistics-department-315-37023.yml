# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-315-37023`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [iip_component, ccyy, q, currency]
checks:
  - kind: unique
    columns: [iip_component, ccyy, q, currency]
  - kind: not_null
    col: iip_component
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: q
  - kind: not_null
    col: currency
  - kind: row_floor
    min: 1840
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
