# compiled by `hardened compile-checks` — post-publish audit of `census-and-statistics-department-220-23012`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ccyy, age, stat_pres, emp_nature]
checks:
  - kind: unique
    columns: [ccyy, age, stat_pres, emp_nature]
  - kind: not_null
    col: ccyy
  - kind: not_null
    col: age
  - kind: not_null
    col: stat_pres
  - kind: not_null
    col: emp_nature
  - kind: row_floor
    min: 306
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
