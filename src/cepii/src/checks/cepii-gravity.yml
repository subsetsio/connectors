# compiled by `hardened compile-checks` — post-publish audit of `cepii-gravity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_id_o, country_id_d, iso3_o, iso3_d, year]
temporal: year
checks:
  - kind: unique
    columns: [country_id_o, country_id_d, iso3_o, iso3_d, year]
  - kind: not_null
    col: country_id_o
  - kind: not_null
    col: country_id_d
  - kind: not_null
    col: iso3_o
  - kind: not_null
    col: iso3_d
  - kind: not_null
    col: year
  - kind: row_floor
    min: 2349648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
