# compiled by `hardened compile-checks` — post-publish audit of `cepii-baci`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, exporter_iso, importer_iso, product_hs92]
temporal: year
checks:
  - kind: unique
    columns: [year, exporter_iso, importer_iso, product_hs92]
  - kind: not_null
    col: year
  - kind: not_null
    col: exporter_iso
  - kind: not_null
    col: importer_iso
  - kind: not_null
    col: product_hs92
  - kind: row_floor
    min: 134947250
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
