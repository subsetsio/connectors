# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cepii-intense`. Regenerate after model-verify.
# caution: Rows are directional actor-pair category observations; reverse directions are distinct.
key: [date, actor1countrycode, actor2countrycode, category]
sort: [date, actor1countrycode, actor2countrycode, category]
columns:
  - name: date
    type: BIGINT
  - name: actor1countrycode
    type: VARCHAR
  - name: actor2countrycode
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: numevents
    type: BIGINT
  - name: numarticles
    type: BIGINT
  - name: shade
    type: DOUBLE
  - name: intensity
    type: DOUBLE
