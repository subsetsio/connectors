spec_id: entsog-transparency-platform-cmpunsuccessfulrequests
status: active
tests:
  - not_null: id
    certainty: 95
  - not_null: operatorKey
    certainty: 90
  - not_null: pointKey
    certainty: 90
  - unique: id
    reason: full-corpus pull, one row per CMP unsuccessful-request id
    certainty: 80
  - row_count: {min: 100}
    reason: CMP unsuccessful-request records are sparse but number in the hundreds
    certainty: 80
