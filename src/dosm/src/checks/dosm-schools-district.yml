# compiled by `hardened compile-checks` — post-publish audit of `dosm-schools-district`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, district, state, stage, type]
temporal: date
checks:
  - kind: unique
    columns: [date, district, state, stage, type]
  - kind: not_null
    col: date
  - kind: not_null
    col: district
  - kind: not_null
    col: state
  - kind: not_null
    col: stage
  - kind: not_null
    col: type
  - kind: row_floor
    min: 1496
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
