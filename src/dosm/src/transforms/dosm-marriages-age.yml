# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-marriages-age`. Regenerate after model-verify.
key: [date, age, sex]
temporal: date
sort: [date, age, sex]
columns:
  - name: date
    type: DATE
  - name: sex
    type: VARCHAR
  - name: age
    type: VARCHAR
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
