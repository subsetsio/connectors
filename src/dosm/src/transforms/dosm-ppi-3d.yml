# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-ppi-3d`. Regenerate after model-verify.
key: [date, group, series]
temporal: date
sort: [date, group, series]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: TIMESTAMP_NS
  - name: group
    type: VARCHAR
  - name: index
    type: DOUBLE
