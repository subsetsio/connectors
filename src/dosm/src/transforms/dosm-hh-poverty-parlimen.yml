# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-hh-poverty-parlimen`. Regenerate after model-verify.
key: [date, parlimen, state]
temporal: date
sort: [date, parlimen, state]
columns:
  - name: date
    type: DATE
  - name: state
    type: VARCHAR
  - name: parlimen
    type: VARCHAR
  - name: poverty_absolute
    type: DOUBLE
