# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-fish-landings`. Regenerate after model-verify.
key: [date, state, coast]
temporal: date
sort: [date, state, coast]
columns:
  - name: date
    type: TIMESTAMP
  - name: coast
    type: VARCHAR
  - name: state
    type: VARCHAR
  - name: landings
    type: BIGINT
