# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-sppi`. Regenerate after model-verify.
key: [date, series]
temporal: date
sort: [date, series]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: TIMESTAMP_NS
  - name: index
    type: DOUBLE
