# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-forest-reserve`. Regenerate after model-verify.
key: [date]
temporal: date
sort: [date]
columns:
  - name: date
    type: DATE
  - name: area
    type: DOUBLE
