# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-ppi`. Regenerate after model-verify.
key: [date, series]
temporal: date
sort: [date, series]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: TIMESTAMP_NS
  - name: index
    type: DOUBLE
  - name: index_sa
    type: DOUBLE
