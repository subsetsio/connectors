# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-crime-district`. Regenerate after model-verify.
key: [date, district, state, type, category]
temporal: date
sort: [date, district, state, type, category]
columns:
  - name: state
    type: VARCHAR
  - name: district
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: type
    type: VARCHAR
  - name: date
    type: DATE
  - name: crimes
    type: BIGINT
