# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-gdp-qtr-real-sa-demand`. Regenerate after model-verify.
key: [date, type]
temporal: date
sort: [date, type]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: DATE
  - name: type
    type: VARCHAR
  - name: value
    type: DOUBLE
