# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-marriages`. Regenerate after model-verify.
key: [date, sex]
temporal: date
sort: [date, sex]
columns:
  - name: date
    type: DATE
  - name: sex
    type: VARCHAR
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
