# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-prisoners-state`. Regenerate after model-verify.
key: [date, state, sex]
temporal: date
sort: [date, state, sex]
columns:
  - name: date
    type: DATE
  - name: state
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: prisoners
    type: BIGINT
