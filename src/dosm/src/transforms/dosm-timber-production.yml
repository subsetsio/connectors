# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-timber-production`. Regenerate after model-verify.
key: [date, state, product]
temporal: date
sort: [date, state, product]
columns:
  - name: date
    type: DATE
  - name: state
    type: VARCHAR
  - name: product
    type: VARCHAR
  - name: production
    type: BIGINT
