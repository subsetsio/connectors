# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-hies-malaysia-percentile`. Regenerate after model-verify.
# caution: No stable row key was verified in the raw profile; treat rows as source observations at the published granularity.
key: []
temporal: date
sort: [date]
columns:
  - name: date
    type: DATE
  - name: percentile
    type: BIGINT
  - name: variable
    type: VARCHAR
  - name: income
    type: DOUBLE
