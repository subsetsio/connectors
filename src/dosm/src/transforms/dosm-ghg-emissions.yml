# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-ghg-emissions`. Regenerate after model-verify.
key: [date, source]
temporal: date
sort: [date, source]
columns:
  - name: date
    type: TIMESTAMP
  - name: source
    type: VARCHAR
  - name: emissions
    type: DOUBLE
