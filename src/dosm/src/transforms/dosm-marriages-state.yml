# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-marriages-state`. Regenerate after model-verify.
key: [date, state, sex]
temporal: date
sort: [date, state, sex]
columns:
  - name: state
    type: VARCHAR
  - name: date
    type: DATE
  - name: sex
    type: VARCHAR
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
