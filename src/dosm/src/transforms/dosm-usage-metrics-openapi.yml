# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-usage-metrics-openapi`. Regenerate after model-verify.
key: [date, endpoint]
temporal: date
sort: [date, endpoint]
columns:
  - name: date
    type: DATE
  - name: endpoint
    type: VARCHAR
  - name: hits
    type: BIGINT
