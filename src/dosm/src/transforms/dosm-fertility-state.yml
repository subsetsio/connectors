# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-fertility-state`. Regenerate after model-verify.
key: [date, state, age_group]
temporal: date
sort: [date, state, age_group]
columns:
  - name: state
    type: VARCHAR
  - name: date
    type: DATE
  - name: age_group
    type: VARCHAR
  - name: fertility_rate
    type: DOUBLE
