# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-deaths`. Regenerate after model-verify.
key: [date]
temporal: date
sort: [date]
columns:
  - name: date
    type: DATE
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
