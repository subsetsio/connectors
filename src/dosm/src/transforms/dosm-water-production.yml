# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-water-production`. Regenerate after model-verify.
key: [date, state]
temporal: date
sort: [date, state]
columns:
  - name: state
    type: VARCHAR
  - name: date
    type: TIMESTAMP
  - name: value
    type: BIGINT
