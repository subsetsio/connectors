# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-completion-school-state`. Regenerate after model-verify.
key: [date, state, stage, sex]
temporal: date
sort: [date, state, stage, sex]
columns:
  - name: state
    type: VARCHAR
  - name: stage
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: date
    type: DATE
  - name: completion
    type: DOUBLE
