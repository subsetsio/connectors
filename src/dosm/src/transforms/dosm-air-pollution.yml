# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-air-pollution`. Regenerate after model-verify.
key: [date, pollutant]
temporal: date
sort: [date, pollutant]
columns:
  - name: date
    type: DATE
  - name: pollutant
    type: VARCHAR
  - name: concentration
    type: DOUBLE
