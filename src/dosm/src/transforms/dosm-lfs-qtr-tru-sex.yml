# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-lfs-qtr-tru-sex`. Regenerate after model-verify.
key: [date, sex, variable]
temporal: date
sort: [date, sex, variable]
columns:
  - name: date
    type: DATE
  - name: variable
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: tru
    type: DOUBLE
