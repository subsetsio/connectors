# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-lfs-qtr-tru-age`. Regenerate after model-verify.
key: [date, age, variable]
temporal: date
sort: [date, age, variable]
columns:
  - name: date
    type: DATE
  - name: variable
    type: VARCHAR
  - name: age
    type: VARCHAR
  - name: tru
    type: DOUBLE
