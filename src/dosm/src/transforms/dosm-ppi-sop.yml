# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-ppi-sop`. Regenerate after model-verify.
key: [date, stage, series]
temporal: date
sort: [date, stage, series]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: TIMESTAMP_NS
  - name: stage
    type: VARCHAR
  - name: index
    type: DOUBLE
