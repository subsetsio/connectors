# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-gdp-annual-nominal-income`. Regenerate after model-verify.
key: [date, component, series]
temporal: date
sort: [date, component, series]
columns:
  - name: series
    type: VARCHAR
  - name: date
    type: DATE
  - name: component
    type: VARCHAR
  - name: value
    type: DOUBLE
