# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-teachers-district`. Regenerate after model-verify.
# caution: No stable row key was verified in the raw profile; treat rows as source observations at the published granularity.
key: []
temporal: date
sort: [date]
columns:
  - name: state
    type: VARCHAR
  - name: district
    type: VARCHAR
  - name: stage
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: date
    type: DATE
  - name: teachers
    type: BIGINT
