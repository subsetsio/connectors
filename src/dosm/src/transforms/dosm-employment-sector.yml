# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-employment-sector`. Regenerate after model-verify.
key: [date, sector, sex]
temporal: date
sort: [date, sector, sex]
columns:
  - name: date
    type: TIMESTAMP
  - name: sector
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: proportion
    type: DOUBLE
