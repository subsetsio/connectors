# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-sdg-04-6-1`. Regenerate after model-verify.
key: [date, state, sex, age]
temporal: date
sort: [date, state, sex, age]
columns:
  - name: state
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: age
    type: VARCHAR
  - name: date
    type: DATE
  - name: proportion
    type: DOUBLE
