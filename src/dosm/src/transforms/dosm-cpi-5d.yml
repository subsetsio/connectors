# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-cpi-5d`. Regenerate after model-verify.
key: [date, subclass]
temporal: date
sort: [date, subclass]
columns:
  - name: date
    type: DATE
  - name: subclass
    type: VARCHAR
  - name: index
    type: DOUBLE
