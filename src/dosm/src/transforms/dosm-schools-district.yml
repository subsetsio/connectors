# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-schools-district`. Regenerate after model-verify.
key: [date, district, state, stage, type]
temporal: date
sort: [date, district, state, stage, type]
columns:
  - name: state
    type: VARCHAR
  - name: district
    type: VARCHAR
  - name: stage
    type: VARCHAR
  - name: type
    type: VARCHAR
  - name: date
    type: DATE
  - name: schools
    type: BIGINT
