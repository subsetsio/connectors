# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-sdg-03-3-1`. Regenerate after model-verify.
key: [date, sex]
temporal: date
sort: [date, sex]
columns:
  - name: sex
    type: VARCHAR
  - name: date
    type: DATE
  - name: incidence
    type: DOUBLE
