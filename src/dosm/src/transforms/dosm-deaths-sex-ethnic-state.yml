# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-deaths-sex-ethnic-state`. Regenerate after model-verify.
key: [date, state, ethnicity, sex]
temporal: date
sort: [date, state, ethnicity, sex]
columns:
  - name: state
    type: VARCHAR
  - name: date
    type: DATE
  - name: sex
    type: VARCHAR
  - name: ethnicity
    type: VARCHAR
  - name: abs
    type: DOUBLE
