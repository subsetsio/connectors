# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-births-district-sex`. Regenerate after model-verify.
key: [district, state, date, sex]
temporal: date
sort: [date, district, state, sex]
columns:
  - name: date
    type: DATE
  - name: state
    type: VARCHAR
  - name: district
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
