# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dosm-marriages-state-age`. Regenerate after model-verify.
key: [date, state, age, sex]
temporal: date
sort: [date, state, age, sex]
columns:
  - name: state
    type: VARCHAR
  - name: date
    type: DATE
  - name: sex
    type: VARCHAR
  - name: age
    type: VARCHAR
  - name: abs
    type: BIGINT
  - name: rate
    type: DOUBLE
