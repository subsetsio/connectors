spec_id: dosm-usage-metrics-openapi
# Catalog connector: OpenDOSM dataset; the column list is materialized
# from the first pull. Freshness/coverage authored from the observed shape.
status: deferred
tests:
  - freshness: {col: date, reaches: today - 45d}
    reason: >-
      data.gov.my OpenAPI usage metrics are a recent daily endpoint aggregate.
      The 45 day slack allows normal publication lag while still catching a
      stalled source feed.
    points_outward: true
    certainty: 75
