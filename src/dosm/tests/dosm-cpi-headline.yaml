spec_id: dosm-cpi-headline
# Catalog connector: each OpenDOSM dataset has its own column list,
# unknown until the first pull. Deferred per the download-test schema;
# concrete assertions are authored from the materialized shape.
status: deferred
