spec_id: dosm-gdp-qtr-real-supply
# Catalog connector: OpenDOSM dataset; the column list is materialized
# from the first pull. Freshness/coverage authored from the observed shape.
status: deferred
tests:
  - freshness: {col: date, reaches: today - 400d}
    reason: >-
      DOSM still refreshes this quarterly series (source data_as_of 2025-Q4); latest observed date is 2026-01-01. reaches slack = one quarterly period plus publication lag, so a normal release keeps max(date) within range; a miss points at DOSM, not our fetch.
    points_outward: true
    certainty: 75
