spec_id: dosm-fuelprice
# Catalog connector: OpenDOSM dataset; the column list is materialized
# from the first pull. Freshness/coverage authored from the observed shape.
status: deferred
tests:
  - freshness: {col: date, reaches: today - 50d}
    reason: >-
      DOSM still refreshes this high-frequency series (source data_as_of 2026-06-18 00:01); latest observed date is 2026-07-01. reaches slack = one high-frequency period plus publication lag, so a normal release keeps max(date) within range; a miss points at DOSM, not our fetch.
    points_outward: true
    certainty: 75
