# placeholder pass-through for first full run; regenerate with `hardened compile-transforms idb --write --force` after model-verify profiles raw.
write_mode: overwrite
columns:
  - name: source_resource
    type: VARCHAR
