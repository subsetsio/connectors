# compiled by `hardened compile-checks` — post-publish audit of `idb-community-nutrition-program-in-el-alto-bolivia-data-2014-2017`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [folio_anon, id_s8, p08_1]
checks:
  - kind: unique
    columns: [folio_anon, id_s8, p08_1]
  - kind: not_null
    col: folio_anon
  - kind: not_null
    col: id_s8
  - kind: not_null
    col: p08_1
  - kind: row_floor
    min: 41020
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
