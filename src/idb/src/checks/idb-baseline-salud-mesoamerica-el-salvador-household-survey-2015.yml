# compiled by `hardened compile-checks` — post-publish audit of `idb-baseline-salud-mesoamerica-el-salvador-household-survey-2015`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id_hh, nhogar, weight_hh, codigo, seg2]
checks:
  - kind: unique
    columns: [id_hh, nhogar, weight_hh, codigo, seg2]
  - kind: not_null
    col: id_hh
  - kind: not_null
    col: nhogar
  - kind: not_null
    col: weight_hh
  - kind: not_null
    col: codigo
  - kind: not_null
    col: seg2
  - kind: row_floor
    min: 1812
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
