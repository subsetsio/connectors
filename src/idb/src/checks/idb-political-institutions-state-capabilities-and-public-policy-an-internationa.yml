# compiled by `hardened compile-checks` — post-publish audit of `idb-political-institutions-state-capabilities-and-public-policy-an-internationa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [field_name, type, source_resource]
checks:
  - kind: unique
    columns: [field_name, type, source_resource]
  - kind: not_null
    col: field_name
  - kind: not_null
    col: type
  - kind: not_null
    col: source_resource
  - kind: row_floor
    min: 638
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
