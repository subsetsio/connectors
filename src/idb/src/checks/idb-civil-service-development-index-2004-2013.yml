# compiled by `hardened compile-checks` — post-publish audit of `idb-civil-service-development-index-2004-2013`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [pais, nombre_indicador, ano, categoria_indicador]
checks:
  - kind: unique
    columns: [pais, nombre_indicador, ano, categoria_indicador]
  - kind: not_null
    col: pais
  - kind: not_null
    col: nombre_indicador
  - kind: not_null
    col: ano
  - kind: not_null
    col: categoria_indicador
  - kind: row_floor
    min: 304
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
