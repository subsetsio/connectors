# compiled by `hardened compile-checks` — post-publish audit of `idb-latin-american-and-caribbean-enterprise-survey-2011`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [idstd, unique_id, d1a1x, id, d2]
checks:
  - kind: unique
    columns: [idstd, unique_id, d1a1x, id, d2]
  - kind: not_null
    col: idstd
  - kind: not_null
    col: unique_id
  - kind: not_null
    col: d1a1x
  - kind: not_null
    col: id
  - kind: not_null
    col: d2
  - kind: row_floor
    min: 1390
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
