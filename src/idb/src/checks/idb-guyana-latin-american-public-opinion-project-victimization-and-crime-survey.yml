# compiled by `hardened compile-checks` — post-publish audit of `idb-guyana-latin-american-public-opinion-project-victimization-and-crime-survey`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [uniq_id, gpslocation, cluster, q2y, q2]
checks:
  - kind: unique
    columns: [uniq_id, gpslocation, cluster, q2y, q2]
  - kind: not_null
    col: uniq_id
  - kind: not_null
    col: gpslocation
  - kind: not_null
    col: cluster
  - kind: not_null
    col: q2y
  - kind: not_null
    col: q2
  - kind: row_floor
    min: 788
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
