# compiled by `hardened compile-checks` — post-publish audit of `idb-replication-data-for-how-effective-is-energy-efficient-housing-evidence-fro`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lecturashr, fecha, folio_simo, hour]
checks:
  - kind: unique
    columns: [lecturashr, fecha, folio_simo, hour]
  - kind: not_null
    col: lecturashr
  - kind: not_null
    col: fecha
  - kind: not_null
    col: folio_simo
  - kind: not_null
    col: hour
  - kind: row_floor
    min: 2489244
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
