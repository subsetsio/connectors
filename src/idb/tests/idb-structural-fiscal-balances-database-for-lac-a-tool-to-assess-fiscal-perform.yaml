spec_id: idb-structural-fiscal-balances-database-for-lac-a-tool-to-assess-fiscal-perform
status: active
coverage:
  frozen: true
  reason: IDB CKAN package is a versioned dataset publication; temporal fields describe the covered period rather than an ongoing refresh cadence.
tests:
  - not_null: source_resource
    reason: every published row is tagged with the CKAN resource name it came from; nulls mean the provenance column broke
    certainty: 95
  - row_count:
      min: 1
    reason: rank-accepted packages expose at least one non-empty tabular resource; 0 rows means the dump/file fetch silently returned nothing
    certainty: 80
