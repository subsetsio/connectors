# compiled by `hardened compile-checks` — post-publish audit of `statistics-mauritius-external-merchandise-trade-statistics-republic-mauritius-year-1938-4th-quarter-2019`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [resource_id, row_number]
temporal: package_metadata_modified
checks:
  - kind: unique
    columns: [resource_id, row_number]
  - kind: not_null
    col: resource_id
  - kind: not_null
    col: row_number
  - kind: row_floor
    min: 111
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
