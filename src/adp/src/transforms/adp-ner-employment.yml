# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `adp-ner-employment`. Regenerate after model-verify.
# caution: Contains multiple aggregation dimensions in one long table; filter aggregation before comparing or summing categories.
# caution: Contains both monthly and weekly observations; filter timestep before time-series analysis.
key: [timestep, aggregation, category, date]
temporal: date
sort: [date, timestep, aggregation, category]
columns:
  - name: timestep
    type: VARCHAR
  - name: aggregation
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: date
    type: DATE
  - name: ner
    type: DOUBLE
  - name: ner_sa
    type: DOUBLE
