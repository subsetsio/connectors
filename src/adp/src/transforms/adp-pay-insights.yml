# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `adp-pay-insights`. Regenerate after model-verify.
# caution: Contains multiple aggregation dimensions in one long table; filter aggregation before comparing or summing categories.
key: [category, date]
temporal: date
sort: [date, category]
columns:
  - name: timestep
    type: VARCHAR
  - name: aggregation
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: date
    type: DATE
  - name: median_pay_change
    type: DOUBLE
  - name: median_annual_pay
    type: DOUBLE
