# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `coin-metrics-asset-metrics-values`. Regenerate after model-verify.
# caution: Rows mix metrics with different definitions and units; filter or group by metric, and use the metric catalog before aggregating values.
key: [date, asset, metric]
temporal: date
sort: [date, asset, metric]
columns:
  - name: asset
    type: VARCHAR
  - name: metric
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
