# compiled by `hardened compile-checks` — post-publish audit of `north-american-breeding-bird-survey-fifty-stop-counts`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [routedataid, aou, route, stop8, stop6]
temporal: year
checks:
  - kind: unique
    columns: [routedataid, aou, route, stop8, stop6]
  - kind: not_null
    col: routedataid
  - kind: not_null
    col: aou
  - kind: not_null
    col: route
  - kind: not_null
    col: stop8
  - kind: not_null
    col: stop6
  - kind: row_floor
    min: 2198831
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
