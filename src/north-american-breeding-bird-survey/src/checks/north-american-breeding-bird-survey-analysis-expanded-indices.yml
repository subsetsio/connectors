# compiled by `hardened compile-checks` — post-publish audit of `north-american-breeding-bird-survey-analysis-expanded-indices`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [index, "2_5_ci", aou, region, year]
temporal: year
checks:
  - kind: unique
    columns: [index, "2_5_ci", aou, region, year]
  - kind: not_null
    col: index
  - kind: not_null
    col: "2_5_ci"
  - kind: not_null
    col: aou
  - kind: not_null
    col: region
  - kind: not_null
    col: year
  - kind: row_floor
    min: 327343
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
