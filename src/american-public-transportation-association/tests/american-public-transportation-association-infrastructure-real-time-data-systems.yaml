spec_id: american-public-transportation-association-infrastructure-real-time-data-systems
status: active
tests:
  - not_null: organization_name
    reason: every infrastructure row belongs to a named organization
    certainty: 88
  - row_count: {min: 200}
    reason: the source workbook is stable; far fewer rows means the parse or fetch broke
    certainty: 85
