# compiled by `hardened compile-checks` — post-publish audit of `rbnz-c41`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, series_name, indicator_label, series_id, unit]
temporal: date
checks:
  - kind: unique
    columns: [date, series_name, indicator_label, series_id, unit]
  - kind: not_null
    col: date
  - kind: not_null
    col: series_name
  - kind: not_null
    col: indicator_label
  - kind: not_null
    col: series_id
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 3213
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
