# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `rbnz-m1`. Regenerate after model-verify.
# caution: Some source workbook rows share date and indicator descriptors; workbook context and value identify raw observations.
key: [date, indicator_label, sheet, file_stem, value]
temporal: date
sort: [date, indicator_label, sheet, file_stem, value]
columns:
  - name: series_code
    type: VARCHAR
  - name: option
    type: VARCHAR
  - name: file_stem
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: indicator_label
    type: VARCHAR
  - name: series_id
    type: VARCHAR
  - name: series_name
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
