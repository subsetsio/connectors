# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `rbnz-t45`. Regenerate after model-verify.
# caution: Workbook rows are published as source observation records; preserve option, file_stem, sheet, indicator_label, series_id, series_name, unit, date, and value when deduplicating or comparing variants.
key: [date, series_name, indicator_label, series_id]
temporal: date
sort: [date, series_name, indicator_label, series_id]
columns:
  - name: series_code
    type: VARCHAR
  - name: option
    type: VARCHAR
  - name: file_stem
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: indicator_label
    type: VARCHAR
  - name: series_id
    type: VARCHAR
  - name: series_name
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
