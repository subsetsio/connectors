spec_id: rbnz-c22
status: active
coverage:
  frozen: true
  reason: RBNZ marks C22 discontinued; household balance sheet statistics moved to Stats NZ after the June 2021 quarter.
tests:
  - not_null: date
  - not_null: series_code
  - not_null: value
  - column_type: {col: date, type: date}
  - column_type: {col: value, type: float}
  - row_count: {min: 1}
    reason: each RBNZ workbook should parse to at least one numeric observation
    certainty: 95
