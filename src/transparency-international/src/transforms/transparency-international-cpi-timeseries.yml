# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `transparency-international-cpi-timeseries`. Regenerate after model-verify.
# caution: The workbook restates the full back-series each edition, so historical values should be treated as the latest published CPI series rather than immutable vintages.
key: [iso3, country, year, region]
temporal: year
sort: [year, iso3, country, region]
columns:
  - name: country
    type: VARCHAR
  - name: iso3
    type: VARCHAR
  - name: region
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: cpi_score
    type: DOUBLE
  - name: rank
    type: INTEGER
  - name: num_sources
    type: INTEGER
  - name: standard_error
    type: DOUBLE
