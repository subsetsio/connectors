spec_id: global-volcanism-program-e3webapp-emissions
# Deferred: the E3WebApp_Emissions layer could not be probed from the authoring
# environment (the GeoServer host was unreachable from this egress), so its exact
# attribute schema is unknown. Assertions will be authored from the first pulled
# chunk once the column shape is observed.
status: deferred
