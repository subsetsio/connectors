spec_id: global-volcanism-program-e3webapp-holocenevolcanoes
# Deferred: the E3WebApp_HoloceneVolcanoes layer could not be probed from the
# authoring environment (the GeoServer host was unreachable from this egress),
# so its exact attribute schema is unknown. This layer backs the E3 web app and
# largely mirrors Smithsonian_VOTW_Holocene_Volcanoes; assertions will be
# authored from the first pulled chunk once the column shape is observed.
status: deferred
