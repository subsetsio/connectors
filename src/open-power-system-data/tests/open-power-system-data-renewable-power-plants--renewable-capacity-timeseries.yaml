spec_id: open-power-system-data-renewable-power-plants--renewable-capacity-timeseries
status: active
coverage:
  frozen: true
  reason: OPSD renewable_power_plants latest points to the 2020-08-25 snapshot; the archive is a historical release, not a live feed
tests:
  - row_count: {min: 100}
    reason: full-snapshot table; far fewer rows means a truncated or empty download
    certainty: 80
    severity: warn
