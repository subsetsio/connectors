spec_id: open-power-system-data-renewable-power-plants--renewable-power-plants-de
status: active
coverage:
  frozen: true
  reason: OPSD renewable_power_plants latest points to the 2020-08-25 snapshot; the archive is a historical plant registry release, not a live feed
tests:
  - row_count: {min: 1000}
    reason: full-snapshot table; far fewer rows means a truncated or empty download
    certainty: 80
    severity: block
  - not_null: electrical_capacity
    reason: a plant registry row should carry an installed-capacity value
    certainty: 70
    severity: warn
