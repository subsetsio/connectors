spec_id: open-power-system-data-ninja-pv-wind-profiles--ninja-pv-wind-profiles-singleindex
status: active
coverage:
  frozen: true
  reason: OPSD ninja_pv_wind_profiles latest points to the 2020-09-16 snapshot; the archive is a historical release, not a live feed
tests:
  - row_count: {min: 1000}
    reason: full-snapshot table; far fewer rows means a truncated or empty download
    certainty: 80
    severity: block
  - not_null: time
    reason: every observation row is timestamped; a null timestamp means a header/parse shift
    certainty: 75
    severity: warn
