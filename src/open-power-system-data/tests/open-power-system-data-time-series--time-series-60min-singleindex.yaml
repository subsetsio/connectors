spec_id: open-power-system-data-time-series--time-series-60min-singleindex
status: active
coverage:
  frozen: true
  reason: OPSD time_series latest points to the 2020-10-06 snapshot; the archive is a historical release, not a live feed
tests:
  - row_count: {min: 10000}
    reason: full-snapshot table; far fewer rows means a truncated or empty download
    certainty: 80
    severity: block
  - not_null: utc_timestamp
    reason: every observation row is timestamped; a null timestamp means a header/parse shift
    certainty: 90
    severity: warn
