spec_id: open-power-system-data-renewable-power-plants--res-plants-separated-fr-outvalidated-plants
status: active
coverage:
  frozen: true
  reason: OPSD renewable_power_plants latest points to the 2020-08-25 snapshot; this validation extract is versioned with that release
tests:
  - row_count: {min: 100}
    reason: outvalidated French renewable-plant extract should contain many rows; far fewer rows means a truncated or empty download
    certainty: 75
    severity: block
  - not_null: electrical_capacity
    reason: a plant-registry row should carry an installed-capacity value
    certainty: 70
    severity: warn
