# compiled by `hardened compile-checks` — post-publish audit of `open-power-system-data-national-generation-capacity--national-generation-capacity-stacked`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id, country, technology, source, year]
temporal: year
checks:
  - kind: unique
    columns: [id, country, technology, source, year]
  - kind: not_null
    col: id
  - kind: not_null
    col: country
  - kind: not_null
    col: technology
  - kind: not_null
    col: source
  - kind: not_null
    col: year
  - kind: row_floor
    min: 18639
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
