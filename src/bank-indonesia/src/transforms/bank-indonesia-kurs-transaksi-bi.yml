# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bank-indonesia-kurs-transaksi-bi`. Regenerate after model-verify.
# caution: Rates are quoted against IDR with the source's unit field; consumers should account for unit before comparing currency levels.
# caution: This is the BI transaction-rate product, distinct from physical foreign banknote rates.
key: [date, currency]
temporal: date
sort: [date, currency]
columns:
  - name: date
    type: DATE
  - name: currency
    type: VARCHAR
  - name: unit
    type: DOUBLE
  - name: buy
    type: DOUBLE
  - name: sell
    type: DOUBLE
