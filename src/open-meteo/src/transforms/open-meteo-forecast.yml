# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `open-meteo-forecast`. Regenerate after model-verify.
# caution: Operational forecast rows are forward-looking and ephemeral; each refresh replaces the current forecast horizon rather than extending a stable history.
key: [date, country, name]
temporal: date
sort: [date, country, name]
columns:
  - name: name
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: date
    type: DATE
  - name: temperature_2m_max
    type: DOUBLE
  - name: temperature_2m_min
    type: DOUBLE
  - name: precipitation_sum
    type: DOUBLE
