# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `open-meteo-marine`. Regenerate after model-verify.
# caution: Marine wave rows are forecast-only and ephemeral; each refresh replaces the current forecast horizon rather than extending a stable history.
key: [time, country, name]
temporal: time
sort: [time, country, name]
columns:
  - name: name
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: time
    type: TIMESTAMP
  - name: wave_height
    type: DOUBLE
  - name: wave_period
    type: DOUBLE
  - name: wave_direction
    type: DOUBLE
