# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `open-meteo-climate-projections`. Regenerate after model-verify.
# caution: Rows are model-specific CMIP6 projections; filter or group by `model` before comparing or aggregating scenarios.
key: [date, country, name]
temporal: date
sort: [date, country, name]
columns:
  - name: name
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: model
    type: VARCHAR
  - name: date
    type: DATE
  - name: temperature_2m_max
    type: DOUBLE
