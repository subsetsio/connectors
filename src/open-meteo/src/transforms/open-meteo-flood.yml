# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `open-meteo-flood`. Regenerate after model-verify.
# caution: River discharge is sampled at the connector's curated locations and only rows with a source-reported discharge value are published.
key: [date, country, name]
temporal: date
sort: [date, country, name]
columns:
  - name: name
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: date
    type: DATE
  - name: river_discharge
    type: DOUBLE
