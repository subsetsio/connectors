# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `fsa-e94f4c4c-c753-4bc0-b106-30feb28fbf66`. Regenerate after model-verify.
# caution: This is a normalized spreadsheet-cell table. Use resource_id, sheet, row_idx, and col_idx to reconstruct source rows and columns; do not aggregate num_value without interpreting the surrounding labels in value.
key: [resource_id, sheet, row_idx, col_idx]
sort: [resource_id, sheet, row_idx, col_idx]
columns:
  - name: resource_id
    type: VARCHAR
  - name: resource_name
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: row_idx
    type: INTEGER
  - name: col_idx
    type: INTEGER
  - name: value
    type: VARCHAR
  - name: num_value
    type: DOUBLE
