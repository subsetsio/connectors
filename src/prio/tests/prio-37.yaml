spec_id: prio-37
status: active
coverage:
  frozen: true
  reason: PRIO hosts a versioned Global Data Lab mirror snapshot; the connector fetches the current full file.
tests:
  - row_count: {min: 1}
    reason: the GDL corruption CSV should contain at least one data row
    certainty: 95
