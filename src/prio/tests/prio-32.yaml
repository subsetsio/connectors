spec_id: prio-32
status: active
coverage:
  frozen: true
  reason: PRIO publishes this dataset as a versioned historical snapshot; the connector fetches the current full file.
tests:
  - row_count: {min: 1}
    reason: the urban social disorder v3 dataset should contain at least one data row
    certainty: 95
