spec_id: prio-3
status: active
coverage:
  frozen: true
  reason: PRIO publishes this dataset as a versioned historical snapshot; the connector fetches the current full file.
tests:
  - row_count: {min: 1}
    reason: the onset and duration dataset should contain at least one data row
    certainty: 95
