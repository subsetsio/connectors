spec_id: prio-28
status: active
coverage:
  frozen: true
  reason: PRIO publishes this dataset as a versioned historical snapshot; the connector fetches the current full file.
tests:
  - row_count: {min: 1}
    reason: the institutional variance data file should contain at least one data row
    certainty: 95
