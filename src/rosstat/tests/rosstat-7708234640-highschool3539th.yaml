spec_id: rosstat-7708234640-highschool3539th
status: deferred
coverage:
  frozen: true
  reason: "Rosstat open-data entries are static published files; treat each accepted file as a frozen upstream release until the catalog entry changes."
