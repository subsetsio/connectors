# compiled by `hardened compile-checks` — post-publish audit of `rosstat-7708234640-budgetexecution-2020-3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: ["2020", column_6, column, column_4, column_5]
checks:
  - kind: unique
    columns: ["2020", column_6, column, column_4, column_5]
  - kind: not_null
    col: "2020"
  - kind: not_null
    col: column_6
  - kind: not_null
    col: column
  - kind: not_null
    col: column_4
  - kind: not_null
    col: column_5
  - kind: row_floor
    min: 57
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
