# compiled by `hardened compile-checks` — post-publish audit of `rosstat-7708234640-belgorodregion`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [col_0, col_7, col_2, col_18, col_19]
checks:
  - kind: unique
    columns: [col_0, col_7, col_2, col_18, col_19]
  - kind: not_null
    col: col_0
  - kind: not_null
    col: col_7
  - kind: not_null
    col: col_2
  - kind: not_null
    col: col_18
  - kind: not_null
    col: col_19
  - kind: row_floor
    min: 4
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
