# compiled by `hardened compile-checks` — post-publish audit of `rosstat-7708234640-allowanceaslivelihood`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [measure45, dimension1, dimension2, dimension3]
checks:
  - kind: unique
    columns: [measure45, dimension1, dimension2, dimension3]
  - kind: not_null
    col: measure45
  - kind: not_null
    col: dimension1
  - kind: not_null
    col: dimension2
  - kind: not_null
    col: dimension3
  - kind: row_floor
    min: 403
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
