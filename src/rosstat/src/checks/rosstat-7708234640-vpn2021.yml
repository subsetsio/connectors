# compiled by `hardened compile-checks` — post-publish audit of `rosstat-7708234640-vpn2021`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, terson_parent, measures, terson_urbanrural, p04_gender_id]
temporal: time
checks:
  - kind: unique
    columns: [value, terson_parent, measures, terson_urbanrural, p04_gender_id]
  - kind: not_null
    col: value
  - kind: not_null
    col: terson_parent
  - kind: not_null
    col: measures
  - kind: not_null
    col: terson_urbanrural
  - kind: not_null
    col: p04_gender_id
  - kind: row_floor
    min: 1305
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
