# compiled by `hardened compile-checks` — post-publish audit of `rosstat-7708234640-vshp2016506`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, oktmo_parent, range01500_code, measures, range01500_have]
temporal: time
checks:
  - kind: unique
    columns: [value, oktmo_parent, range01500_code, measures, range01500_have]
  - kind: not_null
    col: value
  - kind: not_null
    col: oktmo_parent
  - kind: not_null
    col: range01500_code
  - kind: not_null
    col: measures
  - kind: not_null
    col: range01500_have
  - kind: row_floor
    min: 3062
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
