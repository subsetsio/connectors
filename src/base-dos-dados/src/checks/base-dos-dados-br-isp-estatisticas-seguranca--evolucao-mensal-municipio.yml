# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-isp-estatisticas-seguranca--evolucao-mensal-municipio`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [municipio, mes_ano, regiao_rj]
checks:
  - kind: unique
    columns: [municipio, mes_ano, regiao_rj]
  - kind: not_null
    col: municipio
  - kind: not_null
    col: mes_ano
  - kind: not_null
    col: regiao_rj
  - kind: row_floor
    min: 4140
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
