# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-me-clima-organizacional--microdados`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [subclasse, classe, area_empresa]
checks:
  - kind: unique
    columns: [subclasse, classe, area_empresa]
  - kind: not_null
    col: subclasse
  - kind: not_null
    col: classe
  - kind: not_null
    col: area_empresa
  - kind: row_floor
    min: 8218
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
