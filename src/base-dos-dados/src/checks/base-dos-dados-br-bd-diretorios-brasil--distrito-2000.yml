# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-bd-diretorios-brasil--distrito-2000`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id_distrito, nome, id_municipio, sigla_uf]
checks:
  - kind: unique
    columns: [id_distrito, nome, id_municipio, sigla_uf]
  - kind: not_null
    col: id_distrito
  - kind: not_null
    col: nome
  - kind: not_null
    col: id_municipio
  - kind: not_null
    col: sigla_uf
  - kind: row_floor
    min: 4924
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
