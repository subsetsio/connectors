# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-inep-ideb--brasil-ano-escolar`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, rede, ensino, ano_escolar, taxa_aprovacao]
checks:
  - kind: unique
    columns: [ano, rede, ensino, ano_escolar, taxa_aprovacao]
  - kind: not_null
    col: ano
  - kind: not_null
    col: rede
  - kind: not_null
    col: ensino
  - kind: not_null
    col: ano_escolar
  - kind: not_null
    col: taxa_aprovacao
  - kind: row_floor
    min: 244
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
