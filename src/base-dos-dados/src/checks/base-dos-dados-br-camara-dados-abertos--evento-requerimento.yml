# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-camara-dados-abertos--evento-requerimento`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id_evento, titulo_requerimento, id_proposicao]
checks:
  - kind: unique
    columns: [id_evento, titulo_requerimento, id_proposicao]
  - kind: not_null
    col: id_evento
  - kind: not_null
    col: titulo_requerimento
  - kind: not_null
    col: id_proposicao
  - kind: row_floor
    min: 8756
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
