# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-ibge-censo-2022--populacao-idade-sexo-territorio-quilombola`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [territorio_quilombola, idade, sigla_uf, grupo_idade, sexo]
checks:
  - kind: unique
    columns: [territorio_quilombola, idade, sigla_uf, grupo_idade, sexo]
  - kind: not_null
    col: territorio_quilombola
  - kind: not_null
    col: idade
  - kind: not_null
    col: sigla_uf
  - kind: not_null
    col: grupo_idade
  - kind: not_null
    col: sexo
  - kind: row_floor
    min: 56224
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
