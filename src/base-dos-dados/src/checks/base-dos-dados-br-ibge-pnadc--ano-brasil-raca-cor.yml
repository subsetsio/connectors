# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-ibge-pnadc--ano-brasil-raca-cor`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, sexo, raca_cor, populacao]
checks:
  - kind: unique
    columns: [ano, sexo, raca_cor, populacao]
  - kind: not_null
    col: ano
  - kind: not_null
    col: sexo
  - kind: not_null
    col: raca_cor
  - kind: not_null
    col: populacao
  - kind: row_floor
    min: 48
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
