# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-camara-dados-abertos--evento-orgao`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id_evento, id_orgao, sigla_orgao]
checks:
  - kind: unique
    columns: [id_evento, id_orgao, sigla_orgao]
  - kind: not_null
    col: id_evento
  - kind: not_null
    col: id_orgao
  - kind: not_null
    col: sigla_orgao
  - kind: row_floor
    min: 28552
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
