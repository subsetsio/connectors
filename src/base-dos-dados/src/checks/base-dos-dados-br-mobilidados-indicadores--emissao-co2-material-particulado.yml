# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-mobilidados-indicadores--emissao-co2-material-particulado`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, sigla_uf, id_municipio, emissao_co2, emissao_mp]
checks:
  - kind: unique
    columns: [ano, sigla_uf, id_municipio, emissao_co2, emissao_mp]
  - kind: not_null
    col: ano
  - kind: not_null
    col: sigla_uf
  - kind: not_null
    col: id_municipio
  - kind: not_null
    col: emissao_co2
  - kind: not_null
    col: emissao_mp
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
