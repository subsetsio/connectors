# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-camara-dados-abertos--funcionario`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ponto, data_inicio_historico, lotacao, grupo]
temporal: data_inicio_historico
checks:
  - kind: unique
    columns: [ponto, data_inicio_historico, lotacao, grupo]
  - kind: not_null
    col: ponto
  - kind: not_null
    col: data_inicio_historico
  - kind: not_null
    col: lotacao
  - kind: not_null
    col: grupo
  - kind: row_floor
    min: 7286
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
