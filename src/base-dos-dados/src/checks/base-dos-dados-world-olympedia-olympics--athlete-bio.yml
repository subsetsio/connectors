# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-world-olympedia-olympics--athlete-bio`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [athlete_id, country, country_noc, sex]
temporal: birth_date
checks:
  - kind: unique
    columns: [athlete_id, country, country_noc, sex]
  - kind: not_null
    col: athlete_id
  - kind: not_null
    col: country
  - kind: not_null
    col: country_noc
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 77930
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
