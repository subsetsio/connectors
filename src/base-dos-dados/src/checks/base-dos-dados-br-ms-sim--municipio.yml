# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-ms-sim--municipio`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, sigla_uf, id_municipio, numero_obitos]
checks:
  - kind: unique
    columns: [ano, sigla_uf, id_municipio, numero_obitos]
  - kind: not_null
    col: ano
  - kind: not_null
    col: sigla_uf
  - kind: not_null
    col: id_municipio
  - kind: not_null
    col: numero_obitos
  - kind: row_floor
    min: 66434
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
