# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-inep-ana--dicionario`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [valor, coluna, chave, id_tabela, cobertura_temporal]
checks:
  - kind: unique
    columns: [valor, coluna, chave, id_tabela, cobertura_temporal]
  - kind: not_null
    col: valor
  - kind: not_null
    col: coluna
  - kind: not_null
    col: chave
  - kind: not_null
    col: id_tabela
  - kind: not_null
    col: cobertura_temporal
  - kind: row_floor
    min: 112
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
