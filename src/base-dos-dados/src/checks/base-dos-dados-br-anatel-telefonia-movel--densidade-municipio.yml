# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-anatel-telefonia-movel--densidade-municipio`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, mes, sigla_uf, id_municipio, densidade]
checks:
  - kind: unique
    columns: [ano, mes, sigla_uf, id_municipio, densidade]
  - kind: not_null
    col: ano
  - kind: not_null
    col: mes
  - kind: not_null
    col: sigla_uf
  - kind: not_null
    col: id_municipio
  - kind: not_null
    col: densidade
  - kind: row_floor
    min: 183810
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
