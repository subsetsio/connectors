# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-world-olympedia-olympics--result`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [result_description, result_id, result_date, result_participants, event_title]
checks:
  - kind: unique
    columns: [result_description, result_id, result_date, result_participants, event_title]
  - kind: not_null
    col: result_description
  - kind: not_null
    col: result_id
  - kind: not_null
    col: result_date
  - kind: not_null
    col: result_participants
  - kind: not_null
    col: event_title
  - kind: row_floor
    min: 3697
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
