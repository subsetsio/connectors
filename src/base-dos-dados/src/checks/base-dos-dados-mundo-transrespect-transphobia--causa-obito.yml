# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-mundo-transrespect-transphobia--causa-obito`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, causa_obito, homicidios]
checks:
  - kind: unique
    columns: [ano, causa_obito, homicidios]
  - kind: not_null
    col: ano
  - kind: not_null
    col: causa_obito
  - kind: not_null
    col: homicidios
  - kind: row_floor
    min: 66
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
