# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-inep-indicador-nivel-socioeconomico--dicionario`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [valor, chave, cobertura_temporal, id_tabela, coluna]
checks:
  - kind: unique
    columns: [valor, chave, cobertura_temporal, id_tabela, coluna]
  - kind: not_null
    col: valor
  - kind: not_null
    col: chave
  - kind: not_null
    col: cobertura_temporal
  - kind: not_null
    col: id_tabela
  - kind: not_null
    col: coluna
  - kind: row_floor
    min: 28
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
