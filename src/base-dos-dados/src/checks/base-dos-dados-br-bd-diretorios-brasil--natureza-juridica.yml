# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-bd-diretorios-brasil--natureza-juridica`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id_natureza_juridica, descricao, escopo]
checks:
  - kind: unique
    columns: [id_natureza_juridica, descricao, escopo]
  - kind: not_null
    col: id_natureza_juridica
  - kind: not_null
    col: descricao
  - kind: not_null
    col: escopo
  - kind: row_floor
    min: 51
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
