# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-sp-alesp--assessores-parlamentares`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [servidor, nome_deputado, lotacao, cargo, regime]
checks:
  - kind: unique
    columns: [servidor, nome_deputado, lotacao, cargo, regime]
  - kind: not_null
    col: servidor
  - kind: not_null
    col: nome_deputado
  - kind: not_null
    col: lotacao
  - kind: not_null
    col: cargo
  - kind: not_null
    col: regime
  - kind: row_floor
    min: 937
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
