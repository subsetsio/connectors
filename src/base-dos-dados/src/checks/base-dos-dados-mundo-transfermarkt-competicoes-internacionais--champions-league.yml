# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-mundo-transfermarkt-competicoes-internacionais--champions-league`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data, arbitro, time_mandante, time_visitante, temporada]
temporal: data
checks:
  - kind: unique
    columns: [data, arbitro, time_mandante, time_visitante, temporada]
  - kind: not_null
    col: data
  - kind: not_null
    col: arbitro
  - kind: not_null
    col: time_mandante
  - kind: not_null
    col: time_visitante
  - kind: not_null
    col: temporada
  - kind: row_floor
    min: 1245
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
