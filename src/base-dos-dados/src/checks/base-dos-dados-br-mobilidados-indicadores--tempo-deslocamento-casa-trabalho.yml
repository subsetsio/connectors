# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-mobilidados-indicadores--tempo-deslocamento-casa-trabalho`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ano, sigla_uf, id_municipio, tempo_medio_deslocamento, prop_deslocamento_acima_1_hora]
checks:
  - kind: unique
    columns: [ano, sigla_uf, id_municipio, tempo_medio_deslocamento, prop_deslocamento_acima_1_hora]
  - kind: not_null
    col: ano
  - kind: not_null
    col: sigla_uf
  - kind: not_null
    col: id_municipio
  - kind: not_null
    col: tempo_medio_deslocamento
  - kind: not_null
    col: prop_deslocamento_acima_1_hora
  - kind: row_floor
    min: 114
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
