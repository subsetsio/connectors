# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-br-cvm-administradores-carteira--pessoa-juridica`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [denominacao_social, data_inicio_situacao, data_registro, situacao]
temporal: data_registro
checks:
  - kind: unique
    columns: [denominacao_social, data_inicio_situacao, data_registro, situacao]
  - kind: not_null
    col: denominacao_social
  - kind: not_null
    col: data_inicio_situacao
  - kind: not_null
    col: data_registro
  - kind: not_null
    col: situacao
  - kind: row_floor
    min: 1412
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
