# compiled by `hardened compile-checks` — post-publish audit of `base-dos-dados-world-olympedia-olympics--game-medal-tally`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_noc, country, edition, edition_id, year]
temporal: year
checks:
  - kind: unique
    columns: [country_noc, country, edition, edition_id, year]
  - kind: not_null
    col: country_noc
  - kind: not_null
    col: country
  - kind: not_null
    col: edition
  - kind: not_null
    col: edition_id
  - kind: not_null
    col: year
  - kind: row_floor
    min: 903
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
