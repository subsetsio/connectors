# compiled by `hardened compile-checks` — post-publish audit of `movebank-move-697`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [event_id, longitude, latitude, individual_id, tag_id]
temporal: timestamp
checks:
  - kind: unique
    columns: [event_id, longitude, latitude, individual_id, tag_id]
  - kind: not_null
    col: event_id
  - kind: not_null
    col: longitude
  - kind: not_null
    col: latitude
  - kind: not_null
    col: individual_id
  - kind: not_null
    col: tag_id
  - kind: row_floor
    min: 181699
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
