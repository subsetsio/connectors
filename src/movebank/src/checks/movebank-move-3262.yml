# compiled by `hardened compile-checks` — post-publish audit of `movebank-move-3262`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [event_id, latitude, tag_id, individual_id]
temporal: timestamp
checks:
  - kind: unique
    columns: [event_id, latitude, tag_id, individual_id]
  - kind: not_null
    col: event_id
  - kind: not_null
    col: latitude
  - kind: not_null
    col: tag_id
  - kind: not_null
    col: individual_id
  - kind: row_floor
    min: 11260
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
