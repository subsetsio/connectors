# compiled by `hardened compile-checks` — post-publish audit of `movebank-move-1132`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [event_id, tag_id, individual_id, longitude, latitude]
temporal: timestamp
checks:
  - kind: unique
    columns: [event_id, tag_id, individual_id, longitude, latitude]
  - kind: not_null
    col: event_id
  - kind: not_null
    col: tag_id
  - kind: not_null
    col: individual_id
  - kind: not_null
    col: longitude
  - kind: not_null
    col: latitude
  - kind: row_floor
    min: 1
