# compiled by `hardened compile-checks` — post-publish audit of `movebank-move-461`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [event_id, individual_id, tag_id, taxon, study_name]
temporal: timestamp
checks:
  - kind: unique
    columns: [event_id, individual_id, tag_id, taxon, study_name]
  - kind: not_null
    col: event_id
  - kind: not_null
    col: individual_id
  - kind: not_null
    col: tag_id
  - kind: not_null
    col: taxon
  - kind: not_null
    col: study_name
  - kind: row_floor
    min: 1356
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
