# compiled by `hardened compile-checks` — post-publish audit of `statswales-c5f57ce9-18c3-4ac2-8164-7e472d86f0c4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [academic_year, month, entry_cohort, support_type]
temporal: month
checks:
  - kind: unique
    columns: [academic_year, month, entry_cohort, support_type]
  - kind: not_null
    col: academic_year
  - kind: not_null
    col: month
  - kind: not_null
    col: entry_cohort
  - kind: not_null
    col: support_type
  - kind: row_floor
    min: 1530
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
