# compiled by `hardened compile-checks` — post-publish audit of `statswales-3ba96615-4707-49ea-a519-cfde41b0a4d1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, patient_residence, date, country_of_general_practice]
temporal: date
checks:
  - kind: unique
    columns: [data_values, patient_residence, date, country_of_general_practice]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: patient_residence
  - kind: not_null
    col: date
  - kind: not_null
    col: country_of_general_practice
  - kind: row_floor
    min: 49403
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
