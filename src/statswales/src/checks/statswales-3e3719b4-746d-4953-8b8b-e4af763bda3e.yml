# compiled by `hardened compile-checks` — post-publish audit of `statswales-3e3719b4-746d-4953-8b8b-e4af763bda3e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, annual_change, area, component_of_population_change]
checks:
  - kind: unique
    columns: [data_values, annual_change, area, component_of_population_change]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: annual_change
  - kind: not_null
    col: area
  - kind: not_null
    col: component_of_population_change
  - kind: row_floor
    min: 3450
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
