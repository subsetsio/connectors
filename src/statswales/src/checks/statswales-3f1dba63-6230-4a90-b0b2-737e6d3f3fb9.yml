# compiled by `hardened compile-checks` — post-publish audit of `statswales-3f1dba63-6230-4a90-b0b2-737e6d3f3fb9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_description, head_of_household_used_a_foodbank, time_period]
checks:
  - kind: unique
    columns: [data_values, data_description, head_of_household_used_a_foodbank, time_period]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_description
  - kind: not_null
    col: head_of_household_used_a_foodbank
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 13
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
