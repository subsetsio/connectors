# compiled by `hardened compile-checks` — post-publish audit of `statswales-3546ba10-51a2-431a-b3d8-102e84fbf3e1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, data_description, whether_there_is_disability_within_the_family]
checks:
  - kind: unique
    columns: [data_values, time_period, data_description, whether_there_is_disability_within_the_family]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: data_description
  - kind: not_null
    col: whether_there_is_disability_within_the_family
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
