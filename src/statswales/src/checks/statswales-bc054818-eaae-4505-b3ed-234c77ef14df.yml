# compiled by `hardened compile-checks` — post-publish audit of `statswales-bc054818-eaae-4505-b3ed-234c77ef14df`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area, year, bmi_group, mothers_weight_gain, data_description]
temporal: year
checks:
  - kind: unique
    columns: [area, year, bmi_group, mothers_weight_gain, data_description]
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: bmi_group
  - kind: not_null
    col: mothers_weight_gain
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 888
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
