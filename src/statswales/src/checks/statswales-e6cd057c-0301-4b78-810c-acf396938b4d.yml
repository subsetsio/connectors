# compiled by `hardened compile-checks` — post-publish audit of `statswales-e6cd057c-0301-4b78-810c-acf396938b4d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_description, local_authority, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, data_description, local_authority, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_description
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: row_floor
    min: 7473
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
