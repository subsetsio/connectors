# compiled by `hardened compile-checks` — post-publish audit of `statswales-fa870373-3d4e-4b1d-8673-b7238453c17d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, month, area, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, month, area, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: month
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: row_floor
    min: 336
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
