# compiled by `hardened compile-checks` — post-publish audit of `statswales-c67b9e08-dacc-439a-a244-2fce79b11400`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, data_values, indicator, data_description]
checks:
  - kind: unique
    columns: [area_name, area_code, data_values, indicator, data_description]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: data_values
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 19390
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
