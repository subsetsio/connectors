# compiled by `hardened compile-checks` — post-publish audit of `statswales-6f0abb91-09f3-4a86-b1c5-037937078102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, date, activity, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, area, date, activity, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: date
  - kind: not_null
    col: activity
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 25560
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
