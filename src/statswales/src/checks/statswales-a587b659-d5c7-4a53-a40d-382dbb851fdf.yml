# compiled by `hardened compile-checks` — post-publish audit of `statswales-a587b659-d5c7-4a53-a40d-382dbb851fdf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, row_description, year, local_authority, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, row_description, year, local_authority, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: row_description
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 9361
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
