# compiled by `hardened compile-checks` — post-publish audit of `statswales-124c6534-29ee-49ba-8412-2ae0f9fa63a2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, age_group, area_name, area_code]
checks:
  - kind: unique
    columns: [data_values, age_group, area_name, area_code]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age_group
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: row_floor
    min: 56
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
