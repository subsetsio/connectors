# compiled by `hardened compile-checks` — post-publish audit of `statswales-3ca7a492-8256-4e2e-a926-3863c49d4972`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, quarter, local_health_board, age_of_baby, milk_type]
temporal: quarter
checks:
  - kind: unique
    columns: [data_values, quarter, local_health_board, age_of_baby, milk_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: quarter
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: age_of_baby
  - kind: not_null
    col: milk_type
  - kind: row_floor
    min: 2160
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
