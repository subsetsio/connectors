# compiled by `hardened compile-checks` — post-publish audit of `statswales-33696ac8-4549-4a93-9ea3-ce94db129822`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area_code, area_name, domain, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area_code, area_name, domain, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area_code
  - kind: not_null
    col: area_name
  - kind: not_null
    col: domain
  - kind: not_null
    col: year
  - kind: row_floor
    min: 41725
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
