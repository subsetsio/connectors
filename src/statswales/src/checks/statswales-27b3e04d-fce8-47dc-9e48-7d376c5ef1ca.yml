# compiled by `hardened compile-checks` — post-publish audit of `statswales-27b3e04d-fce8-47dc-9e48-7d376c5ef1ca`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, authority, row, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, authority, row, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: row
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 4992
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
