# compiled by `hardened compile-checks` — post-publish audit of `statswales-2c6c0dcb-5463-4027-b7cb-8f32fe4b0cec`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, number_of_children_in_the_household, data_description]
checks:
  - kind: unique
    columns: [data_values, time_period, number_of_children_in_the_household, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: number_of_children_in_the_household
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 96
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
