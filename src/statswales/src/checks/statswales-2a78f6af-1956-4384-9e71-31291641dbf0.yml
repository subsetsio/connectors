# compiled by `hardened compile-checks` — post-publish audit of `statswales-2a78f6af-1956-4384-9e71-31291641dbf0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, religion, deprivation_group, age_group, sex]
checks:
  - kind: unique
    columns: [data_values, religion, deprivation_group, age_group, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: religion
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
