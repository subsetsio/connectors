# compiled by `hardened compile-checks` — post-publish audit of `statswales-56bb61f4-5fea-44d1-a66b-d2d509448ede`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, region, data_description, tenure]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, region, data_description, tenure]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: region
  - kind: not_null
    col: data_description
  - kind: not_null
    col: tenure
  - kind: row_floor
    min: 225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
