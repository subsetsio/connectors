# compiled by `hardened compile-checks` — post-publish audit of `statswales-cf7e1c71-9912-40ef-866e-8fdf3b308470`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, school_sector_authority, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, school_sector_authority, year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: school_sector_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 113448
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
