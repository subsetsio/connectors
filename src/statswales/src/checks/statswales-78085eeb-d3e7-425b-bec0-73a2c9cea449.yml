# compiled by `hardened compile-checks` — post-publish audit of `statswales-78085eeb-d3e7-425b-bec0-73a2c9cea449`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, year, data_description, type_of_course]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, year, data_description, type_of_course]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: type_of_course
  - kind: row_floor
    min: 552
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
