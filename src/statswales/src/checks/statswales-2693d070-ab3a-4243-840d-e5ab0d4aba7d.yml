# compiled by `hardened compile-checks` — post-publish audit of `statswales-2693d070-ab3a-4243-840d-e5ab0d4aba7d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, area, visual_acuity]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, area, visual_acuity]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: visual_acuity
  - kind: row_floor
    min: 262
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
