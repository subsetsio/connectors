# compiled by `hardened compile-checks` — post-publish audit of `statswales-9e8df32b-2152-45cd-bb3c-52516fadebbe`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, year, local_authority, data_description]
temporal: year
checks:
  - kind: unique
    columns: [area_code, year, local_authority, data_description]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1197
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
