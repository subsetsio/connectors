# compiled by `hardened compile-checks` — post-publish audit of `statswales-e8b561fd-858c-44c7-8fcc-43b544372584`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, region, year, data_description, tenure]
temporal: year
checks:
  - kind: unique
    columns: [data_values, region, year, data_description, tenure]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: region
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: tenure
  - kind: row_floor
    min: 43
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
