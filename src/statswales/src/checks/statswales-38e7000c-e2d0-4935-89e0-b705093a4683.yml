# compiled by `hardened compile-checks` — post-publish audit of `statswales-38e7000c-e2d0-4935-89e0-b705093a4683`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, data_description, year, sex, free_school_meals]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, data_description, year, sex, free_school_meals]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: free_school_meals
  - kind: row_floor
    min: 4450
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
