# compiled by `hardened compile-checks` — post-publish audit of `statswales-3465f664-6811-42b4-a284-d80e848def7c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, resident_health_board, age_and_service, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, resident_health_board, age_and_service, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: resident_health_board
  - kind: not_null
    col: age_and_service
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 6732
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
