# compiled by `hardened compile-checks` — post-publish audit of `statswales-4c5c7577-8c65-4025-a997-f5c8c263b47a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, category]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, category]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: category
  - kind: row_floor
    min: 1012
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
