# compiled by `hardened compile-checks` — post-publish audit of `statswales-676b04a1-143a-4366-805c-4511c8671573`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year_ending, area, national_identity]
temporal: year_ending
checks:
  - kind: unique
    columns: [data_values, year_ending, area, national_identity]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year_ending
  - kind: not_null
    col: area
  - kind: not_null
    col: national_identity
  - kind: row_floor
    min: 2656
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
