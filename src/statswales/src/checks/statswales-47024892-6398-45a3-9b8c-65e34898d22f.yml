# compiled by `hardened compile-checks` — post-publish audit of `statswales-47024892-6398-45a3-9b8c-65e34898d22f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age, reason_for_attendance, data_description]
temporal: year
checks:
  - kind: unique
    columns: [year, age, reason_for_attendance, data_description]
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: reason_for_attendance
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 252
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
