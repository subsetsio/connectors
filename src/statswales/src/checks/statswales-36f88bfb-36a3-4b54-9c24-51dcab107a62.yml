# compiled by `hardened compile-checks` — post-publish audit of `statswales-36f88bfb-36a3-4b54-9c24-51dcab107a62`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, year, pollutant]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, year, pollutant]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: pollutant
  - kind: row_floor
    min: 810
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
