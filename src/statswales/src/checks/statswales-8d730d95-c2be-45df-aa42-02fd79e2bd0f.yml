# compiled by `hardened compile-checks` — post-publish audit of `statswales-8d730d95-c2be-45df-aa42-02fd79e2bd0f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, country, traffic_direction]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, country, traffic_direction]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: country
  - kind: not_null
    col: traffic_direction
  - kind: row_floor
    min: 144
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
