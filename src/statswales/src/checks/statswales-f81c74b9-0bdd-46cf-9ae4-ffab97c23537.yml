# compiled by `hardened compile-checks` — post-publish audit of `statswales-f81c74b9-0bdd-46cf-9ae4-ffab97c23537`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sexual_orientation, age_group, data_description, sex]
temporal: year
checks:
  - kind: unique
    columns: [sexual_orientation, age_group, data_description, sex]
  - kind: not_null
    col: sexual_orientation
  - kind: not_null
    col: age_group
  - kind: not_null
    col: data_description
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
