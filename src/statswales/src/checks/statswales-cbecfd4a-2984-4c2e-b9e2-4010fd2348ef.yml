# compiled by `hardened compile-checks` — post-publish audit of `statswales-cbecfd4a-2984-4c2e-b9e2-4010fd2348ef`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, year, deep_rooted_deprivation_category]
temporal: year
checks:
  - kind: unique
    columns: [area_name, area_code, year, deep_rooted_deprivation_category]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: year
  - kind: not_null
    col: deep_rooted_deprivation_category
  - kind: row_floor
    min: 5751
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
