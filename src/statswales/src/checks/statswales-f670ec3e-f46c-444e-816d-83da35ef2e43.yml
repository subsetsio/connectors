# compiled by `hardened compile-checks` — post-publish audit of `statswales-f670ec3e-f46c-444e-816d-83da35ef2e43`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, ability, local_health_board, welsh_skill]
temporal: date
checks:
  - kind: unique
    columns: [date, ability, local_health_board, welsh_skill]
  - kind: not_null
    col: date
  - kind: not_null
    col: ability
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: welsh_skill
  - kind: row_floor
    min: 1927
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
