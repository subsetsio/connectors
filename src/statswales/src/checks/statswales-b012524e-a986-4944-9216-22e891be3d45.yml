# compiled by `hardened compile-checks` — post-publish audit of `statswales-b012524e-a986-4944-9216-22e891be3d45`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [organisation, date, staff_group, data_description]
temporal: date
checks:
  - kind: unique
    columns: [organisation, date, staff_group, data_description]
  - kind: not_null
    col: organisation
  - kind: not_null
    col: date
  - kind: not_null
    col: staff_group
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1274
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
