# compiled by `hardened compile-checks` — post-publish audit of `statswales-82d9faea-b515-41cd-aedb-8f4594f66ce5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, age, year, area, sex]
temporal: year
checks:
  - kind: unique
    columns: [data_values, age, year, area, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 164560
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
