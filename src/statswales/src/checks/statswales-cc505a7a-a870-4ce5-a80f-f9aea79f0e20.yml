# compiled by `hardened compile-checks` — post-publish audit of `statswales-cc505a7a-a870-4ce5-a80f-f9aea79f0e20`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, cost_heading, year, farm_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, cost_heading, year, farm_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: cost_heading
  - kind: not_null
    col: year
  - kind: not_null
    col: farm_type
  - kind: row_floor
    min: 1008
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
