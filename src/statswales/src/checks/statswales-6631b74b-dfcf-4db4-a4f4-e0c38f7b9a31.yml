# compiled by `hardened compile-checks` — post-publish audit of `statswales-6631b74b-dfcf-4db4-a4f4-e0c38f7b9a31`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, age_group, gender_identity, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, age_group, gender_identity, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age_group
  - kind: not_null
    col: gender_identity
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
