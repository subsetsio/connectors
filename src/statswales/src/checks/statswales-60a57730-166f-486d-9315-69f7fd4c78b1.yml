# compiled by `hardened compile-checks` — post-publish audit of `statswales-60a57730-166f-486d-9315-69f7fd4c78b1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, description, transaction_type, notes]
temporal: period
checks:
  - kind: unique
    columns: [period, description, transaction_type, notes]
  - kind: not_null
    col: period
  - kind: not_null
    col: description
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: notes
  - kind: row_floor
    min: 36
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
