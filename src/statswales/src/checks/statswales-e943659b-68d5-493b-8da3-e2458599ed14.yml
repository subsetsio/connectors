# compiled by `hardened compile-checks` — post-publish audit of `statswales-e943659b-68d5-493b-8da3-e2458599ed14`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_period_quarter, provider_health_board, data_description, assessment_outcome]
checks:
  - kind: unique
    columns: [data_values, data_period_quarter, provider_health_board, data_description, assessment_outcome]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_period_quarter
  - kind: not_null
    col: provider_health_board
  - kind: not_null
    col: data_description
  - kind: not_null
    col: assessment_outcome
  - kind: row_floor
    min: 1102
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
