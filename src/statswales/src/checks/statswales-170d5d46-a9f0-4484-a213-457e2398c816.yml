# compiled by `hardened compile-checks` — post-publish audit of `statswales-170d5d46-a9f0-4484-a213-457e2398c816`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, economic_status_of_household, data_description]
checks:
  - kind: unique
    columns: [data_values, time_period, economic_status_of_household, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: economic_status_of_household
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
