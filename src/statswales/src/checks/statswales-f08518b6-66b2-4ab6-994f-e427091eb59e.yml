# compiled by `hardened compile-checks` — post-publish audit of `statswales-f08518b6-66b2-4ab6-994f-e427091eb59e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, family_type, data_description]
checks:
  - kind: unique
    columns: [data_values, time_period, family_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: family_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 96
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
