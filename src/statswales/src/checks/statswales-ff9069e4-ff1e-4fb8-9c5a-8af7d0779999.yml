# compiled by `hardened compile-checks` — post-publish audit of `statswales-ff9069e4-ff1e-4fb8-9c5a-8af7d0779999`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_period_quarter, provider_health_board, ethnicity, data_description]
checks:
  - kind: unique
    columns: [data_values, data_period_quarter, provider_health_board, ethnicity, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_period_quarter
  - kind: not_null
    col: provider_health_board
  - kind: not_null
    col: ethnicity
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 847
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
