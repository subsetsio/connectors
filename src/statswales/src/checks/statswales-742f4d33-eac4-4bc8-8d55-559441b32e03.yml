# compiled by `hardened compile-checks` — post-publish audit of `statswales-742f4d33-eac4-4bc8-8d55-559441b32e03`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, health_board, data_description, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, health_board, data_description, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: health_board
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: row_floor
    min: 24
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
