# compiled by `hardened compile-checks` — post-publish audit of `statswales-15ecbdb1-b885-4723-9dda-3c7f59675f2c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, source_of_referral, specialty, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, source_of_referral, specialty, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: source_of_referral
  - kind: not_null
    col: specialty
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 69
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
