# compiled by `hardened compile-checks` — post-publish audit of `statswales-d8947742-b61d-4598-8a27-47ae82193dd2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, local_health_board, sex, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, local_health_board, sex, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: sex
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 718
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
