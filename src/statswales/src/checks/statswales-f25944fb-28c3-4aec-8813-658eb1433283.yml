# compiled by `hardened compile-checks` — post-publish audit of `statswales-f25944fb-28c3-4aec-8813-658eb1433283`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, deprivation_group, age_group, disability, sex]
checks:
  - kind: unique
    columns: [data_values, deprivation_group, age_group, disability, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: age_group
  - kind: not_null
    col: disability
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 60
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
