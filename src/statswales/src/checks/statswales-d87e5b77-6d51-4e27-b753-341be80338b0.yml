# compiled by `hardened compile-checks` — post-publish audit of `statswales-d87e5b77-6d51-4e27-b753-341be80338b0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year_group, academic_year, data_description, fsm_eligibility]
checks:
  - kind: unique
    columns: [data_values, year_group, academic_year, data_description, fsm_eligibility]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year_group
  - kind: not_null
    col: academic_year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: fsm_eligibility
  - kind: row_floor
    min: 594
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
