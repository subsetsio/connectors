# compiled by `hardened compile-checks` — post-publish audit of `statswales-8c4e387a-d221-4a24-9fd4-50bdaacbe273`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, indicator, data_description]
checks:
  - kind: unique
    columns: [area_name, area_code, indicator, data_description]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 9695
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
