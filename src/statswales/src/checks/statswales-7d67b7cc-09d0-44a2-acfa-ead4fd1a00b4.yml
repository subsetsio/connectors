# compiled by `hardened compile-checks` — post-publish audit of `statswales-7d67b7cc-09d0-44a2-acfa-ead4fd1a00b4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, month, age, data_description, type_of_payment]
temporal: month
checks:
  - kind: unique
    columns: [data_values, month, age, data_description, type_of_payment]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: month
  - kind: not_null
    col: age
  - kind: not_null
    col: data_description
  - kind: not_null
    col: type_of_payment
  - kind: row_floor
    min: 756
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
