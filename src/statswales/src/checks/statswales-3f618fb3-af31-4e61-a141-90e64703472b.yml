# compiled by `hardened compile-checks` — post-publish audit of `statswales-3f618fb3-af31-4e61-a141-90e64703472b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, indicator_name, year, authority, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, indicator_name, year, authority, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: indicator_name
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 36570
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
