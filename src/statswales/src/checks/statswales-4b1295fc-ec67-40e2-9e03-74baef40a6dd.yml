# compiled by `hardened compile-checks` — post-publish audit of `statswales-4b1295fc-ec67-40e2-9e03-74baef40a6dd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, location, period, public_sector_land, funding]
temporal: period
checks:
  - kind: unique
    columns: [data_values, location, period, public_sector_land, funding]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: location
  - kind: not_null
    col: period
  - kind: not_null
    col: public_sector_land
  - kind: not_null
    col: funding
  - kind: row_floor
    min: 5209
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
