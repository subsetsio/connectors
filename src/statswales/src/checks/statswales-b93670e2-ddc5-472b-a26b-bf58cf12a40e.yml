# compiled by `hardened compile-checks` — post-publish audit of `statswales-b93670e2-ddc5-472b-a26b-bf58cf12a40e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, row, year, authority, financial_category]
temporal: year
checks:
  - kind: unique
    columns: [data_values, row, year, authority, financial_category]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: row
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: financial_category
  - kind: row_floor
    min: 206063
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
