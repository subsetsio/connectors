# compiled by `hardened compile-checks` — post-publish audit of `statswales-1c543574-42e8-4933-b8d2-487e773b04bd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area, outcomes, period, age, data_description]
temporal: period
checks:
  - kind: unique
    columns: [area, outcomes, period, age, data_description]
  - kind: not_null
    col: area
  - kind: not_null
    col: outcomes
  - kind: not_null
    col: period
  - kind: not_null
    col: age
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 40080
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
