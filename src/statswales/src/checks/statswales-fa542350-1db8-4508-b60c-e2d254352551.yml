# compiled by `hardened compile-checks` — post-publish audit of `statswales-fa542350-1db8-4508-b60c-e2d254352551`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, rolling_12_month_period, area, data_description]
checks:
  - kind: unique
    columns: [data_values, rolling_12_month_period, area, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: rolling_12_month_period
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 3415
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
