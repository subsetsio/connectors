# compiled by `hardened compile-checks` — post-publish audit of `statswales-8e99b9ce-8dc3-4aac-b5aa-b560f72cae08`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, type_of_employment, data_description]
checks:
  - kind: unique
    columns: [data_values, time_period, type_of_employment, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: type_of_employment
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
