# compiled by `hardened compile-checks` — post-publish audit of `statswales-017e2e6e-872f-483f-9f21-0e25750509de`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, domain, settlement_type, deprivation_group, data_description]
checks:
  - kind: unique
    columns: [data_values, domain, settlement_type, deprivation_group, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: domain
  - kind: not_null
    col: settlement_type
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 243
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
