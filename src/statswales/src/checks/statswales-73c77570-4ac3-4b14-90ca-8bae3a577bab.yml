# compiled by `hardened compile-checks` — post-publish audit of `statswales-73c77570-4ac3-4b14-90ca-8bae3a577bab`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, sex, full_time_or_part_time]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, sex, full_time_or_part_time]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: sex
  - kind: not_null
    col: full_time_or_part_time
  - kind: row_floor
    min: 96
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
