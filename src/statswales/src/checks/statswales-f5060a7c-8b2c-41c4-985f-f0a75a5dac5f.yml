# compiled by `hardened compile-checks` — post-publish audit of `statswales-f5060a7c-8b2c-41c4-985f-f0a75a5dac5f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, year, flying_start_status, data_description, immunisation_status]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, year, flying_start_status, data_description, immunisation_status]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: flying_start_status
  - kind: not_null
    col: data_description
  - kind: not_null
    col: immunisation_status
  - kind: row_floor
    min: 1138
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
