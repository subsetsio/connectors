# compiled by `hardened compile-checks` — post-publish audit of `statswales-3a8f9e22-fcf6-451e-b358-a2f6266dcfe5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, senedd_constituency_area, transaction_type, data_description]
temporal: period
checks:
  - kind: unique
    columns: [period, senedd_constituency_area, transaction_type, data_description]
  - kind: not_null
    col: period
  - kind: not_null
    col: senedd_constituency_area
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 4959
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
