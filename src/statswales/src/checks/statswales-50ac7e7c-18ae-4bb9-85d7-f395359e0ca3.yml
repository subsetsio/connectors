# compiled by `hardened compile-checks` — post-publish audit of `statswales-50ac7e7c-18ae-4bb9-85d7-f395359e0ca3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, area, number_of_vehicles_involved, severity]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, area, number_of_vehicles_involved, severity]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: area
  - kind: not_null
    col: number_of_vehicles_involved
  - kind: not_null
    col: severity
  - kind: row_floor
    min: 51975
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
