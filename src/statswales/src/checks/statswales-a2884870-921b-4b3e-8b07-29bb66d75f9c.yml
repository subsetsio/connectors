# compiled by `hardened compile-checks` — post-publish audit of `statswales-a2884870-921b-4b3e-8b07-29bb66d75f9c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, year_group]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, year_group]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year_group
  - kind: row_floor
    min: 4234
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
