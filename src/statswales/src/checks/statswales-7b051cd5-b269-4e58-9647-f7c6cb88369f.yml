# compiled by `hardened compile-checks` — post-publish audit of `statswales-7b051cd5-b269-4e58-9647-f7c6cb88369f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, characteristic_or_condition, area]
temporal: year
checks:
  - kind: unique
    columns: [data_values, characteristic_or_condition, area]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: characteristic_or_condition
  - kind: not_null
    col: area
  - kind: row_floor
    min: 69
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
