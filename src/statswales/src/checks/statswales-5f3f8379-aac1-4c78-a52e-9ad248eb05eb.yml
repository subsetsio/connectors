# compiled by `hardened compile-checks` — post-publish audit of `statswales-5f3f8379-aac1-4c78-a52e-9ad248eb05eb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, collection_year_at_31_march]
checks:
  - kind: unique
    columns: [local_authority, collection_year_at_31_march]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: collection_year_at_31_march
  - kind: row_floor
    min: 207
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
