# compiled by `hardened compile-checks` — post-publish audit of `statswales-f5d4b78e-c46d-4dbc-b3f9-5ed1c3e797d4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, year, setting_type, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, year, setting_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: setting_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 4100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
