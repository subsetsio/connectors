# compiled by `hardened compile-checks` — post-publish audit of `statswales-1bf80f79-aec0-4333-a452-b3cbace2ef2a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, service_location, appointment_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, service_location, appointment_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: service_location
  - kind: not_null
    col: appointment_type
  - kind: row_floor
    min: 35
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
