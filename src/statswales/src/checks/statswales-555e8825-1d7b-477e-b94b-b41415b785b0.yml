# compiled by `hardened compile-checks` — post-publish audit of `statswales-555e8825-1d7b-477e-b94b-b41415b785b0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, provider, area, sale_type, period]
temporal: period
checks:
  - kind: unique
    columns: [data_values, provider, area, sale_type, period]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: provider
  - kind: not_null
    col: area
  - kind: not_null
    col: sale_type
  - kind: not_null
    col: period
  - kind: row_floor
    min: 13800
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
