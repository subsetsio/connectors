# compiled by `hardened compile-checks` — post-publish audit of `statswales-acd9529c-979a-48ef-a773-870b3ebe4391`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, specialty, local_health_board_provider, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, specialty, local_health_board_provider, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: specialty
  - kind: not_null
    col: local_health_board_provider
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 108591
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
