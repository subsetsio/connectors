# compiled by `hardened compile-checks` — post-publish audit of `statswales-4224c0ad-ef58-4c6d-9400-704875c3158f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, contact, year, reason_for_non_contact]
temporal: year
checks:
  - kind: unique
    columns: [data_values, contact, year, reason_for_non_contact]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: contact
  - kind: not_null
    col: year
  - kind: not_null
    col: reason_for_non_contact
  - kind: row_floor
    min: 240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
