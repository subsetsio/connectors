# compiled by `hardened compile-checks` — post-publish audit of `statswales-b3ee1ff7-efd7-45d6-b1a3-022563ea5708`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, data_values, age_group]
checks:
  - kind: unique
    columns: [area_name, area_code, data_values, age_group]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age_group
  - kind: row_floor
    min: 1560
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
