# compiled by `hardened compile-checks` — post-publish audit of `statswales-d98174d3-8732-4dfe-9458-ded500606dbf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, authority, service, column, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, authority, service, column, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: authority
  - kind: not_null
    col: service
  - kind: not_null
    col: column
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1480
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
