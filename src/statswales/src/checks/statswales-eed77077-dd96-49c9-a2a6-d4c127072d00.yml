# compiled by `hardened compile-checks` — post-publish audit of `statswales-eed77077-dd96-49c9-a2a6-d4c127072d00`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, primary_diagnosis]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, primary_diagnosis]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: primary_diagnosis
  - kind: row_floor
    min: 35
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
