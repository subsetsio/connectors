# compiled by `hardened compile-checks` — post-publish audit of `statswales-ced15178-40a7-481b-b35f-92f9423fc6de`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, item, treatment_band, year, patient_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, item, treatment_band, year, patient_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: item
  - kind: not_null
    col: treatment_band
  - kind: not_null
    col: year
  - kind: not_null
    col: patient_type
  - kind: row_floor
    min: 1249
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
