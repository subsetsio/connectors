# compiled by `hardened compile-checks` — post-publish audit of `statswales-f6e80d0b-e7ce-4f57-a739-a2a54e48cc4f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [effective_period, refund_approved_period, notes, data_description]
checks:
  - kind: unique
    columns: [effective_period, refund_approved_period, notes, data_description]
  - kind: not_null
    col: effective_period
  - kind: not_null
    col: refund_approved_period
  - kind: not_null
    col: notes
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 652
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
