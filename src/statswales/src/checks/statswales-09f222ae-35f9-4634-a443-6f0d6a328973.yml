# compiled by `hardened compile-checks` — post-publish audit of `statswales-09f222ae-35f9-4634-a443-6f0d6a328973`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, geography, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, geography, year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: geography
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 882
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
