# compiled by `hardened compile-checks` — post-publish audit of `statswales-6133565c-36d2-4392-b8ca-d947d680fedd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, farm_type, npk_type, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, farm_type, npk_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: farm_type
  - kind: not_null
    col: npk_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 133
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
