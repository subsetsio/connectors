# compiled by `hardened compile-checks` — post-publish audit of `statswales-581f59d6-1a4f-4f24-9236-e28e5b66c611`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, local_health_board, age, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, local_health_board, age, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: age
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1149
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
