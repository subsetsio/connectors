# compiled by `hardened compile-checks` — post-publish audit of `statswales-14dc0136-262a-43a7-8383-3ab3a3f3c0c3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_description, year, additional_learning_needs_aln]
temporal: year
checks:
  - kind: unique
    columns: [data_values, data_description, year, additional_learning_needs_aln]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: additional_learning_needs_aln
  - kind: row_floor
    min: 130
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
