# compiled by `hardened compile-checks` — post-publish audit of `statswales-7a0f76ce-2f99-4249-9956-44fd44fdac28`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, year, indicator, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, year, indicator, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 552
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
