# compiled by `hardened compile-checks` — post-publish audit of `statswales-9706edd9-73ad-4902-bb12-7ccd7038626e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, domain, data_description]
checks:
  - kind: unique
    columns: [area_name, area_code, domain, data_description]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: domain
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 43231
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
