# compiled by `hardened compile-checks` — post-publish audit of `statswales-64d58bef-c97c-4413-8aed-2298eac3a419`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, geography, sex, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, geography, sex, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: geography
  - kind: not_null
    col: sex
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 13104
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
