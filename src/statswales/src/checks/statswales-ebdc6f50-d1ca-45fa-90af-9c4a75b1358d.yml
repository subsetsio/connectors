# compiled by `hardened compile-checks` — post-publish audit of `statswales-ebdc6f50-d1ca-45fa-90af-9c4a75b1358d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, food_security_status_of_household, data_description, time_period]
checks:
  - kind: unique
    columns: [data_values, food_security_status_of_household, data_description, time_period]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: food_security_status_of_household
  - kind: not_null
    col: data_description
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 22
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
