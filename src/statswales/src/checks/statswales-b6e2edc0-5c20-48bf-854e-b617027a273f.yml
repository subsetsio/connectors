# compiled by `hardened compile-checks` — post-publish audit of `statswales-b6e2edc0-5c20-48bf-854e-b617027a273f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, month, local_authority, data_description]
temporal: month
checks:
  - kind: unique
    columns: [data_values, month, local_authority, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: month
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 931
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
