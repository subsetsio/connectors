# compiled by `hardened compile-checks` — post-publish audit of `statswales-e16d5333-5165-4806-954d-9f5e9da41df7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, predominant_issues, local_authority, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, predominant_issues, local_authority, year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: predominant_issues
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 11020
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
