# compiled by `hardened compile-checks` — post-publish audit of `statswales-213aeb99-18eb-42c9-b00e-e4b716e82cdf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [authority, year, service, data_description]
temporal: year
checks:
  - kind: unique
    columns: [authority, year, service, data_description]
  - kind: not_null
    col: authority
  - kind: not_null
    col: year
  - kind: not_null
    col: service
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 7669
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
