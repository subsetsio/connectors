# compiled by `hardened compile-checks` — post-publish audit of `statswales-dc5ab135-ec49-44b3-9c70-f8148a876818`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, ability, local_health_board, staff, welsh_skill]
temporal: date
checks:
  - kind: unique
    columns: [date, ability, local_health_board, staff, welsh_skill]
  - kind: not_null
    col: date
  - kind: not_null
    col: ability
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: staff
  - kind: not_null
    col: welsh_skill
  - kind: row_floor
    min: 6428
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
