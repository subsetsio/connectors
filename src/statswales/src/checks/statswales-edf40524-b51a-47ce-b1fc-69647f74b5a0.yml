# compiled by `hardened compile-checks` — post-publish audit of `statswales-edf40524-b51a-47ce-b1fc-69647f74b5a0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, area, variant]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, area, variant]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: variant
  - kind: row_floor
    min: 1794
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
