# compiled by `hardened compile-checks` — post-publish audit of `statswales-6042a2eb-3475-4fe1-95a5-6b810e0eac7e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, location_type, financial_year, fire_and_rescue_authority_area, motive]
checks:
  - kind: unique
    columns: [data_values, location_type, financial_year, fire_and_rescue_authority_area, motive]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: location_type
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: fire_and_rescue_authority_area
  - kind: not_null
    col: motive
  - kind: row_floor
    min: 6240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
