# compiled by `hardened compile-checks` — post-publish audit of `statswales-f778750e-2492-474d-b2e4-bffd9edab8ef`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, sex, data_description, geography]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, sex, data_description, geography]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: data_description
  - kind: not_null
    col: geography
  - kind: row_floor
    min: 306
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
