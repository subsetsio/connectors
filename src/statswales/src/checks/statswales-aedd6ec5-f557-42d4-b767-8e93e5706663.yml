# compiled by `hardened compile-checks` — post-publish audit of `statswales-aedd6ec5-f557-42d4-b767-8e93e5706663`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, sector, school_language, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, sector, school_language, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: sector
  - kind: not_null
    col: school_language
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 329
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
