# compiled by `hardened compile-checks` — post-publish audit of `statswales-3c23f43e-66a3-4b2e-95b9-6daea7c48ee0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, year, characteristics_of_children, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, year, characteristics_of_children, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: characteristics_of_children
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 2089
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
