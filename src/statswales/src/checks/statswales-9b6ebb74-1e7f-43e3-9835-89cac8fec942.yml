# compiled by `hardened compile-checks` — post-publish audit of `statswales-9b6ebb74-1e7f-43e3-9835-89cac8fec942`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, period, area, data_description, dwelling_type]
temporal: period
checks:
  - kind: unique
    columns: [data_values, period, area, data_description, dwelling_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: period
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: dwelling_type
  - kind: row_floor
    min: 13593
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
