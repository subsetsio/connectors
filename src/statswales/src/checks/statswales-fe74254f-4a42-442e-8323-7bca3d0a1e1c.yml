# compiled by `hardened compile-checks` — post-publish audit of `statswales-fe74254f-4a42-442e-8323-7bca3d0a1e1c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, subject, data_description, sex, assessment_type]
temporal: year
checks:
  - kind: unique
    columns: [year, subject, data_description, sex, assessment_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: subject
  - kind: not_null
    col: data_description
  - kind: not_null
    col: sex
  - kind: not_null
    col: assessment_type
  - kind: row_floor
    min: 4002
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
