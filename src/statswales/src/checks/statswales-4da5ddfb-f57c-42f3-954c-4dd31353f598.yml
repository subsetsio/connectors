# compiled by `hardened compile-checks` — post-publish audit of `statswales-4da5ddfb-f57c-42f3-954c-4dd31353f598`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, year, data_description, flying_start_status]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, year, data_description, flying_start_status]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: flying_start_status
  - kind: row_floor
    min: 414
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
