# compiled by `hardened compile-checks` — post-publish audit of `statswales-f6f4c152-3212-4c20-b94d-38cd4211039e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, domain, area_code, area_name, deprivation_group]
checks:
  - kind: unique
    columns: [data_values, domain, area_code, area_name, deprivation_group]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: domain
  - kind: not_null
    col: area_code
  - kind: not_null
    col: area_name
  - kind: not_null
    col: deprivation_group
  - kind: row_floor
    min: 283
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
