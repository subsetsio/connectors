# compiled by `hardened compile-checks` — post-publish audit of `statswales-b52951f7-d1e0-4b04-9250-c983f69003f4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, academic_year, data_description]
checks:
  - kind: unique
    columns: [data_values, local_authority, academic_year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: academic_year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 80
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
