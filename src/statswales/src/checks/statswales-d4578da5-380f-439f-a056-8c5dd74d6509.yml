# compiled by `hardened compile-checks` — post-publish audit of `statswales-d4578da5-380f-439f-a056-8c5dd74d6509`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, organisation, staff_group]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, organisation, staff_group]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: organisation
  - kind: not_null
    col: staff_group
  - kind: row_floor
    min: 10801
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
