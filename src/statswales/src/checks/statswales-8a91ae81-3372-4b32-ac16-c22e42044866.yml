# compiled by `hardened compile-checks` — post-publish audit of `statswales-8a91ae81-3372-4b32-ac16-c22e42044866`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, sub_domain]
checks:
  - kind: unique
    columns: [area_name, area_code, sub_domain]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: sub_domain
  - kind: row_floor
    min: 7756
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
