# compiled by `hardened compile-checks` — post-publish audit of `statswales-38a0c155-8408-4681-af24-2755b4d22287`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, data_description, outcome]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, data_description, outcome]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: outcome
  - kind: row_floor
    min: 24
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
