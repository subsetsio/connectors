# compiled by `hardened compile-checks` — post-publish audit of `statswales-9c46d2fb-9716-4146-a59a-c2d0bfe86424`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, ailment, area, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, ailment, area, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: ailment
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: row_floor
    min: 720
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
