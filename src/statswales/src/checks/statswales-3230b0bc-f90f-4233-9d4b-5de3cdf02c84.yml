# compiled by `hardened compile-checks` — post-publish audit of `statswales-3230b0bc-f90f-4233-9d4b-5de3cdf02c84`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, authority, exemption_class]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, authority, exemption_class]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: exemption_class
  - kind: row_floor
    min: 7831
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
