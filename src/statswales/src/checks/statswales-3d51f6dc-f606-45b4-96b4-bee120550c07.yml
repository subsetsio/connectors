# compiled by `hardened compile-checks` — post-publish audit of `statswales-3d51f6dc-f606-45b4-96b4-bee120550c07`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, area, sex, collective_bargaining]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, area, sex, collective_bargaining]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: sex
  - kind: not_null
    col: collective_bargaining
  - kind: row_floor
    min: 2835
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
