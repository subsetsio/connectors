# compiled by `hardened compile-checks` — post-publish audit of `statswales-3347c6f5-f17c-40e0-a6b2-186b43782a64`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, year, mode_of_birth, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, year, mode_of_birth, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: mode_of_birth
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 810
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
