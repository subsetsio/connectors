# compiled by `hardened compile-checks` — post-publish audit of `statswales-76a26423-8795-4baf-bd9d-b3408f8117f5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, month, local_authority, data_description]
temporal: month
checks:
  - kind: unique
    columns: [data_values, month, local_authority, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: month
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1228
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
