# compiled by `hardened compile-checks` — post-publish audit of `statswales-db008e6e-9bd5-484e-bed7-fa2d5486dee2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, occupation, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, occupation, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: occupation
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 675
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
