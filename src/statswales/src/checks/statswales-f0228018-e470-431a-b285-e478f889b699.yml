# compiled by `hardened compile-checks` — post-publish audit of `statswales-f0228018-e470-431a-b285-e478f889b699`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, area, number_of_casualties, severity]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, area, number_of_casualties, severity]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: area
  - kind: not_null
    col: number_of_casualties
  - kind: not_null
    col: severity
  - kind: row_floor
    min: 66825
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
