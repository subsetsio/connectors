# compiled by `hardened compile-checks` — post-publish audit of `statswales-3c05f9d1-e536-4426-8270-9ef77b40778b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_description, year, additional_learning_needs_aln]
temporal: year
checks:
  - kind: unique
    columns: [data_description, year, additional_learning_needs_aln]
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: additional_learning_needs_aln
  - kind: row_floor
    min: 110
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
