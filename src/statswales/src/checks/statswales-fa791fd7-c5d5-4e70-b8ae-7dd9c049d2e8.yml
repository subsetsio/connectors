# compiled by `hardened compile-checks` — post-publish audit of `statswales-fa791fd7-c5d5-4e70-b8ae-7dd9c049d2e8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, ethnic_background, year]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, ethnic_background, year]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: ethnic_background
  - kind: not_null
    col: year
  - kind: row_floor
    min: 3705
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
