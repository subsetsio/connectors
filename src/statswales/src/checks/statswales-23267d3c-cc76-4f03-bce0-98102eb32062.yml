# compiled by `hardened compile-checks` — post-publish audit of `statswales-23267d3c-cc76-4f03-bce0-98102eb32062`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, age_of_head_of_household, data_description]
checks:
  - kind: unique
    columns: [time_period, age_of_head_of_household, data_description]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: age_of_head_of_household
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
