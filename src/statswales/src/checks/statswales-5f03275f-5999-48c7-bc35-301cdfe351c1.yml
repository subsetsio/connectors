# compiled by `hardened compile-checks` — post-publish audit of `statswales-5f03275f-5999-48c7-bc35-301cdfe351c1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, category]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, category]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: category
  - kind: row_floor
    min: 1012
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
