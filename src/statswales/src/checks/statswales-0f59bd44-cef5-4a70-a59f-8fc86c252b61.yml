# compiled by `hardened compile-checks` — post-publish audit of `statswales-0f59bd44-cef5-4a70-a59f-8fc86c252b61`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, indicator, data_description, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, indicator, data_description, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: row_floor
    min: 161
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
