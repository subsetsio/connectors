# compiled by `hardened compile-checks` — post-publish audit of `statswales-395828cb-b2e6-495a-9903-3f1f873c110f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, sickness_absence_reason, date, organisation, staff_group]
temporal: date
checks:
  - kind: unique
    columns: [data_values, sickness_absence_reason, date, organisation, staff_group]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: sickness_absence_reason
  - kind: not_null
    col: date
  - kind: not_null
    col: organisation
  - kind: not_null
    col: staff_group
  - kind: row_floor
    min: 9756
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
