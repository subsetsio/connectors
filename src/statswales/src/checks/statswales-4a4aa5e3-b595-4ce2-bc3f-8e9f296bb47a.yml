# compiled by `hardened compile-checks` — post-publish audit of `statswales-4a4aa5e3-b595-4ce2-bc3f-8e9f296bb47a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, school_type, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, school_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: school_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 3917
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
