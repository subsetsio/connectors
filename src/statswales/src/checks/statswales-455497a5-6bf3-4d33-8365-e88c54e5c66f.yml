# compiled by `hardened compile-checks` — post-publish audit of `statswales-455497a5-6bf3-4d33-8365-e88c54e5c66f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, contact, year, status]
temporal: year
checks:
  - kind: unique
    columns: [data_values, contact, year, status]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: contact
  - kind: not_null
    col: year
  - kind: not_null
    col: status
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
