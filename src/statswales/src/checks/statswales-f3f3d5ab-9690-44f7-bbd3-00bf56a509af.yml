# compiled by `hardened compile-checks` — post-publish audit of `statswales-f3f3d5ab-9690-44f7-bbd3-00bf56a509af`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, ethnic_group, mode_of_birth, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, ethnic_group, mode_of_birth, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: mode_of_birth
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 567
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
