# compiled by `hardened compile-checks` — post-publish audit of `statswales-3802a00b-ef52-43a7-9046-1f9ea5e9309c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, governance, sector]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, governance, sector]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: governance
  - kind: not_null
    col: sector
  - kind: row_floor
    min: 7887
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
