# compiled by `hardened compile-checks` — post-publish audit of `statswales-65a51e06-08de-4f50-a584-3c80644bc118`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, age_group, deprivation_group, sex, general_health]
checks:
  - kind: unique
    columns: [data_values, age_group, deprivation_group, sex, general_health]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age_group
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: sex
  - kind: not_null
    col: general_health
  - kind: row_floor
    min: 67
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
