# compiled by `hardened compile-checks` — post-publish audit of `statswales-6a20829c-3653-4f55-b0e5-72134bb2f0ee`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, domain, deprivation_group, area_code, area_name]
checks:
  - kind: unique
    columns: [data_values, domain, deprivation_group, area_code, area_name]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: domain
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: area_code
  - kind: not_null
    col: area_name
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
