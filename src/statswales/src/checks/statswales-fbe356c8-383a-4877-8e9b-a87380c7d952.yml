# compiled by `hardened compile-checks` — post-publish audit of `statswales-fbe356c8-383a-4877-8e9b-a87380c7d952`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, welsh_port, vessel]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, welsh_port, vessel]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: welsh_port
  - kind: not_null
    col: vessel
  - kind: row_floor
    min: 427
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
