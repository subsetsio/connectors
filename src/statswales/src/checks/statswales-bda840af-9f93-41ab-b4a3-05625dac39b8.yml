# compiled by `hardened compile-checks` — post-publish audit of `statswales-bda840af-9f93-41ab-b4a3-05625dac39b8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, local_authority, data_description]
checks:
  - kind: unique
    columns: [data_values, time_period, local_authority, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1822
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
