# compiled by `hardened compile-checks` — post-publish audit of `statswales-73ee24ba-c092-4509-a2a9-e719fe53a0ae`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, data_description, age_group, sex]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, data_description, age_group, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 2052
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
