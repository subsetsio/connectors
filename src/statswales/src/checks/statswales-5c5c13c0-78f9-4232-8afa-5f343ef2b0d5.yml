# compiled by `hardened compile-checks` — post-publish audit of `statswales-5c5c13c0-78f9-4232-8afa-5f343ef2b0d5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, area, accommodation_type, data_description, length_of_stay]
temporal: period
checks:
  - kind: unique
    columns: [period, area, accommodation_type, data_description, length_of_stay]
  - kind: not_null
    col: period
  - kind: not_null
    col: area
  - kind: not_null
    col: accommodation_type
  - kind: not_null
    col: data_description
  - kind: not_null
    col: length_of_stay
  - kind: row_floor
    min: 93983
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
