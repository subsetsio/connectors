# compiled by `hardened compile-checks` — post-publish audit of `statswales-062488e2-3fd6-4755-9b0b-a64547fc570f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, row, year, authority, band]
temporal: year
checks:
  - kind: unique
    columns: [data_values, row, year, authority, band]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: row
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: band
  - kind: row_floor
    min: 76181
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
