# compiled by `hardened compile-checks` — post-publish audit of `statswales-0545d51b-53da-4183-ac4b-2f3e7a2a4fc0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, local_health_board, service]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, local_health_board, service]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: service
  - kind: row_floor
    min: 5021
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
