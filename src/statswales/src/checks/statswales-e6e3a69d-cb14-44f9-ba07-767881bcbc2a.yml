# compiled by `hardened compile-checks` — post-publish audit of `statswales-e6e3a69d-cb14-44f9-ba07-767881bcbc2a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, provision_type, local_authority, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, provision_type, local_authority, year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: provision_type
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 17315
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
