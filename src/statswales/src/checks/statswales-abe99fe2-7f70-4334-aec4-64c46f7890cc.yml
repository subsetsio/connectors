# compiled by `hardened compile-checks` — post-publish audit of `statswales-abe99fe2-7f70-4334-aec4-64c46f7890cc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, year, flying_start_status, data_description]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, year, flying_start_status, data_description]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: flying_start_status
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 621
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
