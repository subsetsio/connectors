# compiled by `hardened compile-checks` — post-publish audit of `statswales-34f5ec92-1447-45be-9dab-ec73708e3e8b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hazard, area, year, data_description, dwelling]
temporal: year
checks:
  - kind: unique
    columns: [hazard, area, year, data_description, dwelling]
  - kind: not_null
    col: hazard
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: dwelling
  - kind: row_floor
    min: 55200
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
