# compiled by `hardened compile-checks` — post-publish audit of `statswales-d2120b16-0406-4242-a515-0dd7eaf79c98`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, year, local_authority, age_group, data_description]
temporal: year
checks:
  - kind: unique
    columns: [area_code, year, local_authority, age_group, data_description]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: age_group
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 7330
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
