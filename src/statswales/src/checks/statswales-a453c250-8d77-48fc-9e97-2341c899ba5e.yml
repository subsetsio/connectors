# compiled by `hardened compile-checks` — post-publish audit of `statswales-a453c250-8d77-48fc-9e97-2341c899ba5e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year_ending, area, data_description, ethnicity]
temporal: year_ending
checks:
  - kind: unique
    columns: [year_ending, area, data_description, ethnicity]
  - kind: not_null
    col: year_ending
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: ethnicity
  - kind: row_floor
    min: 3542
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
