# compiled by `hardened compile-checks` — post-publish audit of `statswales-0d92ca36-d278-4259-985c-9b5bfe434c1f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, component, year, data_description, sector]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, component, year, data_description, sector]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: component
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: sector
  - kind: row_floor
    min: 2852
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
