# compiled by `hardened compile-checks` — post-publish audit of `statswales-478cd8ba-25d2-4ad0-ad22-c1e33418c586`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, provider_health_board, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, provider_health_board, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: provider_health_board
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1388
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
