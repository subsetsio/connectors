# compiled by `hardened compile-checks` — post-publish audit of `statswales-30dfb2df-b446-4356-9cf4-0a75f410bd18`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, data_description, geography]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, data_description, geography]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: geography
  - kind: row_floor
    min: 48
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
