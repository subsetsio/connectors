# compiled by `hardened compile-checks` — post-publish audit of `statswales-2f764027-85bb-4877-845b-229d2f746cf6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, year, living_wage, sex]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, year, living_wage, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: living_wage
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1638
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
