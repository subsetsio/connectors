# compiled by `hardened compile-checks` — post-publish audit of `statswales-66fe7610-afca-4b4b-a25a-3e93f9f8466d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, need_category, year, data_description, disability]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, need_category, year, data_description, disability]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: need_category
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: disability
  - kind: row_floor
    min: 12880
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
