# compiled by `hardened compile-checks` — post-publish audit of `statswales-8573088d-58b2-4727-8a20-ed87a7a37cb2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, data_description, subject, sex, fsm]
temporal: year
checks:
  - kind: unique
    columns: [year, data_description, subject, sex, fsm]
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: subject
  - kind: not_null
    col: sex
  - kind: not_null
    col: fsm
  - kind: row_floor
    min: 4669
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
