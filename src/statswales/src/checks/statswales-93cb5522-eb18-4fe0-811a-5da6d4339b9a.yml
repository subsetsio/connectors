# compiled by `hardened compile-checks` — post-publish audit of `statswales-93cb5522-eb18-4fe0-811a-5da6d4339b9a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, welsh_skill, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, welsh_skill, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: welsh_skill
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 5865
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
