# compiled by `hardened compile-checks` — post-publish audit of `statswales-5eeb55b8-ba2b-44c6-8dca-282923028e1e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, authority, row, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, authority, row, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: row
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1148
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
