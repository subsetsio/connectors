# compiled by `hardened compile-checks` — post-publish audit of `statswales-c42bf771-54a3-4dcf-8ba8-f2f59f94a5ac`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, referral_source_destination, location]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, referral_source_destination, location]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: referral_source_destination
  - kind: not_null
    col: location
  - kind: row_floor
    min: 98
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
