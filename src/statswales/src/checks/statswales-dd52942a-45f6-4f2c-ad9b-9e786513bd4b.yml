# compiled by `hardened compile-checks` — post-publish audit of `statswales-dd52942a-45f6-4f2c-ad9b-9e786513bd4b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, school_sector_authority, year, column]
temporal: year
checks:
  - kind: unique
    columns: [data_values, school_sector_authority, year, column]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: school_sector_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: column
  - kind: row_floor
    min: 108584
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
