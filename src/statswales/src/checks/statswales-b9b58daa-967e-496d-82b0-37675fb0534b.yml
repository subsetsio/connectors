# compiled by `hardened compile-checks` — post-publish audit of `statswales-b9b58daa-967e-496d-82b0-37675fb0534b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, area, age, variant, sex]
temporal: year
checks:
  - kind: unique
    columns: [year, area, age, variant, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: age
  - kind: not_null
    col: variant
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 86112
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
