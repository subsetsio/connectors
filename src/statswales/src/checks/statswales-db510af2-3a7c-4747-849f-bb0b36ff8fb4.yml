# compiled by `hardened compile-checks` — post-publish audit of `statswales-db510af2-3a7c-4747-849f-bb0b36ff8fb4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, presenting_issues, local_authority, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, presenting_issues, local_authority, year, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: presenting_issues
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 11021
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
