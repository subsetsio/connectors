# compiled by `hardened compile-checks` — post-publish audit of `statswales-4c96bd4b-9cb1-4bff-b071-21c6f2ba158a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, data_description, fire_and_rescue_service]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, data_description, fire_and_rescue_service]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: fire_and_rescue_service
  - kind: row_floor
    min: 430
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
