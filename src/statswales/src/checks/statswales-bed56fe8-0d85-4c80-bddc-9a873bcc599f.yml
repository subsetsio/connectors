# compiled by `hardened compile-checks` — post-publish audit of `statswales-bed56fe8-0d85-4c80-bddc-9a873bcc599f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, transaction_type, transaction_description, data_description]
temporal: period
checks:
  - kind: unique
    columns: [period, transaction_type, transaction_description, data_description]
  - kind: not_null
    col: period
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: transaction_description
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 5180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
