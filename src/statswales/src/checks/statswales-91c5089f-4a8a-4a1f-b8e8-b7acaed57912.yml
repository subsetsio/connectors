# compiled by `hardened compile-checks` — post-publish audit of `statswales-91c5089f-4a8a-4a1f-b8e8-b7acaed57912`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, prescribed_item, treatment_band, year, patient_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, prescribed_item, treatment_band, year, patient_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: prescribed_item
  - kind: not_null
    col: treatment_band
  - kind: not_null
    col: year
  - kind: not_null
    col: patient_type
  - kind: row_floor
    min: 493
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
