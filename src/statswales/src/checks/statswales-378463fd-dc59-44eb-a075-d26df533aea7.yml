# compiled by `hardened compile-checks` — post-publish audit of `statswales-378463fd-dc59-44eb-a075-d26df533aea7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, data_description, year, gender]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, data_description, year, gender]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 5980
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
