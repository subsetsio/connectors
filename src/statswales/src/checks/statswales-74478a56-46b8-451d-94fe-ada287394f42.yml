# compiled by `hardened compile-checks` — post-publish audit of `statswales-74478a56-46b8-451d-94fe-ada287394f42`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [financial_year, data_description, fire_and_rescue_authority_area]
checks:
  - kind: unique
    columns: [financial_year, data_description, fire_and_rescue_authority_area]
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: fire_and_rescue_authority_area
  - kind: row_floor
    min: 192
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
