# compiled by `hardened compile-checks` — post-publish audit of `statswales-57d40353-5906-4ea1-ae29-9800be3fce06`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, local_health_board, measure]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, local_health_board, measure]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: measure
  - kind: row_floor
    min: 266
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
