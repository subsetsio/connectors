# compiled by `hardened compile-checks` — post-publish audit of `statswales-3643d017-ff94-479b-a5ef-52a807665bce`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [financial_year, data_description, cause_of_injury_or_death, fire_and_rescue_authority_area]
checks:
  - kind: unique
    columns: [financial_year, data_description, cause_of_injury_or_death, fire_and_rescue_authority_area]
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: cause_of_injury_or_death
  - kind: not_null
    col: fire_and_rescue_authority_area
  - kind: row_floor
    min: 2080
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
