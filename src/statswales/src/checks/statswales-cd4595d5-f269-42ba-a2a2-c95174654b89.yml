# compiled by `hardened compile-checks` — post-publish audit of `statswales-cd4595d5-f269-42ba-a2a2-c95174654b89`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [census_date, local_health_board, age_group, sex]
temporal: census_date
checks:
  - kind: unique
    columns: [census_date, local_health_board, age_group, sex]
  - kind: not_null
    col: census_date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1275
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
