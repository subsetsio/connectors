# compiled by `hardened compile-checks` — post-publish audit of `statswales-3091a6ef-3ad4-43f0-acf6-e1ebb89e68ec`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, deprivation_group, welsh_language_skills]
checks:
  - kind: unique
    columns: [data_values, deprivation_group, welsh_language_skills]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: welsh_language_skills
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
