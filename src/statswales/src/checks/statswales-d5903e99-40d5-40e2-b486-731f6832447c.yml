# compiled by `hardened compile-checks` — post-publish audit of `statswales-d5903e99-40d5-40e2-b486-731f6832447c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, type_of_care_and_support, age_group, residence, year]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, type_of_care_and_support, age_group, residence, year]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: type_of_care_and_support
  - kind: not_null
    col: age_group
  - kind: not_null
    col: residence
  - kind: not_null
    col: year
  - kind: row_floor
    min: 9108
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
