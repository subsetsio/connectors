# compiled by `hardened compile-checks` — post-publish audit of `statswales-51721e13-a08e-4517-8db7-c75ebbd2fd1e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, characteristic_or_condition, year, area, assessment_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, characteristic_or_condition, year, area, assessment_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: characteristic_or_condition
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: assessment_type
  - kind: row_floor
    min: 1261
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
