# compiled by `hardened compile-checks` — post-publish audit of `statswales-54e5cbcf-adf2-4484-95e7-76e03c8a33c4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, local_authority, age_group, disabled_or_non_disabled_people, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, local_authority, age_group, disabled_or_non_disabled_people, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: age_group
  - kind: not_null
    col: disabled_or_non_disabled_people
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 594
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
