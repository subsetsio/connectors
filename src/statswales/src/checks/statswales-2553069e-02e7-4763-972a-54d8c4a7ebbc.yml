# compiled by `hardened compile-checks` — post-publish audit of `statswales-2553069e-02e7-4763-972a-54d8c4a7ebbc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, farm_size, income_type, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, farm_size, income_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: farm_size
  - kind: not_null
    col: income_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 270
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
