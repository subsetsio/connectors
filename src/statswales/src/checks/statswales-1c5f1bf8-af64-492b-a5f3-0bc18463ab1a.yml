# compiled by `hardened compile-checks` — post-publish audit of `statswales-1c5f1bf8-af64-492b-a5f3-0bc18463ab1a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, subject, data_description, sex, assessment_type]
temporal: year
checks:
  - kind: unique
    columns: [year, subject, data_description, sex, assessment_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: subject
  - kind: not_null
    col: data_description
  - kind: not_null
    col: sex
  - kind: not_null
    col: assessment_type
  - kind: row_floor
    min: 5229
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
