# compiled by `hardened compile-checks` — post-publish audit of `statswales-4cb5e809-d794-44ef-9f98-0f4e44ad0647`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, month, area, data_description, type_of_payment]
temporal: month
checks:
  - kind: unique
    columns: [data_values, month, area, data_description, type_of_payment]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: month
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: type_of_payment
  - kind: row_floor
    min: 2484
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
