# compiled by `hardened compile-checks` — post-publish audit of `statswales-d4cc43d3-b96a-42d9-8dfa-1ebc6e5e3ae1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, hospital, age, sex]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, hospital, age, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: hospital
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 130773
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
