# compiled by `hardened compile-checks` — post-publish audit of `statswales-dba5d285-3177-451e-bacf-6aa2788c4282`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: ["24_months_ending", area, measure, data_description]
temporal: "24_months_ending"
checks:
  - kind: unique
    columns: ["24_months_ending", area, measure, data_description]
  - kind: not_null
    col: "24_months_ending"
  - kind: not_null
    col: area
  - kind: not_null
    col: measure
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1560
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
