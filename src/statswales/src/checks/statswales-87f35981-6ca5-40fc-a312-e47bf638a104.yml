# compiled by `hardened compile-checks` — post-publish audit of `statswales-87f35981-6ca5-40fc-a312-e47bf638a104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, transaction_value, transaction_type, data_description]
temporal: period
checks:
  - kind: unique
    columns: [period, transaction_value, transaction_type, data_description]
  - kind: not_null
    col: period
  - kind: not_null
    col: transaction_value
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 6005
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
