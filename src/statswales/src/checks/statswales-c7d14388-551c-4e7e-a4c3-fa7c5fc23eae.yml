# compiled by `hardened compile-checks` — post-publish audit of `statswales-c7d14388-551c-4e7e-a4c3-fa7c5fc23eae`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, area_name, data_values, indicator, data_description]
checks:
  - kind: unique
    columns: [area_code, area_name, data_values, indicator, data_description]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: area_name
  - kind: not_null
    col: data_values
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 17388
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
