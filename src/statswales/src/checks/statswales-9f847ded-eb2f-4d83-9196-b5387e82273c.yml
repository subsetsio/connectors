# compiled by `hardened compile-checks` — post-publish audit of `statswales-9f847ded-eb2f-4d83-9196-b5387e82273c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, data_description, local_health_board]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, data_description, local_health_board]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: data_description
  - kind: not_null
    col: local_health_board
  - kind: row_floor
    min: 14976
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
