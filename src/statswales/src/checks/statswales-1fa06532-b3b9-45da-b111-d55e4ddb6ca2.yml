# compiled by `hardened compile-checks` — post-publish audit of `statswales-1fa06532-b3b9-45da-b111-d55e4ddb6ca2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, age_group, caring_responsibilities, year]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, age_group, caring_responsibilities, year]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: age_group
  - kind: not_null
    col: caring_responsibilities
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1035
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
