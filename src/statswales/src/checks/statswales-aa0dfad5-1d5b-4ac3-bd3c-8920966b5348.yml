# compiled by `hardened compile-checks` — post-publish audit of `statswales-aa0dfad5-1d5b-4ac3-bd3c-8920966b5348`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, age, local_health_board, staff, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, age, local_health_board, staff, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: age
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: staff
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 3648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
