# compiled by `hardened compile-checks` — post-publish audit of `statswales-3a324dc0-3594-447f-9b4a-1a08bb923bf8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, geography, data_description, age, sex]
temporal: year
checks:
  - kind: unique
    columns: [year, geography, data_description, age, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: geography
  - kind: not_null
    col: data_description
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 414960
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
