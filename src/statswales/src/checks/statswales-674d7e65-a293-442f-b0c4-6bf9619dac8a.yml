# compiled by `hardened compile-checks` — post-publish audit of `statswales-674d7e65-a293-442f-b0c4-6bf9619dac8a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, data_description, year, fire_and_rescue_service_area]
temporal: year
checks:
  - kind: unique
    columns: [data_values, data_description, year, fire_and_rescue_service_area]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: fire_and_rescue_service_area
  - kind: row_floor
    min: 1134
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
