# compiled by `hardened compile-checks` — post-publish audit of `statswales-4cd3c522-4a30-479f-bd16-e85c4733dece`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, national_park_area, transaction_type, data_description]
temporal: period
checks:
  - kind: unique
    columns: [period, national_park_area, transaction_type, data_description]
  - kind: not_null
    col: period
  - kind: not_null
    col: national_park_area
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 3581
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
