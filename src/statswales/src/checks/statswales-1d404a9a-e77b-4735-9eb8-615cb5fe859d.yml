# compiled by `hardened compile-checks` — post-publish audit of `statswales-1d404a9a-e77b-4735-9eb8-615cb5fe859d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ethnic_group, data_description, gender, strand, financial_year]
checks:
  - kind: unique
    columns: [ethnic_group, data_description, gender, strand, financial_year]
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: data_description
  - kind: not_null
    col: gender
  - kind: not_null
    col: strand
  - kind: not_null
    col: financial_year
  - kind: row_floor
    min: 752
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
