# compiled by `hardened compile-checks` — post-publish audit of `statswales-25ec8faa-b3f8-404b-9bb5-a89fef88b54c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, local_authority, year, data_description, child_status]
temporal: year
checks:
  - kind: unique
    columns: [area_code, local_authority, year, data_description, child_status]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: not_null
    col: child_status
  - kind: row_floor
    min: 1610
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
