# compiled by `hardened compile-checks` — post-publish audit of `statswales-72dc2a23-1845-4c47-83fe-b4091960ac5c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, local_authority, collection_year_at_31_march, data_description, child_status]
checks:
  - kind: unique
    columns: [area_code, local_authority, collection_year_at_31_march, data_description, child_status]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: collection_year_at_31_march
  - kind: not_null
    col: data_description
  - kind: not_null
    col: child_status
  - kind: row_floor
    min: 1610
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
