# compiled by `hardened compile-checks` — post-publish audit of `statswales-a4bbe4b4-f849-4cb4-9e09-f0273cf1ed7e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, age, treatment_status]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, age, treatment_status]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: age
  - kind: not_null
    col: treatment_status
  - kind: row_floor
    min: 747
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
