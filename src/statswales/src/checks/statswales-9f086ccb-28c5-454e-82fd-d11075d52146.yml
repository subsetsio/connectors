# compiled by `hardened compile-checks` — post-publish audit of `statswales-9f086ccb-28c5-454e-82fd-d11075d52146`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_disease, area, outcome, data_description]
temporal: year
checks:
  - kind: unique
    columns: [type_of_disease, area, outcome, data_description]
  - kind: not_null
    col: type_of_disease
  - kind: not_null
    col: area
  - kind: not_null
    col: outcome
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
