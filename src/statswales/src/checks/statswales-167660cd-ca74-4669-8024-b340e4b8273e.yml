# compiled by `hardened compile-checks` — post-publish audit of `statswales-167660cd-ca74-4669-8024-b340e4b8273e`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, authority, period, data_description, planning_type]
temporal: period
checks:
  - kind: unique
    columns: [data_values, authority, period, data_description, planning_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: authority
  - kind: not_null
    col: period
  - kind: not_null
    col: data_description
  - kind: not_null
    col: planning_type
  - kind: row_floor
    min: 1002
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
