# compiled by `hardened compile-checks` — post-publish audit of `statswales-2604676e-4495-4a87-8482-d3eaf8e97a2d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, breastfeeding_status, data_description, number_of_babies]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, breastfeeding_status, data_description, number_of_babies]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: breastfeeding_status
  - kind: not_null
    col: data_description
  - kind: not_null
    col: number_of_babies
  - kind: row_floor
    min: 135
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
