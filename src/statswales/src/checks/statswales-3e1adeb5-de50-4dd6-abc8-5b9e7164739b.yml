# compiled by `hardened compile-checks` — post-publish audit of `statswales-3e1adeb5-de50-4dd6-abc8-5b9e7164739b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sector, data_description, multiplier_or_effect_type]
temporal: year
checks:
  - kind: unique
    columns: [sector, data_description, multiplier_or_effect_type]
  - kind: not_null
    col: sector
  - kind: not_null
    col: data_description
  - kind: not_null
    col: multiplier_or_effect_type
  - kind: row_floor
    min: 440
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
