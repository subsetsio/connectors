# compiled by `hardened compile-checks` — post-publish audit of `statswales-6e533581-3be9-4f8f-8fb9-3abfca467742`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, "12_months_ending", area, data_description]
temporal: "12_months_ending"
checks:
  - kind: unique
    columns: [data_values, "12_months_ending", area, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: "12_months_ending"
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 238
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
