# compiled by `hardened compile-checks` — post-publish audit of `statswales-1d289a5d-f2e5-4e51-bc34-0d86558ad332`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, age, population, sex]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, age, population, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: population
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 6120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
