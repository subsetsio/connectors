# compiled by `hardened compile-checks` — post-publish audit of `statswales-a4631afe-b48c-493a-9c38-260fb4a275c6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_description, year, area_of_learning, sex]
temporal: year
checks:
  - kind: unique
    columns: [data_description, year, area_of_learning, sex]
  - kind: not_null
    col: data_description
  - kind: not_null
    col: year
  - kind: not_null
    col: area_of_learning
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 552
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
