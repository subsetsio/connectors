# compiled by `hardened compile-checks` — post-publish audit of `statswales-d3e75d01-e044-43eb-bd1c-94231baf3de3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, quarter, contact]
temporal: quarter
checks:
  - kind: unique
    columns: [data_values, area, quarter, contact]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: quarter
  - kind: not_null
    col: contact
  - kind: row_floor
    min: 1350
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
