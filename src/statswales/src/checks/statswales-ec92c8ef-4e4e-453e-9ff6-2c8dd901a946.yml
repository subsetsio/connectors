# compiled by `hardened compile-checks` — post-publish audit of `statswales-ec92c8ef-4e4e-453e-9ff6-2c8dd901a946`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, local_health_board, sex, staff, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, local_health_board, sex, staff, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: sex
  - kind: not_null
    col: staff
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 2373
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
