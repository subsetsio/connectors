# compiled by `hardened compile-checks` — post-publish audit of `statswales-9745d051-b579-48fe-ae8c-a60b2962afee`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, employment, age, gender]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, employment, age, gender]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: employment
  - kind: not_null
    col: age
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 945
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
