# compiled by `hardened compile-checks` — post-publish audit of `statswales-35c9613c-0b8f-4861-8738-97d13d05e7df`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_name, area_code, indicator]
checks:
  - kind: unique
    columns: [area_name, area_code, indicator]
  - kind: not_null
    col: area_name
  - kind: not_null
    col: area_code
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 5796
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
