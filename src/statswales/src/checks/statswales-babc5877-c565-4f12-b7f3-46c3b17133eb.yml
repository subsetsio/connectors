# compiled by `hardened compile-checks` — post-publish audit of `statswales-babc5877-c565-4f12-b7f3-46c3b17133eb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, farm_type, income_type, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, farm_type, income_type, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: farm_type
  - kind: not_null
    col: income_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 315
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
