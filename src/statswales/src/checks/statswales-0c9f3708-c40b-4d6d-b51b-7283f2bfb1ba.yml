# compiled by `hardened compile-checks` — post-publish audit of `statswales-0c9f3708-c40b-4d6d-b51b-7283f2bfb1ba`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, year, indicator, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, year, indicator, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 280
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
