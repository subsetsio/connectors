# compiled by `hardened compile-checks` — post-publish audit of `statswales-ff85e2ff-6253-4289-9051-311846aad266`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 250
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
