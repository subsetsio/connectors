# compiled by `hardened compile-checks` — post-publish audit of `statswales-05c7242b-4e5e-42ff-acee-f44e2de5fedd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, local_authority, sex, boarding_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, local_authority, sex, boarding_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: sex
  - kind: not_null
    col: boarding_type
  - kind: row_floor
    min: 1332
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
