# compiled by `hardened compile-checks` — post-publish audit of `statswales-32003566-2d0e-44da-8c7b-4324048b4196`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, welsh_port, freight_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, welsh_port, freight_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: welsh_port
  - kind: not_null
    col: freight_type
  - kind: row_floor
    min: 203
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
