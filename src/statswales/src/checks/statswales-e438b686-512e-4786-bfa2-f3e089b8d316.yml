# compiled by `hardened compile-checks` — post-publish audit of `statswales-e438b686-512e-4786-bfa2-f3e089b8d316`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, time_period, data_description, ethnic_group_of_head_of_household]
checks:
  - kind: unique
    columns: [data_values, time_period, data_description, ethnic_group_of_head_of_household]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: time_period
  - kind: not_null
    col: data_description
  - kind: not_null
    col: ethnic_group_of_head_of_household
  - kind: row_floor
    min: 63
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
