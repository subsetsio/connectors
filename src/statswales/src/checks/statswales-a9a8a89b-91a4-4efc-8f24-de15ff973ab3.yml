# compiled by `hardened compile-checks` — post-publish audit of `statswales-a9a8a89b-91a4-4efc-8f24-de15ff973ab3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, local_health_board, data_description]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, local_health_board, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 2808
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
