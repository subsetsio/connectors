# compiled by `hardened compile-checks` — post-publish audit of `statswales-4303331f-e8c3-4f51-83f1-b439c296d9bb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, age_group, reason, gender, year]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, age_group, reason, gender, year]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: age_group
  - kind: not_null
    col: reason
  - kind: not_null
    col: gender
  - kind: not_null
    col: year
  - kind: row_floor
    min: 8280
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
