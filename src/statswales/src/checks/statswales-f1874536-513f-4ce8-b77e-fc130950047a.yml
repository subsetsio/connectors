# compiled by `hardened compile-checks` — post-publish audit of `statswales-f1874536-513f-4ce8-b77e-fc130950047a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, quarter, contact, flying_start_status]
temporal: quarter
checks:
  - kind: unique
    columns: [data_values, quarter, contact, flying_start_status]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: quarter
  - kind: not_null
    col: contact
  - kind: not_null
    col: flying_start_status
  - kind: row_floor
    min: 370
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
