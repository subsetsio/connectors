# compiled by `hardened compile-checks` — post-publish audit of `statswales-01f31768-1255-47be-8233-0f0e31c3d1c8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, local_authority_area, transaction_type, data_description]
temporal: period
checks:
  - kind: unique
    columns: [period, local_authority_area, transaction_type, data_description]
  - kind: not_null
    col: period
  - kind: not_null
    col: local_authority_area
  - kind: not_null
    col: transaction_type
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 8337
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
