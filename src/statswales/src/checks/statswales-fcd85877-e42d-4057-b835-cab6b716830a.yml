# compiled by `hardened compile-checks` — post-publish audit of `statswales-fcd85877-e42d-4057-b835-cab6b716830a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, local_authority, sector, type]
temporal: year
checks:
  - kind: unique
    columns: [year, local_authority, sector, type]
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: sector
  - kind: not_null
    col: type
  - kind: row_floor
    min: 2089
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
