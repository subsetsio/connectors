# compiled by `hardened compile-checks` — post-publish audit of `statswales-e9a68c90-13d8-4a6c-a9bb-a37b3b89795c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, local_authority, category, sex]
temporal: year
checks:
  - kind: unique
    columns: [year, local_authority, category, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: category
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 2958
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
