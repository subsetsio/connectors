# compiled by `hardened compile-checks` — post-publish audit of `statswales-596ed3cf-32b4-4b3f-8b1d-5d9a0b8336e2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, ethnic_group, deprivation_group, sex]
checks:
  - kind: unique
    columns: [data_values, ethnic_group, deprivation_group, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
