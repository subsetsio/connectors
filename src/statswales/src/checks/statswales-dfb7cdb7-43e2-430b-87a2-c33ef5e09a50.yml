# compiled by `hardened compile-checks` — post-publish audit of `statswales-dfb7cdb7-43e2-430b-87a2-c33ef5e09a50`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, area, data_description, before_or_after_housing_costs]
checks:
  - kind: unique
    columns: [time_period, area, data_description, before_or_after_housing_costs]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: before_or_after_housing_costs
  - kind: row_floor
    min: 1624
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
