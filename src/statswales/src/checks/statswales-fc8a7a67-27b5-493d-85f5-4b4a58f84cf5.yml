# compiled by `hardened compile-checks` — post-publish audit of `statswales-fc8a7a67-27b5-493d-85f5-4b4a58f84cf5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, gender, data_description, asylum_seeker]
temporal: year
checks:
  - kind: unique
    columns: [year, gender, data_description, asylum_seeker]
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: data_description
  - kind: not_null
    col: asylum_seeker
  - kind: row_floor
    min: 280
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
