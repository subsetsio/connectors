# compiled by `hardened compile-checks` — post-publish audit of `statswales-d6f06177-edcf-43dd-920f-c227f9543bfb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, period, area, data_description]
temporal: period
checks:
  - kind: unique
    columns: [data_values, period, area, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: period
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 653
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
