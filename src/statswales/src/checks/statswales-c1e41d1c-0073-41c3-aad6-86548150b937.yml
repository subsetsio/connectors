# compiled by `hardened compile-checks` — post-publish audit of `statswales-c1e41d1c-0073-41c3-aad6-86548150b937`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [food_security_status_of_household, data_description, time_period]
checks:
  - kind: unique
    columns: [food_security_status_of_household, data_description, time_period]
  - kind: not_null
    col: food_security_status_of_household
  - kind: not_null
    col: data_description
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 22
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
