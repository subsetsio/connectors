# compiled by `hardened compile-checks` — post-publish audit of `statswales-6fe55e52-ff8f-4d49-b577-797065e661c9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, tumour_site, local_health_board, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, tumour_site, local_health_board, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: tumour_site
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 24225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
