# compiled by `hardened compile-checks` — post-publish audit of `statswales-3d0a39dc-1580-48f4-8392-9677ac46c6ed`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, organisation, sex, legal_status]
temporal: year
checks:
  - kind: unique
    columns: [year, organisation, sex, legal_status]
  - kind: not_null
    col: year
  - kind: not_null
    col: organisation
  - kind: not_null
    col: sex
  - kind: not_null
    col: legal_status
  - kind: row_floor
    min: 742
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
