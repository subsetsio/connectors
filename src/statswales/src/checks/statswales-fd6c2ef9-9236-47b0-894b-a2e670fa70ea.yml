# compiled by `hardened compile-checks` — post-publish audit of `statswales-fd6c2ef9-9236-47b0-894b-a2e670fa70ea`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, ethnic_group, year, age_of_baby, breastfeeding_status]
temporal: year
checks:
  - kind: unique
    columns: [data_values, ethnic_group, year, age_of_baby, breastfeeding_status]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: year
  - kind: not_null
    col: age_of_baby
  - kind: not_null
    col: breastfeeding_status
  - kind: row_floor
    min: 1225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
