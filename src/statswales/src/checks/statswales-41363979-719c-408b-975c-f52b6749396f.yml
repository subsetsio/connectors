# compiled by `hardened compile-checks` — post-publish audit of `statswales-41363979-719c-408b-975c-f52b6749396f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, age_group, deprivation_group, sex]
checks:
  - kind: unique
    columns: [data_values, age_group, deprivation_group, sex]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: age_group
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 50
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
