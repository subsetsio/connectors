# compiled by `hardened compile-checks` — post-publish audit of `statswales-c769f107-ed7c-4d25-a0c4-7dd29bc317b6`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, apgar_score_at_5_minutes, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, apgar_score_at_5_minutes, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: apgar_score_at_5_minutes
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 98
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
