# compiled by `hardened compile-checks` — post-publish audit of `statswales-f5df167d-d979-4647-87ac-5d1ba5c9f437`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, area, item]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, area, item]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: item
  - kind: row_floor
    min: 225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
