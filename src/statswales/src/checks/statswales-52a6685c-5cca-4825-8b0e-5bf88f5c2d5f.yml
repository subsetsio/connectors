# compiled by `hardened compile-checks` — post-publish audit of `statswales-52a6685c-5cca-4825-8b0e-5bf88f5c2d5f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, indicator, period, data_description]
temporal: period
checks:
  - kind: unique
    columns: [data_values, area, indicator, period, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: indicator
  - kind: not_null
    col: period
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 345
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
