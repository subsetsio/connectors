# compiled by `hardened compile-checks` — post-publish audit of `statswales-99c2bf7c-1f09-4260-9e48-b096019a7beb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, area, data_description, appointment_type, outcome]
temporal: year
checks:
  - kind: unique
    columns: [data_values, area, data_description, appointment_type, outcome]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area
  - kind: not_null
    col: data_description
  - kind: not_null
    col: appointment_type
  - kind: not_null
    col: outcome
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
