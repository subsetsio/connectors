# compiled by `hardened compile-checks` — post-publish audit of `statswales-f40980fd-05bc-4e8a-9576-21021be38913`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, date, local_health_board_provider, service]
temporal: date
checks:
  - kind: unique
    columns: [data_values, date, local_health_board_provider, service]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: date
  - kind: not_null
    col: local_health_board_provider
  - kind: not_null
    col: service
  - kind: row_floor
    min: 5946
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
