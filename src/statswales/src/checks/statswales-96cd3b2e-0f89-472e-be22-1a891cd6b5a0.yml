# compiled by `hardened compile-checks` — post-publish audit of `statswales-96cd3b2e-0f89-472e-be22-1a891cd6b5a0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, characteristics, outcome, data_description]
temporal: date
checks:
  - kind: unique
    columns: [date, characteristics, outcome, data_description]
  - kind: not_null
    col: date
  - kind: not_null
    col: characteristics
  - kind: not_null
    col: outcome
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 590
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
