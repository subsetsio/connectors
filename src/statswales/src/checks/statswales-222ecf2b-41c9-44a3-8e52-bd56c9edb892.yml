# compiled by `hardened compile-checks` — post-publish audit of `statswales-222ecf2b-41c9-44a3-8e52-bd56c9edb892`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, geography, sex, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, geography, sex, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: geography
  - kind: not_null
    col: sex
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 2940
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
