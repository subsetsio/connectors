# compiled by `hardened compile-checks` — post-publish audit of `statswales-017be573-d9be-4699-8847-b7d87480a11d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_description, area_code, local_authority, year]
temporal: year
checks:
  - kind: unique
    columns: [data_description, area_code, local_authority, year]
  - kind: not_null
    col: data_description
  - kind: not_null
    col: area_code
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: year
  - kind: row_floor
    min: 3680
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
