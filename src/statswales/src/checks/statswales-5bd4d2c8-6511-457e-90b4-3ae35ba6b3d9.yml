# compiled by `hardened compile-checks` — post-publish audit of `statswales-5bd4d2c8-6511-457e-90b4-3ae35ba6b3d9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, geography, data_description, sex, duration_of_unemployment]
temporal: year
checks:
  - kind: unique
    columns: [year, geography, data_description, sex, duration_of_unemployment]
  - kind: not_null
    col: year
  - kind: not_null
    col: geography
  - kind: not_null
    col: data_description
  - kind: not_null
    col: sex
  - kind: not_null
    col: duration_of_unemployment
  - kind: row_floor
    min: 24784
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
