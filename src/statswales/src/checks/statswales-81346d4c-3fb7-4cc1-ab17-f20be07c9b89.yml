# compiled by `hardened compile-checks` — post-publish audit of `statswales-81346d4c-3fb7-4cc1-ab17-f20be07c9b89`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [local_authority, type_of_licence, year]
temporal: year
checks:
  - kind: unique
    columns: [local_authority, type_of_licence, year]
  - kind: not_null
    col: local_authority
  - kind: not_null
    col: type_of_licence
  - kind: not_null
    col: year
  - kind: row_floor
    min: 2049
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
