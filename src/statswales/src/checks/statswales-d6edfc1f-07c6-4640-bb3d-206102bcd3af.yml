# compiled by `hardened compile-checks` — post-publish audit of `statswales-d6edfc1f-07c6-4640-bb3d-206102bcd3af`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, treatment_band, other_clinical_activity, year, patient_type]
temporal: year
checks:
  - kind: unique
    columns: [data_values, treatment_band, other_clinical_activity, year, patient_type]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: treatment_band
  - kind: not_null
    col: other_clinical_activity
  - kind: not_null
    col: year
  - kind: not_null
    col: patient_type
  - kind: row_floor
    min: 304
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
