# compiled by `hardened compile-checks` — post-publish audit of `statswales-972b22c8-1847-4145-aa42-dbb135a066c0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_code, data_values, area_name, age_group]
checks:
  - kind: unique
    columns: [area_code, data_values, area_name, age_group]
  - kind: not_null
    col: area_code
  - kind: not_null
    col: data_values
  - kind: not_null
    col: area_name
  - kind: not_null
    col: age_group
  - kind: row_floor
    min: 328
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
