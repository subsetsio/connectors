# compiled by `hardened compile-checks` — post-publish audit of `statswales-6480bc9d-4285-4287-a01d-5ac397898451`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, quarter, local_health_board, contact]
temporal: quarter
checks:
  - kind: unique
    columns: [data_values, quarter, local_health_board, contact]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: quarter
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: contact
  - kind: row_floor
    min: 1332
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
