# compiled by `hardened compile-checks` — post-publish audit of `statswales-c7f7fa3f-fbf3-41bc-998b-15e15e670751`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, geography, year, sex, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, geography, year, sex, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: geography
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 1764
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
