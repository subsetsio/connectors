# compiled by `hardened compile-checks` — post-publish audit of `statswales-0b5dfc5e-98b4-4def-811e-d15afe87b101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, material, data_description, area, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, material, data_description, area, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: material
  - kind: not_null
    col: data_description
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: row_floor
    min: 239648
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
