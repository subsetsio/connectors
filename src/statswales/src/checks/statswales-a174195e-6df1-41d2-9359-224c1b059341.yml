# compiled by `hardened compile-checks` — post-publish audit of `statswales-a174195e-6df1-41d2-9359-224c1b059341`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area, period, data_description, length_of_stay]
temporal: period
checks:
  - kind: unique
    columns: [area, period, data_description, length_of_stay]
  - kind: not_null
    col: area
  - kind: not_null
    col: period
  - kind: not_null
    col: data_description
  - kind: not_null
    col: length_of_stay
  - kind: row_floor
    min: 7695
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
