# compiled by `hardened compile-checks` — post-publish audit of `statswales-8e0d656a-50ea-4509-8363-c52d8315b750`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_description, authority, year, data_description]
temporal: year
checks:
  - kind: unique
    columns: [row_description, authority, year, data_description]
  - kind: not_null
    col: row_description
  - kind: not_null
    col: authority
  - kind: not_null
    col: year
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 3185
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
