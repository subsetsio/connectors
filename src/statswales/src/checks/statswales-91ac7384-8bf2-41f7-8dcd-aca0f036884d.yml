# compiled by `hardened compile-checks` — post-publish audit of `statswales-91ac7384-8bf2-41f7-8dcd-aca0f036884d`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, farm_type, asset_type, year]
temporal: year
checks:
  - kind: unique
    columns: [data_values, farm_type, asset_type, year]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: farm_type
  - kind: not_null
    col: asset_type
  - kind: not_null
    col: year
  - kind: row_floor
    min: 180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
