# compiled by `hardened compile-checks` — post-publish audit of `statswales-763d4890-8984-49fe-a6fc-1ac46ca0f889`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, quarter, local_health_board, milk_type, age_of_baby]
temporal: quarter
checks:
  - kind: unique
    columns: [data_values, quarter, local_health_board, milk_type, age_of_baby]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: quarter
  - kind: not_null
    col: local_health_board
  - kind: not_null
    col: milk_type
  - kind: not_null
    col: age_of_baby
  - kind: row_floor
    min: 5760
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
