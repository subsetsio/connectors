# compiled by `hardened compile-checks` — post-publish audit of `statswales-aa88473f-4e25-4771-b7e4-0243212558ac`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, deprivation_group, uk_armed_forces_veterans]
checks:
  - kind: unique
    columns: [data_values, deprivation_group, uk_armed_forces_veterans]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: deprivation_group
  - kind: not_null
    col: uk_armed_forces_veterans
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
