# compiled by `hardened compile-checks` — post-publish audit of `statswales-e3aa4de2-5084-4f57-afeb-f21d17fa9bd5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data_values, year, authority, type_of_support, data_description]
temporal: year
checks:
  - kind: unique
    columns: [data_values, year, authority, type_of_support, data_description]
  - kind: not_null
    col: data_values
  - kind: not_null
    col: year
  - kind: not_null
    col: authority
  - kind: not_null
    col: type_of_support
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 4168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
