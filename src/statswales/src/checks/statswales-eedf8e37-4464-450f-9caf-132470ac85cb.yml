# compiled by `hardened compile-checks` — post-publish audit of `statswales-eedf8e37-4464-450f-9caf-132470ac85cb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area, year, size_band, variable, data_description]
temporal: year
checks:
  - kind: unique
    columns: [area, year, size_band, variable, data_description]
  - kind: not_null
    col: area
  - kind: not_null
    col: year
  - kind: not_null
    col: size_band
  - kind: not_null
    col: variable
  - kind: not_null
    col: data_description
  - kind: row_floor
    min: 32016
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
