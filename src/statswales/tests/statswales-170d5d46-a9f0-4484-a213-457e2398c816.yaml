spec_id: statswales-170d5d46-a9f0-4484-a213-457e2398c816
status: active
tests:
  - row_count: {min: 1}
    reason: every published StatsWales dataset returns at least one long-format data row; an empty CSV means the download failed or the dataset was withdrawn
    certainty: 90
  - column_type: {col: Data description, type: string}
    reason: the standard StatsWales long-format export always carries a textual measure label in the "Data description" column
    certainty: 80
    severity: warn
