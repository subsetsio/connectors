# compiled by `hardened compile-checks` — post-publish audit of `cms-a174-a962`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [npi, provider_last_name, org_pac_id, provider_first_name, final_mips_score]
checks:
  - kind: unique
    columns: [npi, provider_last_name, org_pac_id, provider_first_name, final_mips_score]
  - kind: not_null
    col: npi
  - kind: not_null
    col: provider_last_name
  - kind: not_null
    col: org_pac_id
  - kind: not_null
    col: provider_first_name
  - kind: not_null
    col: final_mips_score
  - kind: row_floor
    min: 270667
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
