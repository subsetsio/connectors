# compiled by `hardened compile-checks` — post-publish audit of `cms-27ea-46a8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ind_pac_id, npi, provider_last_name, provider_first_name, facility_affiliations_certification_number]
checks:
  - kind: unique
    columns: [ind_pac_id, npi, provider_last_name, provider_first_name, facility_affiliations_certification_number]
  - kind: not_null
    col: ind_pac_id
  - kind: not_null
    col: npi
  - kind: not_null
    col: provider_last_name
  - kind: not_null
    col: provider_first_name
  - kind: not_null
    col: facility_affiliations_certification_number
  - kind: row_floor
    min: 1130096
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
