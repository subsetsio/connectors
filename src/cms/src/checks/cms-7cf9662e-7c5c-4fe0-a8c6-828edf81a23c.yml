# compiled by `hardened compile-checks` — post-publish audit of `cms-7cf9662e-7c5c-4fe0-a8c6-828edf81a23c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hosp_id, adm_disc, interval_higher_limit, interval_lower_limit, rate]
temporal: start_date
checks:
  - kind: unique
    columns: [hosp_id, adm_disc, interval_higher_limit, interval_lower_limit, rate]
  - kind: not_null
    col: hosp_id
  - kind: not_null
    col: adm_disc
  - kind: not_null
    col: interval_higher_limit
  - kind: not_null
    col: interval_lower_limit
  - kind: not_null
    col: rate
  - kind: row_floor
    min: 1659
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
