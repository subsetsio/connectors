# compiled by `hardened compile-checks` — post-publish audit of `cms-d3eb38ac-d8e9-40d3-b7b7-6205d3d1dc16`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [phne_num, fac_name, prvdr_num, st_adr, fax_phne_num]
checks:
  - kind: unique
    columns: [phne_num, fac_name, prvdr_num, st_adr, fax_phne_num]
  - kind: not_null
    col: phne_num
  - kind: not_null
    col: fac_name
  - kind: not_null
    col: prvdr_num
  - kind: not_null
    col: st_adr
  - kind: not_null
    col: fax_phne_num
  - kind: row_floor
    min: 338025
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
