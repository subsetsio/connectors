# compiled by `hardened compile-checks` — post-publish audit of `cms-95rg-2usp`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [zip_code, cms_certification_number_ccn, state]
checks:
  - kind: unique
    columns: [zip_code, cms_certification_number_ccn, state]
  - kind: not_null
    col: zip_code
  - kind: not_null
    col: cms_certification_number_ccn
  - kind: not_null
    col: state
  - kind: row_floor
    min: 174879
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
