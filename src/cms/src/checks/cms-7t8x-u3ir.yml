# compiled by `hardened compile-checks` — post-publish audit of `cms-7t8x-u3ir`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [cms_certification_number_ccn, address_line_1, city_town, certification_date, county_parish]
temporal: certification_date
checks:
  - kind: unique
    columns: [cms_certification_number_ccn, address_line_1, city_town, certification_date, county_parish]
  - kind: not_null
    col: cms_certification_number_ccn
  - kind: not_null
    col: address_line_1
  - kind: not_null
    col: city_town
  - kind: not_null
    col: certification_date
  - kind: not_null
    col: county_parish
  - kind: row_floor
    min: 611
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
