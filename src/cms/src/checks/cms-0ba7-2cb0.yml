# compiled by `hardened compile-checks` — post-publish audit of `cms-0ba7-2cb0`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [facility_name, org_pac_id, aco_id_1, aco_nm_1, measure_cd]
checks:
  - kind: unique
    columns: [facility_name, org_pac_id, aco_id_1, aco_nm_1, measure_cd]
  - kind: not_null
    col: facility_name
  - kind: not_null
    col: org_pac_id
  - kind: not_null
    col: aco_id_1
  - kind: not_null
    col: aco_nm_1
  - kind: not_null
    col: measure_cd
  - kind: row_floor
    min: 99614
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
