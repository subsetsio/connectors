# compiled by `hardened compile-checks` — post-publish audit of `cms-cc3bd6db-2ae9-4d86-95be-20267f159f73`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [end_date, description, url, unique_id, title]
temporal: start_date
checks:
  - kind: unique
    columns: [end_date, description, url, unique_id, title]
  - kind: not_null
    col: end_date
  - kind: not_null
    col: description
  - kind: not_null
    col: url
  - kind: not_null
    col: unique_id
  - kind: not_null
    col: title
  - kind: row_floor
    min: 144
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
