# compiled by `hardened compile-checks` — post-publish audit of `cms-2935c3fe-b18a-4e39-a0c5-e70573664f19`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tot_mdcr_stdzd_pymt_amt, prvdr_id, tot_chrg_amt, prvdr_zip, tot_alowd_amt]
checks:
  - kind: unique
    columns: [tot_mdcr_stdzd_pymt_amt, prvdr_id, tot_chrg_amt, prvdr_zip, tot_alowd_amt]
  - kind: not_null
    col: tot_mdcr_stdzd_pymt_amt
  - kind: not_null
    col: prvdr_id
  - kind: not_null
    col: tot_chrg_amt
  - kind: not_null
    col: prvdr_zip
  - kind: not_null
    col: tot_alowd_amt
  - kind: row_floor
    min: 186
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
