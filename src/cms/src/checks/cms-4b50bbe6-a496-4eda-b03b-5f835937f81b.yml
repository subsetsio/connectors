# compiled by `hardened compile-checks` — post-publish audit of `cms-4b50bbe6-a496-4eda-b03b-5f835937f81b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [total_residents, percent, mds_item_question_description, mds_item_response, geographical_location]
checks:
  - kind: unique
    columns: [total_residents, percent, mds_item_question_description, mds_item_response, geographical_location]
  - kind: not_null
    col: total_residents
  - kind: not_null
    col: percent
  - kind: not_null
    col: mds_item_question_description
  - kind: not_null
    col: mds_item_response
  - kind: not_null
    col: geographical_location
  - kind: row_floor
    min: 66960
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
