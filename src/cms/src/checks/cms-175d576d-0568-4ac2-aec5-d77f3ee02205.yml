# compiled by `hardened compile-checks` — post-publish audit of `cms-175d576d-0568-4ac2-aec5-d77f3ee02205`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [last_name, first_name, organization_legal_name, state, asm_cohort]
checks:
  - kind: unique
    columns: [last_name, first_name, organization_legal_name, state, asm_cohort]
  - kind: not_null
    col: last_name
  - kind: not_null
    col: first_name
  - kind: not_null
    col: organization_legal_name
  - kind: not_null
    col: state
  - kind: not_null
    col: asm_cohort
  - kind: row_floor
    min: 3318
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
