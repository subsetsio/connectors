# compiled by `hardened compile-checks` — post-publish audit of `cms-113eb0bc-0c9a-4d91-9f93-3f6b28c0bf6b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [provider_taxonomy_code, provider_taxonomy_description_type_classification_specialization, medicare_provider_supplier_type_description, medicare_specialty_code]
checks:
  - kind: unique
    columns: [provider_taxonomy_code, provider_taxonomy_description_type_classification_specialization, medicare_provider_supplier_type_description, medicare_specialty_code]
  - kind: not_null
    col: provider_taxonomy_code
  - kind: not_null
    col: provider_taxonomy_description_type_classification_specialization
  - kind: not_null
    col: medicare_provider_supplier_type_description
  - kind: not_null
    col: medicare_specialty_code
  - kind: row_floor
    min: 282
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
