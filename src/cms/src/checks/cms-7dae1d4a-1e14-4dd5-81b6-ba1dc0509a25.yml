# compiled by `hardened compile-checks` — post-publish audit of `cms-7dae1d4a-1e14-4dd5-81b6-ba1dc0509a25`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [total_fee_for_service_medicare_dollars_paid_through_apm_category]
checks:
  - kind: unique
    columns: [total_fee_for_service_medicare_dollars_paid_through_apm_category]
  - kind: not_null
    col: total_fee_for_service_medicare_dollars_paid_through_apm_category
  - kind: row_floor
    min: 6
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
