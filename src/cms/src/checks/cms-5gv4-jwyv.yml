# compiled by `hardened compile-checks` — post-publish audit of `cms-5gv4-jwyv`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [net_change_in_base_operating_drg_payment_amount, number_of_hospitals_receiving_this_range]
checks:
  - kind: unique
    columns: [net_change_in_base_operating_drg_payment_amount, number_of_hospitals_receiving_this_range]
  - kind: not_null
    col: net_change_in_base_operating_drg_payment_amount
  - kind: not_null
    col: number_of_hospitals_receiving_this_range
  - kind: row_floor
    min: 16
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
