# compiled by `hardened compile-checks` — post-publish audit of `cms-pudb-wetr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [mort_30_copd_baseline_rate, mort_30_hf_performance_rate, address, facility_id, mort_30_pn_baseline_rate]
checks:
  - kind: unique
    columns: [mort_30_copd_baseline_rate, mort_30_hf_performance_rate, address, facility_id, mort_30_pn_baseline_rate]
  - kind: not_null
    col: mort_30_copd_baseline_rate
  - kind: not_null
    col: mort_30_hf_performance_rate
  - kind: not_null
    col: address
  - kind: not_null
    col: facility_id
  - kind: not_null
    col: mort_30_pn_baseline_rate
  - kind: row_floor
    min: 1227
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
