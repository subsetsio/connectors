# compiled by `hardened compile-checks` — post-publish audit of `cms-4c2a8bf6-8560-4b00-bc56-1a0322677b7f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indvdl_ot_mnts_stay, prvdr_id, indvdl_pt_mnts_stay, tot_slp_mnts_stay, tot_pt_mnts_stay]
checks:
  - kind: unique
    columns: [indvdl_ot_mnts_stay, prvdr_id, indvdl_pt_mnts_stay, tot_slp_mnts_stay, tot_pt_mnts_stay]
  - kind: not_null
    col: indvdl_ot_mnts_stay
  - kind: not_null
    col: prvdr_id
  - kind: not_null
    col: indvdl_pt_mnts_stay
  - kind: not_null
    col: tot_slp_mnts_stay
  - kind: not_null
    col: tot_pt_mnts_stay
  - kind: row_floor
    min: 7107
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
