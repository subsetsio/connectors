# compiled by `hardened compile-checks` — post-publish audit of `cms-43ef03ce-2b60-40a8-958e-146195b5fec7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tot_outlier_pymt_amt, tot_mdcr_stdzd_pymt_amt, tot_alowd_amt, tot_mdcr_pymt_amt, tot_pt_mnts]
checks:
  - kind: unique
    columns: [tot_outlier_pymt_amt, tot_mdcr_stdzd_pymt_amt, tot_alowd_amt, tot_mdcr_pymt_amt, tot_pt_mnts]
  - kind: not_null
    col: tot_outlier_pymt_amt
  - kind: not_null
    col: tot_mdcr_stdzd_pymt_amt
  - kind: not_null
    col: tot_alowd_amt
  - kind: not_null
    col: tot_mdcr_pymt_amt
  - kind: not_null
    col: tot_pt_mnts
  - kind: row_floor
    min: 4259
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
