spec_id: cms-84jm-wiui
status: active
coverage:
  frozen: true
  reason: >-
    CMS publishes this accepted catalog table as a point-in-time or irregularly
    refreshed open-data extract, and the catalog metadata does not provide a
    stable per-dataset freshness cadence for this temporal field.
tests:
  - row_count: {min: 1}
    reason: >-
      collect dropped the empty/header-only and ZIP-only CMS datasets, so every
      pulled dataset should stream at least one data row; 0 rows means a
      truncated or failed bulk-CSV download, or a rotated/empty endpoint.
    certainty: 90
    points_outward: false
    severity: block
