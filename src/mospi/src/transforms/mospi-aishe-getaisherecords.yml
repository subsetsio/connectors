# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mospi-aishe-getaisherecords`. Regenerate after model-verify.
# caution: `year` is a fiscal academic year (e.g. '2020-21').
# caution: Long-format indicator table: a dimension column is NULL when the row is not disaggregated along that dimension (the row is a total over it). Never aggregate `value` across rows without pinning every dimension column — totals and their components coexist.
key: []
columns:
  - name: indicator
    type: VARCHAR
  - name: year
    type: VARCHAR
  - name: state
    type: VARCHAR
  - name: value
    type: VARCHAR
  - name: gender
    type: VARCHAR
  - name: education_level
    type: VARCHAR
  - name: social_category
    type: VARCHAR
  - name: social_group
    type: VARCHAR
  - name: learning_mode
    type: VARCHAR
  - name: institution_type
    type: VARCHAR
  - name: faculty_type
    type: VARCHAR
