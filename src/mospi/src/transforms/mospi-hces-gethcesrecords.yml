# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mospi-hces-gethcesrecords`. Regenerate after model-verify.
# caution: `imputation_type` distinguishes two parallel estimates of the same observation — pin it before comparing across states or years.
# caution: Long-format indicator table: a dimension column is NULL when the row is not disaggregated along that dimension (the row is a total over it). Never aggregate `value` across rows without pinning every dimension column — totals and their components coexist.
key: []
columns:
  - name: year
    type: VARCHAR
  - name: state
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: imputation_type
    type: VARCHAR
  - name: value
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: mpce_fractile_classes
    type: VARCHAR
  - name: item_category
    type: VARCHAR
  - name: cereal
    type: JSON
  - name: employment_of_households
    type: JSON
  - name: social_group
    type: JSON
