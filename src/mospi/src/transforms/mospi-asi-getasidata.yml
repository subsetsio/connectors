# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mospi-asi-getasidata`. Regenerate after model-verify.
# caution: `state` and `nic_code` both carry all-India / all-industry aggregate members alongside the individual states and NIC classes — filter before summing.
# caution: `year` is a fiscal-year label (e.g. '2019-20'), not a calendar year.
# caution: `unit` varies by `indicator`; values are only comparable within one indicator.
key: []
columns:
  - name: nic_classification
    type: BIGINT
  - name: year
    type: VARCHAR
  - name: state
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: nic_code
    type: VARCHAR
  - name: nic_description
    type: VARCHAR
  - name: nic_type
    type: VARCHAR
  - name: value
    type: BIGINT
  - name: unit
    type: VARCHAR
