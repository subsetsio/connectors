# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mospi-nss78-getnss78records`. Regenerate after model-verify.
# caution: Long-format indicator table: a dimension column is NULL when the row is not disaggregated along that dimension (the row is a total over it). Never aggregate `value` across rows without pinning every dimension column — totals and their components coexist.
key: []
columns:
  - name: state
    type: VARCHAR
  - name: age_group
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: gender
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: sub_indicator
    type: VARCHAR
  - name: internet_access
    type: VARCHAR
  - name: household
    type: VARCHAR
  - name: source_of_finance
    type: VARCHAR
  - name: main_reason_for_migration
    type: VARCHAR
  - name: value
    type: VARCHAR
