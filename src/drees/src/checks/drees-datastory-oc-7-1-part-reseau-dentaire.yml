# compiled by `hardened compile-checks` — post-publish audit of `drees-datastory-oc-7-1-part-reseau-dentaire`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [familles_d_organismes, type_de_contrat, annee]
checks:
  - kind: unique
    columns: [familles_d_organismes, type_de_contrat, annee]
  - kind: not_null
    col: familles_d_organismes
  - kind: not_null
    col: type_de_contrat
  - kind: not_null
    col: annee
  - kind: row_floor
    min: 18
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
