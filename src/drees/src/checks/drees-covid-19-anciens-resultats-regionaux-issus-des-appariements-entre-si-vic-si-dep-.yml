# compiled by `hardened compile-checks` — post-publish audit of `drees-covid-19-anciens-resultats-regionaux-issus-des-appariements-entre-si-vic-si-dep-`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, nb_pcr_sympt, region, vac_statut]
temporal: date
checks:
  - kind: unique
    columns: [date, nb_pcr_sympt, region, vac_statut]
  - kind: not_null
    col: date
  - kind: not_null
    col: nb_pcr_sympt
  - kind: not_null
    col: region
  - kind: not_null
    col: vac_statut
  - kind: row_floor
    min: 4284
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
