# compiled by `hardened compile-checks` — post-publish audit of `drees-repartition-des-souscripteurs-par-age-selon-le-type-de-contrat-copie`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_de_contrats, annee, age_des_souscripteurs]
checks:
  - kind: unique
    columns: [type_de_contrats, annee, age_des_souscripteurs]
  - kind: not_null
    col: type_de_contrats
  - kind: not_null
    col: annee
  - kind: not_null
    col: age_des_souscripteurs
  - kind: row_floor
    min: 13
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
