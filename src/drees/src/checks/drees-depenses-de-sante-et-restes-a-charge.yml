# compiled by `hardened compile-checks` — post-publish audit of `drees-depenses-de-sante-et-restes-a-charge`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [annee, variable, poste, classe_d_age, sexe, ald, c2s, echantillon]
checks:
  - kind: unique
    columns: [annee, variable, poste, classe_d_age, sexe, ald, c2s, echantillon]
  - kind: not_null
    col: annee
  - kind: not_null
    col: variable
  - kind: not_null
    col: poste
  - kind: not_null
    col: classe_d_age
  - kind: not_null
    col: sexe
  - kind: not_null
    col: ald
  - kind: not_null
    col: c2s
  - kind: not_null
    col: echantillon
  - kind: row_floor
    min: 617341
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
