# compiled by `hardened compile-checks` — post-publish audit of `drees-graph1-er-rsa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [montants_mensuels_de_rsa_par_unite_de_consommation_en_euros]
checks:
  - kind: unique
    columns: [montants_mensuels_de_rsa_par_unite_de_consommation_en_euros]
  - kind: not_null
    col: montants_mensuels_de_rsa_par_unite_de_consommation_en_euros
  - kind: row_floor
    min: 3
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
