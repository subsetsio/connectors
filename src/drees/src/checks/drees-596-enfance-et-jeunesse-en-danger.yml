# compiled by `hardened compile-checks` — post-publish audit of `drees-596-enfance-et-jeunesse-en-danger`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [annee, code_reg, code_dep, id_indicateur]
checks:
  - kind: unique
    columns: [annee, code_reg, code_dep, id_indicateur]
  - kind: not_null
    col: annee
  - kind: not_null
    col: code_reg
  - kind: not_null
    col: code_dep
  - kind: not_null
    col: id_indicateur
  - kind: row_floor
    min: 17286
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
