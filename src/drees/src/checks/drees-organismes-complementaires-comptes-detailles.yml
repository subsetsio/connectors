# compiled by `hardened compile-checks` — post-publish audit of `drees-organismes-complementaires-comptes-detailles`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [annee, type_d_organismes_complementaires, type_de_contrats, code_poste]
temporal: annee
checks:
  - kind: unique
    columns: [annee, type_d_organismes_complementaires, type_de_contrats, code_poste]
  - kind: not_null
    col: annee
  - kind: not_null
    col: type_d_organismes_complementaires
  - kind: not_null
    col: type_de_contrats
  - kind: not_null
    col: code_poste
  - kind: row_floor
    min: 924
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
