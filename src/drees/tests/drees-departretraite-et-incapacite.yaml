spec_id: drees-departretraite-et-incapacite
status: active
tests:
  - row_count: {min: 36}
    reason: full ODS parquet export held 72 rows when authored; a much smaller count means the export was truncated or the fetch failed.
    certainty: 90
  - not_null: "sexe"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "sexe", type: string}
    certainty: 90
  - not_null: "niveau_d_incapacite_personnes"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "niveau_d_incapacite_personnes", type: string}
    certainty: 90
  - column_type: {col: "age_conjoncturel_de_depart_a_la_retraite", type: float}
    reason: numeric measure column; type drift here means the export schema changed.
    certainty: 80
coverage:
  frozen: true
  reason: Study snapshot (through 2020); a fixed publication.
