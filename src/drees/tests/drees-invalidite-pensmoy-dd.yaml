spec_id: drees-invalidite-pensmoy-dd
status: active
tests:
  - row_count: {min: 151}
    reason: full ODS parquet export held 303 rows when authored; a much smaller count means the export was truncated or the fetch failed.
    certainty: 90
  - not_null: "sexe"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "sexe", type: string}
    certainty: 90
  - not_null: "regime"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "regime", type: string}
    certainty: 90
  - column_type: {col: "pension_d_invalidite_de_droits_directs_moyenne", type: float}
    reason: numeric measure column; type drift here means the export schema changed.
    certainty: 80
