spec_id: drees-drees-projections-infirmieres-2024
status: active
tests:
  - row_count: {min: 1620}
    reason: full ODS parquet export held 3240 rows when authored; a much smaller count means the export was truncated or the fetch failed.
    certainty: 90
  - not_null: "annee"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "annee", type: string}
    certainty: 90
  - not_null: "scenario"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "scenario", type: string}
    certainty: 90
  - column_type: {col: "nombre_d_infirmieres_en_emploi", type: integer}
    reason: numeric measure column; type drift here means the export schema changed.
    certainty: 80
