spec_id: drees-fichier-maternites-112021
status: active
tests:
  - row_count: {min: 2944}
    reason: full ODS parquet export held 5889 rows when authored; a much smaller count means the export was truncated or the fetch failed.
    certainty: 90
  - not_null: "annee"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "annee", type: string}
    certainty: 90
  - not_null: "nom_mat"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "nom_mat", type: string}
    certainty: 90
  - column_type: {col: "acctot", type: integer}
    reason: numeric measure column; type drift here means the export schema changed.
    certainty: 80
