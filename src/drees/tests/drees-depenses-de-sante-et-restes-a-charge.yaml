spec_id: drees-depenses-de-sante-et-restes-a-charge
status: active
tests:
  - row_count: {min: 617341}
    reason: full ODS parquet export held 1234682 rows when authored; a much smaller count means the export was truncated or the fetch failed.
    certainty: 90
  - not_null: "annee"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "annee", type: string}
    certainty: 90
  - not_null: "variable"
    reason: dimension column, observed fully populated across the export.
    certainty: 80
  - column_type: {col: "variable", type: string}
    certainty: 90
  - column_type: {col: "valeur", type: integer}
    reason: numeric measure column; type drift here means the export schema changed.
    certainty: 80
