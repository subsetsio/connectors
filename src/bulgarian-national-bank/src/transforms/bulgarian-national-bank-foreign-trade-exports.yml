# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bulgarian-national-bank-foreign-trade-exports`. Regenerate after model-verify.
# caution: Series keys encode SDMX dimensions; filter to the intended export breakdown before comparing or aggregating observations.
key: [series_key, series_name, period, page_id, freq]
temporal: period
sort: [period, series_key, series_name, page_id, freq]
columns:
  - name: keyfamily
    type: VARCHAR
  - name: freq
    type: VARCHAR
  - name: page_id
    type: BIGINT
  - name: series_key
    type: VARCHAR
  - name: series_name
    type: VARCHAR
  - name: period
    type: VARCHAR
  - name: value
    type: DOUBLE
