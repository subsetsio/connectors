# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `journey-north-sightings`. Regenerate after model-verify.
# caution: The table combines many Journey North map layers and event types; filter or group by `map_slug` before interpreting counts as a specific species, life stage, or seasonal event.
key: [sighting_id, image_url, date, duration, map_slug]
temporal: date
sort: [date, sighting_id, image_url, duration, map_slug]
columns:
  - name: sighting_id
    type: BIGINT
    description: Journey North sighting record identifier.
  - name: map_slug
    type: VARCHAR
    description: Journey North map layer the sighting was fetched from.
  - name: year
    type: BIGINT
    description: Calendar year used for the source query.
  - name: season
    type: VARCHAR
    description: Season label supplied by the source feature collection.
  - name: date
    type: DATE
    description: Observation or report date.
  - name: observed_unix
    type: BIGINT
    description: Source Unix epoch timestamp for the observation.
  - name: longitude
    type: DOUBLE
    description: Observation longitude in decimal degrees.
  - name: latitude
    type: DOUBLE
    description: Observation latitude in decimal degrees.
  - name: elevation
    type: DOUBLE
    description: Elevation from the source coordinate tuple, when present.
  - name: interval
    type: BIGINT
    description: Source interval bucket for map display.
  - name: pin_id
    type: BIGINT
    description: Source map pin identifier.
  - name: duration
    type: VARCHAR
    description: Source-reported duration value, when present.
  - name: image_url
    type: VARCHAR
    description: URL of the submitted image associated with the sighting, when present.
