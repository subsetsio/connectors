# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `kff-share-of-premiums-paid-out-as-claims-on-the-individual-market`. Regenerate after model-verify.
# caution: Each row is one cell from a KFF State Health Facts indicator table; do not sum across metrics or locations without first selecting the desired metric/timeframe and excluding aggregate locations when appropriate.
key: [location, timeframe, col_index, metric]
temporal: timeframe
sort: [timeframe, location, col_index, metric]
columns:
  - name: location
    type: VARCHAR
  - name: timeframe
    type: VARCHAR
  - name: col_index
    type: INTEGER
  - name: metric
    type: BIGINT
  - name: value_raw
    type: DOUBLE
  - name: value
    type: DOUBLE
