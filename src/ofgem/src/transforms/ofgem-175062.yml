# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ofgem-175062`. Regenerate after model-verify.
key: [category, series]
temporal: category
sort: [category, series]
columns:
  - name: category
    type: VARCHAR
  - name: series
    type: VARCHAR
  - name: value
    type: DOUBLE
