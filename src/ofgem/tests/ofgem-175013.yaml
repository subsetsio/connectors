spec_id: ofgem-175013
status: active
coverage:
  frozen: true
  reason: Domestic RHI tariff payments chart ends in 2022; the underlying scheme is closed to new applicants and this historical chart does not advance.
tests:
  - not_null: category
  - not_null: series
  - column_type: {col: category, type: string}
  - column_type: {col: series, type: string}
  - column_type: {col: value, type: float}
  - row_count: {min: 1}
    reason: each Ofgem data-portal chart's everviz data.csv yields at least one (category, series) data row.
    certainty: 85
