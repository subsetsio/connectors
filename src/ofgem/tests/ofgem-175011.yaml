spec_id: ofgem-175011
status: active
tests:
  - not_null: category
  - not_null: series
  - column_type: {col: category, type: string}
  - column_type: {col: series, type: string}
  - column_type: {col: value, type: float}
  - freshness: {col: category, reaches: "2024"}
    reason: temporal Ofgem chart labels should include observations reaching at least 2024; a lower maximum suggests the chart stopped advancing or parsing regressed.
    certainty: 70
  - row_count: {min: 1}
    reason: each Ofgem data-portal chart's everviz data.csv yields at least one (category, series) data row.
    certainty: 85
