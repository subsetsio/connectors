# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `eu-klems-variable-dictionary`. Regenerate after model-verify.
# caution: Variable codes are scoped by the File column; join on both the module/file context and the variable code where available.
key: [file, variable]
sort: [file, variable]
columns:
  - name: file
    type: VARCHAR
  - name: sort_cat
    type: INTEGER
  - name: category
    type: VARCHAR
  - name: sort_id
    type: INTEGER
  - name: variable
    type: VARCHAR
  - name: tool
    type: VARCHAR
  - name: excel
    type: VARCHAR
