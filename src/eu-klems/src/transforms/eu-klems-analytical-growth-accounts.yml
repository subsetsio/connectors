# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `eu-klems-analytical-growth-accounts`. Regenerate after model-verify.
# caution: The country column includes multi-country aggregates such as EU and euro-area totals alongside individual countries; filter it before summing across economies.
# caution: Rows combine multiple industries and measure variables; aggregate only after filtering or grouping by the relevant industry and variable dimensions.
key: [country, year, sort_id, indnr, nace_r2, var]
temporal: year
sort: [year, country, sort_id, indnr, nace_r2, var]
columns:
  - name: country
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: sort_id
    type: BIGINT
  - name: indnr
    type: VARCHAR
  - name: nace_r2
    type: VARCHAR
  - name: var
    type: VARCHAR
  - name: value
    type: DOUBLE
