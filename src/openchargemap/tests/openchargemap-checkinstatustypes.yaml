spec_id: openchargemap-checkinstatustypes
status: active
tests:
  - not_null: id
    reason: CheckinStatusTypes entries are keyed by integer ID in referencedata.json
    certainty: 99
  - unique: id
    reason: check-in status ID is the primary key of this lookup table
    certainty: 98
  - not_null: title
    certainty: 95
  - row_count: {min: 10, max: 50}
    reason: referencedata.json contained 18 check-in status types when researched
    certainty: 90
  - column_type: {col: is_positive, type: boolean}
    certainty: 90
