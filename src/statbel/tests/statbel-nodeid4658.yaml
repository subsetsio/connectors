spec_id: statbel-nodeid4658
status: deferred
coverage:
  frozen: true
  reason: Statbel catalog node is a fixed published extract or historical/reference release; freshness is tracked by catalog re-observation rather than a rolling raw-file SLA.
