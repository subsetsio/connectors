# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `crates-io-version-downloads-daily`. Regenerate after model-verify.
# caution: The source dump contains a rolling recent window, so this table reflects that recent window.
key: [version_id, date]
temporal: date
sort: [date, version_id]
columns:
  - name: version_id
    type: BIGINT
  - name: date
    type: DATE
  - name: downloads
    type: BIGINT
