# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `crates-io-dependencies`. Regenerate after model-verify.
# caution: The `kind` column is an integer dependency type code from crates.io rather than a decoded label.
key: [id]
sort: [id]
columns:
  - name: id
    type: BIGINT
  - name: version_id
    type: BIGINT
  - name: crate_id
    type: BIGINT
  - name: req
    type: VARCHAR
  - name: kind
    type: BIGINT
  - name: optional
    type: VARCHAR
  - name: default_features
    type: VARCHAR
