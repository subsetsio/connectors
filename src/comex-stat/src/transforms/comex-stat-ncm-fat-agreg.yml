# temporary pass-through; replace via compile-transforms after model reconcile
columns:
  - name: CO_FAT_AGREG
    type: VARCHAR
  - name: NO_FAT_AGREG
    type: VARCHAR
  - name: NO_FAT_AGREG_GP
    type: VARCHAR
