key: [factor_aggregate_code]
sort: [factor_aggregate_code]
columns:
  - name: factor_aggregate_code
    type: VARCHAR
    description: "Factor-aggregate class code."
  - name: name_pt
    type: VARCHAR
    description: "Factor-aggregate class name in Portuguese (basic, semi-manufactured, manufactured, special operations)."
  - name: group_name_pt
    type: VARCHAR
    description: "Name of the coarser factor-aggregate group this class rolls up to, in Portuguese."
