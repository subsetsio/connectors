# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `central-bank-taiwan-eg3wm01`. Regenerate after model-verify.
# caution: Each row is one observation for a source-defined series and period; series values represent distinct measures or units, so filter or group by series before aggregating value.
key: [series, date, period]
temporal: date
sort: [date, series, period]
columns:
  - name: period
    type: VARCHAR
  - name: date
    type: DATE
  - name: series
    type: VARCHAR
  - name: value
    type: DOUBLE
