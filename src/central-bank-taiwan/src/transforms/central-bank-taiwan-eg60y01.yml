# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `central-bank-taiwan-eg60y01`. Regenerate after model-verify.
# caution: Each row is one observation for a source-defined series and period; series values represent distinct measures or units, so filter or group by series before aggregating value.
key: [period, date, series]
temporal: date
sort: [date, period, series]
columns:
  - name: period
    type: BIGINT
  - name: date
    type: DATE
  - name: series
    type: VARCHAR
  - name: value
    type: DOUBLE
