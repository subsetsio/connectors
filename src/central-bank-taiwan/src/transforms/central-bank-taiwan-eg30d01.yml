# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `central-bank-taiwan-eg30d01`. Regenerate after model-verify.
# caution: Each row is one observation for a source-defined series and period; series values represent distinct measures or units, so filter or group by series before aggregating value.
key: [date, period, series]
temporal: period
sort: [period, date, series]
columns:
  - name: period
    type: DATE
  - name: date
    type: DATE
  - name: series
    type: VARCHAR
  - name: value
    type: DOUBLE
