# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-belgium-df-finacc2010`. Regenerate after model-verify.
# caution: Each table is one NBB.Stat SDMX dataflow; dimensions and attributes are source-specific codes, so filter the relevant dimensions before aggregating observations.
key: [dataflow, freq, account, sector, instrument, balance, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
sort: [dataflow, freq, account, sector, instrument, balance, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
columns:
  - name: dataflow
    type: VARCHAR
  - name: freq
    type: VARCHAR
  - name: account
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: instrument
    type: VARCHAR
  - name: balance
    type: VARCHAR
  - name: time_period
    type: VARCHAR
  - name: obs_value
    type: VARCHAR
  - name: obs_status
    type: VARCHAR
  - name: conf_status
    type: VARCHAR
  - name: unit_mult
    type: BIGINT
  - name: decimals
    type: BIGINT
