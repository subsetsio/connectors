# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-belgium-df-employ-diss`. Regenerate after model-verify.
# caution: Each table is one NBB.Stat SDMX dataflow; dimensions and attributes are source-specific codes, so filter the relevant dimensions before aggregating observations.
key: [dataflow, freq, indicator, branch, concept, adjustment, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
sort: [dataflow, freq, indicator, branch, concept, adjustment, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
columns:
  - name: dataflow
    type: VARCHAR
  - name: freq
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: branch
    type: VARCHAR
  - name: concept
    type: VARCHAR
  - name: adjustment
    type: VARCHAR
  - name: time_period
    type: VARCHAR
  - name: obs_value
    type: DOUBLE
  - name: obs_status
    type: VARCHAR
  - name: conf_status
    type: VARCHAR
  - name: unit_mult
    type: BIGINT
  - name: decimals
    type: BIGINT
