# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-belgium-df-irescb`. Regenerate after model-verify.
# caution: Each table is one NBB.Stat SDMX dataflow; dimensions and attributes are source-specific codes, so filter the relevant dimensions before aggregating observations.
key: [dataflow, freq, irescb_rate, time_period, obs_status, decimals, obs_value]
temporal: time_period
sort: [time_period, dataflow, freq, irescb_rate, obs_status, decimals, obs_value]
columns:
  - name: dataflow
    type: VARCHAR
  - name: freq
    type: VARCHAR
  - name: irescb_rate
    type: VARCHAR
  - name: time_period
    type: DATE
  - name: obs_value
    type: VARCHAR
  - name: obs_status
    type: VARCHAR
  - name: decimals
    type: BIGINT
