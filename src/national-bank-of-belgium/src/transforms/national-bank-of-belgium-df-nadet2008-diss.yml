# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `national-bank-of-belgium-df-nadet2008-diss`. Regenerate after model-verify.
# caution: Each table is one NBB.Stat SDMX dataflow; dimensions and attributes are source-specific codes, so filter the relevant dimensions before aggregating observations.
key: [dataflow, freq, indicator, branch, sector, prices, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
sort: [dataflow, freq, indicator, branch, sector, prices, time_period, obs_status, conf_status, unit_mult, decimals, obs_value]
columns:
  - name: dataflow
    type: VARCHAR
  - name: freq
    type: VARCHAR
  - name: indicator
    type: VARCHAR
  - name: branch
    type: VARCHAR
  - name: sector
    type: VARCHAR
  - name: prices
    type: VARCHAR
  - name: time_period
    type: BIGINT
  - name: obs_value
    type: VARCHAR
  - name: obs_status
    type: VARCHAR
  - name: conf_status
    type: VARCHAR
  - name: unit_mult
    type: VARCHAR
  - name: decimals
    type: BIGINT
