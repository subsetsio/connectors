# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-belgium-df-sutap38-diss`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_value, industry, product, time_period, type]
checks:
  - kind: unique
    columns: [obs_value, industry, product, time_period, type]
  - kind: not_null
    col: obs_value
  - kind: not_null
    col: industry
  - kind: not_null
    col: product
  - kind: not_null
    col: time_period
  - kind: not_null
    col: type
  - kind: row_floor
    min: 28222
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
