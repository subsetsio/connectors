# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-belgium-df-iroloyld`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, obs_value, iroloyld_area, obs_status]
temporal: time_period
checks:
  - kind: unique
    columns: [time_period, obs_value, iroloyld_area, obs_status]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: obs_value
  - kind: not_null
    col: iroloyld_area
  - kind: not_null
    col: obs_status
  - kind: row_floor
    min: 37412
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
