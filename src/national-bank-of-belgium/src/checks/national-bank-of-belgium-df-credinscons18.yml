# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-belgium-df-credinscons18`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_value, credinscons18_item, time_period, credinscons18_table, decimals]
checks:
  - kind: unique
    columns: [obs_value, credinscons18_item, time_period, credinscons18_table, decimals]
  - kind: not_null
    col: obs_value
  - kind: not_null
    col: credinscons18_item
  - kind: not_null
    col: time_period
  - kind: not_null
    col: credinscons18_table
  - kind: not_null
    col: decimals
  - kind: row_floor
    min: 4759
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
