# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-belgium-df-agreed-wages`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_period, worker_category, index_base, index_type, wages_sector]
checks:
  - kind: unique
    columns: [time_period, worker_category, index_base, index_type, wages_sector]
  - kind: not_null
    col: time_period
  - kind: not_null
    col: worker_category
  - kind: not_null
    col: index_base
  - kind: not_null
    col: index_type
  - kind: not_null
    col: wages_sector
  - kind: row_floor
    min: 149
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
