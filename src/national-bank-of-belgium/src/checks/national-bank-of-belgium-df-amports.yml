# compiled by `hardened compile-checks` — post-publish audit of `national-bank-of-belgium-df-amports`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_value, amports_sector, time_period, amports_port, amports_variable]
checks:
  - kind: unique
    columns: [obs_value, amports_sector, time_period, amports_port, amports_variable]
  - kind: not_null
    col: obs_value
  - kind: not_null
    col: amports_sector
  - kind: not_null
    col: time_period
  - kind: not_null
    col: amports_port
  - kind: not_null
    col: amports_variable
  - kind: row_floor
    min: 2541
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
