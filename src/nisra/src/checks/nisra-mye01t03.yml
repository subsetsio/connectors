# compiled by `hardened compile-checks` — post-publish audit of `nisra-mye01t03`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tlist_a1, year, broadage4, broad_age_band_4_cat, sex]
checks:
  - kind: unique
    columns: [tlist_a1, year, broadage4, broad_age_band_4_cat, sex]
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: year
  - kind: not_null
    col: broadage4
  - kind: not_null
    col: broad_age_band_4_cat
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 405
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
