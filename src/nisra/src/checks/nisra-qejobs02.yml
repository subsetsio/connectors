# compiled by `hardened compile-checks` — post-publish audit of `nisra-qejobs02`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [statistic, statistic_label, tlist_q1, quarter, headline, industry_headline, genwp, gender_and_working_pattern, unit]
checks:
  - kind: unique
    columns: [statistic, statistic_label, tlist_q1, quarter, headline, industry_headline, genwp, gender_and_working_pattern, unit]
  - kind: not_null
    col: statistic
  - kind: not_null
    col: statistic_label
  - kind: not_null
    col: tlist_q1
  - kind: not_null
    col: quarter
  - kind: not_null
    col: headline
  - kind: not_null
    col: industry_headline
  - kind: not_null
    col: genwp
  - kind: not_null
    col: gender_and_working_pattern
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 1662
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
