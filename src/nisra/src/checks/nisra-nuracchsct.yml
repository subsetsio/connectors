# compiled by `hardened compile-checks` — post-publish audit of `nisra-nuracchsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [statistic, statistic_label, tlist_a1, year, hsct, health_and_social_care_trust, ccnursacc, nursing_accommodation_type, unit]
checks:
  - kind: unique
    columns: [statistic, statistic_label, tlist_a1, year, hsct, health_and_social_care_trust, ccnursacc, nursing_accommodation_type, unit]
  - kind: not_null
    col: statistic
  - kind: not_null
    col: statistic_label
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: year
  - kind: not_null
    col: hsct
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: ccnursacc
  - kind: not_null
    col: nursing_accommodation_type
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
