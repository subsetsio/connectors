# compiled by `hardened compile-checks` — post-publish audit of `nisra-sslgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lgd2014, local_government_district, tlist_a1, financial_year, arrested]
checks:
  - kind: unique
    columns: [lgd2014, local_government_district, tlist_a1, financial_year, arrested]
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: arrested
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
