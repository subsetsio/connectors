# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21005hsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, ethnic_group, ethnic_group_intermediate, health_and_social_care_trust, hsct]
checks:
  - kind: unique
    columns: [value, ethnic_group, ethnic_group_intermediate, health_and_social_care_trust, hsct]
  - kind: not_null
    col: value
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: ethnic_group_intermediate
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: hsct
  - kind: row_floor
    min: 32
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
