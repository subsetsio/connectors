# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21023ni`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_band_agg8, age_8_cats, health_condition_blind, blindness_or_partial_sight_loss]
checks:
  - kind: unique
    columns: [value, age_band_agg8, age_8_cats, health_condition_blind, blindness_or_partial_sight_loss]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_band_agg8
  - kind: not_null
    col: age_8_cats
  - kind: not_null
    col: health_condition_blind
  - kind: not_null
    col: blindness_or_partial_sight_loss
  - kind: row_floor
    min: 16
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
