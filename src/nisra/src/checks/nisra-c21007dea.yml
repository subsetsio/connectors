# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21007dea`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, district_electoral_area, dea2014, general_health, general_health_2]
checks:
  - kind: unique
    columns: [value, district_electoral_area, dea2014, general_health, general_health_2]
  - kind: not_null
    col: value
  - kind: not_null
    col: district_electoral_area
  - kind: not_null
    col: dea2014
  - kind: not_null
    col: general_health
  - kind: not_null
    col: general_health_2
  - kind: row_floor
    min: 200
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
