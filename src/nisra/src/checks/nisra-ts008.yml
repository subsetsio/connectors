# compiled by `hardened compile-checks` — post-publish audit of `nisra-ts008`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district, passport_held_agg5, passports_held]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district, passport_held_agg5, passports_held]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: passport_held_agg5
  - kind: not_null
    col: passports_held
  - kind: row_floor
    min: 55
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
