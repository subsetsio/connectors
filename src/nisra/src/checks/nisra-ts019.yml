# compiled by `hardened compile-checks` — post-publish audit of `nisra-ts019`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district, health_condition_num_tc5, number_of_long_term_health_conditions]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district, health_condition_num_tc5, number_of_long_term_health_conditions]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: health_condition_num_tc5
  - kind: not_null
    col: number_of_long_term_health_conditions
  - kind: row_floor
    min: 66
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
