# compiled by `hardened compile-checks` — post-publish audit of `nisra-cwt62tumour`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [statistic, statistic_label, tlist_m1, month, tumoursite, site_of_tumour, unit]
checks:
  - kind: unique
    columns: [statistic, statistic_label, tlist_m1, month, tumoursite, site_of_tumour, unit]
  - kind: not_null
    col: statistic
  - kind: not_null
    col: statistic_label
  - kind: not_null
    col: tlist_m1
  - kind: not_null
    col: month
  - kind: not_null
    col: tumoursite
  - kind: not_null
    col: site_of_tumour
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 3529
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
