# compiled by `hardened compile-checks` — post-publish audit of `nisra-fpsgdsdtlcg`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tlist_a1, financial_year, local_commissioning_group, lcg, statistic]
checks:
  - kind: unique
    columns: [tlist_a1, financial_year, local_commissioning_group, lcg, statistic]
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: local_commissioning_group
  - kind: not_null
    col: lcg
  - kind: not_null
    col: statistic
  - kind: row_floor
    min: 156
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
