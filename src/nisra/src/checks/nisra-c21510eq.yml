# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21510eq`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, eqgrp, equality_group, unpaid_care_agg4, provision_of_unpaid_care]
checks:
  - kind: unique
    columns: [value, eqgrp, equality_group, unpaid_care_agg4, provision_of_unpaid_care]
  - kind: not_null
    col: value
  - kind: not_null
    col: eqgrp
  - kind: not_null
    col: equality_group
  - kind: not_null
    col: unpaid_care_agg4
  - kind: not_null
    col: provision_of_unpaid_care
  - kind: row_floor
    min: 190
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
