# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks605nilgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [broadindustrygroup, broad_industry_group, lgd2014, local_government_district, sex]
checks:
  - kind: unique
    columns: [broadindustrygroup, broad_industry_group, lgd2014, local_government_district, sex]
  - kind: not_null
    col: broadindustrygroup
  - kind: not_null
    col: broad_industry_group
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 342
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
