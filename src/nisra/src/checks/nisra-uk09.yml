# compiled by `hardened compile-checks` — post-publish audit of `nisra-uk09`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_band_5yr_85, age_20_cats, ctry24cd, country]
checks:
  - kind: unique
    columns: [value, age_band_5yr_85, age_20_cats, ctry24cd, country]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_band_5yr_85
  - kind: not_null
    col: age_20_cats
  - kind: not_null
    col: ctry24cd
  - kind: not_null
    col: country
  - kind: row_floor
    min: 420
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
