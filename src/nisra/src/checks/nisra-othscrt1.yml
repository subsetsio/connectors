# compiled by `hardened compile-checks` — post-publish audit of `nisra-othscrt1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [health_and_social_care_trust, tlist_a1, financial_year, hsct, statistic]
checks:
  - kind: unique
    columns: [health_and_social_care_trust, tlist_a1, financial_year, hsct, statistic]
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: hsct
  - kind: not_null
    col: statistic
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
