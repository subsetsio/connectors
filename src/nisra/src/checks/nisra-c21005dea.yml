# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21005dea`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, district_electoral_area, dea2014, ethnic_group, ethnic_group_intermediate]
checks:
  - kind: unique
    columns: [value, district_electoral_area, dea2014, ethnic_group, ethnic_group_intermediate]
  - kind: not_null
    col: value
  - kind: not_null
    col: district_electoral_area
  - kind: not_null
    col: dea2014
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: ethnic_group_intermediate
  - kind: row_floor
    min: 520
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
