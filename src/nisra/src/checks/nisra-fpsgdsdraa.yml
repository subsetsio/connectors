# compiled by `hardened compile-checks` — post-publish audit of `nisra-fpsgdsdraa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, aa, assembly_area, tlist_a1, financial_year]
checks:
  - kind: unique
    columns: [value, aa, assembly_area, tlist_a1, financial_year]
  - kind: not_null
    col: value
  - kind: not_null
    col: aa
  - kind: not_null
    col: assembly_area
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: row_floor
    min: 494
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
