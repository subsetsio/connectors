# compiled by `hardened compile-checks` — post-publish audit of `nisra-cpni05`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, niroilgd, administrative_counties_and_local_government_districts, statistic, statistic_label]
checks:
  - kind: unique
    columns: [value, niroilgd, administrative_counties_and_local_government_districts, statistic, statistic_label]
  - kind: not_null
    col: value
  - kind: not_null
    col: niroilgd
  - kind: not_null
    col: administrative_counties_and_local_government_districts
  - kind: not_null
    col: statistic
  - kind: not_null
    col: statistic_label
  - kind: row_floor
    min: 215
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
