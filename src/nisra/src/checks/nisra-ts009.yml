# compiled by `hardened compile-checks` — post-publish audit of `nisra-ts009`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district, nat_id_basic, national_identity]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district, nat_id_basic, national_identity]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: nat_id_basic
  - kind: not_null
    col: national_identity
  - kind: row_floor
    min: 88
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
