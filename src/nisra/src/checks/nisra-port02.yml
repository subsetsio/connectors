# compiled by `hardened compile-checks` — post-publish audit of `nisra-port02`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [statistic, statistic_label, tlist_a1, year, majorport, ni_major_port, region, world_region, direction, direction_of_flow, cargodescriptionu, cargo_description, unit]
checks:
  - kind: unique
    columns: [statistic, statistic_label, tlist_a1, year, majorport, ni_major_port, region, world_region, direction, direction_of_flow, cargodescriptionu, cargo_description, unit]
  - kind: not_null
    col: statistic
  - kind: not_null
    col: statistic_label
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: year
  - kind: not_null
    col: majorport
  - kind: not_null
    col: ni_major_port
  - kind: not_null
    col: region
  - kind: not_null
    col: world_region
  - kind: not_null
    col: direction
  - kind: not_null
    col: direction_of_flow
  - kind: not_null
    col: cargodescriptionu
  - kind: not_null
    col: cargo_description
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 40320
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
