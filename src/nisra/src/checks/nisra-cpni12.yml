# compiled by `hardened compile-checks` — post-publish audit of `nisra-cpni12`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [countylgd, county_and_local_government_district, value]
checks:
  - kind: unique
    columns: [countylgd, county_and_local_government_district, value]
  - kind: not_null
    col: countylgd
  - kind: not_null
    col: county_and_local_government_district
  - kind: not_null
    col: value
  - kind: row_floor
    min: 22
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
