# compiled by `hardened compile-checks` — post-publish audit of `nisra-nimdm17t8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, super_output_area, soa, est, education_skills_and_training]
checks:
  - kind: unique
    columns: [value, super_output_area, soa, est, education_skills_and_training]
  - kind: not_null
    col: value
  - kind: not_null
    col: super_output_area
  - kind: not_null
    col: soa
  - kind: not_null
    col: est
  - kind: not_null
    col: education_skills_and_training
  - kind: row_floor
    min: 7120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
