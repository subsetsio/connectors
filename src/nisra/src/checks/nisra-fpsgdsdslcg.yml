# compiled by `hardened compile-checks` — post-publish audit of `nisra-fpsgdsdslcg`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, tlist_a1, financial_year, local_commissioning_group, lcg]
checks:
  - kind: unique
    columns: [value, tlist_a1, financial_year, local_commissioning_group, lcg]
  - kind: not_null
    col: value
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: local_commissioning_group
  - kind: not_null
    col: lcg
  - kind: row_floor
    min: 78
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
