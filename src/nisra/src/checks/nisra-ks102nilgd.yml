# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks102nilgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_band_agg17, age_17_cats, lgd2014, local_government_district]
checks:
  - kind: unique
    columns: [value, age_band_agg17, age_17_cats, lgd2014, local_government_district]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_band_agg17
  - kind: not_null
    col: age_17_cats
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: row_floor
    min: 102
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
