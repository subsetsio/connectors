# compiled by `hardened compile-checks` — post-publish audit of `nisra-mye01t07`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tlist_a1, year, yearbased, "2011_based_or_2021_based"]
checks:
  - kind: unique
    columns: [tlist_a1, year, yearbased, "2011_based_or_2021_based"]
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: year
  - kind: not_null
    col: yearbased
  - kind: not_null
    col: "2011_based_or_2021_based"
  - kind: row_floor
    min: 11
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
