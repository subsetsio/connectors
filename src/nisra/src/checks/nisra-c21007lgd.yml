# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21007lgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district, general_health, general_health_2]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district, general_health, general_health_2]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: general_health
  - kind: not_null
    col: general_health_2
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
