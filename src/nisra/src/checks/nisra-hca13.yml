# compiled by `hardened compile-checks` — post-publish audit of `nisra-hca13`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ded, district_electoral_division, value, occupants_residing, occupantsresiding]
checks:
  - kind: unique
    columns: [ded, district_electoral_division, value, occupants_residing, occupantsresiding]
  - kind: not_null
    col: ded
  - kind: not_null
    col: district_electoral_division
  - kind: not_null
    col: value
  - kind: not_null
    col: occupants_residing
  - kind: not_null
    col: occupantsresiding
  - kind: row_floor
    min: 23874
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
