# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks201nidea`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, dea2014, district_electoral_area, ethnic_group, ethnic_group_agg13]
checks:
  - kind: unique
    columns: [value, dea2014, district_electoral_area, ethnic_group, ethnic_group_agg13]
  - kind: not_null
    col: value
  - kind: not_null
    col: dea2014
  - kind: not_null
    col: district_electoral_area
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: ethnic_group_agg13
  - kind: row_floor
    min: 526
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
