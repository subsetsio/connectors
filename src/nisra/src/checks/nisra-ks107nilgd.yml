# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks107nilgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district_2014, emp_status, sex]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district_2014, emp_status, sex]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district_2014
  - kind: not_null
    col: emp_status
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
