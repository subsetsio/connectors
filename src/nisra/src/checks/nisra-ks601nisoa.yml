# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks601nisoa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, super_output_area, soa, econ_activity_agg16, economic_activity]
checks:
  - kind: unique
    columns: [value, super_output_area, soa, econ_activity_agg16, economic_activity]
  - kind: not_null
    col: value
  - kind: not_null
    col: super_output_area
  - kind: not_null
    col: soa
  - kind: not_null
    col: econ_activity_agg16
  - kind: not_null
    col: economic_activity
  - kind: row_floor
    min: 7128
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
