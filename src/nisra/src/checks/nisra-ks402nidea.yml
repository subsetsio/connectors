# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks402nidea`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dea2014, district_electoral_area, hh_tenure_agg9, tenure]
checks:
  - kind: unique
    columns: [dea2014, district_electoral_area, hh_tenure_agg9, tenure]
  - kind: not_null
    col: dea2014
  - kind: not_null
    col: district_electoral_area
  - kind: not_null
    col: hh_tenure_agg9
  - kind: not_null
    col: tenure
  - kind: row_floor
    min: 364
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
