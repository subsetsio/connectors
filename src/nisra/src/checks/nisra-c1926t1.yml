# compiled by `hardened compile-checks` — post-publish audit of `nisra-c1926t1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_syoa, age, ur_sex, sex]
checks:
  - kind: unique
    columns: [value, age_syoa, age, ur_sex, sex]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_syoa
  - kind: not_null
    col: age
  - kind: not_null
    col: ur_sex
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 306
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
