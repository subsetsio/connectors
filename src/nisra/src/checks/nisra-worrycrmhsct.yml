# compiled by `hardened compile-checks` — post-publish audit of `nisra-worrycrmhsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, tlist_a1, financial_year, statistic_label, health_and_social_care_trust]
checks:
  - kind: unique
    columns: [value, tlist_a1, financial_year, statistic_label, health_and_social_care_trust]
  - kind: not_null
    col: value
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: statistic_label
  - kind: not_null
    col: health_and_social_care_trust
  - kind: row_floor
    min: 162
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
