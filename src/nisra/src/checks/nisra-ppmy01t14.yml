# compiled by `hardened compile-checks` — post-publish audit of `nisra-ppmy01t14`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tlist_a1, year, education_and_library_board, elb, sex]
checks:
  - kind: unique
    columns: [tlist_a1, year, education_and_library_board, elb, sex]
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: year
  - kind: not_null
    col: education_and_library_board
  - kind: not_null
    col: elb
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 234
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
