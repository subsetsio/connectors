# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks102niaa`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, aa, age_band_agg17, age_17_cats, assembly_area]
checks:
  - kind: unique
    columns: [value, aa, age_band_agg17, age_17_cats, assembly_area]
  - kind: not_null
    col: value
  - kind: not_null
    col: aa
  - kind: not_null
    col: age_band_agg17
  - kind: not_null
    col: age_17_cats
  - kind: not_null
    col: assembly_area
  - kind: row_floor
    min: 161
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
