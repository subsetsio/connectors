# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks207nihsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [languagespoken, language_spoken, health_and_social_care_trust, hsct]
checks:
  - kind: unique
    columns: [languagespoken, language_spoken, health_and_social_care_trust, hsct]
  - kind: not_null
    col: languagespoken
  - kind: not_null
    col: language_spoken
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: hsct
  - kind: row_floor
    min: 42
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
