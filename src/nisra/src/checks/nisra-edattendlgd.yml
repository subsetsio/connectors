# compiled by `hardened compile-checks` — post-publish audit of `nisra-edattendlgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lgd2014, tlist_a1, financial_year, local_government_district, sex]
checks:
  - kind: unique
    columns: [lgd2014, tlist_a1, financial_year, local_government_district, sex]
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: tlist_a1
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
