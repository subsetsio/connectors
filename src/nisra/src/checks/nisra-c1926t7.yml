# compiled by `hardened compile-checks` — post-publish audit of `nisra-c1926t7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, county, county_label, ppr, persons_per_room]
checks:
  - kind: unique
    columns: [value, county, county_label, ppr, persons_per_room]
  - kind: not_null
    col: value
  - kind: not_null
    col: county
  - kind: not_null
    col: county_label
  - kind: not_null
    col: ppr
  - kind: not_null
    col: persons_per_room
  - kind: row_floor
    min: 35
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
