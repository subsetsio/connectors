# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21502eq`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, eqgrp, equality_group, industry, industry_current]
checks:
  - kind: unique
    columns: [value, eqgrp, equality_group, industry, industry_current]
  - kind: not_null
    col: value
  - kind: not_null
    col: eqgrp
  - kind: not_null
    col: equality_group
  - kind: not_null
    col: industry
  - kind: not_null
    col: industry_current
  - kind: row_floor
    min: 333
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
