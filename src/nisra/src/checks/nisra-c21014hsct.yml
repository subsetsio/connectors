# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21014hsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_band_agg8, age_8_cats, health_and_social_care_trust, hsct]
checks:
  - kind: unique
    columns: [value, age_band_agg8, age_8_cats, health_and_social_care_trust, hsct]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_band_agg8
  - kind: not_null
    col: age_8_cats
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: hsct
  - kind: row_floor
    min: 80
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
