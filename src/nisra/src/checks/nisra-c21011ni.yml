# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21011ni`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, age_bands_agg8, age_8_cats, health_condition_num_tc5, number_of_long_term_conditions]
checks:
  - kind: unique
    columns: [value, age_bands_agg8, age_8_cats, health_condition_num_tc5, number_of_long_term_conditions]
  - kind: not_null
    col: value
  - kind: not_null
    col: age_bands_agg8
  - kind: not_null
    col: age_8_cats
  - kind: not_null
    col: health_condition_num_tc5
  - kind: not_null
    col: number_of_long_term_conditions
  - kind: row_floor
    min: 48
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
