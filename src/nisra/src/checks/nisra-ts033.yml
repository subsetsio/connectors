# compiled by `hardened compile-checks` — post-publish audit of `nisra-ts033`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, lgd2014, local_government_district, hh_dependent_children_agg10, dependent_children_in_household]
checks:
  - kind: unique
    columns: [value, lgd2014, local_government_district, hh_dependent_children_agg10, dependent_children_in_household]
  - kind: not_null
    col: value
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: hh_dependent_children_agg10
  - kind: not_null
    col: dependent_children_in_household
  - kind: row_floor
    min: 110
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
