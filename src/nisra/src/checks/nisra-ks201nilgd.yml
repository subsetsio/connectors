# compiled by `hardened compile-checks` — post-publish audit of `nisra-ks201nilgd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ethnic_group, ethnic_group_agg13, lgd2014, local_government_district]
checks:
  - kind: unique
    columns: [ethnic_group, ethnic_group_agg13, lgd2014, local_government_district]
  - kind: not_null
    col: ethnic_group
  - kind: not_null
    col: ethnic_group_agg13
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: row_floor
    min: 78
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
