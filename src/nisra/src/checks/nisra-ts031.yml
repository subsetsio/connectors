# compiled by `hardened compile-checks` — post-publish audit of `nisra-ts031`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lgd2014, local_government_district, hh_tenure_agg8, tenure, tlist_a1]
checks:
  - kind: unique
    columns: [lgd2014, local_government_district, hh_tenure_agg8, tenure, tlist_a1]
  - kind: not_null
    col: lgd2014
  - kind: not_null
    col: local_government_district
  - kind: not_null
    col: hh_tenure_agg8
  - kind: not_null
    col: tenure
  - kind: not_null
    col: tlist_a1
  - kind: row_floor
    min: 88
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
