# compiled by `hardened compile-checks` — post-publish audit of `nisra-c21004hsct`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value, hh_dependent_children_agg11, number_of_dependent_children, health_and_social_care_trust, hsct]
checks:
  - kind: unique
    columns: [value, hh_dependent_children_agg11, number_of_dependent_children, health_and_social_care_trust, hsct]
  - kind: not_null
    col: value
  - kind: not_null
    col: hh_dependent_children_agg11
  - kind: not_null
    col: number_of_dependent_children
  - kind: not_null
    col: health_and_social_care_trust
  - kind: not_null
    col: hsct
  - kind: row_floor
    min: 27
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
