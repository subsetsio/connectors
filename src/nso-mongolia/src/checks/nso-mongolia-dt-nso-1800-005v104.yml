# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-1800-005v104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, name_of_border_crossings, purpose_of_travel]
temporal: year
checks:
  - kind: unique
    columns: [year, name_of_border_crossings, purpose_of_travel]
  - kind: not_null
    col: year
  - kind: not_null
    col: name_of_border_crossings
  - kind: not_null
    col: purpose_of_travel
  - kind: row_floor
    min: 2362
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
