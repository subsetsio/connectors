# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-2400-015v4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [unified_land_territory, year, aimags_and_the_capital]
temporal: year
checks:
  - kind: unique
    columns: [unified_land_territory, year, aimags_and_the_capital]
  - kind: not_null
    col: unified_land_territory
  - kind: not_null
    col: year
  - kind: not_null
    col: aimags_and_the_capital
  - kind: row_floor
    min: 9809
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
