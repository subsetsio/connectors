# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-0400-012v1-20`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarter, region, age_group, gender]
temporal: quarter
checks:
  - kind: unique
    columns: [quarter, region, age_group, gender]
  - kind: not_null
    col: quarter
  - kind: not_null
    col: region
  - kind: not_null
    col: age_group
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 39019
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
