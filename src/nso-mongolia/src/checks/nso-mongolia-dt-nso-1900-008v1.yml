# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-1900-008v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [main_categories_of_consumption, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [main_categories_of_consumption, year, indicator]
  - kind: not_null
    col: main_categories_of_consumption
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 327
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
