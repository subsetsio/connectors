# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-3800-005v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [relationship_to_the_perpetrator, indicator, location]
temporal: year
checks:
  - kind: unique
    columns: [relationship_to_the_perpetrator, indicator, location]
  - kind: not_null
    col: relationship_to_the_perpetrator
  - kind: not_null
    col: indicator
  - kind: not_null
    col: location
  - kind: row_floor
    min: 21
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
