# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-0600-002v21`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month, region, group, reference_year]
temporal: month
checks:
  - kind: unique
    columns: [month, region, group, reference_year]
  - kind: not_null
    col: month
  - kind: not_null
    col: region
  - kind: not_null
    col: group
  - kind: not_null
    col: reference_year
  - kind: row_floor
    min: 1737
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
