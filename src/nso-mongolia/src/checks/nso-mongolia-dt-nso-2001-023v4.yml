# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-2001-023v4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [professional_field, year, field_of_study]
temporal: year
checks:
  - kind: unique
    columns: [professional_field, year, field_of_study]
  - kind: not_null
    col: professional_field
  - kind: not_null
    col: year
  - kind: not_null
    col: field_of_study
  - kind: row_floor
    min: 183
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
