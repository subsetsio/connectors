# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-1.dt-nso-0902-002v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarterly_cumulative, region, indicator]
checks:
  - kind: unique
    columns: [quarterly_cumulative, region, indicator]
  - kind: not_null
    col: quarterly_cumulative
  - kind: not_null
    col: region
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 11702
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
