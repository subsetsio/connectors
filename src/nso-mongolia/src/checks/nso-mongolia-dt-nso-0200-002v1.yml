# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-0200-002v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [name_of_political_party, year, indicator]
temporal: year
checks:
  - kind: unique
    columns: [name_of_political_party, year, indicator]
  - kind: not_null
    col: name_of_political_party
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 190
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
