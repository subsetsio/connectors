# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-0100-01t04`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, aimags_and_the_capital, livestock_type]
temporal: year
checks:
  - kind: unique
    columns: [year, aimags_and_the_capital, livestock_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: aimags_and_the_capital
  - kind: not_null
    col: livestock_type
  - kind: row_floor
    min: 3611
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
