# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-2600-014v3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarter, economic_activity, size_group_of_employment]
temporal: quarter
checks:
  - kind: unique
    columns: [quarter, economic_activity, size_group_of_employment]
  - kind: not_null
    col: quarter
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: size_group_of_employment
  - kind: row_floor
    min: 2622
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
