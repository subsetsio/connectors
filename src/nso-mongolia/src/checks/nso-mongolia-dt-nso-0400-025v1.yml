# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-0400-025v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [time_annual, occupation_classification, sex]
checks:
  - kind: unique
    columns: [time_annual, occupation_classification, sex]
  - kind: not_null
    col: time_annual
  - kind: not_null
    col: occupation_classification
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 394
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
