# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-2001-024v2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [professional_field, educational_degree, year]
temporal: year
checks:
  - kind: unique
    columns: [professional_field, educational_degree, year]
  - kind: not_null
    col: professional_field
  - kind: not_null
    col: educational_degree
  - kind: not_null
    col: year
  - kind: row_floor
    min: 442
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
