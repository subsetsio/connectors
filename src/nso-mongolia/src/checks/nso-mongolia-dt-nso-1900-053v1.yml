# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-1900-053v1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [types_of_expenditure, five_equal_household_groups, quarter]
temporal: quarter
checks:
  - kind: unique
    columns: [types_of_expenditure, five_equal_household_groups, quarter]
  - kind: not_null
    col: types_of_expenditure
  - kind: not_null
    col: five_equal_household_groups
  - kind: not_null
    col: quarter
  - kind: row_floor
    min: 112
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
