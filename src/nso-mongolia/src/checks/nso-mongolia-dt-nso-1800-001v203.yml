# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-1800-001v203`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarterly, name_of_border_crossings, pass_direction]
checks:
  - kind: unique
    columns: [quarterly, name_of_border_crossings, pass_direction]
  - kind: not_null
    col: quarterly
  - kind: not_null
    col: name_of_border_crossings
  - kind: not_null
    col: pass_direction
  - kind: row_floor
    min: 2308
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
