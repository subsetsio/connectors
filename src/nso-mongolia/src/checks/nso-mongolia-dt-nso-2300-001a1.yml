# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-2300-001a1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month, region, classification_of_crimes]
temporal: month
checks:
  - kind: unique
    columns: [month, region, classification_of_crimes]
  - kind: not_null
    col: month
  - kind: not_null
    col: region
  - kind: not_null
    col: classification_of_crimes
  - kind: row_floor
    min: 8245
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
