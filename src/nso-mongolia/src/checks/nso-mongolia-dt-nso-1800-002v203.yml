# compiled by `hardened compile-checks` — post-publish audit of `nso-mongolia-dt-nso-1800-002v203`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarter, name_of_border_crossings, purpose_of_travel]
temporal: quarter
checks:
  - kind: unique
    columns: [quarter, name_of_border_crossings, purpose_of_travel]
  - kind: not_null
    col: quarter
  - kind: not_null
    col: name_of_border_crossings
  - kind: not_null
    col: purpose_of_travel
  - kind: row_floor
    min: 4439
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
