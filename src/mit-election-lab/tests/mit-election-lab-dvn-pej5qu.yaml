spec_id: mit-election-lab-dvn-pej5qu
# Heterogeneous Dataverse catalog: each dataset has its own schema,
# unknown until first pull. Assertions are authored from the first
# materialized chunk once the column shape is observed.
status: deferred
coverage:
  frozen: true
  reason: MIT Election Lab Dataverse files are versioned static dataset releases; future changes are expected as new Dataverse versions rather than an in-place rolling feed.
