spec_id: mit-election-lab-dvn-gszg1o
# Heterogeneous Dataverse catalog: each dataset has its own schema,
# unknown until first pull. Assertions are authored from the first
# materialized chunk once the column shape is observed.
status: deferred
