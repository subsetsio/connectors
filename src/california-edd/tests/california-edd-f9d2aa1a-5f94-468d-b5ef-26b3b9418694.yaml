spec_id: california-edd-f9d2aa1a-5f94-468d-b5ef-26b3b9418694
status: active
coverage:
  frozen: true
  reason: the CKAN monthly UI claims package is a historical series ending in 2022, not the current monthly release feed
# Unemployment Insurance Program Monthly Claims Data for California.
tests:
  - row_count: {min: 1}
    reason: the fetch fn raises on a 0-row pull, so a materialized raw asset must carry monthly UI rows
    certainty: 95
  - not_null: Area Name
    reason: every monthly UI observation is scoped to a named geography
    certainty: 85
  - not_null: Date
    reason: the report month is the row's time key; a blank means the export shape changed
    certainty: 85
    severity: warn
