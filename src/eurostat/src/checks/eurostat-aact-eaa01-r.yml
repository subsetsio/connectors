# compiled by `hardened compile-checks` — post-publish audit of `eurostat-aact-eaa01-r`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, am_item, indic_agr, unit, geo, time_period]
checks:
  - kind: unique
    columns: [freq, am_item, indic_agr, unit, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: am_item
  - kind: not_null
    col: indic_agr
  - kind: not_null
    col: unit
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 2464971
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
