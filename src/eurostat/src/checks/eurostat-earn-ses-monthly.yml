# compiled by `hardened compile-checks` — post-publish audit of `eurostat-earn-ses-monthly`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, nace_r2, isco08, worktime, age, sex, indic_se, geo, time_period]
checks:
  - kind: unique
    columns: [freq, nace_r2, isco08, worktime, age, sex, indic_se, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: nace_r2
  - kind: not_null
    col: isco08
  - kind: not_null
    col: worktime
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: not_null
    col: indic_se
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 6191858
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
