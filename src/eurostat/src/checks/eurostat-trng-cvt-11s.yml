# compiled by `hardened compile-checks` — post-publish audit of `eurostat-trng-cvt-11s`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, size_emp, reaction, unit, geo, time_period]
checks:
  - kind: unique
    columns: [freq, size_emp, reaction, unit, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: size_emp
  - kind: not_null
    col: reaction
  - kind: not_null
    col: unit
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 744
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
