# compiled by `hardened compile-checks` — post-publish audit of `eurostat-ef-lf-fam`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, fam_farm, statinfo, leg_form, age, sex, farmtype, unit, geo, time_period]
checks:
  - kind: unique
    columns: [freq, fam_farm, statinfo, leg_form, age, sex, farmtype, unit, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: fam_farm
  - kind: not_null
    col: statinfo
  - kind: not_null
    col: leg_form
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: not_null
    col: farmtype
  - kind: not_null
    col: unit
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 13902454
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
