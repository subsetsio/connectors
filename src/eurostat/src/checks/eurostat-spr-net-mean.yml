# compiled by `hardened compile-checks` — post-publish audit of `eurostat-spr-net-mean`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, spdepm, spdeps, indic_sp, geo, time_period]
checks:
  - kind: unique
    columns: [freq, spdepm, spdeps, indic_sp, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: spdepm
  - kind: not_null
    col: spdeps
  - kind: not_null
    col: indic_sp
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 6546
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
