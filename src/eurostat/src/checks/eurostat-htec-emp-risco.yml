# compiled by `hardened compile-checks` — post-publish audit of `eurostat-htec-emp-risco`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, nace_r1, unit, isco88, geo, time_period]
checks:
  - kind: unique
    columns: [freq, nace_r1, unit, isco88, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: nace_r1
  - kind: not_null
    col: unit
  - kind: not_null
    col: isco88
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 147351
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
