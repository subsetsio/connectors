# compiled by `hardened compile-checks` — post-publish audit of `eurostat-earn-ses22-36`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, sex, indic_se, emp_cont, sizeclas, nace_r2, unit, geo, time_period]
checks:
  - kind: unique
    columns: [freq, sex, indic_se, emp_cont, sizeclas, nace_r2, unit, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: sex
  - kind: not_null
    col: indic_se
  - kind: not_null
    col: emp_cont
  - kind: not_null
    col: sizeclas
  - kind: not_null
    col: nace_r2
  - kind: not_null
    col: unit
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 22873
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
