# compiled by `hardened compile-checks` — post-publish audit of `eurostat-ilc-atsb02b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, yn_esb, yn_eub, worktime, wstatus, lev_limit, unit, geo, time_period]
checks:
  - kind: unique
    columns: [freq, yn_esb, yn_eub, worktime, wstatus, lev_limit, unit, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: yn_esb
  - kind: not_null
    col: yn_eub
  - kind: not_null
    col: worktime
  - kind: not_null
    col: wstatus
  - kind: not_null
    col: lev_limit
  - kind: not_null
    col: unit
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 6885
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
