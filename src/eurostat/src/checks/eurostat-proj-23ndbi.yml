# compiled by `hardened compile-checks` — post-publish audit of `eurostat-proj-23ndbi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, indic_de, projection, geo, time_period]
checks:
  - kind: unique
    columns: [freq, indic_de, projection, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: indic_de
  - kind: not_null
    col: projection
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 156420
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
