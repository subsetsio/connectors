# compiled by `hardened compile-checks` — post-publish audit of `eurostat-tet00037`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [freq, indic_et, sitc06, partner, geo, time_period]
checks:
  - kind: unique
    columns: [freq, indic_et, sitc06, partner, geo, time_period]
  - kind: not_null
    col: freq
  - kind: not_null
    col: indic_et
  - kind: not_null
    col: sitc06
  - kind: not_null
    col: partner
  - kind: not_null
    col: geo
  - kind: not_null
    col: time_period
  - kind: row_floor
    min: 336
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
