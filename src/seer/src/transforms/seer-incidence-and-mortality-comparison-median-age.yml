# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `seer-incidence-and-mortality-comparison-median-age`. Regenerate after model-verify.
# caution: Rows include overlapping aggregate and subgroup strata such as all races and individual race groups; filter to one stratum before comparing median age or summing counts.
key: [sex, race, data_type, site]
sort: [sex, race, data_type, site]
columns:
  - name: sex
    type: VARCHAR
  - name: race
    type: VARCHAR
  - name: data_type
    type: VARCHAR
  - name: site
    type: VARCHAR
  - name: median_age
    type: DOUBLE
  - name: count
    type: BIGINT
