# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `seer-seer-incidence-median-age`. Regenerate after model-verify.
# caution: Rows include overlapping race and sex strata; filter to one stratum before comparing median age or summing counts.
key: [sex, race, site]
sort: [sex, race, site]
columns:
  - name: sex
    type: VARCHAR
  - name: race
    type: VARCHAR
  - name: site
    type: VARCHAR
  - name: median_age
    type: DOUBLE
  - name: count
    type: BIGINT
