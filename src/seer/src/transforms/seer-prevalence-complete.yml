# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `seer-prevalence-complete`. Regenerate after model-verify.
# caution: Rows include overlapping age and sex strata; filter to one stratum before aggregating complete-prevalence counts or percentages.
key: [sex, age_range, site]
sort: [sex, age_range, site]
columns:
  - name: sex
    type: VARCHAR
  - name: age_range
    type: VARCHAR
  - name: site
    type: VARCHAR
  - name: count
    type: BIGINT
  - name: percent
    type: DOUBLE
