# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `seer-seer-incidence-stage-distribution`. Regenerate after model-verify.
# caution: Rows include overlapping demographic strata; filter to one stratum before aggregating stage-distribution percentages or counts.
key: [sex, race, age_range, stage, site]
sort: [sex, race, age_range, stage, site]
columns:
  - name: sex
    type: VARCHAR
  - name: race
    type: VARCHAR
  - name: age_range
    type: VARCHAR
  - name: stage
    type: VARCHAR
  - name: site
    type: VARCHAR
  - name: percent
    type: DOUBLE
  - name: count
    type: BIGINT
