# compiled by `hardened compile-checks` — post-publish audit of `colorado-department-of-labor-and-employment-bynd-i2hj`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area_code, period_year, period_type, period, series_type]
temporal: period_year
checks:
  - kind: unique
    columns: [area_type, area_code, period_year, period_type, period, series_type]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area_code
  - kind: not_null
    col: period_year
  - kind: not_null
    col: period_type
  - kind: not_null
    col: period
  - kind: not_null
    col: series_type
  - kind: row_floor
    min: 2174
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
