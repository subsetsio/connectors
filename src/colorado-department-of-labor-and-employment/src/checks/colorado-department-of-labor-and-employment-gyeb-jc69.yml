# compiled by `hardened compile-checks` — post-publish audit of `colorado-department-of-labor-and-employment-gyeb-jc69`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area_code, projection_id, industry_code, occupation_code]
checks:
  - kind: unique
    columns: [area_type, area_code, projection_id, industry_code, occupation_code]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area_code
  - kind: not_null
    col: projection_id
  - kind: not_null
    col: industry_code
  - kind: not_null
    col: occupation_code
  - kind: row_floor
    min: 45686
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
