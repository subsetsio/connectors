# compiled by `hardened compile-checks` — post-publish audit of `colorado-department-of-labor-and-employment-busm-qa5b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area_code, period_year, occupation_code, rate_type]
temporal: period_year
checks:
  - kind: unique
    columns: [area_type, area_code, period_year, occupation_code, rate_type]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area_code
  - kind: not_null
    col: period_year
  - kind: not_null
    col: occupation_code
  - kind: not_null
    col: rate_type
  - kind: row_floor
    min: 164705
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
