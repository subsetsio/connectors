# compiled by `hardened compile-checks` — post-publish audit of `colorado-department-of-labor-and-employment-4e3w-qire`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [area_type, area_code, period_year, period_type, period, seasonally_adjusted]
temporal: period_year
checks:
  - kind: unique
    columns: [area_type, area_code, period_year, period_type, period, seasonally_adjusted]
  - kind: not_null
    col: area_type
  - kind: not_null
    col: area_code
  - kind: not_null
    col: period_year
  - kind: not_null
    col: period_type
  - kind: not_null
    col: period
  - kind: not_null
    col: seasonally_adjusted
  - kind: row_floor
    min: 18344
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
