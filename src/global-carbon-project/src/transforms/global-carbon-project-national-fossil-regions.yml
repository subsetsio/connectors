# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `global-carbon-project-national-fossil-regions`. Regenerate after model-verify.
# caution: Rows are region memberships, so a country or reporting area can appear in multiple regions.
key: [country, region]
sort: [country, region]
columns:
  - name: region
    type: VARCHAR
  - name: country
    type: VARCHAR
