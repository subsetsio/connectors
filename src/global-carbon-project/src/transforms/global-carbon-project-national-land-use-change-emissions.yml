# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `global-carbon-project-national-land-use-change-emissions`. Regenerate after model-verify.
# caution: The country column follows the source's reporting-area vocabulary and can include aggregate regions or special areas alongside sovereign countries; filter model before comparing a single estimate series.
key: [year, country, model]
temporal: year
sort: [year, country, model]
columns:
  - name: model
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: value
    type: DOUBLE
