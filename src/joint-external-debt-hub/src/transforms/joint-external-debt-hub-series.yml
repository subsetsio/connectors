# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `joint-external-debt-hub-series`. Regenerate after model-verify.
# caution: Series codes are SDMX-style identifiers and may not be unique in the raw catalog; use the series name as the catalog row identity and keep the code as a join/filter attribute.
key: [series_name]
sort: [series_name]
columns:
  - name: series_code
    type: VARCHAR
  - name: series_name
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: source_id
    type: BIGINT
  - name: source_name
    type: VARCHAR
  - name: source_note
    type: VARCHAR
  - name: source_organization
    type: VARCHAR
  - name: topics_json
    type: VARCHAR
