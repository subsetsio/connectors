# hand-curated from the compiled draft — do not regenerate blindly.
# of raw asset `statsnz-alcohol-available-for-consumption-year-ended-december-2025`.
# The source CSV publishes two unnamed leading columns, which the node named
# column_1/column_2. They decompose `period` (e.g. period 1935.12 == year 1935,
# month Dec) and are renamed period_year / period_month here.
key: [row_number, series_reference, period, period_year, series_title_1]
temporal: period
sort: [period, row_number, series_reference, period_year, series_title_1]
columns:
  - name: source_file
    type: VARCHAR
  - name: row_number
    type: BIGINT
  - name: period_year
    type: BIGINT
    description: >-
      Year component of `period`, which the source publishes as the first of two
      unnamed leading columns (e.g. period 1935.12 has period_year 1935).
  - name: period_month
    type: VARCHAR
    description: >-
      Month component of `period` — the three-letter abbreviation of the
      quarter/period end month (Mar/Jun/Sep/Dec), which the source publishes as
      the second of two unnamed leading columns (e.g. period 1935.12 has
      period_month Dec).
  - name: series_reference
    type: VARCHAR
  - name: period
    type: DOUBLE
  - name: data_value
    type: VARCHAR
  - name: status
    type: VARCHAR
  - name: units
    type: VARCHAR
  - name: magntude
    type: BIGINT
  - name: subject
    type: VARCHAR
  - name: group
    type: VARCHAR
  - name: series_title_1
    type: VARCHAR
  - name: series_title_2
    type: VARCHAR
  - name: series_title_3
    type: VARCHAR
  - name: series_title_4
    type: VARCHAR
  - name: series_title_5
    type: VARCHAR
