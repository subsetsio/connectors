# hand-curated from the compiled draft — do not regenerate blindly.
# of raw asset `statsnz-annual-enterprise-survey-2025-financial-year-provisional-size-bands`.
# The source CSV header has 9 trailing blank cells (column_8..column_16).
# column_8/column_9 are empty across all 23220 rows and are dropped here.
# For exactly 43 rows the published source file spills a SECOND COMPLETE
# observation onto the same physical line, pasted side-by-side into fields
# 10-16; those mirror the 7 named data columns (year, industry_code_ANZSIC,
# industry_name_ANZSIC, rme_size_grp, variable, value, unit). They are exposed
# as the `extra_*` columns below and are empty for every other row. This is a
# defect in the source file, not in the extraction.
key: [row_number, value, industry_code_anzsic, industry_name_anzsic, year]
temporal: year
sort: [year, row_number, value, industry_code_anzsic, industry_name_anzsic]
columns:
  - name: source_file
    type: VARCHAR
  - name: row_number
    type: BIGINT
  - name: year
    type: BIGINT
  - name: industry_code_anzsic
    type: VARCHAR
  - name: industry_name_anzsic
    type: VARCHAR
  - name: rme_size_grp
    type: VARCHAR
  - name: variable
    type: VARCHAR
  - name: value
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: extra_year
    type: VARCHAR
    description: >-
      `year` of a second observation that the source file spills onto the same
      physical line for 43 rows. Empty for every other row.
  - name: extra_industry_code_anzsic
    type: VARCHAR
    description: >-
      `industry_code_ANZSIC` of a second observation that the source file spills
      onto the same physical line for 43 rows. Empty for every other row.
  - name: extra_industry_name_anzsic
    type: VARCHAR
    description: >-
      `industry_name_ANZSIC` of a second observation that the source file spills
      onto the same physical line for 43 rows. Empty for every other row.
  - name: extra_rme_size_grp
    type: VARCHAR
    description: >-
      `rme_size_grp` of a second observation that the source file spills onto the
      same physical line for 43 rows. Empty for every other row.
  - name: extra_variable
    type: VARCHAR
    description: >-
      `variable` of a second observation that the source file spills onto the
      same physical line for 43 rows. Empty for every other row.
  - name: extra_value
    type: VARCHAR
    description: >-
      `value` of a second observation that the source file spills onto the same
      physical line for 43 rows. Empty for every other row.
  - name: extra_unit
    type: VARCHAR
    description: >-
      `unit` of a second observation that the source file spills onto the same
      physical line for 43 rows. Empty for every other row.
