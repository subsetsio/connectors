# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `statsnz-geographic-units-by-industry-and-statistical-area-2000-2025-descending-order`. Regenerate after model-verify.
# NOTE: the codebook/metadata passthrough columns below were named by hand (this file
# was a compiled draft that emitted positional `column_N` names).
key: []
temporal: year
sort: [year]
columns:
  - name: source_file
    type: VARCHAR
  - name: row_number
    type: BIGINT
  - name: anzsic06
    type: VARCHAR
  - name: area
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: geo_count
    type: BIGINT
  - name: ec_count
    type: BIGINT
  - name: sheet_name
    type: VARCHAR
  - name: metadata_identifier
    type: VARCHAR
    description: >-
      Column 1 of the companion metadata workbook bundled in the source zip (sheet given by `sheet_name`): the table name on 'Business demography', or the lookup code on 'LookupAREA'/'LookupANZSIC06'. Populated only on metadata rows; null on data rows.
  - name: metadata_variable
    type: VARCHAR
    description: >-
      Column 2 of the companion metadata workbook (sheet given by `sheet_name`): the variable name on 'Business demography', or the looked-up name on 'LookupAREA'/'LookupANZSIC06'. Populated only on metadata rows; null on data rows.
  - name: metadata_description
    type: VARCHAR
    description: >-
      Column 3 of the companion metadata workbook (sheet given by `sheet_name`): the description text. Populated only on metadata rows; null on data rows.
