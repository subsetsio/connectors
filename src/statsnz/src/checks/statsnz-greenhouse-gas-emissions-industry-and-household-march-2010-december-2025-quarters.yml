# compiled by `hardened compile-checks` — post-publish audit of `statsnz-greenhouse-gas-emissions-industry-and-household-march-2010-december-2025-quarters`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, data_value, period, anzsic, anzsic_descriptor]
temporal: period
checks:
  - kind: unique
    columns: [row_number, data_value, period, anzsic, anzsic_descriptor]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: data_value
  - kind: not_null
    col: period
  - kind: not_null
    col: anzsic
  - kind: not_null
    col: anzsic_descriptor
  - kind: row_floor
    min: 3136
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
