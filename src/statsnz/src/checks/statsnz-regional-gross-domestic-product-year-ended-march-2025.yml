# compiled by `hardened compile-checks` — post-publish audit of `statsnz-regional-gross-domestic-product-year-ended-march-2025`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, data_value, series_reference, series_title_3, period]
temporal: period
checks:
  - kind: unique
    columns: [row_number, data_value, series_reference, series_title_3, period]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: data_value
  - kind: not_null
    col: series_reference
  - kind: not_null
    col: series_title_3
  - kind: not_null
    col: period
  - kind: row_floor
    min: 5868
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
