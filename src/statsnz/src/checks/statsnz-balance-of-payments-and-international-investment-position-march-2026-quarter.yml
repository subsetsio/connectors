# compiled by `hardened compile-checks` — post-publish audit of `statsnz-balance-of-payments-and-international-investment-position-march-2026-quarter`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, data_value, series_reference, period, group]
temporal: period
checks:
  - kind: unique
    columns: [row_number, data_value, series_reference, period, group]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: data_value
  - kind: not_null
    col: series_reference
  - kind: not_null
    col: period
  - kind: not_null
    col: group
  - kind: row_floor
    min: 112277
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
