# compiled by `hardened compile-checks` — post-publish audit of `statsnz-na-isal-march-2026-quarter-institutional-sector-accounts`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, value, series_reference, transaction_label, transaction]
temporal: period
checks:
  - kind: unique
    columns: [row_number, value, series_reference, transaction_label, transaction]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: value
  - kind: not_null
    col: series_reference
  - kind: not_null
    col: transaction_label
  - kind: not_null
    col: transaction
  - kind: row_floor
    min: 16440
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
