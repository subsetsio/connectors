# compiled by `hardened compile-checks` — post-publish audit of `statsnz-national-accounts-income-and-expenditure-year-ended-march-2025-csv`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, data_value, series_reference, series_title_2, series_title_1]
temporal: period
checks:
  - kind: unique
    columns: [row_number, data_value, series_reference, series_title_2, series_title_1]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: data_value
  - kind: not_null
    col: series_reference
  - kind: not_null
    col: series_title_2
  - kind: not_null
    col: series_title_1
  - kind: row_floor
    min: 53307
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
