# compiled by `hardened compile-checks` — post-publish audit of `statsnz-household-living-costs-price-indexes-march-2026-quarter-time-series-indexes`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, index, series_ref, change_a, change_q]
temporal: quarter
checks:
  - kind: unique
    columns: [row_number, index, series_ref, change_a, change_q]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: index
  - kind: not_null
    col: series_ref
  - kind: not_null
    col: change_a
  - kind: not_null
    col: change_q
  - kind: row_floor
    min: 30744
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
