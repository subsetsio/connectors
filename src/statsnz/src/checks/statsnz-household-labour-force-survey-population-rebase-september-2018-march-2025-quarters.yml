# compiled by `hardened compile-checks` — post-publish audit of `statsnz-household-labour-force-survey-population-rebase-september-2018-march-2025-quarters`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, series_reference, ser_nbr, data_value, period]
temporal: period
checks:
  - kind: unique
    columns: [row_number, series_reference, ser_nbr, data_value, period]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: series_reference
  - kind: not_null
    col: ser_nbr
  - kind: not_null
    col: data_value
  - kind: not_null
    col: period
  - kind: row_floor
    min: 605378
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
