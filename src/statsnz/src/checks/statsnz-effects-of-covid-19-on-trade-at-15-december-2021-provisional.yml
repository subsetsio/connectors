# compiled by `hardened compile-checks` — post-publish audit of `statsnz-effects-of-covid-19-on-trade-at-15-december-2021-provisional`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, cumulative, date, value, country]
temporal: date
checks:
  - kind: unique
    columns: [row_number, cumulative, date, value, country]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: cumulative
  - kind: not_null
    col: date
  - kind: not_null
    col: value
  - kind: not_null
    col: country
  - kind: row_floor
    min: 55719
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
