# compiled by `hardened compile-checks` — post-publish audit of `statsnz-lgbtiq-population-of-aotearoa-new-zealand-2023`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [row_number, source_file, title, subject_population, source]
temporal: year
checks:
  - kind: unique
    columns: [row_number, source_file, title, subject_population, source]
  - kind: not_null
    col: row_number
  - kind: not_null
    col: source_file
  - kind: not_null
    col: title
  - kind: not_null
    col: subject_population
  - kind: not_null
    col: source
  - kind: row_floor
    min: 713
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
