# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cbe-foreign-trade--imports-by-geographical-distribution`. Regenerate after model-verify.
# caution: Geographic categories include totals and regional groupings from the source workbook; filter `indicator_en` and `dimension` before summing.
# caution: Rows combine annual and quarterly observations; use `frequency` to avoid mixing time granularities.
key: [date, period_label, frequency, indicator_en, source_file, year, value]
temporal: date
sort: [date, period_label, frequency, indicator_en, source_file, year, value]
columns:
  - name: indicator_en
    type: VARCHAR
  - name: indicator_ar
    type: VARCHAR
  - name: dimension
    type: VARCHAR
  - name: period_label
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
  - name: source_file
    type: VARCHAR
