# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `cbe-bop--egypts-balance-of-payments`. Regenerate after model-verify.
# caution: Rows are concatenated from annual and quarterly workbook partitions; filter `frequency` before comparing or aggregating periods.
# caution: `dimension` carries workbook header sub-dimensions where present, while many indicators have no sub-dimension.
key: [date, period_label, frequency, indicator_en, source_file, year, value]
temporal: date
sort: [date, period_label, frequency, indicator_en, source_file, year, value]
columns:
  - name: indicator_en
    type: VARCHAR
  - name: indicator_ar
    type: VARCHAR
  - name: dimension
    type: VARCHAR
  - name: period_label
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
  - name: source_file
    type: VARCHAR
