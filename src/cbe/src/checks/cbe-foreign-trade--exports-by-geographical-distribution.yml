# compiled by `hardened compile-checks` — post-publish audit of `cbe-foreign-trade--exports-by-geographical-distribution`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, period_label, frequency, indicator_en, source_file, year, value]
temporal: date
checks:
  - kind: unique
    columns: [date, period_label, frequency, indicator_en, source_file, year, value]
  - kind: not_null
    col: date
  - kind: not_null
    col: period_label
  - kind: not_null
    col: frequency
  - kind: not_null
    col: indicator_en
  - kind: not_null
    col: source_file
  - kind: not_null
    col: year
  - kind: not_null
    col: value
  - kind: row_floor
    min: 920
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
