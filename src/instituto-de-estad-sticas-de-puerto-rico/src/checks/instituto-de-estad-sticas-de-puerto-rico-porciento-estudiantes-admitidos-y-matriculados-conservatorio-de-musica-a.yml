# compiled by `hardened compile-checks` — post-publish audit of `instituto-de-estad-sticas-de-puerto-rico-porciento-estudiantes-admitidos-y-matriculados-conservatorio-de-musica-a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [agosto_diciembre_fall]
checks:
  - kind: unique
    columns: [agosto_diciembre_fall]
  - kind: not_null
    col: agosto_diciembre_fall
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
