# Cell-level long table parsed from PANGAEA .tab payloads in the GlacierLengthsAustria OAI set.
key: [dataset_doi, file_name, source_row_number, column_position]
temporal: observation_year
sort: [observation_year, dataset_doi, file_name, source_row_number, column_position]
columns:
  - name: collection
    type: VARCHAR
    description: PANGAEA OAI set collected by this connector.
  - name: source_doi
    type: VARCHAR
    description: DOI record discovered from the GlacierLengthsAustria OAI set.
  - name: dataset_doi
    type: VARCHAR
    description: DOI of the parsed tabular dataset, extracted from the file metadata when available.
  - name: file_name
    type: VARCHAR
    description: Name of the downloaded .tab payload or archive member.
  - name: source_row_number
    type: BIGINT
    description: One-based row number within the parsed source table.
  - name: observation_year
    type: BIGINT
    description: Year parsed from the Date/Time cell for the same source table row.
  - name: column_position
    type: BIGINT
    description: One-based column position within the parsed source table.
  - name: source_column_name
    type: VARCHAR
    description: Original PANGAEA source column name, including units or comments where provided.
  - name: value_text
    type: VARCHAR
    description: Cell value as published by PANGAEA, kept as text because source columns carry mixed units and types.
  - name: metadata_text
    type: VARCHAR
    description: Header metadata block from the source .tab file, including citation, coverage, parameters, and license text.
