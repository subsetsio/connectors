spec_id: federal-statistical-office-px-x-1502010000-101
status: active
tests:
  - row_count: {min: 1}
    reason: every published FSO cube has at least one non-null observation cell
    certainty: 95
  - not_null: value
    reason: the fetch skips null cells, so every raw row carries a numeric value
    certainty: 95
  - not_null: cube_id
    reason: constant provenance column stamped on every row by the fetch fn
    certainty: 99
coverage:
  frozen: true
  reason: Temporal coverage is treated as a source snapshot for factory publication; future refreshes are governed by catalog re-observation and scheduled connector runs.
