# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1703030000-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [datum_und_vorlage, kanton, ergebnis]
checks:
  - kind: unique
    columns: [datum_und_vorlage, kanton, ergebnis]
  - kind: not_null
    col: datum_und_vorlage
  - kind: not_null
    col: kanton
  - kind: not_null
    col: ergebnis
  - kind: row_floor
    min: 63916
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
