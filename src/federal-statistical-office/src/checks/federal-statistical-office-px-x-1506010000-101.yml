# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1506010000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, bildungsstufe, ausgabenart, verwaltungsebene]
checks:
  - kind: unique
    columns: [jahr, bildungsstufe, ausgabenart, verwaltungsebene]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: bildungsstufe
  - kind: not_null
    col: ausgabenart
  - kind: not_null
    col: verwaltungsebene
  - kind: row_floor
    min: 9690
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
