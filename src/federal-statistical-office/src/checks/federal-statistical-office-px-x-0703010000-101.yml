# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0703010000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, canton, type_of_owner, forest_zone, observation_unit]
temporal: year
checks:
  - kind: unique
    columns: [year, canton, type_of_owner, forest_zone, observation_unit]
  - kind: not_null
    col: year
  - kind: not_null
    col: canton
  - kind: not_null
    col: type_of_owner
  - kind: not_null
    col: forest_zone
  - kind: not_null
    col: observation_unit
  - kind: row_floor
    min: 109350
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
