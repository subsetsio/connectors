# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0702000000-206`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [innerbetriebliche_diversifikation_und_anteil_am_gesamtumsatz, gr_ssenklasse_ln, jahr, resultat]
checks:
  - kind: unique
    columns: [innerbetriebliche_diversifikation_und_anteil_am_gesamtumsatz, gr_ssenklasse_ln, jahr, resultat]
  - kind: not_null
    col: innerbetriebliche_diversifikation_und_anteil_am_gesamtumsatz
  - kind: not_null
    col: gr_ssenklasse_ln
  - kind: not_null
    col: jahr
  - kind: not_null
    col: resultat
  - kind: row_floor
    min: 480
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
