# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1106010100-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, kanton, strassenart, unfallschwere, unfallort]
checks:
  - kind: unique
    columns: [jahr, kanton, strassenart, unfallschwere, unfallort]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: kanton
  - kind: not_null
    col: strassenart
  - kind: not_null
    col: unfallschwere
  - kind: not_null
    col: unfallort
  - kind: row_floor
    min: 15912
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
