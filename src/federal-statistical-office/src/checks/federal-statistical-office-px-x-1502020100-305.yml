# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1502020100-305`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [lehrbetriebskanton, jahr, ausbildungstyp, geschlecht]
checks:
  - kind: unique
    columns: [lehrbetriebskanton, jahr, ausbildungstyp, geschlecht]
  - kind: not_null
    col: lehrbetriebskanton
  - kind: not_null
    col: jahr
  - kind: not_null
    col: ausbildungstyp
  - kind: not_null
    col: geschlecht
  - kind: row_floor
    min: 3402
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
