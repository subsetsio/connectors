# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1509090000-104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, jahr, szenario, abschlusstyp, geschlecht]
checks:
  - kind: unique
    columns: [kanton, jahr, szenario, abschlusstyp, geschlecht]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: jahr
  - kind: not_null
    col: szenario
  - kind: not_null
    col: abschlusstyp
  - kind: not_null
    col: geschlecht
  - kind: row_floor
    min: 2673
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
