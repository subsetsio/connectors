# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1502040400-161`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [isced_field, year, level_of_study, sex]
temporal: year
checks:
  - kind: unique
    columns: [isced_field, year, level_of_study, sex]
  - kind: not_null
    col: isced_field
  - kind: not_null
    col: year
  - kind: not_null
    col: level_of_study
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 4872
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
