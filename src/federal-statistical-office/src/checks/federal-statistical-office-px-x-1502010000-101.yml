# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1502010000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, education_level, citizenship_category, first_language, sex]
temporal: year
checks:
  - kind: unique
    columns: [year, education_level, citizenship_category, first_language, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: education_level
  - kind: not_null
    col: citizenship_category
  - kind: not_null
    col: first_language
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 4992
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
