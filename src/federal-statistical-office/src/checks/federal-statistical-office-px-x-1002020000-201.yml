# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1002020000-201`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indicator, product, year, reference_value]
temporal: year
checks:
  - kind: unique
    columns: [indicator, product, year, reference_value]
  - kind: not_null
    col: indicator
  - kind: not_null
    col: product
  - kind: not_null
    col: year
  - kind: not_null
    col: reference_value
  - kind: row_floor
    min: 591
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
