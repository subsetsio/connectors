# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0102020300-102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, alter, beobachtungseinheit, geschlecht]
checks:
  - kind: unique
    columns: [jahr, alter, beobachtungseinheit, geschlecht]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: alter
  - kind: not_null
    col: beobachtungseinheit
  - kind: not_null
    col: geschlecht
  - kind: row_floor
    min: 133100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
