# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1304030000-305`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, altersklasse, jahr, aufenthaltsstatus, geschlecht]
checks:
  - kind: unique
    columns: [kanton, altersklasse, jahr, aufenthaltsstatus, geschlecht]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: altersklasse
  - kind: not_null
    col: jahr
  - kind: not_null
    col: aufenthaltsstatus
  - kind: not_null
    col: geschlecht
  - kind: row_floor
    min: 18932
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
