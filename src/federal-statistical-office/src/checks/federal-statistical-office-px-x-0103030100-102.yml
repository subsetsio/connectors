# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0103030100-102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [canton_district_commune, year, previous_citizenship_selection, sex]
temporal: year
checks:
  - kind: unique
    columns: [canton_district_commune, year, previous_citizenship_selection, sex]
  - kind: not_null
    col: canton_district_commune
  - kind: not_null
    col: year
  - kind: not_null
    col: previous_citizenship_selection
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 580104
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
