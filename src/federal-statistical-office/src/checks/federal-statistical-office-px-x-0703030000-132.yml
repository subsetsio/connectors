# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0703030000-132`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, indicator, forest_zone, main_product_area]
temporal: year
checks:
  - kind: unique
    columns: [year, indicator, forest_zone, main_product_area]
  - kind: not_null
    col: year
  - kind: not_null
    col: indicator
  - kind: not_null
    col: forest_zone
  - kind: not_null
    col: main_product_area
  - kind: row_floor
    min: 2210
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
