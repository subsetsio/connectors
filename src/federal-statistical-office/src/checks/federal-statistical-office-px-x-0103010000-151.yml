# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0103010000-151`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [citizenship, year, demographic_component, sex]
temporal: year
checks:
  - kind: unique
    columns: [citizenship, year, demographic_component, sex]
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: year
  - kind: not_null
    col: demographic_component
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 221760
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
