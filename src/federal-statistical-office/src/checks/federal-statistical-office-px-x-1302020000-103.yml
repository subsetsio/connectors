# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1302020000-103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [land, jahr, masseinheit, einnahmenart]
checks:
  - kind: unique
    columns: [land, jahr, masseinheit, einnahmenart]
  - kind: not_null
    col: land
  - kind: not_null
    col: jahr
  - kind: not_null
    col: masseinheit
  - kind: not_null
    col: einnahmenart
  - kind: row_floor
    min: 46324
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
