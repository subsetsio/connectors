# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1702020000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton_bezirk_gemeinde, ergebnisse, jahr]
checks:
  - kind: unique
    columns: [kanton_bezirk_gemeinde, ergebnisse, jahr]
  - kind: not_null
    col: kanton_bezirk_gemeinde
  - kind: not_null
    col: ergebnisse
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 276224
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
