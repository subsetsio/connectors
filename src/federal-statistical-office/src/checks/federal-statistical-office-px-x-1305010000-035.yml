# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1305010000-035`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, jahr, hilflosigkeitsgrad, beobachtungseinheit]
checks:
  - kind: unique
    columns: [kanton, jahr, hilflosigkeitsgrad, beobachtungseinheit]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: jahr
  - kind: not_null
    col: hilflosigkeitsgrad
  - kind: not_null
    col: beobachtungseinheit
  - kind: row_floor
    min: 2916
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
