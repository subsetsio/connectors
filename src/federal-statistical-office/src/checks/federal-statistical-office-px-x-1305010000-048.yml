# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1305010000-048`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, kategorie, beobachtungseinheit]
checks:
  - kind: unique
    columns: [jahr, kategorie, beobachtungseinheit]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: kategorie
  - kind: not_null
    col: beobachtungseinheit
  - kind: row_floor
    min: 320
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
