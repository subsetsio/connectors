# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-2105000000-302`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quartier, demografischer_indikator, jahr]
checks:
  - kind: unique
    columns: [quartier, demografischer_indikator, jahr]
  - kind: not_null
    col: quartier
  - kind: not_null
    col: demografischer_indikator
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 20069
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
