# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1506030100-204`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hochschule, fachbereich, jahr, erl_skategorie, leistung]
checks:
  - kind: unique
    columns: [hochschule, fachbereich, jahr, erl_skategorie, leistung]
  - kind: not_null
    col: hochschule
  - kind: not_null
    col: fachbereich
  - kind: not_null
    col: jahr
  - kind: not_null
    col: erl_skategorie
  - kind: not_null
    col: leistung
  - kind: row_floor
    min: 84350
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
