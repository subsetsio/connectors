# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0304010000-121`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nettolohnh_henklasse, grossregion, jahr, besch_ftigungsgrad, geschlecht]
checks:
  - kind: unique
    columns: [nettolohnh_henklasse, grossregion, jahr, besch_ftigungsgrad, geschlecht]
  - kind: not_null
    col: nettolohnh_henklasse
  - kind: not_null
    col: grossregion
  - kind: not_null
    col: jahr
  - kind: not_null
    col: besch_ftigungsgrad
  - kind: not_null
    col: geschlecht
  - kind: row_floor
    min: 2856
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
