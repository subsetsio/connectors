# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1506030200-241`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indikator, fachbereich, hochschule, jahr]
checks:
  - kind: unique
    columns: [indikator, fachbereich, hochschule, jahr]
  - kind: not_null
    col: indikator
  - kind: not_null
    col: fachbereich
  - kind: not_null
    col: hochschule
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 10296
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
