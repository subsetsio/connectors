# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0904030000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton_gemeinde, jahr, anzahl_zimmer]
checks:
  - kind: unique
    columns: [kanton_gemeinde, jahr, anzahl_zimmer]
  - kind: not_null
    col: kanton_gemeinde
  - kind: not_null
    col: jahr
  - kind: not_null
    col: anzahl_zimmer
  - kind: row_floor
    min: 174503
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
