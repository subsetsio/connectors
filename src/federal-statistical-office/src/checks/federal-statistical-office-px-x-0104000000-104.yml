# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0104000000-104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, szenario_variante, beobachtungseinheit]
checks:
  - kind: unique
    columns: [jahr, szenario_variante, beobachtungseinheit]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: szenario_variante
  - kind: not_null
    col: beobachtungseinheit
  - kind: row_floor
    min: 4992
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
