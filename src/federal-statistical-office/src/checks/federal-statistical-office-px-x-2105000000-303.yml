# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-2105000000-303`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quartier, jahr, indikator_zur_wohnsituation]
checks:
  - kind: unique
    columns: [quartier, jahr, indikator_zur_wohnsituation]
  - kind: not_null
    col: quartier
  - kind: not_null
    col: jahr
  - kind: not_null
    col: indikator_zur_wohnsituation
  - kind: row_floor
    min: 13090
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
