# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1602010000-102`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_of_origin, year, all_films_new_releases, observation_unit]
temporal: year
checks:
  - kind: unique
    columns: [country_of_origin, year, all_films_new_releases, observation_unit]
  - kind: not_null
    col: country_of_origin
  - kind: not_null
    col: year
  - kind: not_null
    col: all_films_new_releases
  - kind: not_null
    col: observation_unit
  - kind: row_floor
    min: 6572
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
