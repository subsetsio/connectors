# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0104020000-107`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, kanton, beobachtungseinheit, szenario_variante]
checks:
  - kind: unique
    columns: [jahr, kanton, beobachtungseinheit, szenario_variante]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: kanton
  - kind: not_null
    col: beobachtungseinheit
  - kind: not_null
    col: szenario_variante
  - kind: row_floor
    min: 34944
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
