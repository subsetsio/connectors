# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0903020000-123`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [gemeinde, anzahl_zimmer, bewohnertyp, jahr]
checks:
  - kind: unique
    columns: [gemeinde, anzahl_zimmer, bewohnertyp, jahr]
  - kind: not_null
    col: gemeinde
  - kind: not_null
    col: anzahl_zimmer
  - kind: not_null
    col: bewohnertyp
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 463520
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
