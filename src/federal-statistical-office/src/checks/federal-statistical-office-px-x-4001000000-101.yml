# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-4001000000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [canton_district_commune, household_size, year]
temporal: year
checks:
  - kind: unique
    columns: [canton_district_commune, household_size, year]
  - kind: not_null
    col: canton_district_commune
  - kind: not_null
    col: household_size
  - kind: not_null
    col: year
  - kind: row_floor
    min: 68354
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
