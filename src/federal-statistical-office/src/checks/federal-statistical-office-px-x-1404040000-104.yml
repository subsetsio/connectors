# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1404040000-104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [aufwand_und_ertrag, kanton, jahr, leistungserbringertyp]
checks:
  - kind: unique
    columns: [aufwand_und_ertrag, kanton, jahr, leistungserbringertyp]
  - kind: not_null
    col: aufwand_und_ertrag
  - kind: not_null
    col: kanton
  - kind: not_null
    col: jahr
  - kind: not_null
    col: leistungserbringertyp
  - kind: row_floor
    min: 15390
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
