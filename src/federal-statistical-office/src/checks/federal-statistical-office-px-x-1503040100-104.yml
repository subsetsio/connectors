# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1503040100-104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, altersklasse, fachbereichsgruppe, examensstufe]
checks:
  - kind: unique
    columns: [jahr, altersklasse, fachbereichsgruppe, examensstufe]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: altersklasse
  - kind: not_null
    col: fachbereichsgruppe
  - kind: not_null
    col: examensstufe
  - kind: row_floor
    min: 5796
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
