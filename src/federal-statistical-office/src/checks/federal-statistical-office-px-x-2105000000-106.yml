# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-2105000000-106`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [stadt_agglomeration, mobilit_tsindikator, resultat, jahr]
checks:
  - kind: unique
    columns: [stadt_agglomeration, mobilit_tsindikator, resultat, jahr]
  - kind: not_null
    col: stadt_agglomeration
  - kind: not_null
    col: mobilit_tsindikator
  - kind: not_null
    col: resultat
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 249
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
