# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1506010000-106`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [jahr, kanton, bildungsstufe, ausgabenart]
checks:
  - kind: unique
    columns: [jahr, kanton, bildungsstufe, ausgabenart]
  - kind: not_null
    col: jahr
  - kind: not_null
    col: kanton
  - kind: not_null
    col: bildungsstufe
  - kind: not_null
    col: ausgabenart
  - kind: row_floor
    min: 23193
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
