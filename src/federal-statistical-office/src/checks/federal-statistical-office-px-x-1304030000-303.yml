# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1304030000-303`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, jahr, bezugsdauer, struktur_der_unterst_tzungseinheit]
checks:
  - kind: unique
    columns: [kanton, jahr, bezugsdauer, struktur_der_unterst_tzungseinheit]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: jahr
  - kind: not_null
    col: bezugsdauer
  - kind: not_null
    col: struktur_der_unterst_tzungseinheit
  - kind: row_floor
    min: 21814
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
