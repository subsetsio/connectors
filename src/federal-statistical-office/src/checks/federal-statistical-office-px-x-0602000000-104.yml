# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0602000000-104`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [quarter, major_region, job_vacancies]
temporal: quarter
checks:
  - kind: unique
    columns: [quarter, major_region, job_vacancies]
  - kind: not_null
    col: quarter
  - kind: not_null
    col: major_region
  - kind: not_null
    col: job_vacancies
  - kind: row_floor
    min: 1204
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
