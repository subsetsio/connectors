# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1506030300-261`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [hochschule, jahr, leistung, geldgeber]
checks:
  - kind: unique
    columns: [hochschule, jahr, leistung, geldgeber]
  - kind: not_null
    col: hochschule
  - kind: not_null
    col: jahr
  - kind: not_null
    col: leistung
  - kind: not_null
    col: geldgeber
  - kind: row_floor
    min: 2052
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
