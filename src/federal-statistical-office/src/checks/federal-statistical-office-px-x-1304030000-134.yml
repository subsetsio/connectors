# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1304030000-134`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, aufenthaltsstatus, jahr, altersklasse]
checks:
  - kind: unique
    columns: [kanton, aufenthaltsstatus, jahr, altersklasse]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: aufenthaltsstatus
  - kind: not_null
    col: jahr
  - kind: not_null
    col: altersklasse
  - kind: row_floor
    min: 24061
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
