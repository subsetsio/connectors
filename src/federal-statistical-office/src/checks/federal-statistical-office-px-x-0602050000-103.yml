# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0602050000-103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [gemeinde, jahr, gr_ssenklasse, wirtschaftssektor, variable]
checks:
  - kind: unique
    columns: [gemeinde, jahr, gr_ssenklasse, wirtschaftssektor, variable]
  - kind: not_null
    col: gemeinde
  - kind: not_null
    col: jahr
  - kind: not_null
    col: gr_ssenklasse
  - kind: not_null
    col: wirtschaftssektor
  - kind: not_null
    col: variable
  - kind: row_floor
    min: 194904
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
