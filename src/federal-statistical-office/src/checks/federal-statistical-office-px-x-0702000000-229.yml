# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0702000000-229`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [vehicles_machinery_facilities, region, jahr, einheit]
checks:
  - kind: unique
    columns: [vehicles_machinery_facilities, region, jahr, einheit]
  - kind: not_null
    col: vehicles_machinery_facilities
  - kind: not_null
    col: region
  - kind: not_null
    col: jahr
  - kind: not_null
    col: einheit
  - kind: row_floor
    min: 848
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
