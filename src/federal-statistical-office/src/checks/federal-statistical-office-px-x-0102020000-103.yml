# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0102020000-103`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, year, demographic_component, citizenship_category, sex]
temporal: year
checks:
  - kind: unique
    columns: [age, year, demographic_component, citizenship_category, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: year
  - kind: not_null
    col: demographic_component
  - kind: not_null
    col: citizenship_category
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 347004
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
