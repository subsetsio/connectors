# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0902020100-152`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [gemeinde, wohnungsfl_che, anzahl_zimmer, jahr]
checks:
  - kind: unique
    columns: [gemeinde, wohnungsfl_che, anzahl_zimmer, jahr]
  - kind: not_null
    col: gemeinde
  - kind: not_null
    col: wohnungsfl_che
  - kind: not_null
    col: anzahl_zimmer
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 811160
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
