# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0902020100-151`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, anzahl_zimmer, bauperiode, jahr]
checks:
  - kind: unique
    columns: [kanton, anzahl_zimmer, bauperiode, jahr]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: anzahl_zimmer
  - kind: not_null
    col: bauperiode
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 3780
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
