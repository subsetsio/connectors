# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0102020202-110`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, demographic_characteristic_and_indicator]
temporal: year
checks:
  - kind: unique
    columns: [year, demographic_characteristic_and_indicator]
  - kind: not_null
    col: year
  - kind: not_null
    col: demographic_characteristic_and_indicator
  - kind: row_floor
    min: 2028
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
