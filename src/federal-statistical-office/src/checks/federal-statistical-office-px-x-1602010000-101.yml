# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1602010000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [canton_communes, year, cinema_type, infrastructure]
temporal: year
checks:
  - kind: unique
    columns: [canton_communes, year, cinema_type, infrastructure]
  - kind: not_null
    col: canton_communes
  - kind: not_null
    col: year
  - kind: not_null
    col: cinema_type
  - kind: not_null
    col: infrastructure
  - kind: row_floor
    min: 308245
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
