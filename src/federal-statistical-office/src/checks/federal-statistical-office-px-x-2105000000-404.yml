# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-2105000000-404`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [variable, jahr, stadt_land_typologie, resultat]
checks:
  - kind: unique
    columns: [variable, jahr, stadt_land_typologie, resultat]
  - kind: not_null
    col: variable
  - kind: not_null
    col: jahr
  - kind: not_null
    col: stadt_land_typologie
  - kind: not_null
    col: resultat
  - kind: row_floor
    min: 2272
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
