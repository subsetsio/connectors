# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0702000000-230`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [motorisierte_fahrzeuge_nicht_motorisierte_maschinen_und_hofeinrichtungen, gr_ssenklasse_ln, jahr, einheit]
checks:
  - kind: unique
    columns: [motorisierte_fahrzeuge_nicht_motorisierte_maschinen_und_hofeinrichtungen, gr_ssenklasse_ln, jahr, einheit]
  - kind: not_null
    col: motorisierte_fahrzeuge_nicht_motorisierte_maschinen_und_hofeinrichtungen
  - kind: not_null
    col: gr_ssenklasse_ln
  - kind: not_null
    col: jahr
  - kind: not_null
    col: einheit
  - kind: row_floor
    min: 1272
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
