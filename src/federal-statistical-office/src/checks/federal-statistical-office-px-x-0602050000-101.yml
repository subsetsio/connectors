# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0602050000-101`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kanton, variable, jahr, gr_ssenklasse, wirtschaftssektor]
checks:
  - kind: unique
    columns: [kanton, variable, jahr, gr_ssenklasse, wirtschaftssektor]
  - kind: not_null
    col: kanton
  - kind: not_null
    col: variable
  - kind: not_null
    col: jahr
  - kind: not_null
    col: gr_ssenklasse
  - kind: not_null
    col: wirtschaftssektor
  - kind: row_floor
    min: 7096
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
