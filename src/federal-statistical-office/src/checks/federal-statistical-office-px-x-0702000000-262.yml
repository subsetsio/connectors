# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0702000000-262`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [digitalisierung, region, resultat]
checks:
  - kind: unique
    columns: [digitalisierung, region, resultat]
  - kind: not_null
    col: digitalisierung
  - kind: not_null
    col: region
  - kind: not_null
    col: resultat
  - kind: row_floor
    min: 244
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
