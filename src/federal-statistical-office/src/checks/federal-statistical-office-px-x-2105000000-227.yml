# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-2105000000-227`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [stadt_agglomeration, indikator, jahr, absolut_relativ, resultat]
checks:
  - kind: unique
    columns: [stadt_agglomeration, indikator, jahr, absolut_relativ, resultat]
  - kind: not_null
    col: stadt_agglomeration
  - kind: not_null
    col: indikator
  - kind: not_null
    col: jahr
  - kind: not_null
    col: absolut_relativ
  - kind: not_null
    col: resultat
  - kind: row_floor
    min: 585
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
