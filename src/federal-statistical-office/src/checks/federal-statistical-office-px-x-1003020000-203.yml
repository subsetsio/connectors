# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1003020000-203`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, tourist_region, month, indicator]
temporal: year
checks:
  - kind: unique
    columns: [year, tourist_region, month, indicator]
  - kind: not_null
    col: year
  - kind: not_null
    col: tourist_region
  - kind: not_null
    col: month
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 15568
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
