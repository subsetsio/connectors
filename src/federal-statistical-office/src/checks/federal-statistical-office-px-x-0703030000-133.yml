# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-0703030000-133`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [indicator, year, forest_zone, priority_function]
temporal: year
checks:
  - kind: unique
    columns: [indicator, year, forest_zone, priority_function]
  - kind: not_null
    col: indicator
  - kind: not_null
    col: year
  - kind: not_null
    col: forest_zone
  - kind: not_null
    col: priority_function
  - kind: row_floor
    min: 15087
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
