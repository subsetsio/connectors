# compiled by `hardened compile-checks` — post-publish audit of `federal-statistical-office-px-x-1304030000-304`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [staatsangeh_rigkeit_kategorie, aufenthaltsstatus, jahr]
checks:
  - kind: unique
    columns: [staatsangeh_rigkeit_kategorie, aufenthaltsstatus, jahr]
  - kind: not_null
    col: staatsangeh_rigkeit_kategorie
  - kind: not_null
    col: aufenthaltsstatus
  - kind: not_null
    col: jahr
  - kind: row_floor
    min: 11664
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
