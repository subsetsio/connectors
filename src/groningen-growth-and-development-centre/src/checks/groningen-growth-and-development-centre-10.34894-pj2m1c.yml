# compiled by `hardened compile-checks` — post-publish audit of `groningen-growth-and-development-centre-10.34894-pj2m1c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [description, code, country, variable, year]
temporal: year
checks:
  - kind: unique
    columns: [description, code, country, variable, year]
  - kind: not_null
    col: description
  - kind: not_null
    col: code
  - kind: not_null
    col: country
  - kind: not_null
    col: variable
  - kind: not_null
    col: year
  - kind: row_floor
    min: 288120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
