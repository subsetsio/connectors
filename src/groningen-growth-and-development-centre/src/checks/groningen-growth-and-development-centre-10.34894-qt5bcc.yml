# compiled by `hardened compile-checks` — post-publish audit of `groningen-growth-and-development-centre-10.34894-qt5bcc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [countrycode, country, currency_unit, year, variable]
temporal: year
checks:
  - kind: unique
    columns: [countrycode, country, currency_unit, year, variable]
  - kind: not_null
    col: countrycode
  - kind: not_null
    col: country
  - kind: not_null
    col: currency_unit
  - kind: not_null
    col: year
  - kind: not_null
    col: variable
  - kind: row_floor
    min: 194531
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
