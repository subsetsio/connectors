# compiled by `hardened compile-checks` — post-publish audit of `groningen-growth-and-development-centre-10.34894-e7mvox`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, country, cnt, sector, var]
temporal: year
checks:
  - kind: unique
    columns: [year, country, cnt, sector, var]
  - kind: not_null
    col: year
  - kind: not_null
    col: country
  - kind: not_null
    col: cnt
  - kind: not_null
    col: sector
  - kind: not_null
    col: var
  - kind: row_floor
    min: 7735
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
