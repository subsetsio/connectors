spec_id: beijing-municipal-bureau-of-statistics-05-dbw-b08-2
status: active
tests:
  - not_null: report_number
    reason: every unflattened row is stamped with its source reportNumber
    certainty: 98
  - enum: {col: report_number, values: ["DBW-B08-2"]}
    reason: one report table per download node; report_number is constant
    certainty: 98
  - not_null: indicator
    reason: each data cell joins to a row label (indicator) from the template
    certainty: 90
  - not_null: value
    reason: empty/blank cells are dropped in the fetch fn, so persisted values are non-null
    certainty: 90
  - column_type: {col: value, type: string}
    reason: raw cell values are persisted verbatim as strings; numeric cast happens in transform
    certainty: 95
  - freshness: {col: mask, reaches: '2016/12'}
    reason: this historical report currently tops out at 2016/12; requiring at least that period catches incomplete or regressed report pulls without pretending the archived table advances on a live cadence
    certainty: 75
