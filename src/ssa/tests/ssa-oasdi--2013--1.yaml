spec_id: ssa-oasdi--2013--1
status: active
coverage:
  frozen: true
  reason: SSA ArcGIS layer is a fixed 2013 point-in-time state/territory snapshot; the source does not advance this layer.
tests:
  - row_count: {min: 50, max: 70}
    reason: one row per US state/territory
    certainty: 90
  - not_null: State_Territory
  - unique: State_Territory
    reason: one row per state/territory in the 2013 beneficiary-count snapshot
    certainty: 90
  - column_type: {col: State_Territory, type: string}
  - column_type: {col: Total, type: integer}
  - column_type: {col: R_worker, type: integer}
