# compiled by `hardened compile-checks` — post-publish audit of `ons-brazil-importacaoenergia-comercial-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [din_instante, nom_bloco, nom_agente, nom_pais]
temporal: din_instante
checks:
  - kind: unique
    columns: [din_instante, nom_bloco, nom_agente, nom_pais]
  - kind: not_null
    col: din_instante
  - kind: not_null
    col: nom_bloco
  - kind: not_null
    col: nom_agente
  - kind: not_null
    col: nom_pais
  - kind: row_floor
    min: 1163394
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
