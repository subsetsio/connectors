# compiled by `hardened compile-checks` — post-publish audit of `ons-brazil-ena-diario-por-ree-reservatorio-equivalente-de-energia`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [ena_bruta_ree_percentualmlt, ena_data, nom_reservatorioee]
temporal: ena_data
checks:
  - kind: unique
    columns: [ena_bruta_ree_percentualmlt, ena_data, nom_reservatorioee]
  - kind: not_null
    col: ena_bruta_ree_percentualmlt
  - kind: not_null
    col: ena_data
  - kind: not_null
    col: nom_reservatorioee
  - kind: row_floor
    min: 12102
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
