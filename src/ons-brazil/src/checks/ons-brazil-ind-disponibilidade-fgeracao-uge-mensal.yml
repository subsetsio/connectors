# compiled by `hardened compile-checks` — post-publish audit of `ons-brazil-ind-disponibilidade-fgeracao-uge-mensal`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dat_mesreferencia, cod_equipamento]
temporal: dat_mesreferencia
checks:
  - kind: unique
    columns: [dat_mesreferencia, cod_equipamento]
  - kind: not_null
    col: dat_mesreferencia
  - kind: not_null
    col: cod_equipamento
  - kind: row_floor
    min: 21769
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
