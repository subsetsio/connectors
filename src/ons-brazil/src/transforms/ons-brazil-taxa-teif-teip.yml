# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ons-brazil-taxa-teif-teip`. Regenerate after model-verify.
# caution: This table carries TEIFa and TEIP measures in the same structure; filter nom_taxa before comparing rates.
key: [din_calculo, nom_usina, din_mes, tip_usina, nom_taxa]
temporal: din_mes
sort: [din_mes, din_calculo, nom_usina, tip_usina, nom_taxa]
columns:
  - name: nom_usina
    type: VARCHAR
  - name: cod_ceg
    type: VARCHAR
  - name: tip_usina
    type: VARCHAR
  - name: din_mes
    type: TIMESTAMP
  - name: nom_taxa
    type: VARCHAR
  - name: val_taxa
    type: DOUBLE
  - name: num_versao
    type: DOUBLE
  - name: din_calculo
    type: TIMESTAMP
