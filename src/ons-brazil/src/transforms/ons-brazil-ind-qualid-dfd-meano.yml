# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ons-brazil-ind-qualid-dfd-meano`. Regenerate after model-verify.
# caution: This table mixes monthly and annual frequency-performance observations; use id_periodicidade before comparing periods.
key: [din_referencia, nom_faixafrequencia, id_faixafrequencia, id_periodicidade]
temporal: din_referencia
sort: [din_referencia, nom_faixafrequencia, id_faixafrequencia, id_periodicidade]
columns:
  - name: id_periodicidade
    type: VARCHAR
  - name: din_referencia
    type: TIMESTAMP
  - name: id_faixafrequencia
    type: VARCHAR
  - name: nom_faixafrequencia
    type: VARCHAR
  - name: val_dfd
    type: DOUBLE
