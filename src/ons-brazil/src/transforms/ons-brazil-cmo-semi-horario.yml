# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ons-brazil-cmo-semi-horario`. Regenerate after model-verify.
# caution: The verified row identity includes the marginal cost value because duplicate timestamps and subsystems can occur without another exposed discriminator.
key: [din_instante, val_cmo, nom_subsistema, id_subsistema]
temporal: din_instante
sort: [din_instante, val_cmo, nom_subsistema, id_subsistema]
columns:
  - name: id_subsistema
    type: VARCHAR
  - name: nom_subsistema
    type: VARCHAR
  - name: din_instante
    type: TIMESTAMP
  - name: val_cmo
    type: DOUBLE
