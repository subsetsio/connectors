# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `hong-kong-observatory-gregorian-lunar-calendar`. Regenerate after model-verify.
# caution: The served Gregorian year window is rolling, so this reference table covers the calendar window currently exposed by the upstream API.
key: [date]
temporal: date
sort: [date]
columns:
  - name: date
    type: DATE
  - name: lunar_year
    type: VARCHAR
  - name: lunar_date
    type: VARCHAR
