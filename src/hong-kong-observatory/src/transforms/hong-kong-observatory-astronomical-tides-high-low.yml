# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `hong-kong-observatory-astronomical-tides-high-low`. Regenerate after model-verify.
# caution: Predicted tide events are ordinal within each station-day; use `event` with the time and height fields rather than assuming a fixed number of high/low events per day.
key: [station, year, month, day, event]
temporal: year
sort: [year, station, month, day, event]
columns:
  - name: station
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: month
    type: INTEGER
  - name: day
    type: INTEGER
  - name: event
    type: INTEGER
  - name: time
    type: VARCHAR
  - name: height_m
    type: DOUBLE
