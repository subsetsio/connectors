# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `afdb-hbubefg`. Regenerate after model-verify.
key: [date, country_code, country, element_code, element, frequency, unit]
temporal: date
sort: [date, country_code, country, element_code, element, frequency, unit]
columns:
  - name: country
    type: VARCHAR
  - name: country_code
    type: BIGINT
  - name: element
    type: VARCHAR
  - name: element_code
    type: BIGINT
  - name: unit
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
