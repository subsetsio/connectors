# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `afdb-zxckmae`. Regenerate after model-verify.
key: [date, country_code, country, item_code, item, products_code, products, frequency, unit]
temporal: date
sort: [date, country_code, country, item_code, item, products_code, products, frequency, unit]
columns:
  - name: country
    type: VARCHAR
  - name: country_code
    type: BIGINT
  - name: item
    type: VARCHAR
  - name: item_code
    type: BIGINT
  - name: products
    type: VARCHAR
  - name: products_code
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
