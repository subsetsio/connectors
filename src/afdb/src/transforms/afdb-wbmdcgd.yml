# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `afdb-wbmdcgd`. Regenerate after model-verify.
key: [date, pays_code, pays, ports_code, ports, indicateur_code, indicateur, frequency, unit]
temporal: date
sort: [date, pays_code, pays, ports_code, ports, indicateur_code, indicateur, frequency, unit]
columns:
  - name: pays
    type: VARCHAR
  - name: pays_code
    type: VARCHAR
  - name: ports
    type: VARCHAR
  - name: ports_code
    type: VARCHAR
  - name: indicateur
    type: VARCHAR
  - name: indicateur_code
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
