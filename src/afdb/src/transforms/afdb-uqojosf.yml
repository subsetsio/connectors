# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `afdb-uqojosf`. Regenerate after model-verify.
key: [date, country_code, country, classification_code, classification, series_code, series, frequency, unit]
temporal: date
sort: [date, country_code, country, classification_code, classification, series_code, series, frequency, unit]
columns:
  - name: country
    type: VARCHAR
  - name: country_code
    type: VARCHAR
  - name: classification
    type: VARCHAR
  - name: classification_code
    type: VARCHAR
  - name: series
    type: VARCHAR
  - name: series_code
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: frequency
    type: VARCHAR
  - name: date
    type: DATE
  - name: value
    type: DOUBLE
