# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `banco-central-del-ecuador-documentos-publicacionesnotas-catalogo-encuestas-eoe-iee-nueva-metodologia`. Regenerate after model-verify.
# caution: Rows are extracted cells from formatted multi-sheet BCE Excel workbooks; sheet, row, and column context must be used before aggregating numeric values because workbooks can include headers, totals, subtotals, notes, and mixed table layouts.
key: [row_label, col_header]
temporal: row_label
sort: [row_label, col_header]
columns:
  - name: sheet
    type: VARCHAR
  - name: row_index
    type: INTEGER
  - name: col_index
    type: INTEGER
  - name: row_label
    type: DATE
  - name: col_header
    type: VARCHAR
  - name: value
    type: DOUBLE
  - name: value_text
    type: VARCHAR
