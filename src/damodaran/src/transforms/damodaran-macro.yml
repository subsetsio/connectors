# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `damodaran-macro`. Regenerate after model-verify.
# caution: `category` carries the source's period or row label rather than a normalized date column.
key: [category, metric]
sort: [category, metric]
columns:
  - name: region
    type: VARCHAR
  - name: category
    type: BIGINT
  - name: metric
    type: VARCHAR
  - name: value
    type: DOUBLE
