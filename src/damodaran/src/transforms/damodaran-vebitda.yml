# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `damodaran-vebitda`. Regenerate after model-verify.
# caution: The source repeats some industry, multiple, and region labels for distinct EV multiple variants; filter to the intended metric rows before comparing or aggregating.
key: []
columns:
  - name: region
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: metric
    type: VARCHAR
  - name: value
    type: DOUBLE
