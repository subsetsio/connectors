# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `damodaran-ctryprem`. Regenerate after model-verify.
# caution: `category` is a country or market label from the source, not a standardized country code.
key: [category, metric]
sort: [category, metric]
columns:
  - name: region
    type: VARCHAR
  - name: category
    type: VARCHAR
  - name: metric
    type: VARCHAR
  - name: value
    type: DOUBLE
