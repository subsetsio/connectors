spec_id: fca-operating-service-metrics
status: active
coverage:
  frozen: true
  reason: fixed FCA publication/page extraction with CKAN metadata timestamp but no observation-date freshness column in the raw table
tests:
  - not_null: package_name
  - not_null: resource_url
  - not_null: sheet_name
  - not_null: cell_value
  - column_type: {col: row_number, type: integer}
  - column_type: {col: column_number, type: integer}
  - row_count: {min: 20}
    reason: FCA workbook/CSV resources should contain a non-empty tabular body
    certainty: 75
