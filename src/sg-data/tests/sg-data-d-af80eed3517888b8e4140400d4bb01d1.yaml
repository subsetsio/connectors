spec_id: sg-data-d-af80eed3517888b8e4140400d4bb01d1
status: deferred
coverage:
  frozen: true
  reason: Data.gov.sg catalog mirror contains historical/statistical snapshots with heterogeneous release cadences; no per-dataset freshness SLA is asserted for this bulk connector.
# Heterogeneous data.gov.sg dataset: column schema is unknown until
# first pull (wide SingStat tables, long series, and listings all differ).
# Tests are authored from the first materialized chunk; deferred per schema.
