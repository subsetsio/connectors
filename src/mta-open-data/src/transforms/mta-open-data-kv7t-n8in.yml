# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-kv7t-n8in`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
temporal: transit_timestamp
sort: [transit_timestamp]
columns:
  - name: transit_timestamp
    type: TIMESTAMP
  - name: bus_route
    type: VARCHAR
  - name: payment_method
    type: VARCHAR
  - name: fare_class_category
    type: VARCHAR
  - name: ridership
    type: BIGINT
  - name: transfers
    type: BIGINT
