# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-2vcb-zrh4`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
columns:
  - name: object_id
    type: VARCHAR
  - name: route_name
    type: VARCHAR
  - name: route_code
    type: VARCHAR
  - name: branch_code
    type: VARCHAR
  - name: shape_length
    type: DOUBLE
  - name: geometry
    type: VARCHAR
