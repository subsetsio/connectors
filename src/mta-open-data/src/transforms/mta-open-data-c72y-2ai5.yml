# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-c72y-2ai5`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
temporal: date
sort: [date]
columns:
  - name: date
    type: TIMESTAMP
  - name: time_period
    type: VARCHAR
  - name: total_ghg_agps
    type: DOUBLE
  - name: total_ghg_cvd
    type: DOUBLE
  - name: indexed_average_ghg
    type: DOUBLE
