# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-dips-jfpb`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
temporal: date
sort: [date]
columns:
  - name: date
    type: TIMESTAMP
  - name: ulsd_gallons_hedged
    type: BIGINT
  - name: percent_of_expected_ulsd_gallons_purchased
    type: BIGINT
  - name: weighted_average_hedge_price_per_month
    type: DOUBLE
  - name: current_adopted_budget_forecasted_commodity_price
    type: DOUBLE
  - name: posted_date
    type: TIMESTAMP
