# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-3uhz-sej2`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
columns:
  - name: operator
    type: VARCHAR
  - name: service
    type: VARCHAR
  - name: hex_color
    type: VARCHAR
  - name: cmyk
    type: VARCHAR
