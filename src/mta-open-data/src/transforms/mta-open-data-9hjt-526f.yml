# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `mta-open-data-9hjt-526f`. Regenerate after model-verify.
# caution: Published as a full Socrata snapshot table; no stable row key was asserted during schema-only profiling.
key: []
temporal: rundate
sort: [rundate]
columns:
  - name: rundate
    type: TIMESTAMP
  - name: branch
    type: VARCHAR
  - name: asset_type
    type: VARCHAR
  - name: availability
    type: DOUBLE
