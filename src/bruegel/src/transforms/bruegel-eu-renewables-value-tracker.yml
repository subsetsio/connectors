# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bruegel-eu-renewables-value-tracker`. Regenerate after model-verify.
# caution: Zone is a bidding-zone or country code and tech is the generation technology. Capacity factor, market value, and capture value are ratios or prices, not volumes; averaging them across zones without weighting by generation is misleading.
key: [zone, year, tech]
temporal: year
sort: [year, zone, tech]
columns:
  - name: zone
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: tech
    type: VARCHAR
  - name: cf
    type: DOUBLE
  - name: mv
    type: DOUBLE
  - name: cv
    type: DOUBLE
  - name: composition
    type: JSON
