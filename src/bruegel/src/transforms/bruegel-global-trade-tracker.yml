# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bruegel-global-trade-tracker`. Regenerate after model-verify.
# caution: Commodity includes the total ALL alongside components, partner includes aggregates and inconsistent country labels from the source sheet headers, and unit includes spelling variants for the same unit. Values are only comparable within one unit and seasonal_adj combination.
key: [date, commodity, unit, seasonal_adj, flow, partner]
temporal: date
sort: [date, commodity, unit, seasonal_adj, flow, partner]
columns:
  - name: date
    type: DATE
  - name: commodity
    type: VARCHAR
  - name: unit
    type: VARCHAR
  - name: seasonal_adj
    type: VARCHAR
  - name: flow
    type: VARCHAR
  - name: partner
    type: VARCHAR
  - name: value
    type: DOUBLE
