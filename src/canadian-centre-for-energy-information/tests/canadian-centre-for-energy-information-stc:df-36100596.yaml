spec_id: canadian-centre-for-energy-information-stc:df-36100596
status: active
coverage:
  frozen: true
  reason: archived inactive Statistics Canada dataflow; the SDMX corpus is historical
tests:
  - not_null: TIME_PERIOD
    reason: SDMX-CSV always carries the observation dimension (TIME_PERIOD) on every row
    certainty: 95
  - not_null: FREQ
    reason: every SDMX series declares a frequency code
    certainty: 90
  - row_count:
      min: 1
    reason: an accepted archived dataflow should still expose its historical observations
    certainty: 80
