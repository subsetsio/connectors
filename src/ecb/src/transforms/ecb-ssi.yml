# SSI — Banking structural statistical indicators (ECB Data Portal SDMX dataflow SSI)
# freq: constant 'A' — dropped, noted on time_period
# decimals: display metadata — dropped
key: [series_key, time_period]
temporal: time_period
columns:
  - name: series_key
    type: VARCHAR
    description: "full dot-separated SDMX series key identifying one series of the ECB SSI dataflow"
  - name: time_period
    type: BIGINT
    description: "observation year; all series in this flow are annual"
  - name: value
    type: DOUBLE
    description: "numeric observation value"
  - name: obs_status
    type: VARCHAR
    description: "SDMX observation status code (A normal, B break, E estimated, P provisional, U low reliability)"
  - name: title
    type: VARCHAR
    description: "human-readable English series title from the ECB catalog (name plus unit/compilation notes)"
  - name: unit
    type: VARCHAR
    description: "ECB unit code of the observation (e.g. EUR, PC for percent, IX for index, PURE_NUMB)"
  - name: unit_mult
    type: BIGINT
    description: "unit multiplier as a power of ten: the published value is expressed in units of 10^unit_mult"
