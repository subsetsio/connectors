# YC — Financial market data - yield curve (ECB Data Portal SDMX dataflow YC)
# freq: constant 'B' — dropped, noted on time_period
# obs_status: constant 'A' (normal value) — dropped
# unit: constant 'PCPA' — dropped, folded into value description
# unit_mult: constant '0' (no scaling) — dropped
# decimals: display metadata — dropped
key: [series_key, time_period]
temporal: time_period
columns:
  - name: series_key
    type: VARCHAR
    description: "full dot-separated SDMX series key identifying one series of the ECB YC dataflow"
  - name: time_period
    type: VARCHAR
    description: "SDMX observation period; format follows the series frequency (YYYY, YYYY-Qn, YYYY-Sn/Hn, YYYY-MM, YYYY-Wnn or YYYY-MM-DD); all series in this flow are business-daily"
  - name: value
    type: DOUBLE
    description: "numeric observation value (unit code PCPA for every series)"
  - name: title
    type: VARCHAR
    description: "human-readable English series title from the ECB catalog (name plus unit/compilation notes)"
