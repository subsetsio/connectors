# compiled by `hardened compile-checks` — post-publish audit of `dares-dares-defm-age-detaille-mens`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, age_detaille, categorie, champ]
temporal: date
checks:
  - kind: unique
    columns: [date, age_detaille, categorie, champ]
  - kind: not_null
    col: date
  - kind: not_null
    col: age_detaille
  - kind: not_null
    col: categorie
  - kind: not_null
    col: champ
  - kind: row_floor
    min: 66248
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
