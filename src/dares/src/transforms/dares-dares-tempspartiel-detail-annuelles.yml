# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dares-dares-tempspartiel-detail-annuelles`. Regenerate after model-verify.
key: [date, champ, indicateur, indicateur_detaille, sexe]
temporal: date
sort: [date, champ, indicateur, indicateur_detaille, sexe]
columns:
  - name: date
    type: BIGINT
  - name: champ
    type: VARCHAR
  - name: indicateur
    type: VARCHAR
  - name: indicateur_detaille
    type: VARCHAR
  - name: sexe
    type: VARCHAR
  - name: valeur
    type: DOUBLE
