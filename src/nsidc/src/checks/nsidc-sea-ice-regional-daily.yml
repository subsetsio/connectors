# compiled by `hardened compile-checks` — post-publish audit of `nsidc-sea-ice-regional-daily`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, year, region, month, hemisphere]
temporal: date
checks:
  - kind: unique
    columns: [date, year, region, month, hemisphere]
  - kind: not_null
    col: date
  - kind: not_null
    col: year
  - kind: not_null
    col: region
  - kind: not_null
    col: month
  - kind: not_null
    col: hemisphere
  - kind: row_floor
    min: 165138
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
