# compiled by `hardened compile-checks` — post-publish audit of `global-forest-watch-gadm--glad--iso-weekly-alerts`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [alert_week_start, iso, is_confirmed_alert]
temporal: alert_week_start
checks:
  - kind: unique
    columns: [alert_week_start, iso, is_confirmed_alert]
  - kind: not_null
    col: alert_week_start
  - kind: not_null
    col: iso
  - kind: not_null
    col: is_confirmed_alert
  - kind: row_floor
    min: 12961
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
