spec_id: gastat-lfs-2025
status: deferred
# Catalog connector: this GASTAT table's response schema (column names,
# types) is unknown until the first pull. The CData data API was 500ing
# (server-side license outage) during authoring, so assertions are
# authored from the first materialized chunk once the source recovers.
