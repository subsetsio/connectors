# compiled by `hardened compile-checks` — post-publish audit of `ksh-33b9d9ea-4fc0-4d9e-a96e-23459adb470b`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [values, c_birth, age, time, sex]
temporal: time
checks:
  - kind: unique
    columns: [values, c_birth, age, time, sex]
  - kind: not_null
    col: values
  - kind: not_null
    col: c_birth
  - kind: not_null
    col: age
  - kind: not_null
    col: time
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 149202
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
