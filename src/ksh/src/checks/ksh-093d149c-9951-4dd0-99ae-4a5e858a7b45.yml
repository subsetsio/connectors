# compiled by `hardened compile-checks` — post-publish audit of `ksh-093d149c-9951-4dd0-99ae-4a5e858a7b45`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [values, geo, time, indic_de, unit]
temporal: time
checks:
  - kind: unique
    columns: [values, geo, time, indic_de, unit]
  - kind: not_null
    col: values
  - kind: not_null
    col: geo
  - kind: not_null
    col: time
  - kind: not_null
    col: indic_de
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 1408
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
