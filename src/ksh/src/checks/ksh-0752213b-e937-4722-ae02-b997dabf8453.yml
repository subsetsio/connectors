# compiled by `hardened compile-checks` — post-publish audit of `ksh-0752213b-e937-4722-ae02-b997dabf8453`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [values, age, time, isced11, sex]
temporal: time
checks:
  - kind: unique
    columns: [values, age, time, isced11, sex]
  - kind: not_null
    col: values
  - kind: not_null
    col: age
  - kind: not_null
    col: time
  - kind: not_null
    col: isced11
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 11124
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
