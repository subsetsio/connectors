# compiled by `hardened compile-checks` — post-publish audit of `ksh-1cb63b4f-6dee-4fab-8d54-0d4e1ca884dc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [obs_value, time_period, sto, ref_area, accounting_entry]
temporal: last_update
checks:
  - kind: unique
    columns: [obs_value, time_period, sto, ref_area, accounting_entry]
  - kind: not_null
    col: obs_value
  - kind: not_null
    col: time_period
  - kind: not_null
    col: sto
  - kind: not_null
    col: ref_area
  - kind: not_null
    col: accounting_entry
  - kind: row_floor
    min: 2448
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
