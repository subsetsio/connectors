# compiled by `hardened compile-checks` — post-publish audit of `ksh-169201f0-24a4-434a-8900-3c035cd4430f`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [values, age, geo, time, sex]
temporal: time
checks:
  - kind: unique
    columns: [values, age, geo, time, sex]
  - kind: not_null
    col: values
  - kind: not_null
    col: age
  - kind: not_null
    col: geo
  - kind: not_null
    col: time
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 17028
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
