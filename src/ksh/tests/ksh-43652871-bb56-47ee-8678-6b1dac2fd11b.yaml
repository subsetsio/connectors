spec_id: ksh-43652871-bb56-47ee-8678-6b1dac2fd11b
status: active
tests:
  - freshness: {col: TIME_PERIOD, reaches: "2020"}
    points_outward: true
  - row_count: {min: 100}
    reason: KSH HVD distribution served as csv. A multi-dimensional statistical table holds many observations; far fewer rows means the payload was truncated or the parser yielded nothing.
    certainty: 80
  - not_null: DATAFLOW
    reason: Column DATAFLOW is an SDMX dimension key, populated on every observation. Nulls here mean a column-shift in parsing.
    certainty: 85
