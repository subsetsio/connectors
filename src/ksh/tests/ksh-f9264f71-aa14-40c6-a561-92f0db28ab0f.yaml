spec_id: ksh-f9264f71-aa14-40c6-a561-92f0db28ab0f
status: active
tests:
  - freshness: {col: TIME_PERIOD, reaches: "2020"}
    points_outward: true
  - row_count: {min: 1000}
    reason: KSH HVD distribution served as xml. A multi-dimensional statistical table holds many observations; far fewer rows means the payload was truncated or the parser yielded nothing.
    certainty: 80
  - not_null: ACCOUNTING_ENTRY
    reason: Column ACCOUNTING_ENTRY is an SDMX dimension key, populated on every observation. Nulls here mean a column-shift in parsing.
    certainty: 85
