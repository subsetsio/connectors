spec_id: connecticut-department-of-labor-tids-7w95
status: active
tests:
  - not_null: soc_code
    reason: OES rows are keyed by occupation (SOC) code
    certainty: 95
  - not_null: areaname
    certainty: 90
  - row_count: {min: 1000}
    reason: ~800 occupations x several areas; ~3.8k observed
    certainty: 80
  - matches: {col: soc_code, pattern: '^[0-9]{2}-[0-9]{4}$'}
    reason: SOC codes are formatted NN-NNNN (e.g. 00-0000)
    certainty: 90
  - distinct_count: {col: soc_code, min: 100}
    reason: OES covers hundreds of detailed occupations
    certainty: 85
