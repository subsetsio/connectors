# compiled by `hardened compile-checks` — post-publish audit of `edgar-ghg-emissions-by-country-sector`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [country_code, ipcc_sector_code, gas, fossil_bio, year]
temporal: year
checks:
  - kind: unique
    columns: [country_code, ipcc_sector_code, gas, fossil_bio, year]
  - kind: not_null
    col: country_code
  - kind: not_null
    col: ipcc_sector_code
  - kind: not_null
    col: gas
  - kind: not_null
    col: fossil_bio
  - kind: not_null
    col: year
  - kind: row_floor
    min: 278323
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
