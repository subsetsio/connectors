spec_id: public-health-scotland-workforce
status: active
coverage:
  frozen: true
  reason: CKAN group metadata for the workforce topic has not been modified since 2020; future statistical data are expected as child datasets rather than this metadata row.
tests:
  - not_null: resource_name
    reason: every row is tagged by us with the source CKAN resource or group it came from
    certainty: 99
  - not_null: resource_id
    reason: every row carries the datastore resource UUID or CKAN group id it came from
    certainty: 99
  - row_count:
      min: 1
    reason: an accepted PHS entity always produces at least one raw row
    certainty: 95
