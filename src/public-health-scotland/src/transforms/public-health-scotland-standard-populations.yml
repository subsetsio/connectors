# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `public-health-scotland-standard-populations`. Regenerate after model-verify.
# caution: Reference-style source extract without a verified compact key; use the source columns as descriptive records rather than aggregating rows.
key: []
columns:
  - name: resource_id
    type: VARCHAR
  - name: resource_name
    type: VARCHAR
  - name: agegroup
    type: VARCHAR
  - name: europeanstandardpopulation
    type: BIGINT
  - name: sex
    type: VARCHAR
  - name: worldstandardpopulation
    type: BIGINT
