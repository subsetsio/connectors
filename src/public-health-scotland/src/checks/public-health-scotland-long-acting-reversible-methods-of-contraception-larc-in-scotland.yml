# compiled by `hardened compile-checks` — post-publish audit of `public-health-scotland-long-acting-reversible-methods-of-contraception-larc-in-scotland`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [count, financialyear, hbt, type, source]
checks:
  - kind: unique
    columns: [count, financialyear, hbt, type, source]
  - kind: not_null
    col: count
  - kind: not_null
    col: financialyear
  - kind: not_null
    col: hbt
  - kind: not_null
    col: type
  - kind: not_null
    col: source
  - kind: row_floor
    min: 607
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
