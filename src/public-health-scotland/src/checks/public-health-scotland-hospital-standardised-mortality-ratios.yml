# compiled by `hardened compile-checks` — post-publish audit of `public-health-scotland-hospital-standardised-mortality-ratios`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [cruderate, numberofpatients, timeperiod, resource_id, resource_name]
checks:
  - kind: unique
    columns: [cruderate, numberofpatients, timeperiod, resource_id, resource_name]
  - kind: not_null
    col: cruderate
  - kind: not_null
    col: numberofpatients
  - kind: not_null
    col: timeperiod
  - kind: not_null
    col: resource_id
  - kind: not_null
    col: resource_name
  - kind: row_floor
    min: 1566
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
