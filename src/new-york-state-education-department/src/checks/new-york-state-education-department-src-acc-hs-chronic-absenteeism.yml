# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-education-department-src-acc-hs-chronic-absenteeism`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [entity_name, entity_cd, subgroup_name, report_year, year]
temporal: year
checks:
  - kind: unique
    columns: [entity_name, entity_cd, subgroup_name, report_year, year]
  - kind: not_null
    col: entity_name
  - kind: not_null
    col: entity_cd
  - kind: not_null
    col: subgroup_name
  - kind: not_null
    col: report_year
  - kind: not_null
    col: year
  - kind: row_floor
    min: 123597
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
