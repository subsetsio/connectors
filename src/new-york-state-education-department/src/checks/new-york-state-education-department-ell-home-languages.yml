# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-education-department-ell-home-languages`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [institution_id, entity_cd, entity_name, home_language, report_year]
checks:
  - kind: unique
    columns: [institution_id, entity_cd, entity_name, home_language, report_year]
  - kind: not_null
    col: institution_id
  - kind: not_null
    col: entity_cd
  - kind: not_null
    col: entity_name
  - kind: not_null
    col: home_language
  - kind: not_null
    col: report_year
  - kind: row_floor
    min: 5368
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
