# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-education-department-ell-enrollment`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [report_year, institution_id, entity_cd, entity_name]
checks:
  - kind: unique
    columns: [report_year, institution_id, entity_cd, entity_name]
  - kind: not_null
    col: report_year
  - kind: not_null
    col: institution_id
  - kind: not_null
    col: entity_cd
  - kind: not_null
    col: entity_name
  - kind: row_floor
    min: 1487
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
