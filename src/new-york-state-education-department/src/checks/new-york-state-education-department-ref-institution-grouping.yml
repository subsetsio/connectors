# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-education-department-ref-institution-grouping`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [entity_name, entity_cd, report_year, group_code, group_name]
checks:
  - kind: unique
    columns: [entity_name, entity_cd, report_year, group_code, group_name]
  - kind: not_null
    col: entity_name
  - kind: not_null
    col: entity_cd
  - kind: not_null
    col: report_year
  - kind: not_null
    col: group_code
  - kind: not_null
    col: group_name
  - kind: row_floor
    min: 22159
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
