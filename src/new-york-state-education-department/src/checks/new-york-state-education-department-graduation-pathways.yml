# compiled by `hardened compile-checks` — post-publish audit of `new-york-state-education-department-graduation-pathways`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [report_year, aggregation_index, aggregation_type, aggregation_code, membership_code, subgroup_code, course_of_study_code]
checks:
  - kind: unique
    columns: [report_year, aggregation_index, aggregation_type, aggregation_code, membership_code, subgroup_code, course_of_study_code]
  - kind: not_null
    col: report_year
  - kind: not_null
    col: aggregation_index
  - kind: not_null
    col: aggregation_type
  - kind: not_null
    col: aggregation_code
  - kind: not_null
    col: membership_code
  - kind: not_null
    col: subgroup_code
  - kind: not_null
    col: course_of_study_code
  - kind: row_floor
    min: 252303
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
