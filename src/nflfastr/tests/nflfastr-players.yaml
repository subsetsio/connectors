spec_id: nflfastr-players
status: active
coverage: {frozen: true, reason: "Player biographical reference rows use birth_date as a data field, not a publication recency axis; source refresh is governed by release rebuilds rather than recent birth dates."}
tests:
  - not_null: gsis_id
  - row_count: {min: 12517}
    certainty: 70
    severity: warn
    reason: at least ~0.5x one observed season (~25034 rows); combined corpus is larger, a lower count means truncation
