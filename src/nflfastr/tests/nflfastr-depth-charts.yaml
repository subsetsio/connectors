spec_id: nflfastr-depth-charts
status: active
tests:
  - freshness: {col: dt, reaches: today - 400d}
  - not_null: team
  - row_count: {min: 130704}
    certainty: 70
    severity: warn
    reason: at least ~0.5x one observed season (~261409 rows); combined corpus is larger, a lower count means truncation
