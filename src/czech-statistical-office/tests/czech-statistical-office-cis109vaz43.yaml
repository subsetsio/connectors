spec_id: czech-statistical-office-cis109vaz43
status: active
tests:
- row_count:
    min: 0
  reason: CSO publishes this accepted relation CSV with zero rows in the current catalog snapshot; the connector should preserve
    the empty table rather than treat it as a failed download.
  certainty: 95
  severity: block
