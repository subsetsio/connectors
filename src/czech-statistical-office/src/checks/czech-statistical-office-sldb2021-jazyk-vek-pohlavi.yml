# compiled by `hardened compile-checks` — post-publish audit of `czech-statistical-office-sldb2021-jazyk-vek-pohlavi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [idhod, hodnota, vek_kod, vek_txt, jazyk_txt]
temporal: sldb_datum
checks:
  - kind: unique
    columns: [idhod, hodnota, vek_kod, vek_txt, jazyk_txt]
  - kind: not_null
    col: idhod
  - kind: not_null
    col: hodnota
  - kind: not_null
    col: vek_kod
  - kind: not_null
    col: vek_txt
  - kind: not_null
    col: jazyk_txt
  - kind: row_floor
    min: 17493
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
