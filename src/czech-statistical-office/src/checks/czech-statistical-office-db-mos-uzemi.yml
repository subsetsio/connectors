# compiled by `hardened compile-checks` — post-publish audit of `czech-statistical-office-db-mos-uzemi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [koduzemi, obec, okres, platiod, platido]
checks:
  - kind: unique
    columns: [koduzemi, obec, okres, platiod, platido]
  - kind: not_null
    col: koduzemi
  - kind: not_null
    col: obec
  - kind: not_null
    col: okres
  - kind: not_null
    col: platiod
  - kind: not_null
    col: platido
  - kind: row_floor
    min: 3225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
