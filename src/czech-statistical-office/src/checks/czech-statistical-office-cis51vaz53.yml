# compiled by `hardened compile-checks` — post-publish audit of `czech-statistical-office-cis51vaz53`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [chodnota2, text2, chodnota1, text1]
checks:
  - kind: unique
    columns: [chodnota2, text2, chodnota1, text1]
  - kind: not_null
    col: chodnota2
  - kind: not_null
    col: text2
  - kind: not_null
    col: chodnota1
  - kind: not_null
    col: text1
  - kind: row_floor
    min: 12183
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
