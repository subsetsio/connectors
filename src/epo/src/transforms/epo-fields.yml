# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `epo-fields`. Regenerate after model-verify.
key: [field_id]
sort: [field_id]
columns:
  - name: field_id
    type: BIGINT
  - name: field_name
    type: VARCHAR
  - name: field_sector_id
    type: VARCHAR
  - name: field_rank
    type: BIGINT
