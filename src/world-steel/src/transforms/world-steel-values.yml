# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `world-steel-values`. Regenerate after model-verify.
# caution: Contains multiple indicators with different units and both country and regional geographies; join the indicator and geography reference tables or filter those dimensions before comparing or aggregating values.
key: [geoitem_id, indicator_id, period, year]
temporal: year
sort: [year, geoitem_id, indicator_id, period]
columns:
  - name: indicator_id
    type: VARCHAR
  - name: geoitem_id
    type: VARCHAR
  - name: period
    type: BIGINT
  - name: year
    type: INTEGER
  - name: month
    type: INTEGER
  - name: value
    type: DOUBLE
