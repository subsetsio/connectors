# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `central-bank-of-sri-lanka-table-1.1`. Regenerate after model-verify.
# caution: Rows are a faithful long-form melt of one CBSL workbook; row_label and col_label encode the workbook stub/header text and may mix totals, subcategories, geographies, units, or period labels depending on the source table.
key: [row_label, col_label, period_year]
sort: [row_label, col_label, period_year]
columns:
  - name: row_label
    type: VARCHAR
  - name: col_label
    type: VARCHAR
  - name: period_year
    type: BIGINT
  - name: value
    type: DOUBLE
  - name: value_text
    type: JSON
