# provisional pass-through for normalized ESS raw; model compilation should settle key/temporal metadata after the full raw run lands
columns:
  - name: row_label
    type: VARCHAR
  - name: col_label
    type: VARCHAR
  - name: period_year
    type: INTEGER
  - name: value
    type: DOUBLE
  - name: value_text
    type: VARCHAR
