spec_id: arcep-tableau-bord-du-new-deal-mobile--indicateurs-couverture-4g
status: active
coverage:
  frozen: true
  reason: This accepted family is a historical 2018-T2 through 2022-T2 New Deal Mobile coverage extract; newer dashboard resources are separate schema families.
tests:
  - row_count: {min: 100}
    reason: quarterly 4G coverage indicators per operator/department; a collapse to near-zero means the CSV family stopped enumerating
    certainty: 80
  - nullable: {col: code_op, max_null_frac: 0.02}
    reason: a small tail of historical rows use alternate operator-code columns across schema vintages, but code_op should remain populated for nearly all rows
    certainty: 80
  - column_type: {col: code_op, type: string}
    reason: raw is preserved verbatim as strings (decimal commas / leading zeros / NA sentinels)
    certainty: 90
  - not_null: trimestre
    reason: trimestre is a key/identifier column present on every source row
    certainty: 85
  - column_type: {col: trimestre, type: string}
    reason: raw is preserved verbatim as strings (decimal commas / leading zeros / NA sentinels)
    certainty: 90
  - not_null: _resource_title
    reason: provenance column stamped on every row by the fetch fn
    certainty: 99
