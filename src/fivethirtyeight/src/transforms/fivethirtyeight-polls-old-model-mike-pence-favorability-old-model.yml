# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `fivethirtyeight-polls-old-model-mike-pence-favorability-old-model`. Regenerate after model-verify.
key: [date, response]
temporal: date
sort: [date, response]
columns:
  - name: politician
    type: VARCHAR
  - name: response
    type: VARCHAR
  - name: date
    type: DATE
  - name: pct_estimate
    type: DOUBLE
