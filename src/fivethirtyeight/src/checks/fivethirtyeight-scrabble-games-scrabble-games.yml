# compiled by `hardened compile-checks` — post-publish audit of `fivethirtyeight-scrabble-games-scrabble-games`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [version_https_git_lfs_github_com_spec_v1]
checks:
  - kind: unique
    columns: [version_https_git_lfs_github_com_spec_v1]
  - kind: not_null
    col: version_https_git_lfs_github_com_spec_v1
  - kind: row_floor
    min: 1
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
