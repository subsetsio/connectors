# compiled by `hardened compile-checks` — post-publish audit of `fivethirtyeight-presidential-campaign-trail-trump`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [city, location, date, state]
checks:
  - kind: unique
    columns: [city, location, date, state]
  - kind: not_null
    col: city
  - kind: not_null
    col: location
  - kind: not_null
    col: date
  - kind: not_null
    col: state
  - kind: row_floor
    min: 53
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
