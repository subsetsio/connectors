# compiled by `hardened compile-checks` — post-publish audit of `fivethirtyeight-womens-world-cup-predictions-wwc-matches-20150613-175552`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, team1, team2, group]
temporal: date
checks:
  - kind: unique
    columns: [date, team1, team2, group]
  - kind: not_null
    col: date
  - kind: not_null
    col: team1
  - kind: not_null
    col: team2
  - kind: not_null
    col: group
  - kind: row_floor
    min: 18
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
