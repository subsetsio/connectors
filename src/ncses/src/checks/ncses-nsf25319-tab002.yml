# compiled by `hardened compile-checks` — post-publish audit of `ncses-nsf25319-tab002`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [field_medical_school_space_and_research_animal_space]
checks:
  - kind: unique
    columns: [field_medical_school_space_and_research_animal_space]
  - kind: not_null
    col: field_medical_school_space_and_research_animal_space
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
