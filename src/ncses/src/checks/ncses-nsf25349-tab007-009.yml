# compiled by `hardened compile-checks` — post-publish audit of `ncses-nsf25349-tab007-009`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: ["2021_carnegie_classification_and_baccalaureate_origin_institution"]
checks:
  - kind: unique
    columns: ["2021_carnegie_classification_and_baccalaureate_origin_institution"]
  - kind: not_null
    col: "2021_carnegie_classification_and_baccalaureate_origin_institution"
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
