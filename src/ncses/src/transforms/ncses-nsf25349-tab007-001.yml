# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ncses-nsf25349-tab007-001`. Regenerate after model-verify.
# caution: Wide NCSES source table with no scan-verified row identifier; treat rows as source cross-tab records and use the table title and column labels to determine aggregation scope before summing.
key: []
columns:
  - name: year
    type: DOUBLE
  - name: doctorate_granting_institutions_number
    type: DOUBLE
  - name: doctorate_recipients_total_number
    type: DOUBLE
  - name: doctorate_recipients_mean_per_institution
    type: DOUBLE
  - name: doctorate_recipients_median_per_institution
    type: DOUBLE
