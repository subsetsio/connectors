# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ncses-nsf25321-tab015-003`. Regenerate after model-verify.
# caution: Wide NCSES source table with no scan-verified row identifier; treat rows as source cross-tab records and use the table title and column labels to determine aggregation scope before summing.
key: []
columns:
  - name: field_of_study
    type: VARCHAR
  - name: all_employed_number
    type: DOUBLE
  - name: all_employed_se
    type: DOUBLE
  - name: primary_work_activitya_any_r_and_db_number
    type: DOUBLE
  - name: primary_work_activitya_any_r_and_db_se
    type: DOUBLE
  - name: primary_work_activitya_otherc_number
    type: DOUBLE
  - name: primary_work_activitya_otherc_se
    type: DOUBLE
