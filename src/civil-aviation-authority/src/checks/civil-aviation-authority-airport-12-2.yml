# compiled by `hardened compile-checks` — post-publish audit of `civil-aviation-authority-airport-12-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [apt1_apt_name, apt2_apt_name, rundate, grp_cd, grp_name]
checks:
  - kind: unique
    columns: [apt1_apt_name, apt2_apt_name, rundate, grp_cd, grp_name]
  - kind: not_null
    col: apt1_apt_name
  - kind: not_null
    col: apt2_apt_name
  - kind: not_null
    col: rundate
  - kind: not_null
    col: grp_cd
  - kind: not_null
    col: grp_name
  - kind: row_floor
    min: 2300
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
