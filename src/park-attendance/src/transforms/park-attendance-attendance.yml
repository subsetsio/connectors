# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `park-attendance-attendance`. Regenerate after model-verify.
# caution: Only parks with a published attendance table are represented; join to the parks table for the broader Queue Times park directory.
key: [park_id, year]
temporal: year
sort: [year, park_id]
columns:
  - name: park_id
    type: BIGINT
  - name: park_name
    type: VARCHAR
  - name: year
    type: BIGINT
  - name: annual_attendance
    type: BIGINT
