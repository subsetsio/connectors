# compiled by `hardened compile-checks` — post-publish audit of `environment-and-climate-change-canada-swob-partner-stations`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [iata_id, feature_id, msc_id, data_attribution_notice_fr, data_attribution_notice_en]
checks:
  - kind: unique
    columns: [iata_id, feature_id, msc_id, data_attribution_notice_fr, data_attribution_notice_en]
  - kind: not_null
    col: iata_id
  - kind: not_null
    col: feature_id
  - kind: not_null
    col: msc_id
  - kind: not_null
    col: data_attribution_notice_fr
  - kind: not_null
    col: data_attribution_notice_en
  - kind: row_floor
    min: 961
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
