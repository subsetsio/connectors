# compiled by `hardened compile-checks` — post-publish audit of `environment-and-climate-change-canada-ahccd-annual`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [feature_id, identifier_identifiant, station_id_id_station, province_province]
checks:
  - kind: unique
    columns: [feature_id, identifier_identifiant, station_id_id_station, province_province]
  - kind: not_null
    col: feature_id
  - kind: not_null
    col: identifier_identifiant
  - kind: not_null
    col: station_id_id_station
  - kind: not_null
    col: province_province
  - kind: row_floor
    min: 56917
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
