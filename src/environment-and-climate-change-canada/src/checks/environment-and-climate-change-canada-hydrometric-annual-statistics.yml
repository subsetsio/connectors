# compiled by `hardened compile-checks` — post-publish audit of `environment-and-climate-change-canada-hydrometric-annual-statistics`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [feature_id, identifier, station_name, station_number, prov_terr_state_loc]
temporal: max_date
checks:
  - kind: unique
    columns: [feature_id, identifier, station_name, station_number, prov_terr_state_loc]
  - kind: not_null
    col: feature_id
  - kind: not_null
    col: identifier
  - kind: not_null
    col: station_name
  - kind: not_null
    col: station_number
  - kind: not_null
    col: prov_terr_state_loc
  - kind: row_floor
    min: 100499
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
