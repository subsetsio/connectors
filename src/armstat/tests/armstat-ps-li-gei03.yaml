spec_id: "armstat-ps-li-gei03"
status: active
tests:
  - row_count: {min: 1}
    reason: every published ArmStatBank table has at least one data cell; an empty raw asset means the PX-Web select-all/export flow returned nothing.
    certainty: 85
