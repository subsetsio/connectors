# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `clio-infra-heightgini`. Regenerate after model-verify.
# caution: Each table is one Clio Infra indicator; the meaning and units of value are specific to that indicator and should not be combined across indicators without consulting the source metadata.
key: [country, year]
temporal: year
sort: [year, country]
columns:
  - name: ccode
    type: BIGINT
  - name: country
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: value
    type: DOUBLE
