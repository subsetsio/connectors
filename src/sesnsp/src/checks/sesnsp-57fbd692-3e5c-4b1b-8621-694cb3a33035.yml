# compiled by `hardened compile-checks` — post-publish audit of `sesnsp-57fbd692-3e5c-4b1b-8621-694cb3a33035`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [fecha, cve_municipio, bien_juridico_afectado, tipo_delito, subtipo_delito, modalidad]
temporal: fecha
checks:
  - kind: unique
    columns: [fecha, cve_municipio, bien_juridico_afectado, tipo_delito, subtipo_delito, modalidad]
  - kind: not_null
    col: fecha
  - kind: not_null
    col: cve_municipio
  - kind: not_null
    col: bien_juridico_afectado
  - kind: not_null
    col: tipo_delito
  - kind: not_null
    col: subtipo_delito
  - kind: not_null
    col: modalidad
  - kind: row_floor
    min: 15377964
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
