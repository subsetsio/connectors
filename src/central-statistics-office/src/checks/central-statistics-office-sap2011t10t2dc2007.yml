# compiled by `hardened compile-checks` — post-publish audit of `central-statistics-office-sap2011t10t2dc2007`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dim1_label, dim1_code, dim2_code, dim2_label, dim3_code]
temporal: period_start
checks:
  - kind: unique
    columns: [dim1_label, dim1_code, dim2_code, dim2_label, dim3_code]
  - kind: not_null
    col: dim1_label
  - kind: not_null
    col: dim1_code
  - kind: not_null
    col: dim2_code
  - kind: not_null
    col: dim2_label
  - kind: not_null
    col: dim3_code
  - kind: row_floor
    min: 129
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
