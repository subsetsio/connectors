# compiled by `hardened compile-checks` — post-publish audit of `reserve-bank-of-australia-j1-historical---brent-oil-price`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [value_text, obs_date, dimension_date, series_title, description]
temporal: obs_date
checks:
  - kind: unique
    columns: [value_text, obs_date, dimension_date, series_title, description]
  - kind: not_null
    col: value_text
  - kind: not_null
    col: obs_date
  - kind: not_null
    col: dimension_date
  - kind: not_null
    col: series_title
  - kind: not_null
    col: description
  - kind: row_floor
    min: 375
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
