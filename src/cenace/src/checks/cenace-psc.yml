# compiled by `hardened compile-checks` — post-publish audit of `cenace-psc`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sistema, proceso, reserve_zone, reserve_type, date, hour]
temporal: date
checks:
  - kind: unique
    columns: [sistema, proceso, reserve_zone, reserve_type, date, hour]
  - kind: not_null
    col: sistema
  - kind: not_null
    col: proceso
  - kind: not_null
    col: reserve_zone
  - kind: not_null
    col: reserve_type
  - kind: not_null
    col: date
  - kind: not_null
    col: hour
  - kind: row_floor
    min: 42300
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
