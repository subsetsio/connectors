# compiled by `hardened compile-checks` — post-publish audit of `nces-ipeds-c-a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [unitid, year, cipcode, majornum, awlevel]
temporal: year
checks:
  - kind: unique
    columns: [unitid, year, cipcode, majornum, awlevel]
  - kind: not_null
    col: unitid
  - kind: not_null
    col: year
  - kind: not_null
    col: cipcode
  - kind: not_null
    col: majornum
  - kind: not_null
    col: awlevel
  - kind: row_floor
    min: 3290832
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
