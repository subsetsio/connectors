# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bls-ci`. Regenerate after model-verify.
# caution: Carries both seasonally adjusted (`seasonal` = 'S') and unadjusted ('U') versions of the same underlying series; filter `seasonal` or you will mix two measurements of the same quantity.
# caution: `value` is untyped across rows: what it measures is selected by `estimate_code`, `subcell_code` and `periodicity_code` (index vs percent change). Never aggregate `value` without first pinning those dimensions to a single measure.
# caution: `industry_code`, `occupation_code` and `owner_code` each carry 'all' aggregate rows alongside their components.
key: [series_id, year, period]
temporal: period_start_date
sort: [period_start_date, series_id, year, period]
columns:
  - name: series_id
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: period
    type: VARCHAR
  - name: period_start_date
    type: DATE
  - name: value
    type: DOUBLE
  - name: footnote_codes
    type: VARCHAR
  - name: seasonal
    type: VARCHAR
  - name: seasonal_name
    type: VARCHAR
  - name: owner_code
    type: BIGINT
  - name: owner_name
    type: VARCHAR
  - name: industry_code
    type: VARCHAR
  - name: industry_name
    type: VARCHAR
  - name: occupation_code
    type: VARCHAR
  - name: occupation_name
    type: VARCHAR
  - name: subcell_code
    type: VARCHAR
  - name: subcell_name
    type: VARCHAR
  - name: area_code
    type: VARCHAR
  - name: area_name
    type: VARCHAR
  - name: periodicity_code
    type: VARCHAR
  - name: periodicity_name
    type: VARCHAR
  - name: estimate_code
    type: VARCHAR
  - name: estimate_name
    type: VARCHAR
  - name: series_title
    type: VARCHAR
