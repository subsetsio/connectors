# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bls-ei`. Regenerate after model-verify.
# caution: `value` is untyped across rows: what it measures is selected by `index_code` (which economic index). Never aggregate `value` without first pinning those dimensions to a single measure.
# caution: Index values are only comparable within one `base_period`.
key: [series_id, year, period]
temporal: period_start_date
sort: [period_start_date, series_id, year, period]
columns:
  - name: series_id
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: period
    type: VARCHAR
  - name: period_start_date
    type: DATE
  - name: value
    type: DOUBLE
  - name: footnote_codes
    type: VARCHAR
  - name: seasonal
    type: VARCHAR
  - name: seasonal_name
    type: VARCHAR
  - name: index_code
    type: VARCHAR
  - name: index_name
    type: VARCHAR
  - name: series_name
    type: VARCHAR
  - name: base_period
    type: VARCHAR
  - name: series_title
    type: VARCHAR
