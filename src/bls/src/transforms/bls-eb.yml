# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `bls-eb`. Regenerate after model-verify.
# caution: `value` is untyped across rows: what it measures is selected by `type_code` and `title_code` (the benefit provision measured). Never aggregate `value` without first pinning those dimensions to a single measure.
key: [series_id, year, period]
temporal: period_start_date
sort: [period_start_date, series_id, year, period]
columns:
  - name: series_id
    type: VARCHAR
  - name: year
    type: INTEGER
  - name: period
    type: VARCHAR
  - name: period_start_date
    type: DATE
  - name: value
    type: DOUBLE
  - name: footnote_codes
    type: VARCHAR
  - name: title_code
    type: VARCHAR
  - name: title_name
    type: VARCHAR
  - name: type_code
    type: VARCHAR
  - name: type_name
    type: VARCHAR
