# compiled by `hardened compile-checks` — post-publish audit of `malaria-atlas-project-malaria:202406-global-pf-parasite-rate-surveys`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [feature_id, id, site_id, title1, citation1]
temporal: time_start
checks:
  - kind: unique
    columns: [feature_id, id, site_id, title1, citation1]
  - kind: not_null
    col: feature_id
  - kind: not_null
    col: id
  - kind: not_null
    col: site_id
  - kind: not_null
    col: title1
  - kind: not_null
    col: citation1
  - kind: row_floor
    min: 28830
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
