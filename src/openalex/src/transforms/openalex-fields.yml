# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `openalex-fields`. Regenerate after model-verify.
# caution: Domain columns are denormalized on each field row; do not sum field and domain rows together as separate hierarchy levels.
key: [id]
temporal: updated_date
sort: [updated_date, id]
columns:
  - name: id
    type: BIGINT
  - name: display_name
    type: VARCHAR
  - name: description
    type: VARCHAR
  - name: domain_id
    type: BIGINT
  - name: domain_name
    type: VARCHAR
  - name: wikidata
    type: VARCHAR
  - name: works_count
    type: BIGINT
  - name: cited_by_count
    type: BIGINT
  - name: updated_date
    type: TIMESTAMP
