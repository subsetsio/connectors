# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `openalex-works-by-dimension-year`. Regenerate after model-verify.
# caution: Each dimension is an independent group-by of works, so counts across different dimension values or dimension families are not additive.
# caution: Several dimension families are limited by the OpenAlex group_by top-200 result cap for each year; absence from a year does not mean zero works.
key: [publication_year, dimension_label, dimension_key, dimension]
temporal: publication_year
sort: [publication_year, dimension_label, dimension_key, dimension]
columns:
  - name: dimension
    type: VARCHAR
  - name: dimension_key
    type: VARCHAR
  - name: dimension_label
    type: VARCHAR
  - name: publication_year
    type: BIGINT
  - name: works_count
    type: BIGINT
