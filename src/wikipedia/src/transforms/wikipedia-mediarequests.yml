# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `wikipedia-mediarequests`. Regenerate after model-verify.
# caution: This is a global Wikimedia media-request series, not scoped to individual wiki projects.
key: [date, media_type]
temporal: date
sort: [date, media_type]
columns:
  - name: media_type
    type: VARCHAR
  - name: date
    type: DATE
  - name: requests
    type: BIGINT
