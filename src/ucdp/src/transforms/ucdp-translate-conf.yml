# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `ucdp-translate-conf`. Regenerate after model-verify.
key: [new_id, old_id]
sort: [new_id, old_id]
columns:
  - name: new_id
    type: BIGINT
  - name: old_id
    type: VARCHAR
