# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dallas-fed-agsurvey`. Regenerate after model-verify.
# caution: Rows span multiple survey topics and workbook sheets; filter topic and sheet before comparing or aggregating values.
key: [date, series, sheet, topic]
temporal: date
sort: [date, series, sheet, topic]
columns:
  - name: date
    type: DATE
  - name: topic
    type: VARCHAR
  - name: sheet
    type: VARCHAR
  - name: series
    type: VARCHAR
  - name: value
    type: DOUBLE
