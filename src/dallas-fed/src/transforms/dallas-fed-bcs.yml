# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dallas-fed-bcs`. Regenerate after model-verify.
# caution: Rows include multiple reported components for each survey question; filter component before comparing values.
key: [date, series_code, component]
temporal: date
sort: [date, series_code, component]
columns:
  - name: date
    type: DATE
  - name: segment
    type: VARCHAR
  - name: basis
    type: VARCHAR
  - name: series_code
    type: VARCHAR
  - name: component
    type: VARCHAR
  - name: value
    type: DOUBLE
