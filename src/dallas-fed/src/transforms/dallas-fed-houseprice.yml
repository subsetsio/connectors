# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `dallas-fed-houseprice`. Regenerate after model-verify.
# caution: Rows include several metrics per country and date; filter metric before comparing country series.
key: [date, country, metric]
temporal: date
sort: [date, country, metric]
columns:
  - name: date
    type: DATE
  - name: metric
    type: VARCHAR
  - name: country
    type: VARCHAR
  - name: value
    type: DOUBLE
