# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-627w-fm4v`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month_of_incident, month_of_publication]
checks:
  - kind: unique
    columns: [month_of_incident, month_of_publication]
  - kind: not_null
    col: month_of_incident
  - kind: not_null
    col: month_of_publication
  - kind: row_floor
    min: 7
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
