# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-s85w-az69`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [dollars, state, year, action, status]
temporal: year
checks:
  - kind: unique
    columns: [dollars, state, year, action, status]
  - kind: not_null
    col: dollars
  - kind: not_null
    col: state
  - kind: not_null
    col: year
  - kind: not_null
    col: action
  - kind: not_null
    col: status
  - kind: row_floor
    min: 4462
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
