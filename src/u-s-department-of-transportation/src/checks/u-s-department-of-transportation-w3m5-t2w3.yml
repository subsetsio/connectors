# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-w3m5-t2w3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [pct_ttl_num_trip, sum_min, the_geom, lat, lon]
temporal: year
checks:
  - kind: unique
    columns: [pct_ttl_num_trip, sum_min, the_geom, lat, lon]
  - kind: not_null
    col: pct_ttl_num_trip
  - kind: not_null
    col: sum_min
  - kind: not_null
    col: the_geom
  - kind: not_null
    col: lat
  - kind: not_null
    col: lon
  - kind: row_floor
    min: 230159
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
