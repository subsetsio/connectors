# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-bkqm-dpy8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [rmc_l_system_length, urban_code, county_code, state_code, ownership]
checks:
  - kind: unique
    columns: [rmc_l_system_length, urban_code, county_code, state_code, ownership]
  - kind: not_null
    col: rmc_l_system_length
  - kind: not_null
    col: urban_code
  - kind: not_null
    col: county_code
  - kind: not_null
    col: state_code
  - kind: not_null
    col: ownership
  - kind: row_floor
    min: 12908
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
