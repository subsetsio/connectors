# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-h4kh-i7b7`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [road_event_id, end_date, beginning_cross_street, start_date, lanes]
temporal: feed_update_date
checks:
  - kind: unique
    columns: [road_event_id, end_date, beginning_cross_street, start_date, lanes]
  - kind: not_null
    col: road_event_id
  - kind: not_null
    col: end_date
  - kind: not_null
    col: beginning_cross_street
  - kind: not_null
    col: start_date
  - kind: not_null
    col: lanes
  - kind: row_floor
    min: 1090
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
