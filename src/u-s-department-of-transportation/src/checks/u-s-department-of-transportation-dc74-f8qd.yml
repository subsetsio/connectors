# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-dc74-f8qd`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [current_ridership, date, week_number, agency]
temporal: date
checks:
  - kind: unique
    columns: [current_ridership, date, week_number, agency]
  - kind: not_null
    col: current_ridership
  - kind: not_null
    col: date
  - kind: not_null
    col: week_number
  - kind: not_null
    col: agency
  - kind: row_floor
    min: 105
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
