# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-xe7p-6h9t`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [vehicles, passengers, state, year]
temporal: year
checks:
  - kind: unique
    columns: [vehicles, passengers, state, year]
  - kind: not_null
    col: vehicles
  - kind: not_null
    col: passengers
  - kind: not_null
    col: state
  - kind: not_null
    col: year
  - kind: row_floor
    min: 34
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
