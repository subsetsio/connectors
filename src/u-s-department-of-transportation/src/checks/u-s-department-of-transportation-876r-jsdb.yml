# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-876r-jsdb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [insp_violation_id, inspection_id, change_date, viol_code, viol_desc]
checks:
  - kind: unique
    columns: [insp_violation_id, inspection_id, change_date, viol_code, viol_desc]
  - kind: not_null
    col: insp_violation_id
  - kind: not_null
    col: inspection_id
  - kind: not_null
    col: change_date
  - kind: not_null
    col: viol_code
  - kind: not_null
    col: viol_desc
  - kind: row_floor
    min: 6671775
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
