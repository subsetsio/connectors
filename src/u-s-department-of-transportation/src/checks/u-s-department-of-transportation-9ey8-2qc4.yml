# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-9ey8-2qc4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [data, report_year, year, item, vehicle_type]
temporal: year
checks:
  - kind: unique
    columns: [data, report_year, year, item, vehicle_type]
  - kind: not_null
    col: data
  - kind: not_null
    col: report_year
  - kind: not_null
    col: year
  - kind: not_null
    col: item
  - kind: not_null
    col: vehicle_type
  - kind: row_floor
    min: 2240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
