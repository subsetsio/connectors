# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-iiy2-kmkn`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, all_u_s_container_ports, all_other_ports, los_angeles_long_beach, savannah]
temporal: date
checks:
  - kind: unique
    columns: [date, all_u_s_container_ports, all_other_ports, los_angeles_long_beach, savannah]
  - kind: not_null
    col: date
  - kind: not_null
    col: all_u_s_container_ports
  - kind: not_null
    col: all_other_ports
  - kind: not_null
    col: los_angeles_long_beach
  - kind: not_null
    col: savannah
  - kind: row_floor
    min: 56
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
