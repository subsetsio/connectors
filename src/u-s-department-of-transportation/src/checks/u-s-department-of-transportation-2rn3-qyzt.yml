# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-2rn3-qyzt`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sample_id, section_id, fut_aadt, end_lrs, aadt]
temporal: year
checks:
  - kind: unique
    columns: [sample_id, section_id, fut_aadt, end_lrs, aadt]
  - kind: not_null
    col: sample_id
  - kind: not_null
    col: section_id
  - kind: not_null
    col: fut_aadt
  - kind: not_null
    col: end_lrs
  - kind: not_null
    col: aadt
  - kind: row_floor
    min: 55949
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
