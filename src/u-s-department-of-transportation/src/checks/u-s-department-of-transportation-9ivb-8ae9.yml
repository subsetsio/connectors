# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-9ivb-8ae9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [incident_description, incident_number, incident_time, incident_date, agency]
temporal: incident_date
checks:
  - kind: unique
    columns: [incident_description, incident_number, incident_time, incident_date, agency]
  - kind: not_null
    col: incident_description
  - kind: not_null
    col: incident_number
  - kind: not_null
    col: incident_time
  - kind: not_null
    col: incident_date
  - kind: not_null
    col: agency
  - kind: row_floor
    min: 56754
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
