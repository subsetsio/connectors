# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-6t2m-6b8t`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [percent_commercial, volume, tmc, time_bin, day_of_week]
checks:
  - kind: unique
    columns: [percent_commercial, volume, tmc, time_bin, day_of_week]
  - kind: not_null
    col: percent_commercial
  - kind: not_null
    col: volume
  - kind: not_null
    col: tmc
  - kind: not_null
    col: time_bin
  - kind: not_null
    col: day_of_week
  - kind: row_floor
    min: 993216
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
