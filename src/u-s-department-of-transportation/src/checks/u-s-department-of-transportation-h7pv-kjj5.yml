# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-h7pv-kjj5`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [week_change, week_ending, week_number, mode, indicator]
temporal: week_ending
checks:
  - kind: unique
    columns: [week_change, week_ending, week_number, mode, indicator]
  - kind: not_null
    col: week_change
  - kind: not_null
    col: week_ending
  - kind: not_null
    col: week_number
  - kind: not_null
    col: mode
  - kind: not_null
    col: indicator
  - kind: row_floor
    min: 220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
