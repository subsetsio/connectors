# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-7kx6-58yb`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [natroute_id, end_point, route_id, begin_point, section_length]
checks:
  - kind: unique
    columns: [natroute_id, end_point, route_id, begin_point, section_length]
  - kind: not_null
    col: natroute_id
  - kind: not_null
    col: end_point
  - kind: not_null
    col: route_id
  - kind: not_null
    col: begin_point
  - kind: not_null
    col: section_length
  - kind: row_floor
    min: 17137
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
