# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-p7d5-hrve`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tmstp_utc, latitude, msg_milliseconds_utc, request_milliseconds_utc, longitude]
temporal: tmstp_utc
checks:
  - kind: unique
    columns: [tmstp_utc, latitude, msg_milliseconds_utc, request_milliseconds_utc, longitude]
  - kind: not_null
    col: tmstp_utc
  - kind: not_null
    col: latitude
  - kind: not_null
    col: msg_milliseconds_utc
  - kind: not_null
    col: request_milliseconds_utc
  - kind: not_null
    col: longitude
  - kind: row_floor
    min: 18446
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
