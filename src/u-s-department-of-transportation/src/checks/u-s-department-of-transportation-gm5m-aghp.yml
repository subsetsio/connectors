# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-gm5m-aghp`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [facility_name, facility_id, agency_name, ntd_id, year_built_or_reconstructed]
temporal: condition_assessment_date
checks:
  - kind: unique
    columns: [facility_name, facility_id, agency_name, ntd_id, year_built_or_reconstructed]
  - kind: not_null
    col: facility_name
  - kind: not_null
    col: facility_id
  - kind: not_null
    col: agency_name
  - kind: not_null
    col: ntd_id
  - kind: not_null
    col: year_built_or_reconstructed
  - kind: row_floor
    min: 7268
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
