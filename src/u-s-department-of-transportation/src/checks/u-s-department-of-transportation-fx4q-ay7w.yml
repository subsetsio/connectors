# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-fx4q-ay7w`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [report_number, inspection_id, snet_sequence_id, insp_carrier_name, insp_carrier_street]
temporal: insp_date
checks:
  - kind: unique
    columns: [report_number, inspection_id, snet_sequence_id, insp_carrier_name, insp_carrier_street]
  - kind: not_null
    col: report_number
  - kind: not_null
    col: inspection_id
  - kind: not_null
    col: snet_sequence_id
  - kind: not_null
    col: insp_carrier_name
  - kind: not_null
    col: insp_carrier_street
  - kind: row_floor
    min: 4146082
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
