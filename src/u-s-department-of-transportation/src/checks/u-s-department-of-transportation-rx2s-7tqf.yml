# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-rx2s-7tqf`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [id, startdate, organizationtypeid]
temporal: startdate
checks:
  - kind: unique
    columns: [id, startdate, organizationtypeid]
  - kind: not_null
    col: id
  - kind: not_null
    col: startdate
  - kind: not_null
    col: organizationtypeid
  - kind: row_floor
    min: 7899
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
