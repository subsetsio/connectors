# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-8mt8-2mdr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [unique_id, dot_number, viol_code, section_desc, insp_date]
checks:
  - kind: unique
    columns: [unique_id, dot_number, viol_code, section_desc, insp_date]
  - kind: not_null
    col: unique_id
  - kind: not_null
    col: dot_number
  - kind: not_null
    col: viol_code
  - kind: not_null
    col: section_desc
  - kind: not_null
    col: insp_date
  - kind: row_floor
    min: 3371418
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
