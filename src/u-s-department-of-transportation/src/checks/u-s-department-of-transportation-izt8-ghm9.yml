# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-izt8-ghm9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [miles, state, year, ownership, area]
temporal: year
checks:
  - kind: unique
    columns: [miles, state, year, ownership, area]
  - kind: not_null
    col: miles
  - kind: not_null
    col: state
  - kind: not_null
    col: year
  - kind: not_null
    col: ownership
  - kind: not_null
    col: area
  - kind: row_floor
    min: 6578
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
