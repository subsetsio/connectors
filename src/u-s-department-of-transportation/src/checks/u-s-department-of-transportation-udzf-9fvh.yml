# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-udzf-9fvh`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [primary_key, total, scheduled, charter, fg_apt_id]
temporal: data_dte
checks:
  - kind: unique
    columns: [primary_key, total, scheduled, charter, fg_apt_id]
  - kind: not_null
    col: primary_key
  - kind: not_null
    col: total
  - kind: not_null
    col: scheduled
  - kind: not_null
    col: charter
  - kind: not_null
    col: fg_apt_id
  - kind: row_floor
    min: 1749847
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
