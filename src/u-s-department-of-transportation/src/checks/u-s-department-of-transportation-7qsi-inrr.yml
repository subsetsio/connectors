# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-7qsi-inrr`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [zone_id, display_name, location_description, latitude, longitude]
checks:
  - kind: unique
    columns: [zone_id, display_name, location_description, latitude, longitude]
  - kind: not_null
    col: zone_id
  - kind: not_null
    col: display_name
  - kind: not_null
    col: location_description
  - kind: not_null
    col: latitude
  - kind: not_null
    col: longitude
  - kind: row_floor
    min: 10
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
