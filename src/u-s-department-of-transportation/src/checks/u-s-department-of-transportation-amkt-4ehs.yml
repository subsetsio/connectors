# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-amkt-4ehs`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [vehicle_passenger_car_revenue, vehicle_passenger_car_miles, agency, ntd_id, agency_voms]
checks:
  - kind: unique
    columns: [vehicle_passenger_car_revenue, vehicle_passenger_car_miles, agency, ntd_id, agency_voms]
  - kind: not_null
    col: vehicle_passenger_car_revenue
  - kind: not_null
    col: vehicle_passenger_car_miles
  - kind: not_null
    col: agency
  - kind: not_null
    col: ntd_id
  - kind: not_null
    col: agency_voms
  - kind: row_floor
    min: 1865
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
