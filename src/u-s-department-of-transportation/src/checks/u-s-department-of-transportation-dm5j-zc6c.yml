# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-dm5j-zc6c`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [docket_number, usdot_number, op_auth_type, reason, op_auth_status]
temporal: status_change_date
checks:
  - kind: unique
    columns: [docket_number, usdot_number, op_auth_type, reason, op_auth_status]
  - kind: not_null
    col: docket_number
  - kind: not_null
    col: usdot_number
  - kind: not_null
    col: op_auth_type
  - kind: not_null
    col: reason
  - kind: not_null
    col: op_auth_status
  - kind: row_floor
    min: 65
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
