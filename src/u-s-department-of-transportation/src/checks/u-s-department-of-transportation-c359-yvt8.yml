# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-c359-yvt8`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sample_id, natroute_id, route_id, begin_point, end_point]
checks:
  - kind: unique
    columns: [sample_id, natroute_id, route_id, begin_point, end_point]
  - kind: not_null
    col: sample_id
  - kind: not_null
    col: natroute_id
  - kind: not_null
    col: route_id
  - kind: not_null
    col: begin_point
  - kind: not_null
    col: end_point
  - kind: row_floor
    min: 66710
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
