# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-vhz2-exyi`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [pos_x_av_m, distance_av_headway, heading_av_m, pos_y_av_m, closest_distance_longitudinal]
temporal: date
checks:
  - kind: unique
    columns: [pos_x_av_m, distance_av_headway, heading_av_m, pos_y_av_m, closest_distance_longitudinal]
  - kind: not_null
    col: pos_x_av_m
  - kind: not_null
    col: distance_av_headway
  - kind: not_null
    col: heading_av_m
  - kind: not_null
    col: pos_y_av_m
  - kind: not_null
    col: closest_distance_longitudinal
  - kind: row_floor
    min: 1265077
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
