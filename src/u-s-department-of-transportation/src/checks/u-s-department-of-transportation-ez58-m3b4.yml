# compiled by `hardened compile-checks` — post-publish audit of `u-s-department-of-transportation-ez58-m3b4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [tt_percentile_75, tt_percentile_50, tt_percentile_25, origin_county_fips, dest_county_fips]
temporal: year
checks:
  - kind: unique
    columns: [tt_percentile_75, tt_percentile_50, tt_percentile_25, origin_county_fips, dest_county_fips]
  - kind: not_null
    col: tt_percentile_75
  - kind: not_null
    col: tt_percentile_50
  - kind: not_null
    col: tt_percentile_25
  - kind: not_null
    col: origin_county_fips
  - kind: not_null
    col: dest_county_fips
  - kind: row_floor
    min: 1821075
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
