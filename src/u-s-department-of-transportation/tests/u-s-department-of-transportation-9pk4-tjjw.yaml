spec_id: u-s-department-of-transportation-9pk4-tjjw
status: active
tests:
  - row_count: {min: 1}
    reason: this Socrata dataset returned rows when probed during research; an empty pull means SODA pagination broke or the dataset was withdrawn upstream.
    certainty: 80
    points_outward: true
    severity: warn
