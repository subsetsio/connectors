# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-health-20care-11-mortality-by-causes-of-death`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, causes_of_death, gender]
checks:
  - kind: unique
    columns: [year, causes_of_death, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: causes_of_death
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 567
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
