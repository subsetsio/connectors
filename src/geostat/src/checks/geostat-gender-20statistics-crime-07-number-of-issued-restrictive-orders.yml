# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-crime-07-number-of-issued-restrictive-orders`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, number_of_issued_restrictive_orders]
checks:
  - kind: unique
    columns: [year, number_of_issued_restrictive_orders]
  - kind: not_null
    col: year
  - kind: not_null
    col: number_of_issued_restrictive_orders
  - kind: row_floor
    min: 18
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
