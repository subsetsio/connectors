# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-households-one-person-households-by-regions-age-and-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, regions, sex]
checks:
  - kind: unique
    columns: [age, regions, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: regions
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 331
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
