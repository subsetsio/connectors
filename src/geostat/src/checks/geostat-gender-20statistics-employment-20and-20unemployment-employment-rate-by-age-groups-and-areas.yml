# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-employment-20and-20unemployment-employment-rate-by-age-groups-and-areas`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, age_group, sex, urban_rural_areas]
checks:
  - kind: unique
    columns: [years, age_group, sex, urban_rural_areas]
  - kind: not_null
    col: years
  - kind: not_null
    col: age_group
  - kind: not_null
    col: sex
  - kind: not_null
    col: urban_rural_areas
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
