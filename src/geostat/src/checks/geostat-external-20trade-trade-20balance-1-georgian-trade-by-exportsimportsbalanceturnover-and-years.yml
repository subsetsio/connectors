# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-trade-20balance-1-georgian-trade-by-exportsimportsbalanceturnover-and-years`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, exports_imports_balance_turnover]
checks:
  - kind: unique
    columns: [years, exports_imports_balance_turnover]
  - kind: not_null
    col: years
  - kind: not_null
    col: exports_imports_balance_turnover
  - kind: row_floor
    min: 50
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
