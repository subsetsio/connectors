# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-social-20protection-2-number-of-state-pension-recepients-by-region-1990-2011`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, region, gender]
checks:
  - kind: unique
    columns: [year, region, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: region
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 182
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
