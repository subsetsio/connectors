# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-households-02-distribution-of-households-by-gender-of-head-of-household-in-urban-an`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, urban_rural, gender_of_household_head]
checks:
  - kind: unique
    columns: [year, urban_rural, gender_of_household_head]
  - kind: not_null
    col: year
  - kind: not_null
    col: urban_rural
  - kind: not_null
    col: gender_of_household_head
  - kind: row_floor
    min: 78
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
