# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-living-20conditions-9`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region, number_of_room, size_of_private_households, urban_rural]
checks:
  - kind: unique
    columns: [region, number_of_room, size_of_private_households, urban_rural]
  - kind: not_null
    col: region
  - kind: not_null
    col: number_of_room
  - kind: not_null
    col: size_of_private_households
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 624
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
