# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-employment-20and-20unemployment-distribution-of-employed-persons`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, status, gender, unit]
checks:
  - kind: unique
    columns: [year, status, gender, unit]
  - kind: not_null
    col: year
  - kind: not_null
    col: status
  - kind: not_null
    col: gender
  - kind: not_null
    col: unit
  - kind: row_floor
    min: 24
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
