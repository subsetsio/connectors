# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-divorces-43-divorces-by-marriage-duration`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, duration_of_marriage_year]
checks:
  - kind: unique
    columns: [year, duration_of_marriage_year]
  - kind: not_null
    col: year
  - kind: not_null
    col: duration_of_marriage_year
  - kind: row_floor
    min: 176
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
