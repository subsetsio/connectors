# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-environmental-20indicators-13-h-3`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, fuel_types, motor_vehicle_cars]
checks:
  - kind: unique
    columns: [year, fuel_types, motor_vehicle_cars]
  - kind: not_null
    col: year
  - kind: not_null
    col: fuel_types
  - kind: not_null
    col: motor_vehicle_cars
  - kind: row_floor
    min: 1050
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
