# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-labour-lfs-by-regions`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, economic_status, regions]
checks:
  - kind: unique
    columns: [years, economic_status, regions]
  - kind: not_null
    col: years
  - kind: not_null
    col: economic_status
  - kind: not_null
    col: regions
  - kind: row_floor
    min: 1644
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
