# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-production-20value-production-value-by-kind-of-ec-act`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, kind_of_economic_activity]
checks:
  - kind: unique
    columns: [period, kind_of_economic_activity]
  - kind: not_null
    col: period
  - kind: not_null
    col: kind_of_economic_activity
  - kind: row_floor
    min: 93
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
