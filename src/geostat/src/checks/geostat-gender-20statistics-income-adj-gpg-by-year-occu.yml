# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-income-adj-gpg-by-year-occu`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [occupation, years, adjusted_gpg]
checks:
  - kind: unique
    columns: [occupation, years, adjusted_gpg]
  - kind: not_null
    col: occupation
  - kind: not_null
    col: years
  - kind: not_null
    col: adjusted_gpg
  - kind: row_floor
    min: 30
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
