# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-living-20conditions-2c-20subsistence-20minimum-relative-poverty`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, area, threshold]
checks:
  - kind: unique
    columns: [year, area, threshold]
  - kind: not_null
    col: year
  - kind: not_null
    col: area
  - kind: not_null
    col: threshold
  - kind: row_floor
    min: 66
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
