# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-cpi-harmonised-20consumer-20price-20index-ecoicop-20rev-2-hicp-in-georgia-to-the-same-month-of-the-previous-year-orenovani2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [groups, month]
checks:
  - kind: unique
    columns: [groups, month]
  - kind: not_null
    col: groups
  - kind: not_null
    col: month
  - kind: row_floor
    min: 42
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
