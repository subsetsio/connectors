# compiled by `hardened compile-checks` — post-publish audit of `geostat-industry-industry-20by-20kind-20of-20economic-20activity-20nace-20rev-2-value-20added-value-added-by-kind-of-economic-activity`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [kind_of_economic_activity, period]
checks:
  - kind: unique
    columns: [kind_of_economic_activity, period]
  - kind: not_null
    col: kind_of_economic_activity
  - kind: not_null
    col: period
  - kind: row_floor
    min: 332
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
