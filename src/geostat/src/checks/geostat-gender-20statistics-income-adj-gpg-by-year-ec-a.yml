# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-income-adj-gpg-by-year-ec-a`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, economic_activity, adjusted_gender_pay_gap]
checks:
  - kind: unique
    columns: [years, economic_activity, adjusted_gender_pay_gap]
  - kind: not_null
    col: years
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: adjusted_gender_pay_gap
  - kind: row_floor
    min: 15
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
