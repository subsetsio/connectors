# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-migration-49-9-citizenship-loss-by-citizenship`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [new_country_of_citizenship, year]
checks:
  - kind: unique
    columns: [new_country_of_citizenship, year]
  - kind: not_null
    col: new_country_of_citizenship
  - kind: not_null
    col: year
  - kind: row_floor
    min: 80
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
