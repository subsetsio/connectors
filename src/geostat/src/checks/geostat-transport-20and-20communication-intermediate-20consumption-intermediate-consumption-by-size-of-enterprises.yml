# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-intermediate-20consumption-intermediate-consumption-by-size-of-enterprises`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [petriod, enterprise_size]
checks:
  - kind: unique
    columns: [petriod, enterprise_size]
  - kind: not_null
    col: petriod
  - kind: not_null
    col: enterprise_size
  - kind: row_floor
    min: 38
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
