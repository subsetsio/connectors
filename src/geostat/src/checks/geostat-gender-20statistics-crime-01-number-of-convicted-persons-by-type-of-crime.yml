# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-crime-01-number-of-convicted-persons-by-type-of-crime`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, type_of_crimes, gender]
checks:
  - kind: unique
    columns: [year, type_of_crimes, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: type_of_crimes
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 448
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
