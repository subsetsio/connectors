# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-crime-12-number-of-persons-injured-or-killed-by-road-accidents`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region, year, gender, status]
checks:
  - kind: unique
    columns: [region, year, gender, status]
  - kind: not_null
    col: region
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: status
  - kind: row_floor
    min: 220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
