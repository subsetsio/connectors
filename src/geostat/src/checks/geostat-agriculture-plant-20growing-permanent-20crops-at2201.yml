# compiled by `hardened compile-checks` — post-publish audit of `geostat-agriculture-plant-20growing-permanent-20crops-at2201`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, regiones, fruit_type]
checks:
  - kind: unique
    columns: [year, regiones, fruit_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: regiones
  - kind: not_null
    col: fruit_type
  - kind: row_floor
    min: 780
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
