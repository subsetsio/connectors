# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-health-morbidity-by-some-infectious`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [by_years, infectious_diseases, population_groups]
checks:
  - kind: unique
    columns: [by_years, infectious_diseases, population_groups]
  - kind: not_null
    col: by_years
  - kind: not_null
    col: infectious_diseases
  - kind: not_null
    col: population_groups
  - kind: row_floor
    min: 225
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
