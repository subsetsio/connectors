# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-sport-20statistics-number-of-medals-won-in-olympic`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [sports, year, medals, age, sex]
checks:
  - kind: unique
    columns: [sports, year, medals, age, sex]
  - kind: not_null
    col: sports
  - kind: not_null
    col: year
  - kind: not_null
    col: medals
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1890
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
