# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-disability-50-distribution-of-population-by-age-health-related-limitations-and-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, disability_categories, health_related_limitations, sex]
checks:
  - kind: unique
    columns: [age, disability_categories, health_related_limitations, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: disability_categories
  - kind: not_null
    col: health_related_limitations
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 1386
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
