# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-imports-by-20countries-2-import-countries-1995-2019`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [countries, years, months]
checks:
  - kind: unique
    columns: [countries, years, months]
  - kind: not_null
    col: countries
  - kind: not_null
    col: years
  - kind: not_null
    col: months
  - kind: row_floor
    min: 32337
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
