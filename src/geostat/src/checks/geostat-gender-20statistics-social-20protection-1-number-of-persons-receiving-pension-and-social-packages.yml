# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-social-20protection-1-number-of-persons-receiving-pension-and-social-packages`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, category, sex]
checks:
  - kind: unique
    columns: [year, category, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: category
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 154
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
