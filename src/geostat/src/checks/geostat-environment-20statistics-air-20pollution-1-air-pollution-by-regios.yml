# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-air-20pollution-1-air-pollution-by-regios`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, regions, pollution]
checks:
  - kind: unique
    columns: [year, regions, pollution]
  - kind: not_null
    col: year
  - kind: not_null
    col: regions
  - kind: not_null
    col: pollution
  - kind: row_floor
    min: 216
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
