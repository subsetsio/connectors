# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-economic-20characteristics-4-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [activity_status, marital_status, urban_rural]
checks:
  - kind: unique
    columns: [activity_status, marital_status, urban_rural]
  - kind: not_null
    col: activity_status
  - kind: not_null
    col: marital_status
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 45
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
