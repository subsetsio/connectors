# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-international-20migration-26-population-by-citizenship-country-and-age`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, citizenship, sex]
checks:
  - kind: unique
    columns: [age, citizenship, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: citizenship
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 541
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
