# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-employment-20and-20unemployment-05-employed-and-unemployed-by-education-attainment`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, education_level, status, gender]
checks:
  - kind: unique
    columns: [year, education_level, status, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: education_level
  - kind: not_null
    col: status
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
