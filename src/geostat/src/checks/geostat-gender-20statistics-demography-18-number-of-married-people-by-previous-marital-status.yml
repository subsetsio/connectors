# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-demography-18-number-of-married-people-by-previous-marital-status`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, status, gender]
checks:
  - kind: unique
    columns: [year, status, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: status
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 140
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
