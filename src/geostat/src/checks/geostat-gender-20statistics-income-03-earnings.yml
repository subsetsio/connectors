# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-income-03-earnings`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [type_of_activitie, year, gender]
checks:
  - kind: unique
    columns: [type_of_activitie, year, gender]
  - kind: not_null
    col: type_of_activitie
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 220
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
