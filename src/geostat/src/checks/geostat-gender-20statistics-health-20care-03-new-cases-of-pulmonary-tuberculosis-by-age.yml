# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-health-20care-03-new-cases-of-pulmonary-tuberculosis-by-age`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age, type, gender]
checks:
  - kind: unique
    columns: [year, age, type, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: type
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 240
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
