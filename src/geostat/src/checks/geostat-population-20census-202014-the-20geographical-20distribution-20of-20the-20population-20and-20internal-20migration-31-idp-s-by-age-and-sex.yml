# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-the-20geographical-20distribution-20of-20the-20population-20and-20internal-20migration-31-idp-s-by-age-and-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, sex, urban_rural]
checks:
  - kind: unique
    columns: [age, sex, urban_rural]
  - kind: not_null
    col: age
  - kind: not_null
    col: sex
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 103
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
