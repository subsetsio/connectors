# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-demography-06-live-births-by-age-of-mother`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age_of_mother, gender]
checks:
  - kind: unique
    columns: [year, age_of_mother, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: age_of_mother
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 168
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
