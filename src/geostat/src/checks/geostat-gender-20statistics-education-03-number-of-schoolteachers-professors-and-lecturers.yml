# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-education-03-number-of-schoolteachers-professors-and-lecturers`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, institutions, sex]
checks:
  - kind: unique
    columns: [years, institutions, sex]
  - kind: not_null
    col: years
  - kind: not_null
    col: institutions
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 620
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
