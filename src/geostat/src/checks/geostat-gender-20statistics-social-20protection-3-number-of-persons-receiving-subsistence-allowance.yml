# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-social-20protection-3-number-of-persons-receiving-subsistence-allowance`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region, year, gender]
checks:
  - kind: unique
    columns: [region, year, gender]
  - kind: not_null
    col: region
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 486
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
