# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-demography-19-number-of-married-and-divorced-people-by-age`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age, gender, maritial_status]
checks:
  - kind: unique
    columns: [year, age, gender, maritial_status]
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: gender
  - kind: not_null
    col: maritial_status
  - kind: row_floor
    min: 448
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
