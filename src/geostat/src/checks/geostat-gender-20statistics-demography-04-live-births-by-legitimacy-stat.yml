# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-demography-04-live-births-by-legitimacy-stat`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, marital_status, gender]
checks:
  - kind: unique
    columns: [year, marital_status, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: marital_status
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 157
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
