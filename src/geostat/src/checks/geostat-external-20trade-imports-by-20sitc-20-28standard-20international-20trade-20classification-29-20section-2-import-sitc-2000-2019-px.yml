# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-imports-by-20sitc-20-28standard-20international-20trade-20classification-29-20section-2-import-sitc-2000-2019-px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, months, sitc_section]
checks:
  - kind: unique
    columns: [years, months, sitc_section]
  - kind: not_null
    col: years
  - kind: not_null
    col: months
  - kind: not_null
    col: sitc_section
  - kind: row_floor
    min: 1430
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
