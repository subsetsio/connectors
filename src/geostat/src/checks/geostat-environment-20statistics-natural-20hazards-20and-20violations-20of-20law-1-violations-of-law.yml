# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-natural-20hazards-20and-20violations-20of-20law-1-violations-of-law`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [violation_types, regions, year]
checks:
  - kind: unique
    columns: [violation_types, regions, year]
  - kind: not_null
    col: violation_types
  - kind: not_null
    col: regions
  - kind: not_null
    col: year
  - kind: row_floor
    min: 702
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
