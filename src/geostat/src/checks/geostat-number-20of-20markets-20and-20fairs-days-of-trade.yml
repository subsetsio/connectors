# compiled by `hardened compile-checks` — post-publish audit of `geostat-number-20of-20markets-20and-20fairs-days-of-trade`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [thursday_saturday_sunday, period]
checks:
  - kind: unique
    columns: [thursday_saturday_sunday, period]
  - kind: not_null
    col: thursday_saturday_sunday
  - kind: not_null
    col: period
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
