# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-education-professional-staff`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, gender, type_of_educational_institution]
checks:
  - kind: unique
    columns: [years, gender, type_of_educational_institution]
  - kind: not_null
    col: years
  - kind: not_null
    col: gender
  - kind: not_null
    col: type_of_educational_institution
  - kind: row_floor
    min: 26
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
