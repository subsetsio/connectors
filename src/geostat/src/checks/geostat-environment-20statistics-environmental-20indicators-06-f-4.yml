# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-environmental-20indicators-06-f-4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, pesticide_consumption_import_minus_export]
checks:
  - kind: unique
    columns: [year, pesticide_consumption_import_minus_export]
  - kind: not_null
    col: year
  - kind: not_null
    col: pesticide_consumption_import_minus_export
  - kind: row_floor
    min: 142
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
