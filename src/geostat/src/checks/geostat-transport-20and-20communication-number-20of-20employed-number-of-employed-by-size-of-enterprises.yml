# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-number-20of-20employed-number-of-employed-by-size-of-enterprises`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [periopd, enterprise_size]
checks:
  - kind: unique
    columns: [periopd, enterprise_size]
  - kind: not_null
    col: periopd
  - kind: not_null
    col: enterprise_size
  - kind: row_floor
    min: 62
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
