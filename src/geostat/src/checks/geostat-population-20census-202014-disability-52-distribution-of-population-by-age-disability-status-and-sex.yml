# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-disability-52-distribution-of-population-by-age-disability-status-and-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, disability_status, sex]
checks:
  - kind: unique
    columns: [age, disability_status, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: disability_status
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 228
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
