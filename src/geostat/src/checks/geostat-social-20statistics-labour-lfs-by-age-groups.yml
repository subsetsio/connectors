# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-labour-lfs-by-age-groups`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, economic_status, age_group]
checks:
  - kind: unique
    columns: [years, economic_status, age_group]
  - kind: not_null
    col: years
  - kind: not_null
    col: economic_status
  - kind: not_null
    col: age_group
  - kind: row_floor
    min: 924
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
