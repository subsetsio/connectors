# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-disability-50-2-distribution-of-population-by-number-of-health-related-limitations-age-and-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, number_of_health_related_limitations, sex]
checks:
  - kind: unique
    columns: [age, number_of_health_related_limitations, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: number_of_health_related_limitations
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
