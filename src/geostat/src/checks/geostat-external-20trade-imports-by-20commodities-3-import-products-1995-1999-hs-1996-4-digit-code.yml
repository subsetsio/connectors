# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-imports-by-20commodities-3-import-products-1995-1999-hs-1996-4-digit-code`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [commodity_positions, months, years]
checks:
  - kind: unique
    columns: [commodity_positions, months, years]
  - kind: not_null
    col: commodity_positions
  - kind: not_null
    col: months
  - kind: not_null
    col: years
  - kind: row_floor
    min: 36042
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
