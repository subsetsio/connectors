# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-marriages-40-mean-age-of-spouses-by-order`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, order_of_marriages, sex]
checks:
  - kind: unique
    columns: [year, order_of_marriages, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: order_of_marriages
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 96
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
