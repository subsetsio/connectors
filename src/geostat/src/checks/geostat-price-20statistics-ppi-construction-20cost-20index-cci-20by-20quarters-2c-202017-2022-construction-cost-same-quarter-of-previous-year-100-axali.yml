# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-ppi-construction-20cost-20index-cci-20by-20quarters-2c-202017-2022-construction-cost-same-quarter-of-previous-year-100-axali`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, groups, quarter]
checks:
  - kind: unique
    columns: [year, groups, quarter]
  - kind: not_null
    col: year
  - kind: not_null
    col: groups
  - kind: not_null
    col: quarter
  - kind: row_floor
    min: 72
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
