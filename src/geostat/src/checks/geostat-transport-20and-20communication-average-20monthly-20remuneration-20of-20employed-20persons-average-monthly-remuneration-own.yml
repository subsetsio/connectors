# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-average-20monthly-20remuneration-20of-20employed-20persons-average-monthly-remuneration-own`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, ownership_type]
checks:
  - kind: unique
    columns: [period, ownership_type]
  - kind: not_null
    col: period
  - kind: not_null
    col: ownership_type
  - kind: row_floor
    min: 60
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
