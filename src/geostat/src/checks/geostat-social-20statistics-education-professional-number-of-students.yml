# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-education-professional-number-of-students`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [programmes_ages, years, gender, status]
checks:
  - kind: unique
    columns: [programmes_ages, years, gender, status]
  - kind: not_null
    col: programmes_ages
  - kind: not_null
    col: years
  - kind: not_null
    col: gender
  - kind: not_null
    col: status
  - kind: row_floor
    min: 728
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
