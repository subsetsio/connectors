# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-environmental-20indicators-07-g-1`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [final_energy_consumption, year]
checks:
  - kind: unique
    columns: [final_energy_consumption, year]
  - kind: not_null
    col: final_energy_consumption
  - kind: not_null
    col: year
  - kind: row_floor
    min: 90
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
