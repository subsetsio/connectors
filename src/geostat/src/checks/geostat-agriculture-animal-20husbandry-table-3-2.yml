# compiled by `hardened compile-checks` — post-publish audit of `geostat-agriculture-animal-20husbandry-table-3-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, livestock_type, holding_type]
checks:
  - kind: unique
    columns: [year, livestock_type, holding_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: livestock_type
  - kind: not_null
    col: holding_type
  - kind: row_floor
    min: 120
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
