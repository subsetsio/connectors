# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-purchases-20of-20goods-20and-20services-20for-20resale-purchases-for-resale-by-regions`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, regions]
checks:
  - kind: unique
    columns: [period, regions]
  - kind: not_null
    col: period
  - kind: not_null
    col: regions
  - kind: row_floor
    min: 108
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
