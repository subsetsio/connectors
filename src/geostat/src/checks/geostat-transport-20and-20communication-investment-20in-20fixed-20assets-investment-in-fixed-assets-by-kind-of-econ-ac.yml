# compiled by `hardened compile-checks` — post-publish audit of `geostat-transport-20and-20communication-investment-20in-20fixed-20assets-investment-in-fixed-assets-by-kind-of-econ-ac`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, king_of_economic_activity]
checks:
  - kind: unique
    columns: [period, king_of_economic_activity]
  - kind: not_null
    col: period
  - kind: not_null
    col: king_of_economic_activity
  - kind: row_floor
    min: 57
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
