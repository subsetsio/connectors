# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-disability-51-distribution-of-population-by-regions-urban-rural-settlements-and-disability-status`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [regions, disability_status, urban_rural]
checks:
  - kind: unique
    columns: [regions, disability_status, urban_rural]
  - kind: not_null
    col: regions
  - kind: not_null
    col: disability_status
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 144
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
