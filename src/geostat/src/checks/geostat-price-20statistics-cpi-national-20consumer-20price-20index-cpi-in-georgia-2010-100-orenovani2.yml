# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-cpi-national-20consumer-20price-20index-cpi-in-georgia-2010-100-orenovani2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, month, region]
checks:
  - kind: unique
    columns: [year, month, region]
  - kind: not_null
    col: year
  - kind: not_null
    col: month
  - kind: not_null
    col: region
  - kind: row_floor
    min: 972
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
