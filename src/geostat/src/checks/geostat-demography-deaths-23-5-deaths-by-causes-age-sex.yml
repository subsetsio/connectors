# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-deaths-23-5-deaths-by-causes-age-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [causes_of_death_in_accordance_with_eurostat_standard, age, year, sex]
checks:
  - kind: unique
    columns: [causes_of_death_in_accordance_with_eurostat_standard, age, year, sex]
  - kind: not_null
    col: causes_of_death_in_accordance_with_eurostat_standard
  - kind: not_null
    col: age
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 25992
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
