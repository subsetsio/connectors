# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-the-20geographical-20distribution-20of-20the-20population-20and-20internal-20migration-06-communities-by-size-of-population`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [regions_municipalites, size, communities]
checks:
  - kind: unique
    columns: [regions_municipalites, size, communities]
  - kind: not_null
    col: regions_municipalites
  - kind: not_null
    col: size
  - kind: not_null
    col: communities
  - kind: row_floor
    min: 2128
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
