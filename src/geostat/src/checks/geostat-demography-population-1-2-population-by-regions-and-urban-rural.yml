# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-population-1-2-population-by-regions-and-urban-rural`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, region, urban_rural]
checks:
  - kind: unique
    columns: [year, region, urban_rural]
  - kind: not_null
    col: year
  - kind: not_null
    col: region
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 693
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
