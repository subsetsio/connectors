# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-ict-5-share-of-population-aged-6-and-older-who-own-mobile-and-smartphone`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, year, sex, ownership]
checks:
  - kind: unique
    columns: [age, year, sex, ownership]
  - kind: not_null
    col: age
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: not_null
    col: ownership
  - kind: row_floor
    min: 40
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
