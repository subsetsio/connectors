# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-ppi-producer-20price-20index-20for-20industrial-20products-cpa-201999-ppi-in-industry-12-month-average-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, groups, month]
checks:
  - kind: unique
    columns: [year, groups, month]
  - kind: not_null
    col: year
  - kind: not_null
    col: groups
  - kind: not_null
    col: month
  - kind: row_floor
    min: 2160
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
