# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-ppi-import-20price-20index-cpa-201999-ipi-previous-month-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [groups, month, year]
checks:
  - kind: unique
    columns: [groups, month, year]
  - kind: not_null
    col: groups
  - kind: not_null
    col: month
  - kind: not_null
    col: year
  - kind: row_floor
    min: 360
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
