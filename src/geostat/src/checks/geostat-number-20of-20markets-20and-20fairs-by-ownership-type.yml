# compiled by `hardened compile-checks` — post-publish audit of `geostat-number-20of-20markets-20and-20fairs-by-ownership-type`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, number_of_markets_and_fairs_by_ownership_type]
checks:
  - kind: unique
    columns: [period, number_of_markets_and_fairs_by_ownership_type]
  - kind: not_null
    col: period
  - kind: not_null
    col: number_of_markets_and_fairs_by_ownership_type
  - kind: row_floor
    min: 18
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
