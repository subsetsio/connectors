# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-economic-20characteristics-4-2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [employment_status, marital_status, urban_rural]
checks:
  - kind: unique
    columns: [employment_status, marital_status, urban_rural]
  - kind: not_null
    col: employment_status
  - kind: not_null
    col: marital_status
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 84
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
