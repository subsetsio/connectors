# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-influence-20and-20power-05-number-of-parliament-members-by-faction-of-convocation`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [factions_of_convocation, year, gender]
checks:
  - kind: unique
    columns: [factions_of_convocation, year, gender]
  - kind: not_null
    col: factions_of_convocation
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 680
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
