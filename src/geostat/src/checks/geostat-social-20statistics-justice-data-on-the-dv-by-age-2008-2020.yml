# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-justice-data-on-the-dv-by-age-2008-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, regions, age_groups, victims_and_perpetrators]
checks:
  - kind: unique
    columns: [years, regions, age_groups, victims_and_perpetrators]
  - kind: not_null
    col: years
  - kind: not_null
    col: regions
  - kind: not_null
    col: age_groups
  - kind: not_null
    col: victims_and_perpetrators
  - kind: row_floor
    min: 780
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
