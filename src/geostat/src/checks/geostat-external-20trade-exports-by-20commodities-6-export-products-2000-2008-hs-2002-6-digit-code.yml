# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-exports-by-20commodities-6-export-products-2000-2008-hs-2002-6-digit-code`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [commodity_positions, months, years]
checks:
  - kind: unique
    columns: [commodity_positions, months, years]
  - kind: not_null
    col: commodity_positions
  - kind: not_null
    col: months
  - kind: not_null
    col: years
  - kind: row_floor
    min: 186439
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
