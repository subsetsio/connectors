# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-population-04-mid-year-population-by-self-governed-units`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [region_self_governed_unit, year]
checks:
  - kind: unique
    columns: [region_self_governed_unit, year]
  - kind: not_null
    col: region_self_governed_unit
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1456
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
