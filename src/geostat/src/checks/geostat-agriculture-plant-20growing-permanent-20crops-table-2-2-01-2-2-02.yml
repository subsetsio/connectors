# compiled by `hardened compile-checks` — post-publish audit of `geostat-agriculture-plant-20growing-permanent-20crops-table-2-2-01-2-2-02`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, crop_categories, holding_type]
checks:
  - kind: unique
    columns: [year, crop_categories, holding_type]
  - kind: not_null
    col: year
  - kind: not_null
    col: crop_categories
  - kind: not_null
    col: holding_type
  - kind: row_floor
    min: 150
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
