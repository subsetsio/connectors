# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-ppi-unit-20value-20index-20of-20agricultural-20products-agro-ppi-same-quarter-of-previous-year-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, quarter, groups]
checks:
  - kind: unique
    columns: [year, quarter, groups]
  - kind: not_null
    col: year
  - kind: not_null
    col: quarter
  - kind: not_null
    col: groups
  - kind: row_floor
    min: 48
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
