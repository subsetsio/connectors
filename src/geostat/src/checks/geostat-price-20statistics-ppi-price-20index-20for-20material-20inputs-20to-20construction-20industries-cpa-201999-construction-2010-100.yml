# compiled by `hardened compile-checks` — post-publish audit of `geostat-price-20statistics-ppi-price-20index-20for-20material-20inputs-20to-20construction-20industries-cpa-201999-construction-2010-100`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [month, year, groups]
checks:
  - kind: unique
    columns: [month, year, groups]
  - kind: not_null
    col: month
  - kind: not_null
    col: year
  - kind: not_null
    col: groups
  - kind: row_floor
    min: 216
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
