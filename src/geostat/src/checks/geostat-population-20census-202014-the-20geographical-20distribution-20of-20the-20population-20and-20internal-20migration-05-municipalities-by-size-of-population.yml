# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-the-20geographical-20distribution-20of-20the-20population-20and-20internal-20migration-05-municipalities-by-size-of-population`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [regions, size, municipalities]
checks:
  - kind: unique
    columns: [regions, size, municipalities]
  - kind: not_null
    col: regions
  - kind: not_null
    col: size
  - kind: not_null
    col: municipalities
  - kind: row_floor
    min: 230
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
