# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-environmental-20indicators-b-2-atmospheric-precipitation`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, precipitation_categories, locations]
checks:
  - kind: unique
    columns: [year, precipitation_categories, locations]
  - kind: not_null
    col: year
  - kind: not_null
    col: precipitation_categories
  - kind: not_null
    col: locations
  - kind: row_floor
    min: 270
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
