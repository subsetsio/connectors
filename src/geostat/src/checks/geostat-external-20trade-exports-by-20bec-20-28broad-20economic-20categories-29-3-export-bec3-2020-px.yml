# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-exports-by-20bec-20-28broad-20economic-20categories-29-3-export-bec3-2020-px`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [bec_broad_economic_categories, months]
checks:
  - kind: unique
    columns: [bec_broad_economic_categories, months]
  - kind: not_null
    col: bec_broad_economic_categories
  - kind: not_null
    col: months
  - kind: row_floor
    min: 39
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
