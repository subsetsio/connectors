# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-deaths-23-4-deaths-by-chapters-age-sex`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, chapters_of_icd_10, year, sex]
checks:
  - kind: unique
    columns: [age, chapters_of_icd_10, year, sex]
  - kind: not_null
    col: age
  - kind: not_null
    col: chapters_of_icd_10
  - kind: not_null
    col: year
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 7182
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
