# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-influence-20and-20power-09-results-of-elections-on-local-authority-mayors-and-governors`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, gender, position]
checks:
  - kind: unique
    columns: [year, gender, position]
  - kind: not_null
    col: year
  - kind: not_null
    col: gender
  - kind: not_null
    col: position
  - kind: row_floor
    min: 8
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
