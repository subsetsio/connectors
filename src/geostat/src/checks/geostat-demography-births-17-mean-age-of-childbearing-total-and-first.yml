# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-births-17-mean-age-of-childbearing-total-and-first`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, mean_age_of_childbearing]
checks:
  - kind: unique
    columns: [year, mean_age_of_childbearing]
  - kind: not_null
    col: year
  - kind: not_null
    col: mean_age_of_childbearing
  - kind: row_floor
    min: 34
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
