# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-imports-by-20countries-8-import-country-groups-1995-2019`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, months, country_groups]
checks:
  - kind: unique
    columns: [years, months, country_groups]
  - kind: not_null
    col: years
  - kind: not_null
    col: months
  - kind: not_null
    col: country_groups
  - kind: row_floor
    min: 975
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
