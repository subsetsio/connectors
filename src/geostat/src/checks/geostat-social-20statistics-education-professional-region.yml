# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-education-professional-region`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, regions, indicators, gender]
checks:
  - kind: unique
    columns: [years, regions, indicators, gender]
  - kind: not_null
    col: years
  - kind: not_null
    col: regions
  - kind: not_null
    col: indicators
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 351
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
