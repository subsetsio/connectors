# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-crime-08-number-of-persons-in-shelters-for-human-trafficking-and-victims-of-domestic`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, education_level]
checks:
  - kind: unique
    columns: [year, education_level]
  - kind: not_null
    col: year
  - kind: not_null
    col: education_level
  - kind: row_floor
    min: 37
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
