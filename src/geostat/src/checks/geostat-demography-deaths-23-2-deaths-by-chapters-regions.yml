# compiled by `hardened compile-checks` — post-publish audit of `geostat-demography-deaths-23-2-deaths-by-chapters-regions`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [chapters_of_icd_10, region, year]
checks:
  - kind: unique
    columns: [chapters_of_icd_10, region, year]
  - kind: not_null
    col: chapters_of_icd_10
  - kind: not_null
    col: region
  - kind: not_null
    col: year
  - kind: row_floor
    min: 1512
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
