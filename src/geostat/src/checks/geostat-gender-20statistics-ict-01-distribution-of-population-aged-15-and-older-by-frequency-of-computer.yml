# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-ict-01-distribution-of-population-aged-15-and-older-by-frequency-of-computer`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, frequency, use, sex]
checks:
  - kind: unique
    columns: [year, frequency, use, sex]
  - kind: not_null
    col: year
  - kind: not_null
    col: frequency
  - kind: not_null
    col: use
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 60
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
