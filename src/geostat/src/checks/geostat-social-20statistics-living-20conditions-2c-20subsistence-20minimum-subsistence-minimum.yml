# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-living-20conditions-2c-20subsistence-20minimum-subsistence-minimum`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, month, target]
checks:
  - kind: unique
    columns: [year, month, target]
  - kind: not_null
    col: year
  - kind: not_null
    col: month
  - kind: not_null
    col: target
  - kind: row_floor
    min: 1188
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
