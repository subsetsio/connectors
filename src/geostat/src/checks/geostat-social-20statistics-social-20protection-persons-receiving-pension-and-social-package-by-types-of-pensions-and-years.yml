# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-social-20protection-persons-receiving-pension-and-social-package-by-types-of-pensions-and-years`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [years, types_of_pensions]
checks:
  - kind: unique
    columns: [years, types_of_pensions]
  - kind: not_null
    col: years
  - kind: not_null
    col: types_of_pensions
  - kind: row_floor
    min: 387
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
