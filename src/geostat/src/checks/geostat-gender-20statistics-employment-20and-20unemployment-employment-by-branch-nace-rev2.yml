# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-employment-20and-20unemployment-employment-by-branch-nace-rev2`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [economic_activity, years, sex]
checks:
  - kind: unique
    columns: [economic_activity, years, sex]
  - kind: not_null
    col: economic_activity
  - kind: not_null
    col: years
  - kind: not_null
    col: sex
  - kind: row_floor
    min: 180
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
