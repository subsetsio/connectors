# compiled by `hardened compile-checks` — post-publish audit of `geostat-gender-20statistics-agriculture-1-classification-of-agricultural-holdings-by-age-and-gender-of-holder-or`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age_of_holder, gender]
checks:
  - kind: unique
    columns: [year, age_of_holder, gender]
  - kind: not_null
    col: year
  - kind: not_null
    col: age_of_holder
  - kind: not_null
    col: gender
  - kind: row_floor
    min: 100
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
