# compiled by `hardened compile-checks` — post-publish audit of `geostat-trade-wholesale-20and-20retail-20trade-2c-20repair-20of-20motor-20vehicles-20and-20motorcycles-turnover-turnover-by-regions`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [period, region]
checks:
  - kind: unique
    columns: [period, region]
  - kind: not_null
    col: period
  - kind: not_null
    col: region
  - kind: row_floor
    min: 114
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
