# compiled by `hardened compile-checks` — post-publish audit of `geostat-environment-20statistics-environmental-20indicators-14-h-4`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, age, motor_vehicle_cars]
checks:
  - kind: unique
    columns: [year, age, motor_vehicle_cars]
  - kind: not_null
    col: year
  - kind: not_null
    col: age
  - kind: not_null
    col: motor_vehicle_cars
  - kind: row_floor
    min: 550
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
