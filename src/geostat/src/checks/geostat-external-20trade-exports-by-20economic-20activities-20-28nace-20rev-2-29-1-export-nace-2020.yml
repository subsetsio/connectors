# compiled by `hardened compile-checks` — post-publish audit of `geostat-external-20trade-exports-by-20economic-20activities-20-28nace-20rev-2-29-1-export-nace-2020`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [nace_section, months]
checks:
  - kind: unique
    columns: [nace_section, months]
  - kind: not_null
    col: nace_section
  - kind: not_null
    col: months
  - kind: row_floor
    min: 30
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
