# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-disability-49-4-population-under-18-with-health-related-limitations-by-school-attendence`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [regions, school_attendence]
checks:
  - kind: unique
    columns: [regions, school_attendence]
  - kind: not_null
    col: regions
  - kind: not_null
    col: school_attendence
  - kind: row_floor
    min: 39
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
