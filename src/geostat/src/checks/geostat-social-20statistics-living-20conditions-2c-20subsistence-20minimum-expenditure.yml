# compiled by `hardened compile-checks` — post-publish audit of `geostat-social-20statistics-living-20conditions-2c-20subsistence-20minimum-expenditure`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [year, purpose, target, area]
checks:
  - kind: unique
    columns: [year, purpose, target, area]
  - kind: not_null
    col: year
  - kind: not_null
    col: purpose
  - kind: not_null
    col: target
  - kind: not_null
    col: area
  - kind: row_floor
    min: 1620
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
