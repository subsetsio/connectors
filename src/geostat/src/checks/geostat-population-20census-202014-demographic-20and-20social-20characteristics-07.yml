# compiled by `hardened compile-checks` — post-publish audit of `geostat-population-20census-202014-demographic-20and-20social-20characteristics-07`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [age, regions, sex, urban_rural]
checks:
  - kind: unique
    columns: [age, regions, sex, urban_rural]
  - kind: not_null
    col: age
  - kind: not_null
    col: regions
  - kind: not_null
    col: sex
  - kind: not_null
    col: urban_rural
  - kind: row_floor
    min: 1026
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
