# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `geostat-population-20census-202014-households-one-person-households-by-regions-age-and-sex`. Regenerate after model-verify.
key: [age, regions, sex]
sort: [age, regions, sex]
columns:
  - name: age
    type: VARCHAR
  - name: sex
    type: VARCHAR
  - name: regions
    type: VARCHAR
  - name: value
    type: BIGINT
