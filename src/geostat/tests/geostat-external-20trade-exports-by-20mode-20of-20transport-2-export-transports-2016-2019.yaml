spec_id: geostat-external-20trade-exports-by-20mode-20of-20transport-2-export-transports-2016-2019
status: active
tests:
  - row_count: {min: 1}
    reason: every PxWeb .px table returns at least one observation cell; an empty raw asset means the json-stat fetch or cell-split logic broke (or the table was withdrawn upstream).
    certainty: 80
    points_outward: true
    severity: warn
