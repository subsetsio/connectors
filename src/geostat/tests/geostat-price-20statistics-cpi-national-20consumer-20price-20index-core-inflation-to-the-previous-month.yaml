spec_id: geostat-price-20statistics-cpi-national-20consumer-20price-20index-core-inflation-to-the-previous-month
status: active
tests:
  - row_count: {min: 1}
    reason: every PxWeb .px table returns at least one observation cell; an empty raw asset means the json-stat fetch or cell-split logic broke (or the table was withdrawn upstream).
    certainty: 80
    points_outward: true
    severity: warn
