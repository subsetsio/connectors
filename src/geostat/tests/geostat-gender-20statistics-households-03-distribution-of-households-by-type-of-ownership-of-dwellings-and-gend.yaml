spec_id: geostat-gender-20statistics-households-03-distribution-of-households-by-type-of-ownership-of-dwellings-and-gend
status: active
tests:
  - row_count: {min: 1}
    reason: every PxWeb .px table returns at least one observation cell; an empty raw asset means the json-stat fetch or cell-split logic broke (or the table was withdrawn upstream).
    certainty: 80
    points_outward: true
    severity: warn
