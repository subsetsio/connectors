spec_id: ppac-natural-gas-city-gas-distribution-network
status: active
tests:
  - not_null: sheet
    certainty: 95
  - not_null: row_number
    certainty: 95
  - not_null: column_number
    certainty: 95
  - unique: [sheet, row_number, column_number]
    reason: raw workbook cell coordinates should identify each preserved non-empty cell
    certainty: 95
  - row_count: {min: 200}
    reason: CGD network workbook lists many state/GA rows and connection metrics
    certainty: 80

