# compiled by `hardened compile-checks` — post-publish audit of `ppac-production-crude-processing`.
# Grain/temporal from the transform contract, row floor from the measured
# profile. Regenerate after model-verify or a transform contract change.
# Not compiled (deliberate): enums (await an authored closed domain),
# absolute freshness (awaits the maintenance contract).
key: [date, financial_year, section, item]
temporal: date
checks:
  - kind: unique
    columns: [date, financial_year, section, item]
  - kind: not_null
    col: date
  - kind: not_null
    col: financial_year
  - kind: not_null
    col: section
  - kind: not_null
    col: item
  - kind: row_floor
    min: 551
regression:
  row_shrink_tolerance: 0.1
  key_shrink_tolerance: 0.1
  temporal_monotonic: true
