spec_id: wgms-fog-agency
status: active
tests:
  - row_count: {min: 100}
    reason: FoG includes a multi-country agency reference table, not a tiny lookup.
    certainty: 85
  - not_null: id
  - not_null: name
  - unique: id
    reason: datapackage declares agency id as the primary key.
    certainty: 95

