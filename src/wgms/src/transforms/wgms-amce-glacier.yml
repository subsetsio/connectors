# compiled by `hardened compile-transforms` — faithful pass-through
# of raw asset `wgms-amce-glacier`. Regenerate after model-verify.
# caution: This table is per glacier outline and year, but the raw AMCE release contains repeated outline-year rows without a captured source row ordinal; aggregate statistics should account for glacier area rather than treating each row as an equal-weight regional observation.
key: []
temporal: year
sort: [year]
columns:
  - name: region
    type: VARCHAR
  - name: outline_id
    type: VARCHAR
  - name: glacier_id
    type: VARCHAR
  - name: latitude
    type: DOUBLE
  - name: longitude
    type: DOUBLE
  - name: area_km2
    type: DOUBLE
  - name: year
    type: BIGINT
  - name: mwe
    type: DOUBLE
