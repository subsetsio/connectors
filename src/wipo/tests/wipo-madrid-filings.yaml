spec_id: wipo-madrid-filings
status: active
tests:
  - not_null: indicator_id
  - not_null: year
  - not_null: value
  - column_type: {col: year, type: integer}
  - column_type: {col: value, type: float}
  - unique: [office, origin, indicator_id, report_type, year, breakdown_index]
    reason: only the yearly report type (4001) is fetched and pmh cells carry a single value, so office x origin x indicator x year is one observation
    certainty: 90
  - between: {col: year, lo: 1980, hi: current_year()}
    reason: the Madrid series in the data center starts in the mid-1990s; the bound is slack on the low side and hard on the high side
    certainty: 90
  - freshness: {col: year, reaches: 2023}
    reason: Madrid yearly statistics are refreshed monthly, so the latest complete year should be recent
    points_outward: true
  - coverage: {col: indicator_id, expected: 24}
    reason: pmhIndicatorMap enumerates 24 Madrid indicators; a shortfall means an indicator returned an empty envelope and was silently skipped
  - distinct_count: {col: report_type, min: 1, max: 1}
    reason: the monthly report type (4003) is deliberately not fetched, so only "Yearly statistics" appears
  - at_least: {col: value, expr: 0}
    reason: Madrid indicators are application/registration/renewal counts, never negative
    certainty: 80
    severity: warn
  - row_count: {min: 5000}
    reason: 24 indicators x ~130 members x ~30 years, sparsely populated
    certainty: 70
