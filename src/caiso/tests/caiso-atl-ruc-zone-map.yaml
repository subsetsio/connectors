spec_id: caiso-atl-ruc-zone-map
status: active
tests:
  - not_null: RUC_ZONE_ID
    reason: PNode to RUC-zone map — RUC_ZONE_ID is the dimension key present on every row
    certainty: 90
  - column_type: {col: RUC_ZONE_ID, type: string}
    reason: OASIS atlas ids are alphanumeric labels, not numerics
    certainty: 85
  - not_null: EFF_START_DT_GMT
    reason: every atlas row carries an effective-start stamp bounding its validity period
    certainty: 80
    severity: warn
